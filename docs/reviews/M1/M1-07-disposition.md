# M1-07 principal disposition

External review: explicit Claude Code2.1.259 / Opus5 High, read-only. Full text in
M1-07-opus.md. Followup Medium focuses corrected boundaries. No claim that review
substitutes actual tests.

- P1 empty data: reject records=[]; expose record count and sequence. A positive
  count scopes host-selected facts, never proves publisher completeness/authenticity.
- P1 source identity: strict kind/URL/precise in lock and advisory selection. Actual
  cargo-lock11.0.1 distinguishes default(None) from parsed registry(locked). Preserve
  this valid marker. Reviewer fragment exploit was not confirmed: fragment is part
  of URL and already rejected. Regression tests prove the actual semantics.
- P1 error mapping: missing files unavailable with project evidence; sandbox,
  timeout, budget, unsupported OS, cancellation and internal failures stay distinct.
- P2 status precedence aligned: fresh known findings Failed even with incomplete
  coverage; stale/unknown Unavailable retaining findings. Integrity is not absence.
- P2 graph cost: no paths built after retention cap; lazy cache once/package.
- P2 metadata: ASCII package names, bounded informational labels and title with
  control/bidirectional-format rejection. Withdrawn test asserts both finding arrays.
- P2 sequence visibility and dependency feature changes documented. No antirollback
  or signed-import claim. Adapter hard cap and MCP envelope defense intentionally
  differ; omissions and first issue retain distinct coverage fields.
- P2 per-request snapshot loading/SQLite staging and metadata-before-availability are
  bounded deliberate choices. No extra cache/activation architecture introduced.
- Snapshot parent is explicit host read authority; platform/no-follow restrictions
  remain. Test-process Seatbelt network deny is not a whole-MCP isolation claim.

Runtime evidence uses actual RSA record plus explicitly synthetic classification
variants. Full Rust gate passed16exact tests before review corrections; only modified
RustSec/MCP audit boundaries are rerun afterward (3tests/15calls), alongside core and
native data gate. Gateway implementation/identity unchanged by these corrections.
Active RustSec audit cancellation is covered at cooperative checkpoints and shared
joined-worker tests, not claimed as a separate timed real-dataset cancellation oracle.

Focused followup confirmed original corrections and found noncanonical advisory
origins silently omitted. Fixed by rejecting the entire snapshot, aligned with the
crates.io-only scope. Explicit advisory-source parser test pins precise=locked;
fragment/alternate origins now invalid, never a clean scan after silent omission.
Also check created_at at MCP boundary and stop graph work after byte budget exhausts.
Reject line/paragraph separators and common invisible format characters in display
fields; no claim of a universal Unicode sanitizer. Larger unsupported metadata hard
failure remains an intentional safe limitation. Provider codes unreachable from the
snapshot filesystem need no simulated runtime claim. Final core rerun after these
small changes; canonical/stale runtime cases will receive post-merge smoke.

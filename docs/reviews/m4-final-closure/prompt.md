# Independent final M4 closure review

Act as Principal security/architecture/evidence reviewer. Read-only: do not edit files, run commands, launch agents, use Docker, commit, merge, or publish. This review is restricted to Read/Grep/Glob. Return findings P0-P3 with exact file/line and concrete evidence, separate blockers from accepted documented limitations. Do not equate a passing review with native test evidence. Model must be Claude Opus 5, effort high.

Review the implementation under /Users/cburgosro/Projects/rust-mcp against docs/prompts/implement-m4.md, its M3 complement, docs/roadmap/m4-security.md, G1-G9 and the final evidence supplied in this directory's inputs.json. All work is uncommitted on ai/m4-security, base c66a3704e1ad290603a3c1d10413df90d15c2b03. No release, remote CI, Linux/Windows native qualification or M5 is claimed.

Focus on these material deltas since docs/reviews/m4-security-confirmation:
1. security_gateway.rs fingerprints now include unsafe_scan.rs, unsafe_port.rs, deny_json.rs and security_port.rs; verify fresh scanner/Miri runtime receipts bind current source and exact final image 25ed3626e710081a571a86a29521eaf2e890e796afd422ba5e409e0ce1891635. Close or retain prior P2-1/P2-2 explicitly.
2. miri_output.rs narrowly permits the pinned benign opt-level warning alongside ordinary panic: unknown JSON/errors must still block TestFailure, never become UB/clean. Inspect optimized-panic golden, mutation unit oracles, native classification and MCP canary proof. Scope of --workspace --tests includes lib/bin/integration. Interpretations remain bounded to selected configuration; no absence-of-UB claim.
3. scripts/test-m4-tampered-plugin.py: stopped private image with deliberately replaced binary; admission must reject altered immutable image before image code execution. Previously Docker deprecated --pause emitted prose before ID, and discovery without --all falsely claimed cleanup. Inspect corrected --no-pause, ownership checks, bounded output, intermediate-image discovery, failure cleanup tests and preserved failure/repair evidence. No arbitrary image deletion.
4. scripts/test-m4-clients.py and m4-inspector-session.mjs: actual production release binary, Inspector 2.5.0 Tasks positive/negative/cancel/Resource; stock Codex 0.153.0 sync 60s conversion plus model-directed calls requiring Passed. Credentials stay outside repository; process cleanup observed. No Tasks support claimed for stock Codex. Check receipt source/binary hashes and failures preserved.
5. Privacy actual HTML/diff test has positive authorized source canary and negative host-only canary; MCP tests read each private artifact and distinguish ordinary panic from UB despite guest prose. Authorized source content may retain its own secrets in private M3 reports; universal secret redaction is explicitly not claimed.
6. Miri Task cancellation/EOF/revocation cleanup before authority release; deny runtime/plugin withdrawal to exact M3 image blocks new operation while retained owner-bound audit artifact remains readable. Native M4 selectors all use 25ed final image (M3 rollback explicit), legacy M3 runtime suite remains separate.
7. Full/core/client receipts, inventory six binaries plus full Miri sysroot, benchmark 300 observations, 23 original snapshots unchanged and five added. Benchmark used explicitly recorded pre-final binary; final routing/failure-parser changes are qualified separately. Check mismatch is disclosed, not silently treated as exact final-binary latency.

Consult docs/reviews/m4-composition-confirmation and m4-traceability dispositions for already reviewed bounded composition/contracts. Existing accepted P3 constraints stay explicit; do not request unrelated redesign. Identify any mandatory DoD or gate evidence missing rather than inventing approval. Return verdict, P0-P3 findings, closure of prior P2, exact reviewed files/evidence, remaining limitations. The Technical Owner alone updates milestone state.

## Phase at dispatch

This is the final code/security review; G4 has passed and the final core/full gates are being executed by the owner. Their absence at dispatch is an acknowledged pending gate, not a newly discovered code finding. Do not claim milestone closure. Review the final source and currently available focused evidence now; the owner will request a separate evidence-only confirmation after the complete gates. Sources are frozen during execution.

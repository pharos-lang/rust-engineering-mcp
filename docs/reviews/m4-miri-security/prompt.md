Revisión independiente READ-ONLY Opus 5 High. Lee solo docs/reviews/m4-miri-security/inputs/ e inputs.json. No editar/ejecutar/Docker/subagentes. Revisa seguridad y correctness del nuevo camino Miri end-to-end, clasificación UB vs test failure/unsupported/compile, contención de productores hostiles, env/argv/permisos/volúmenes, presupuestos/cancel/cleanup y límites de salida/publicación. El sistema usa gateway Docker existente y port CaptureSourceBundle fail closed. Los 10 oráculos nativos pasaron con helper anterior ecade; la imagen final 25ed conserva Miri y cambió solo helper scanner; el rerun final y nuevas pruebas adversas están pendientes. ADVERTISEMENT_READY=false y MIRI_IMAGE=None son puertas temporales, no bugs. No reclamar cierre ni reemplazar evidencia pendiente. Identifica defectos concretos P0–P3 por archivo/línea con escenario discriminante; distingue limitación explícita vs defecto. Incluye veredicto y cobertura. No revisión global fuera de estos archivos; security_gateway es compartido y solo importa el delta Miri y sus prerrequisitos. No pedir adquisición de nuevas dependencias.
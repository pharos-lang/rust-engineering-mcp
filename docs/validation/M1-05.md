# M1-05 — Clippy profiles and structured findings

Date2026-09-04. `ai/m1-05-clippy` from clean main `e8253a7`.
ADR-036; no remote, PR, Actions or M2. No new dependencies/images/downloads.

## Behavior

Live project_ref plus closed package/workspace/features/all_targets/lint_profile.
Default/project honor captured native policy; strict promotes warnings, pedantic
is opt-in warning-level. Frozen/offline JSON execution under the approved calibrated
Linux ARM64 source sandbox, never --fix. Distinct application port/options share
capture/publication; execution adapter narrowly shares Cargo result normalization
with check. Clippy-coded roots and children retain lint-family labels/suggestions.
Same logs/Resources, incomplete, timeout, frozen lock and quota fallback as check.

## Validation

Rust/Cargo1.98.1,CARGO_INCREMENTAL=0,locked/offline.
`python3 scripts/gate.py core --report target/M1-05-core.json`: passed10stages,
372tests including doctests. No double-count of focused package tests.
Actual Clippy MCP six cases pass: warning-default, strict-failure, project alias,
pedantic opt-in, package/features/all-targets and independent workspace member.
Six Resources hashes, structured findings/suggestions, source tree unchanged.
Actual Clippy build.rs/proc-macro containment both pass after six-case calibration;
fixtures only execute in Docker, never host Cargo.

Initial runtime fixture assumptions were corrected: manifests must be finalized
before project.open, Clippy lints path dependencies too, and a new independent
member needs explicit source-tree assertions. No production policy was relaxed.
Final frozen-code nine-test Docker gate passed: `RUST_MCP_TEST_SOCKET=<LOCAL_HOME>/.docker/run/docker.sock python3 scripts/test-rust-execution.py`.
Core was repeated after fixture finalization and passed10stages/372tests.
[Core](M1-05-core-gate.json), [calibration](M1-05-calibration-receipt.json),
[Clippy](M1-05-clippy-receipt.json), [Clippy containment](M1-05-clippy-containment-receipt.json).
All three runtime receipts share `sha256:a98f414d408eec7c7cdf154437d7bf31c54322f6b342f275bc34df98e606a0a0`.
Logs: target/M1-05-core.log, target/M1-05-rust-security.log and target/rust-security/0..8.log.
Diff reviewed; git diff --check clean. Local commit/merge/smoke follow.

[External Sonnet5 Medium review](../reviews/M1-05-sonnet.md),
[principal disposition](../reviews/M1-05-principal-disposition.md). Reviewer concerns
were checked against actual command-table purpose, domain/source invariants,
dedicated payload tests and explicit family-label convention. No confirmed
unresolved P0/P1 in supported scope; final gates now passed.

## Limits

Source lint suppression applies intentionally, including under strict/pedantic.
Passed means the configured command passed, not all possible lints were checked.
Code-less root labels use the historical rustc normalization convention, not
producer authentication. Fixed30s may be insufficient for large projects; partial
and timeout results remain honest. Empty CARGO_HOME supports captured pathdeps/std,
not registry/git download/cache. M0 platform/distribution/semantic/utility limits
remain. Full integral semantic gate still required for final authorized closure.

Integration: commit `464fffb`, merge no-ff `57ca421`; branch retained. Actual MCP Clippy post-merge smoke1/1,19.12s (`target/M1-05-postmerge.log`).

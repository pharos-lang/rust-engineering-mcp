# M0-11 — Initial local CI

ADR-029: local-only CI for the actual M0 supported development matrix. No remote
workflow, push or PR. Initial native verified platform macOS26.6.2/APFS ARM64;
Docker Linux ARM64 is probe containment, not a Rust native runner. Broader matrix
and licenses remain explicit M1 RC gates, never passed by omission.

`python3 scripts/gate.py core --report target/M0-11-core.json` passed all10 steps:
fmt, check, Clippy,173 unit/integration/contract/protocol tests,1 doctest,
architecture, vendor,11 Cargo fixture cases+static RSA, audit, deny.
[Actual step report](artifacts/M0-11-core.json).

Cargo/rustc resolve to the same real installed1.98.1 toolchain. Environment is
filtered, incremental compilation disabled, Cargo locked/offline. Missing full
assets reject before any step, with failed top-level status and no false success.
Python optimized mode fails closed in assertion-based checks. No tool/model/image
is silently installed; cache provisioning of missing platform crates was explicit
before the offline gate and did not alter Cargo.lock.

`cargo audit --no-fetch`: zero known vulnerabilities; paste1.0.15 unmaintained
warning retained. `cargo deny`0.19.7 with all features, offline/no-fetch,
advisories/bans/sources: all passed; duplicate-version warnings remain visible.
The configured30-day advisory DB freshness threshold passed. No advisory ignore,
no license-gate claim. Full Docker and semantic execution is required by M0-12.

[Sonnet5 Medium review and principal disposition](../reviews/M0-11-claude-sonnet-5.md).
Read [CI instructions and platform matrix](../ci.md) for prerequisites and commands.

Local no-ff merge `d227e8c`; post-merge architecture invariants and fmt passed.
The complete full entrypoint is the immediately following M0-12 gate.

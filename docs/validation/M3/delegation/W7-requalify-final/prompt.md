# Package W7 — Re-establish the M3 evidence on the final branch bytes (validator delegate; sole Docker owner)

PR #14 (`ai/m3-quality` → `main`) is green on every **required** check: the three portable builds, supply chain and SonarCloud. The only remaining blocker is the owner's own review, which is not ours to give.

There is, however, one honesty problem to fix before that review: the consolidated evidence was produced at commit `e2ec7da`, and eleven commits have landed since — Linux and Windows portability fixes, the SonarCloud remediation refactors, and the fork/flock test-isolation fix (`480ebd7`). Several of them touched product code. The qualified bytes must be the bytes that get merged.

## CRITICAL operating instruction
The full gate takes roughly forty minutes. Start each long command in the background and **keep polling from inside this same session** until it exits. Never end your turn while a gate is running.

## Work
1. Confirm the tree is clean and the branch tip is what CI tested (`git status --porcelain`, `git log --oneline -1`). Do not create commits until step 3.
2. Re-run, one at a time, on the current bytes: `python3 -B scripts/check-architecture.py`; `python3 -B scripts/gate.py core --report docs/validation/M3-core-gate.json`; `python3 -B scripts/gate.py full --report docs/validation/M3-full-gate.json` with `RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock`, `RUST_MCP_E5_DIR=/private/tmp/rust-mcp-e5-m009/onnx`, `ORT_LIB_LOCATION=/Users/cburgosro/Library/Caches/ort.pyke.io/dfbin/aarch64-apple-darwin/612739f75438dc0a075461e1fb454226b4a1eb175e60a7271ba966bbbb972cd4`. The full gate runs the M3 runtime and Rust security stages inside it; if any receipt they write lands elsewhere, copy it to its canonical name too. Preserve every previous receipt: move the ones you replace to `-preS03.json` (or the next free `-attemptN`) rather than overwriting them, and say which.
3. If a gate fails, diagnose and fix the real cause on the branch, then re-run. Never weaken a stage, never credit a skip as a pass.
4. Update the evidence documents whose numbers or hashes change: `docs/validation/M3-07.md` (the handoff), `docs/validation/M3-matrix.md`, the affected `docs/validation/M3-0X.md` narratives, and `docs/validation/M3-integration.json` (add the re-qualification: the commits since `e2ec7da`, why re-running was necessary, and the new receipt hashes). State plainly in the handoff that the evidence corresponds to the final branch tip.
5. **CodeQL assessment, written down, not acted on silently.** CodeQL reports 33 alerts on the PR (31 `rust/hard-coded-cryptographic-value`, 2 `rust/cleartext-logging`); it is not a required check. Verify each class yourself and record the assessment in `docs/validation/M3-07.md`: the "nonce" the rule flags is the Docker ownership label this project uses to prove a container or volume belongs to this process, not a cryptographic nonce, and the flagged values are test literals (`crates/execution-adapter/src/{rust_applied,mutation_gateway,rust_gateway,resolution_gateway}.rs`); the two logging alerts are inside the test that proves host-shaped identities are redacted (`crates/execution-adapter/src/mutation_outcomes.rs`), where the personal username `cburgosro` is used as a test vector. Do two things about it: replace that personal username with a neutral host-shaped value so no personal identifier is embedded in the repository, and recommend in the handoff that the owner either dismiss the 31 alerts as false positives in the Security tab or schedule a rename of the ownership label away from the word `nonce` in a later maintenance cut — that rename touches qualified execution paths, so it belongs to its own cut with its own gate run, not to this one.
6. Commit the receipts, documents and the username change on `ai/m3-quality` with the repository's message style and this session's two attribution lines, push, and poll `gh pr checks 14` until the required checks are green again. Do not merge and do not approve.

## Rules
Branch `ai/m3-quality` only. Authorized: edits, `git add`/`commit`/`push origin ai/m3-quality`, `gh` reads, and the gates above. Not authorized: merging, approving, tagging, releasing, force-push, history rewriting, repository settings, disabling any rule. Sole Docker owner; check Docker hygiene (zero owned containers and volumes) after each gate. Never commit credentials.
Delivery: Task, Result (per gate, with counts, durations and receipt hashes), what changed between `e2ec7da` and the tip and why it required re-qualification, the CodeQL assessment, Files changed with SHA-256, the final check matrix, Docker hygiene evidence, Risks, Decisions, Open issues.

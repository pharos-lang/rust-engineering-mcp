# Package V17c — Independent review of the ADR-061 durable artifact store implementation (Claude Opus 5, NEW session, read-only)

Object under review: package I17c (implemented by a different Claude Opus 5 session; delivery report with SHA-256 per file: docs/validation/m3-delegation/I17c-artifact-store-opus/last-message.md). Files: crates/domain/src/quality_artifact.rs, crates/domain/tests/quality_artifact.rs, crates/application/src/quality_artifact.rs, crates/application/tests/quality_artifact.rs, crates/project-adapter/src/quality_artifact_store.rs, crates/project-adapter/src/filesystem/macos/quality.rs, crates/project-adapter/src/filesystem/macos/state_primitives.rs, crates/project-adapter/tests/quality_artifact_store.rs, the appended wrapper + tests in crates/execution-adapter/src/mutation_archive.rs, crates/mcp-server/src/stdio/resources/tests.rs (quality tests), and the pub/mod lines. Decision: docs/adr/ADR-061-private-quality-artifact-store.md (Accepted 2026-09-06, revised after V17 — you may be the same model family as V17 but this is a new session reviewing code, not the ADR).
Focus (security/persistence, G8 Opus scope): handle-relative no-follow I/O only (openat/renameat/unlinkat with the XNU flag set; no path-based std::fs on the state root), private-file/link-count/dev-ino revalidation, fsync/F_FULLFSYNC ordering, flock semantics (exclusive non-blocking; busy rejection; no reads under lock as decided), free-space floor with the M2 headroom constants, fallocate semantics on APFS (extends file — check the accepted `size >= size_bytes` rule is safe: could a longer blob leak bytes beyond size_bytes to readers? verify chunk reads are clamped to size_bytes), truncation after commit, clock watermark ordering, reconciliation/quarantine never touching `rust-mcp-mutations-v1`, owner binding derivation (domain separation, length prefixes), uniform not-found masking, TTL non-renewal, index/cursor bounds, 512 KiB response cap, ArchiveBundle revalidation (closed USTAR profile; no extraction), no unwrap/expect/panic on normal paths, no new dependencies, domain/application free of rustix/tokio/rmcp, and whether the tests are discriminating (positive controls) rather than tautological. Also check the `UtcInstant` civil-calendar conversion for correctness at boundaries (leap years, end of month, year 2038+) and the accounting rule until `release`.
Protocol: read-only tools (Read/Grep/Glob); no commands. Deliver: Verdict Accepted/Revise; findings table ID | P0–P3 | file:line | claim | why | fix; files read; contradictions with ADR-061/ADR-060/M2 code; missing tests; limitations; disposition per finding. Severity bar as usual (P0 authority/containment; P1 correctness/data loss; P2 contract/gate; P3 clarity).

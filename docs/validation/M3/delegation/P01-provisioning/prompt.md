# Package P01 — Guest plugin provisioning for M3 (files + ADR-063; no build in this package)

Task / ID: P01 (prerequisite of M3-01..05). Objective: extend the explicit host-only provisioning fixture `fixtures/rust-runtime/` so a later, separately launched provisioning run can build a NEW immutable Linux ARM64 guest image containing Rust 1.98.1 (unchanged) plus cargo-nextest 0.9.143, cargo-llvm-cov 0.9.0 with the official llvm-tools 1.98.1 component, cargo-semver-checks 0.50.0 and cargo-mutants 27.1.0 (built from source in a builder stage). Write ADR-063 (Proposed) documenting the provisioning decision. Do NOT run provision.py, docker build/pull/run, cargo install or any download in this package.
Model / CLI / effort / reason: GPT-5.6 Luna via codex exec, effort high, sandbox workspace-write. Reason: inventories, hashes, licenses and fixture scripting with the decision already made by the owner.
Owner authorization (2026-09-05): explicit, for exactly these five components and versions; downloads only via provision.py during an explicit provisioning run; never by the MCP runtime.

## Common rules
Repo /Users/cburgosro/Projects/rust-mcp, branch ai/m3-quality. Orchestrator: Claude Fable 5.1. No commits/merges/push/installs/downloads/docker. Other workers edit disjoint files concurrently: touch ONLY `fixtures/rust-runtime/**` and `docs/adr/ADR-063-m3-guest-plugin-provisioning.md` (verify 063 is free by listing docs/adr; take the next free number if not and say so). Never run `git add/commit/stash/checkout/reset`. Read AGENTS.md, docs/roadmap/m3-quality.md ("Provisioning explícito"), docs/roadmap/m2-m8.md G7, ADR-031 (image identity, calibration), ADR-033, fixtures/rust-runtime/{README.md,sources.json,provision.py,verify.py,Dockerfile} fully, docs/validation/M2-image-config.json, and the M1 provisioning receipts referenced by README.md. Also read docs/validation/m3-delegation/R01-plugins/report.md and the fetched official sources under /private/tmp/claude-501/-Users-cburgosro-Projects-rust-mcp/df0fc04b-d468-46d9-b6f0-e6ac1541e3c8/scratchpad/orch/pkg-R01-plugins/sources/ (releases-summary.json has asset names, sizes and `sha256:` digests from the GitHub releases API; *-Cargo.toml.txt have upstream license fields; rust-llvm-tools-sha.txt is the official .sha256 sidecar; index.txt lists URL + HTTP status + UTC).

## Facts to encode (revalidate against those files; do not invent)
| component | version | asset / source | SHA-256 | size | license |
| cargo-nextest | 0.9.143 | https://github.com/nextest-rs/nextest/releases/download/cargo-nextest-0.9.143/cargo-nextest-0.9.143-aarch64-unknown-linux-gnu.tar.gz | 2a64b3566a92508550a7ab29c3e8db25472ca37730ecb4d22100b6aa440c2a68 | 11243965 | Apache-2.0 OR MIT |
| cargo-llvm-cov | 0.9.0 | https://github.com/taiki-e/cargo-llvm-cov/releases/download/v0.9.0/cargo-llvm-cov-aarch64-unknown-linux-gnu.tar.gz | 9af53b273e50d01d8bde8785de8541f6738cc4375248cd7683aec8b5768b9d21 | 1613151 | Apache-2.0 OR MIT |
| llvm-tools | 1.98.1 | https://static.rust-lang.org/dist/llvm-tools-1.98.1-aarch64-unknown-linux-gnu.tar.xz (sidecar .sha256 fetched 2026-09-05) | caaf950c65f3e428247dbe9c173d142b7072b2134962a61924c01e39f6b6dc1e | 32493908 | Apache-2.0 WITH LLVM-exception (verify notices inside the tarball at provisioning) |
| cargo-semver-checks | 0.50.0 | https://github.com/obi1kenobi/cargo-semver-checks/releases/download/v0.50.0/cargo-semver-checks-aarch64-unknown-linux-gnu.tar.gz | e35f435ea322659381f52e7034bb4f0470108f5b267d29f13cf08152fa4af29b | 7866573 | Apache-2.0 OR MIT |
| cargo-mutants | 27.1.0 | source build: `cargo install cargo-mutants --version 27.1.0 --locked` in a builder stage (crates.io during explicit provisioning only) | binary SHA-256 recorded at provisioning; crate checksum from the registry index recorded by provision.py | MIT |
Verify the exact GitHub download URL pattern from releases-summary.json (`html_url`/asset names) rather than trusting the table.

## Deliverables
1. `fixtures/rust-runtime/sources.json`: add typed entries for the five components (kind: `release-archive` | `rust-component` | `source-build`), with url, sha256, size, license SPDX, notice source, expected archive members (the executable path inside the archive, e.g. `cargo-nextest`, `cargo-llvm-cov`, `cargo-semver-checks`; for llvm-tools the standalone installer layout `llvm-tools-1.98.1-aarch64-unknown-linux-gnu/install.sh` and component `llvm-tools-preview` containing `lib/rustlib/aarch64-unknown-linux-gnu/bin/{llvm-cov,llvm-profdata}`), and the fixed destination paths in the guest: plugins under `/opt/rust/bin/` (so `cargo` finds `cargo-<name>` — verify that PATH-less resolution works: cargo resolves external subcommands by searching `$CARGO_HOME/bin` and PATH; our gateway builds PATH explicitly, so document the exact PATH/env the gateway must set) and llvm-tools installed via its install.sh into `/opt/rust` (sysroot) so `cargo llvm-cov` finds `llvm-profdata`/`llvm-cov` via `rustc --print sysroot`; document `LLVM_COV`/`LLVM_PROFDATA` env as the fixed fallback.
2. `fixtures/rust-runtime/provision.py`: extend to download/verify the new kinds (SHA-256 + size), refuse anything unexpected in the build context, record the cargo-mutants source-build inputs (crate version, registry index checksum if obtainable offline from the downloaded .crate, build log hash), and keep the existing Rust flow byte-compatible. Add `--plugins`/manifest gating so the old M1 image can still be rebuilt without plugins if ever needed (keep it simple: a separate output tag/dir for the M3 image).
3. `fixtures/rust-runtime/Dockerfile`: multi-stage: `prepared` (unchanged) → `plugins` stage that verifies SHA256SUMS, extracts release archives to `/opt/rust/bin`, runs the llvm-tools installer with `--prefix=/opt/rust --disable-ldconfig`, and a `builder` stage that runs `cargo install cargo-mutants --version 27.1.0 --locked --root /opt/plugins` (network allowed only in `docker build`, never in the runtime container), then the final `FROM scratch` stage copying only the filesystem (no cargo registry cache, no build dirs), same user 65534:65534, same env PATH, same WORKDIR. Ensure `/opt/rust/bin/cargo-mutants` is present and that no package manager, installer or network tooling remains.
4. `fixtures/rust-runtime/verify.py`: extend to run, in bounded read-only network-none containers, fixed-path version probes for all plugins (`/opt/rust/bin/cargo-nextest --version`, `/opt/rust/bin/cargo llvm-cov --version` with env `PATH=/opt/rust/bin:/usr/bin:/bin`, `/opt/rust/bin/cargo-semver-checks --version` or `cargo semver-checks --version`, `/opt/rust/bin/cargo-mutants --version`, `llvm-profdata --version`, `llvm-cov --version` at their fixed sysroot paths), record SHA-256 of each executable, `ldd`-style missing-library check (use `/usr/bin/ldd` if present in the image or document its absence), dpkg list, and license/notice files captured into the receipt. Require exactly the expected component set.
5. `fixtures/rust-runtime/README.md`: document the M3 additions, the owner authorization, the commands, and that a new image ID results and that ADR-031's approved identity must be amended after calibration.
6. `docs/adr/ADR-063-m3-guest-plugin-provisioning.md` (Proposed): Context (plugins absent; owner authorization; upstream facts), Decision (exact versions/assets/hashes/licenses; gnu vs musl choice = gnu on the Debian glibc base; source build for mutants; fixed paths; env; identity recording; SBOM/notices), Alternatives (musl, host binaries, runtime install, defer mutants), Consequences (new image ID, gateway APPROVED_RUST_IMAGE constant change + configuration fingerprint + recalibration, all Docker gates re-run, M2 receipts preserved), Sources.
7. Run `python3 -m py_compile` on the two scripts and `python3 -B scripts/test-fixtures.py` if it covers fixtures/rust-runtime (check first; do not run Docker). Report results.
Delivery: Task, Result, Files changed (+SHA-256), Tests executed, Evidence, Risks, Decisions, Open issues (e.g., anything you could not verify offline, such as archive member layout — state exactly what the provisioning run must check).

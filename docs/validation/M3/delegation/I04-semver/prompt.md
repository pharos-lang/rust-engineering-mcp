# Package I04 — M3-04 `rust.semver.check`: two authorized captures, dual read-only mounts, calibrated outcomes (adapter + application + MCP worker)

Task / ID: I04 (M3-04). Objective: implement `rust.semver.check` end to end per ADR-062 (Accepted): input = two live ProjectRefs (baseline, candidate) captured in stable order with distinct snapshot identities; a gateway extension with a second read-only volume `/baseline` and second ingest phase; closed `cargo semver-checks check-release --baseline-root /baseline --manifest-path /source/Cargo.toml` argv with identical selection on both sides, `GIT_DIR=/nonexistent`, `GIT_CEILING_DIRECTORIES=/`, network none; outcome taxonomy calibrated against the pinned 0.50.0 binary (exit 0/100/101 hypotheses → calibrate; warn-only; exit 100 with zero parsed findings → Blocked); coarse authoritative summary + bounded golden-tested text parser (the pinned binary exposes NO machine-readable findings flag: see docs/validation/m3-provisioning/help/cargo-semver-checks-check-release-help.stdout) with raw output retained as an artifact; `JobKind::SemverCheck`; DTO/schema/snapshot (tool 21); Docker runtime tests over the fixture families; docs/tools.md section.
## Common rules
Same as I03 (repo, branch, no git/docker/network unless the launch record grants Docker, invariants, read ADR-062/060/061/063 and the M3-01/M3-03 landed code). Follow the nextest vertical patterns.
## Ownership
New: crates/domain/src/semver_check.rs (+mod line), crates/domain/tests/semver_check.rs, crates/application/src/semver_check.rs (+mod; `ProjectSemverPort`, `SemverOptions`, `SemverObservation`; the application use case captures baseline then candidate via `ProjectRegistry::source_inner`-style calls, revalidates both, rejects mismatched selections before execution), crates/application/tests/semver_check.rs, crates/execution-adapter/src/semver_gateway.rs, semver_output.rs (bounded text parser; force no color via `--color never` if present in help or `NO_COLOR=1`), semver_port.rs, tests/semver_runtime.rs (Docker, ignored), crates/mcp-server/src/stdio/semver.rs (+tests), tests/snapshots/semver-tool.json, fixtures/semver/{identical,removed-pub-fn,trait-method-default,trait-method-no-default,enum-non-exhaustive,enum-exhaustive,feature-gated,no-lib,broken-baseline} as baseline/candidate pairs with hand-written Cargo.lock (do not cargo-run on host), docs/tools.md section, docs/validation/M3-matrix.md row M3-04, docs/validation/M3-04-semver-calibration.md (the calibration protocol results: observed exit codes, help outputs, parser goldens). Minimal edits: crates/domain/src/rust_execution.rs (variants `SemverCheck(SemverOptions)`, `SemverChecksVersion`), crates/execution-adapter/src/rust_gateway.rs (second `Volume`, `Phase::IngestBaseline`, multi-mount `arguments()`, fingerprint enumeration; keep every existing argv byte-identical — add a test asserting the M1 argv snapshots are unchanged), crates/execution-adapter/src/lib.rs, crates/application/src/job.rs (SemverCheck arm), crates/mcp-server/src/stdio.rs (tool 21), tests/protocol.rs (21). Do not touch nextest/coverage/mutation files beyond these arms.
## Requirements
Both roots read-only; only /work writable; verify at calibration where rustdoc JSON/build output is written for `--baseline-root` (CARGO_TARGET_DIR=/work/target respected?) — if the tool needs a writable baseline dir, copy the baseline into a tmpfs under /work in a fixed phase (never the host) and record it; lib target missing detected from the captured ProjectStructure before running → Unavailable; findings: item/lint/level/required_update/span best-effort with completeness Partial; exit-code mapping pinned only after calibration; report per-side feature set/limitations; Stage 0/Stage 1 artifact publication of the raw output and the parsed summary; budgets per ADR-060.
## Tests
Unit: parser goldens from real captured outputs (record them as fixtures after the calibration run), argv snapshots, selection mismatch rejection, ordering/revalidation (baseline before candidate; changed ref → invalidated), no-lib detection. Docker (ignored, exact names): each fixture family with the expected outcome (breaking vs no-break vs Unavailable), warn-only, `.git` inside /source never discovered, network attempt path (a baseline needing registry) → recognisable failure not hang, cancel/EOF with active child, both roots immutable. Focused gates + architecture check; existing snapshots byte-identical.
Delivery: standard sections with SHA-256 and counts; the calibration document is mandatory evidence.

## Shared-file discipline for this launch (important)
Sol (integrator, package I02b) is concurrently editing crates/application/src/job.rs, crates/execution-adapter/src/rust_gateway.rs (seccomp profile mapping), crates/mcp-server/src/stdio.rs, stdio/nextest.rs, stdio/tasks.rs, main.rs and the quality-store wiring; Claude Opus finished the store (do not touch quality_artifact* files). Build your vertical in YOUR new files first so they compile standalone (own modules, own tests). Only at the END add the minimal arms/lines to the shared files (`rust_execution.rs` variants, `rust_gateway.rs` argv/env arm + fingerprint entry, `job.rs` JobKind arm, `stdio.rs` registration, `protocol.rs` count), re-reading each shared file immediately before each patch and patching only the lines you add. Do NOT run `cargo fmt` on the workspace; format only your files. You have NO Docker access in this session: write the Docker tests as `#[ignore]` with exact names and expected oracles; the integrator/validator runs them with the image afterwards; parser goldens that need real tool output are produced in a follow-up calibration session — design the parser against the pinned `--help` text and the official docs now, with clearly labelled provisional fixtures. Model/CLI/effort per the launch record.

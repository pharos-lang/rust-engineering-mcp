# Package W8 — Post-merge smoke and the M3 integration receipt on `main` (validator delegate; sole Docker owner)

PR #14 is **merged**. The owner authorized the merge with an admin bypass, because CODEOWNERS required a review from the same account that authored the PR and GitHub does not permit self-approval; `enforce_admins` was disabled, so the bypass was the only available mechanism. Record that fact accurately — it is not a check that was skipped, it is a review requirement that was structurally unsatisfiable.

Facts to verify yourself before writing anything:
- Merge commit `57c40373597541ac3d57bc8446ec4e2e598b904e` on `main`, merging `ai/m3-quality` (tip `93991c4cf2994976093d6a28bd912341d8124722`, preserved on the remote).
- The five required checks were green on that tip: the three portable builds, supply chain and SonarCloud. CodeQL is not a required check and its 33 alerts were assessed as false positives (the "nonce" is the Docker ownership label; the two logging alerts are inside the redaction test).
- The consolidated evidence was re-established on those exact bytes in package W7: core gate 14/14, full gate 25/25 (with M3 runtime 62/62 and Rust security 20/20 inside it), 810 inputs, `source_inputs_unchanged: true`.

## Work
1. **Confirm the merge changed no qualified byte.** The local `main` now contains the merge commit. Compare the tree of `main` against the branch tip: `git diff --stat 93991c4 main` must be empty. If it is not, stop and report exactly what differs — everything below depends on this.
2. **Post-merge smoke on `main`, proportional to that result.** If the trees are identical, the expensive gates do not need repeating and repeating them would only prove the same bytes twice; run instead: `cargo fmt --check`, `cargo check --workspace --all-targets --locked --offline`, `cargo clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline`, `python3 -B scripts/check-architecture.py`, plus one real runtime confirmation that the server on `main` still works end to end: the M3 runtime Docker gate limited to a small representative selection (one nextest, one coverage, one semver, one mutation, one Tasks lifecycle case) rather than all 62. Say explicitly in the receipt that the full 62 were qualified pre-merge on identical bytes and which subset you re-ran after it. If the trees differ for any reason, run the complete M3 runtime gate instead.
3. **Docker hygiene** afterwards: zero containers and zero volumes labelled `org.rust-mcp.execution=true`, with the command output.
4. **Write `docs/validation/M3-integration.json`** (it may already exist from the pre-merge package — extend it, preserving what it records, or write the post-merge section into it): the branch tip and every commit hash with its subject, the PR number and URL, the final check matrix with each check's conclusion, the merge commit, the fact and reason of the admin bypass, the post-merge smoke commands with exit codes and counts, the receipt hashes of the pre-merge gates, and the SHA-256 of the sanitized/excluded evidence already recorded there.
5. **Close the milestone records on `main`**: `docs/validation/M3-07.md` gains the integration section (merge commit, date, what was merged, the bypass, the post-merge smoke); `docs/validation/M3-matrix.md` moves the integration row to Done; `docs/implementation-status.md` moves M3 to Done with the merge commit and drops any "pending integration" wording. Also record the two open items that survive the milestone so they are not lost: the unreproduced Linux CLI test failure (documented in commit `93991c4`, needing a Linux reproduction before anyone touches the oracle) and the recommendation to dismiss the CodeQL false positives and rename the `nonce` ownership label in its own cut.
6. **Commit on `main`** as a documentation-only commit, with the repository's message style and this session's two attribution lines, and push it to `origin/main`. That push is authorized: it is the integration receipt the owner asked for. Do not tag, do not release, do not touch the branch.

## Rules
Authorized: edits, `git add`/`commit` on `main`, `git push origin main` for this receipt commit only, `gh` reads, and the smoke commands above. Not authorized: tagging, releasing, crates.io, force-push, history rewriting, deleting the branch, repository settings. Sole Docker owner; never leave a gate running unattended — poll from inside this session until it exits. Never weaken an assertion or credit a skip as a pass. Everything you assert must come from a command you ran.
Delivery: Task, Result, the tree comparison, the smoke commands with exit codes and counts, Docker hygiene evidence, the receipt path and hash, the documents updated, the surviving open items, Risks, Decisions, Open issues.

# Package A17b — Revise ADR-061 per independent review (same Terra session, resumed)

Your ADR-061 draft (SHA-256 25afe84d20ed52b830e9eec400802125db8d31cfb847d967a25abbda74324f0e) received an independent Opus 5 review: read it fully at docs/validation/m3-delegation/V17-adr061-review/last-message.md (SHA-256 b06bbfaafd6026692f6f767d73450e2eb114c4b4f438dce843ef82f364360a00). Verdict: Revise. The orchestrator has disposed every finding below; apply them by editing ONLY docs/adr/ADR-061-private-quality-artifact-store.md. No other file. Keep Status "Proposed — revised after review V17; pending orchestrator acceptance".

Owner decisions since your draft (authoritative): the owner accepted the persistent private store posture for M3 (SECURITY.md/client-configuration will be updated by the docs worker); the owner authorized guest provisioning of nextest/llvm-cov/llvm-tools/semver-checks/mutants. So remove the "does not authorize enabling persistence until the owner accepts" gating; replace it with "accepted posture; enabling still requires the native qualification oracles below".

Dispositions (apply all "fix now"):
- F1 fix now: define a pre-admission free-space check via fstatfs on the state-root handle: `free >= requested_reservation + M2_RECOVERY_HEADROOM (49 MiB: 48 MiB staging + 1 MiB metadata, cite mutation.rs constants) + QUALITY_CONTROL_HEADROOM (name it, propose 16 MiB)`; add the oracle "a maximal quality reservation never pushes the mutation store below its recovery headroom; an M2 commit still succeeds afterwards".
- F2 fix now: state the boundary honestly. Drop the state secret (it adds nothing against a same-uid reader). owner_binding = domain-separated SHA-256 over (state-root device+inode, host uid, granted root device+inode, project workspace root string). Consequence stated plainly: any later stdio session run by the same uid with the same state root and a live host grant for the same root can read retained artifacts (same model as M2 receipts/replay under ADR-059 "autoridad viva"); a different root, uid or state root cannot. Add oracle "same root, two sessions: second session with a fresh live ProjectRef for the same root reads; a session with a different granted root gets Resource not found". Note SECURITY.md must state this.
- F3 fix now: replace "physically preallocated and verified" with the honest APFS reservation: fstatfs check (F1) + best-effort rustix `fallocate` on the reservation file documented as not a hard guarantee + fail closed on ENOSPC/short write at every write and at publication (no descriptor, reservation released). Truncate-to-actual happens after descriptor publication (or state the accounting consequence explicitly). No new `libc` dependency.
- F4 fix now: narrow reuse: (a) extract the fixed-child helper from `prepare_mutation_state` (parent, child_name); (b) introduce a new `pub(crate)` handle-relative primitive module (openat/renameat/unlinkat/fsync discipline, streaming write with exact byte cap, generic error) that both `StateRoot` and the quality adapter use; acceptance oracle: all existing M2 mutation tests pass unchanged and journal bytes are byte-identical.
- F5 fix now: hybrid clock: durable wall-clock bound only for cross-restart expiry; in-session monotonic deadlines; wall-clock may only shorten, never lengthen, an in-session TTL; the watermark detects regression.
- F6 fix now (scoping + stub): the fail-closed block is scoped to quality artifacts only (M1 Resources, M2 commit/receipt, the 18 tools unaffected — add oracle); operator recovery is an explicit CLI subcommand (name it, e.g. `quality-artifacts recover|prune`, authorization: host operator running the CLI with the state root; may delete only quarantined/expired quality objects, never M2 state); its implementation is M3-01 work.
- F7/F14 fix now (joint with ADR-062): multi-file reports (coverage HTML, mutants output) cross the boundary as ONE bounded archive blob produced by a fixed guest program with closed argv over a fixed guest directory (M2 Export pattern: `tar` on stdout), decoded and revalidated on the host with the closed USTAR profile of crates/execution-adapter/src/mutation_archive.rs (regular files/dirs only, bounded count/size/name length, no links, no `..`), re-encoded canonically and stored as a single member of new kind `ArchiveBundle` (MIME `application/x-tar`, sensitivity per content, never previewed, never extracted onto host paths; member names are data). Single-file artifacts (JUnit XML, coverage JSON, LCOV, outcomes.json, diff text) remain regular fixed-path members. Remove the "declared report root" wording.
- F8 fix now: reuse ADR-060's `JobId` (`job_` + 32 hex) for job identity; artifact IDs stay `qart_`; drop `qjob_`/`qjr_` as separate identity vocabularies (reservation files may be named by JobId).
- F9 fix now: a retained `qart_` ID plus a freshly authorized live ProjectRef for the same root IS the re-access path (ID = locator, binding revalidation = gate); the client may construct the URI. TTL maximum recalibration deferred to fixture measurement (mark).
- F10 fix now: narrow the claim to status/error-variant/count/enumeration indistinguishability; note 128-bit unguessable IDs; no constant-time promise.
- F11 fix now: `store.lock` = exclusive non-blocking flock held across admission/reservation/publication/reconciliation (M2 pattern mutation-store.lock); contention → bounded busy rejection; oracle with two concurrent processes.
- F12 fix now: the quality store is never passed to `reap_artifacts`/`retain_owners`; an authorized quality read does NOT renew the ProjectRef idle lease.
- F13 fix now: staged adoption inside M3 (no owner gate): Stage 0 = when no state root is configured or the durable store is unavailable, quality jobs publish bounded JUnit/log evidence through the unchanged M1 in-memory store with explicit `completeness: Truncated` and omission flags; Stage 1 = durable store is the default whenever a state root is configured and the native qualification passed. Both stages are M3 scope.
- F15 fix now: fix index page max members (propose 64) and max cursor bytes (propose 128); state the base64 arithmetic for 320 KiB.
- F16 fix now: guest artifact name is a closed enum over the fixed-path table; `source`/`runtime` are closed typed structs (list fields); `payload_format_version` closed per kind.
- F17 fix now: add the seven missing oracles (ENOSPC mid-stream releases reservation; same-root two sessions; two concurrent processes; quality reservation does not break M2; M1 regression byte-for-byte; Linux/Windows UnsupportedPlatform before any guest output; crash points between blob rename→descriptor rename and descriptor rename→dir fsync).
- Contradiction 6 (G3): budgets table gets columns Unit | Phase | Default | Maximum | Exceed behaviour | Oracle.
- L11 divergence: record explicitly that eviction is rejected (reject-before-produce instead) as a divergence from the traceability row.
- Missing decisions: add an `## Open issues` section covering: secret dropped (state why), `SecretSuspected` semantics (decide: retention refusal unless host grants PotentiallySensitive retention), closed `source`/`runtime` shapes finalized in M3-01 domain design, TTL maximum measurement.
Delivery: final message with Task, Result, Files changed (+SHA-256), Tests executed (`git diff --check` on your file only), Evidence (map each finding ID → section/line where addressed), Risks, Decisions, Open issues.

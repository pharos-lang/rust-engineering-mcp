# Package W5 — M3-06: consolidated gate on final bytes, inventory, matrix and handoff (integrator Sol, resumed; sole Docker owner)

Everything else is done: the four quality tools are implemented and Docker-qualified (M3 runtime 55/55 plus the M3-02 selections you added, Rust security 20/20), the security review dispositions are applied, the decisions are recorded in ADR-060 through ADR-065, and the public documents were corrected against the receipts. This package closes the milestone.

## Work
1. **Consolidated gate on the final bytes.** Run, in this order and one at a time, `python3 -B scripts/check-architecture.py`, `python3 -B scripts/gate.py core`, and then `python3 -B scripts/gate.py full` with the host variables `docs/ci.md` documents: `RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock`, `RUST_MCP_E5_DIR=/private/tmp/rust-mcp-e5-m009/onnx`, `ORT_LIB_LOCATION=/Users/cburgosro/Library/Caches/ort.pyke.io/dfbin/aarch64-apple-darwin/612739f75438dc0a075461e1fb454226b4a1eb175e60a7271ba966bbbb972cd4`. Verify each path exists before you start and say so. Save the reports with `--report` into `docs/validation/M3-core-gate.json` and `docs/validation/M3-full-gate.json`; preserve any failed attempt as `-attemptN.json`. The full gate must include every M3 stage you added to the runtime script; if `gate.py` does not yet invoke the M3 runtime stage, add it the way the M2 stage is wired, and say exactly what you added. Do not duplicate a stage that the receipt already accredits, and do not weaken any stage to make it pass.
2. **Audit and deny.** They run when installed and configured (`cargo-audit 0.22.1`, `cargo-deny 0.19.7` are on this host). If a stage they own fails, fix the cause or report it as a blocker — an absence in a mandatory gate blocks the milestone.
3. **Source inventory.** Record the exact input inventory of the final bytes the way M2 did (`docs/validation/M2-full-gate.json` shows the shape): counts, hashes, and the assertion that the inputs are identical at start and end of the gate. Add the plugin/tool inventory for M3: guest image ID and config digest, the five provisioned components with versions, SHA-256 and licences, and the host tool versions used by the gates.
4. **Matrix and status.** Bring `docs/validation/M3-matrix.md` to its final state: every row of M3-01..M3-06, every decision D06/D17/D18/provisioning, and G1–G9, each with its real status and a link to the evidence that proves it. Any row that is not satisfied must say so plainly with the reason. Update `docs/implementation-status.md`'s M3 rows to match (the documentation worker already rewrote the surrounding text; keep its wording and change only what the final gate changes).
5. **Handoff.** Write `docs/validation/M3-07.md` — the repo-visible closure document, modelled on `docs/validation/M2-07.md`: real state per cut; branch and the commits once they exist; the map of jobs, plugins, formats and Resource schemes; the decisions and their ADRs; commands, hashes and counts for every gate; the client and native matrix; the reviews and their dispositions; residual risks; rollback; and the explicit limits (what M3 does **not** promise: no release, no new platform, no durable execution across restart, Tasks status exactly as qualified, coverage/semver/mutation limits, quotas and TTLs as measured). It must be readable by someone who did not watch any of this work.
6. Re-run the workspace gates at the very end (`cargo fmt --check`, check, clippy, test, architecture) so the final bytes are the qualified bytes, and record Docker hygiene.

## Rules
Branch `ai/m3-quality`; **no commits, merge, push, tag or release** — the integration package handles Git, and only with the owner's authorization. No installs, downloads or image changes. Sole Docker owner; never leave a gate unattended. Do not restate a number you did not observe in this package: cite the receipt that carries it. If a gate fails and the cause is a real defect, fix it and re-run; if it cannot be fixed here, stop and report the reproducible condition, what it blocks and what it would take.
Delivery: Task, Result (per gate, with counts and durations), Files changed with SHA-256, Tests executed (command, exit code, counts, receipt path), the final matrix summary, Docker hygiene evidence, Risks, Decisions, Open issues.

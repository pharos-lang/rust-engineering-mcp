# Package I01 — rust.test.nextest execution adapter: closed command, JUnit parser, artifact egress (Claude Sonnet 5 worker)

You are a Claude Sonnet 5 worker explicitly assigned to implementation. Task / ID: I01 (M3-01 adapter). Objective: implement the execution side of `rust.test.nextest`: a closed `RustCommand` variant and gateway phases that run cargo-nextest inside the approved guest (new image being provisioned concurrently; until its ID is known, keep the image identity behind the existing constant and mark Docker tests `#[ignore]` to be run by the gate with the new ID — the integrator will update `APPROVED_RUST_IMAGE`), a bounded JUnit XML parser (no DTD/entities), typed nextest domain results, and the egress of JUnit/logs as artifact byte streams per ADR-061 (fixed guest paths).
## Common rules (every M3 package)
- Repo /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (base main 52396184). Orchestrator: Claude Fable 5.1. You do not delegate, commit, merge, push, install, download, pull images or change architecture. Never run `git add/commit/stash/checkout/reset`; never `cargo fmt` the workspace (format only your own files with `rustfmt --edition 2024 <file>`); never edit files outside your ownership list. Several workers edit the same checkout concurrently: re-read a shared file immediately before patching it and patch minimally (one-line `pub mod` additions only where allowed); if `cargo check` fails in a module you do not own, report it and keep going on yours — do not fix others' code.
- Accepted decisions (read fully): docs/adr/ADR-060-bounded-job-execution-and-mcp-tasks.md (D06), docs/adr/ADR-061-private-quality-artifact-store.md (D17), docs/adr/ADR-062-coverage-accounting-and-semver-baselines.md (D18), docs/adr/ADR-063-m3-guest-plugin-provisioning.md (provisioning). Normative: AGENTS.md; docs/roadmap/m3-quality.md; docs/roadmap/m2-m8.md G1–G9; ADR-028/030/031/032/037/040/050..059; docs/architecture.md; docs/security-model.md. Map of interfaces with line refs: docs/validation/m3-delegation/S00-baseline/report.md §5.
- Invariants: hexagonal (domain/application free of rmcp/Cargo/SQLite/LanceDB/Docker/std::process/tokio); typed Serde/Schemars; one closed gateway; host authority only; hostile project code; network deny enforced; handle-relative no-follow own I/O; cancel/EOF/timeout/expiry terminate+join before capacity release; uncertain cleanup dominates; poll/cancel never take the job permit; unknown/partial/unavailable/skip never pass; the 18 existing tool schemas/snapshots byte-identical; no `unwrap/expect/panic!` on normal paths; tests first (discriminating, with positive controls); mocks never replace Cargo/Docker/filesystem boundaries.
- Verification you must run for your crates: `cargo check -p <crate> --all-targets --locked --offline`, `cargo clippy -p <crate> --all-targets --locked --offline -- -D warnings`, `cargo test -p <crate> --locked --offline` (non-Docker), plus `python3 -B scripts/check-architecture.py`. Report exact commands, exit codes, counts.
- Delivery (final message): Task, Result, Files changed (+SHA-256), Tests executed, Evidence, Risks, Decisions, Open issues. Return needs for other specialists to the orchestrator.
## Ownership (create/edit ONLY these)
- `crates/domain/src/nextest.rs` (new: `NextestOutcomeCounts`, `NextestTestRow` bounded, `NextestExit` closed exit-code mapping calibrated later: 0 pass, 100 failures, 101 runner/build failure, 104 timeout/cancel are HYPOTHESES from R01 to be calibrated against the pinned 0.9.143 — design the type so calibration can pin them) + one `pub mod nextest;` line in crates/domain/src/lib.rs; domain tests `crates/domain/tests/nextest.rs`.
- `crates/domain/src/rust_execution.rs`: add ONLY a new variant `RustCommand::TestNextest(NextestCommandOptions)` (closed options mirroring the selection grammar; profile name fixed; no free args) — keep all existing variants and their argv byte-identical.
- `crates/execution-adapter/src/nextest_gateway.rs` (new: phases for the nextest run — ingest source (existing), ingest a PRODUCT-OWNED nextest config archive into the writable `/work` tmpfs (preferred; do not require an image change) containing a `nextest.toml` profile `rust-mcp` with junit path fixed, `fail-fast = false`, `retries = 0` (retries only when the caller requests: keep a closed option `retries: 0..=2`), `slow-timeout`/`leak-timeout` fixed and documented, `store-failure-output = true`; run `/opt/rust/bin/cargo nextest run --config-file /work/nextest/nextest.toml --profile rust-mcp --frozen --offline --color=never --no-fail-fast --message-format ... ` with the closed selection args and `--jobs 1`/`--test-threads 1` as the existing test command does; then an export phase that emits the JUnit file at the fixed path `/work/target/nextest/rust-mcp/junit.xml` (verify the exact path nextest uses relative to `target/nextest/<profile>/`) over stdout via `/usr/bin/cat` or the closed `tar` export pattern; capture stdout/stderr bounded), `crates/execution-adapter/src/nextest_junit.rs` (new bounded streaming JUnit parser: reject DTD/ENTITY/external refs before expansion, fixed limits on depth/attributes/text/testcases; extract testsuite/testcase pass/fail/skipped/flaky/rerun/leak markers per the nextest JUnit docs in /private/tmp/claude-501/-Users-cburgosro-Projects-rust-mcp/df0fc04b-d468-46d9-b6f0-e6ac1541e3c8/scratchpad/orch/pkg-R01-plugins/sources/nextest-junit.txt), `crates/execution-adapter/src/rust_gateway.rs` minimal additions for the new command's argv/env (env: `PATH=/opt/rust/bin:/usr/bin:/bin` explicit so cargo finds cargo-nextest; `NEXTEST_*` none unless documented), `crates/execution-adapter/src/lib.rs` mod lines; implementation of Sol'"'"'s `application::nextest::ProjectNextestPort` in `crates/execution-adapter/src/nextest_port.rs` (new) — read `crates/application/src/nextest.rs` as soon as it exists (Sol is writing it concurrently in package I00; if absent when you start, implement the adapter against your own local trait mirroring `ProjectTestPort` and note the rename needed).
- Tests: unit tests for the parser with hostile inputs (billion laughs, external entity, deep nesting, huge attribute, forged markers, flaky/rerun/leak samples from the docs), unit tests for argv closure (snapshot of argv per option), and Docker ignored tests in `crates/execution-adapter/tests/nextest_runtime.rs` (pass/fail/ignored/retry/leak/no-tests/doc-only fixtures; active cancellation with an observed child; output flood; source immutability; cleanup) that the gate will run once the image ID exists. Use fixtures under `fixtures/nextest/*` that Luna is creating concurrently (package F01: `fixtures/nextest/{passing,failing,ignored,flaky,leaky,doc-only,no-tests,hostile-output}`); if a fixture is missing when you test, write the test against the agreed path and report.
Not yours: domain/job.rs, application/job.rs, stdio/*, artifact store, docs, scripts, fixtures (except reading).
## Oracles
Counts come ONLY from the parsed JUnit (never from human text); doctests are not run by nextest — state it in the observation (`doctests: NotRun`); exit-code hypotheses are marked uncalibrated until the Docker calibration test records them; a missing plugin or wrong version → `Unavailable` before running; parse failure → `Incomplete`, never passed; timeout → blocked with partial evidence; the JUnit bytes and both logs are returned as bounded streams with truncation flags.
Tools: Read/Grep/Glob/Edit/Write within your ownership; Bash limited to cargo/rustfmt/shasum/ls/python3 -B scripts/check-architecture.py. No git, no docker.

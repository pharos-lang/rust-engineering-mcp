# Package I04b — Continue M3-04 `rust.semver.check` (Claude Sonnet 5 worker, fresh session; answers the I04 pause)

Your previous session (package I04; its report: docs/validation/m3-delegation/I04-semver/last-message.md) paused because `crates/execution-adapter/src/rust_gateway.rs` gained `RustCommand::SemverCheck`/`SemverChecksVersion` argv arms you did not write. Orchestrator answer: NO other agent is assigned M3-04. The most likely cause is that the new `RustCommand` variants you added early to `crates/domain/src/rust_execution.rs` broke exhaustive matches in the shared gateway while the integrator (Sol, package I02b) or the coverage worker (Terra, I03) were compiling the workspace, and one of them patched the match arms to keep building — using your domain API. That is not your work being duplicated; it is a compile-unblock by a co-worker. Proceed as follows:
1. Take exclusive ownership of the semver arms in rust_gateway.rs from now on: read what is there, keep it if it matches ADR-062 §8 (closed argv `check-release --baseline-root /baseline --manifest-path /source/Cargo.toml`, identical selection both sides, `GIT_DIR=/nonexistent`, `GIT_CEILING_DIRECTORIES=/`, `--color never`/`NO_COLOR=1` per the pinned help at docs/validation/m3-provisioning/help/cargo-semver-checks-check-release-help.stdout, no network, second read-only `/baseline` volume via `Phase::IngestBaseline`), otherwise correct it. Record in your report what you found vs. what you changed.
2. Continue and finish the whole I04 scope exactly as the original package (docs/validation/m3-delegation/I04-semver/prompt.md): domain/application/adapter/parser/port/MCP tool 21/snapshot/protocol count/fixtures/docs/M3-matrix row/calibration document skeleton, with the shared-file discipline (re-read shared files right before each minimal patch; no workspace `cargo fmt`; no Docker; Docker tests `#[ignore]` with exact names).
3. Concurrency reality: Sol (I02b) is editing job.rs/stdio.rs/rust_gateway.rs (seccomp mapping)/main.rs/nextest files; Terra (I03) is adding coverage arms. Expect transient compile breakage from files you do not own; retry every 2 minutes up to 20 minutes and keep working on your files. If a co-worker's edit conflicts with yours in rust_gateway.rs, prefer the version that keeps every existing M1/M2 argv byte-identical and report the conflict.
4. Tool 21 registration order: after tool 20 (`rust.coverage`, being added by Terra). If `rust.coverage` is not yet registered when you get there, register `rust.semver.check` after `rust.test.nextest` and note that the integrator will fix the final order and counts; protocol tests must be written against the ordered inventory helper, not hard-coded counts, if the existing test structure permits.
Delivery: standard sections with SHA-256 and test counts; explicitly list every shared-file hunk you added or changed.

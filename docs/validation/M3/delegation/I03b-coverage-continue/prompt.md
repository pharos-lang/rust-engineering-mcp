# Package I03b — Complete M3-03 `rust.coverage` end to end, including Docker qualification (Terra, resumed; expanded ownership)

Your I03 delivery is accepted as a partial (domain/application contracts with the zero-denominator rule, bounded LLVM JSON parser with shared-file dedupe, closed `RustCommand` variants and argv, tool 20 registered but fail-closed, fixtures, ignored Docker tests, docs/matrix marked Blocked). It stopped because the coverage egress at `/work/coverage` needed the applied-mount verifier extended, which was outside your ownership. **That ownership is now granted**, and the blocker is largely solved for you: while you were stopped, the integrator (Sol, packages I02/I02b) landed the whole M3-01 vertical.

## What landed and that you MUST reuse (read before writing code)
- `crates/execution-adapter/src/nextest_gateway.rs`: bounded report volume + fixed-argv export container (replaces `docker cp`), config ingest before hostile source, decoder accepting only the expected regular file.
- `crates/execution-adapter/src/rust_applied.rs`: the applied-mount verifier as Sol extended it for the nextest report volume — extend it the same way for the coverage report volume; do not invent a second mechanism, and keep nextest's verification byte-identical (its tests must still pass).
- `crates/execution-adapter/src/seccomp-rust-quality.json` + ADR-064 (Proposed, authorized): the quality seccomp profile adding only the AF_UNIX anonymous stream `socketpair` shape. Coverage runs `cargo test` under instrumentation, so it must use the quality profile too (map the phase → profile exactly as nextest does, and extend the profile-mapping test).
- Production `JobExecutor` wiring, Stage 0/Stage 1 artifact publication, `QualityAuthority` over the live registry, `Resources::with_quality_reader`, and the `quality-artifacts recover|prune` CLI: all live. Publish coverage artifacts through the same integration point nextest uses (JSON + LCOV as regular members, HTML as one `ArchiveBundle` via the closed tar export).
- Read `docs/validation/m3-delegation/I02b-v00-fixes/last-message.md` and `docs/validation/M3-01.md` for the exact patterns, budgets and receipt shapes.

## Common rules
Repo /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (uncommitted). Orchestrator: Claude Opus 5 main session. No commits/merge/push/installs/downloads; no image build/pull/delete. **You are the sole Docker owner in this window** (Docker client `/Applications/Docker.app/Contents/Resources/bin/docker`, socket `unix:///Users/cburgosro/.docker/run/docker.sock`, `RUST_MCP_TEST_SOCKET` = that path, image `sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a`); never run two Docker gates at once. A second worker (Sol) is completing `rust.semver.check` in `crates/{domain,application,execution-adapter,mcp-server}` semver files and will add its own arms to the shared `rust_execution.rs` / `rust_gateway.rs` / `stdio.rs` / `protocol.rs`: re-read every shared file immediately before each minimal patch, patch only your lines, never run workspace `cargo fmt` (format only your files), and if a co-worker's in-flight edit breaks the build, retry every 2 minutes for up to 20 minutes while working on what compiles.

## Required completion (all of it — do not deliver another partial)
1. Real execution path: `cargo llvm-cov --no-report` run phase + three closed `report` phases (`--json --output-path`, `--lcov --output-path`, `--html --output-dir`) against the same profdata, with `CARGO_LLVM_COV_TARGET_DIR=/work/llvm-cov-target`, `LLVM_COV`/`LLVM_PROFDATA` at their fixed sysroot paths, `--frozen --offline`, `--jobs 1`, no `--doctests`, no `--branch/--mcdc`, no caller-supplied ignore regex.
2. Egress: JSON and LCOV as regular fixed-path members; the HTML directory as one `ArchiveBundle` through the closed tar export; the mount verifier covers the report volume; a symlink/device planted at a report path is rejected, never followed.
3. `ProjectCoveragePort` implementation + application use case publishing Stage 0/Stage 1 artifacts through the shared integration point; `JobKind::Coverage` wired to the production executor exactly like nextest; the `LlvmCovVersion` probe and the llvm-tools presence check run before execution (mismatch/absence → `Unavailable`, never a best-effort report).
4. Tool 20 stops being fail-closed by construction: real results when identity matches; `Unavailable`/`Blocked` only for the ADR-062 reasons. Keep the response bounded: full JSON is artifact-only, structured content carries the bounded summary with pagination/omission flags.
5. Docker qualification (you have the socket): run every `#[ignore]`d coverage runtime test explicitly and serially, with the exact-name pattern the gate uses; add the coverage selections to `scripts/test-m3-runtime.py` (Sol created it — extend, do not rewrite) and re-run that gate whole so nextest's 19 selections plus yours pass in one receipt; write the receipt to `docs/validation/M3-03-runtime.json` and a narrative `docs/validation/M3-03.md` (commands, exit codes, counts, image ID, hashes, timings). Preserve any failed attempt as `-attemptN.json`.
6. Oracles that must actually discriminate: known-counts fixture with hand-derived line/region/function numbers (record how you derived them; a first calibration run may establish them only if you label it calibration and then assert them as fixed expectations afterwards); shared file across two packages counted once in the aggregate and visible per package; zero-denominator scope absent from percent-bearing metrics (never 0 %/100 %); the three formats' totals agree because they come from one profdata; plugin/llvm identity mismatch → `Unavailable`; timeout mid-instrumented-build → `Blocked` with partial evidence; cancel/EOF with an observed active child → terminated, joined, zero leftover containers/volumes; hostile HTML (script/external URI) stored as an inert `ArchiveBundle`, never previewed; hostile deep JSON (`fixtures/hostile-reports/coverage-deep.json`) → `Invalid`/`Partial`, never a false clean.
7. Docs: `docs/tools.md` section for `rust.coverage` (contract, semantics, limits, artifacts, sync rule), `docs/validation/M3-matrix.md` row M3-03 updated from Blocked to its real state with evidence links, and `docs/implementation-status.md` M3-03 row.
8. Gates before delivering: `cargo fmt --check` (whole workspace is fine as a check), `cargo check/clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline`, `python3 -B scripts/check-architecture.py`; the 19 pre-existing snapshots byte-identical (yours is the 20th).
Delivery: Task, Result, Files changed (+full SHA-256), Tests executed (commands, exit codes, counts, receipts), Evidence (oracle → test name), Risks, Decisions, Open issues.

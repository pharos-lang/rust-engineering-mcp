# Package A06b — Revise ADR-060 and the spike per independent review V06 (same Sol session, resumed)

Your ADR-060 (SHA-256 baed49270c3ce0d7a802f24588094c0e797bdadfd0698f87b3bccd2346c6db2d) and spike (SHA-256 babbb187da18951734bcabc4b1e3a4756d9766d27bff8cf194d1995fa83d3ec2) received an independent Claude Opus 5 review: read docs/validation/m3-delegation/V06-adr060-review/last-message.md fully. Verdict Revise (P2s). Also read the revised ADR-061 (docs/adr/ADR-061-private-quality-artifact-store.md, SHA-256 b798a32fc40c557d2ac179d2c5853ba90919d1208e9caf5e9e05d94312710330) because several dispositions align ADR-060 with it. Edit ONLY docs/adr/ADR-060-bounded-job-execution-and-mcp-tasks.md and crates/mcp-server/tests/rmcp_tasks_spike.rs. Status: "Proposed — revised after review V06; pending orchestrator acceptance".

Orchestrator dispositions (apply all):
- F01 fix now: orphan rule keyed on NON-DELIVERY, not non-poll: record the request id at commit; `AdmittedTransport` marks it delivered on successful send completion; only never-handed-to-send jobs get the short (30 s proposed) orphan deadline; delivered-but-unpolled jobs are bounded by the normal deadlines/TTL. State that after an orphan cancellation the legitimate owner polling sees `cancelled` (not the masked -32602) until record expiry.
- F02 fix now: keep static advertisement on all five versions as the decision; correct the rationale ("chosen, not forced": `initialize`/`discover` are overridable); record the override as the accepted remediation if a stock client rejects `extensions`; gate the advertisement flip on recorded G4 evidence (Inspector + one stock model-driven client) across the five versions including peers that do not declare the extension.
- F03 fix now: make parent budgets ≥ sum of children (job max 3,600 s: capture+prepare max 120 s, execute max 3,360 s, collect+publish max 120 s; defaults 60+180+30 ≤ 300 s or restate); cleanup separate (max 240 s) and explicitly excluded from the job work budget; synchronous row = complete path EXCLUDING cleanup with worst-case return time stated (120 s + cleanup max) — or give sync mode reduced sub-deadlines; restate the record-TTL rationale counting work max + cleanup max.
- F04 fix now: the job permit IS the ADR-030 worker permit (one active worker, no queue): during an async job every other tool and every Resource read that uses run_joined returns the existing busy/SANDBOX_DENIED response; state this consequence plainly; task registry operations (get/cancel/update) do NOT take the worker permit (bounded in-memory registry work under the ordinary request lease); Resource reads keep using the worker permit in M3 (conservative; revisit with measurement).
- F05 fix now: the job's cancellation source is a registry-owned token seeded from the session token only; `RequestContext::ct` is never passed into a job that outlives its request (rmcp cancels it when the response is queued); add to D06-T06 oracle.
- F06 fix now: label startup/gateway reconciliation as NEW M3 work (owner: M3-01 integrator): extend the existing lazy volume-only check to containers; keep fail-closed default; "clean" only objects proven to be this process's own (label + nonce); otherwise quarantine.
- F07 fix now (spike): parameterize over five versions × {declared, not declared}: assert `extensions` advertised in every cell and -32021 with `requiredCapabilities` on every not-declared cell (legacy `peer_info` branch and the 2026-07-28 `_meta` branch).
- F08 fix now (spike): use a long TTL (30_000 ms) for `complete` and `cooperative`; keep `unlimited` for the `ttlMs: null` observation. Add a comment tying the spike to the rmcp 3.2.0 pin as an SDK-regression guard; it is never the evidence for D06-T01..T14.
- F09 fix now: `JobExecutor::shutdown_and_join` is awaited inside the same `block_on` closure before `workers.shutdown(grace)` returns and before `shutdown_timeout`; the watchdog enforces deadlines only by signalling cooperative cancellation and joining, never abort.
- F10 fix now: `JobOwner` = the same derived owner binding as revised ADR-061 (host uid + state-root identity + granted-root physical identity + workspace root string) plus ProjectRef liveness; the session principal is process-constant and adds no discrimination (say so); restate D06-T03 foreign case as cross-ProjectRef/cross-grant.
- F11 fix now: terminal record TTL stays 2 h (proposal) but artifact references are evidence: an expired member resolves to Resource not found and the record marks that member `Unavailable` at read time; no dangling-reference promise.
- F12 fix now: `job_` + 32 hex everywhere (ADR-061 already adopted it); delete or restate the "64/256 align" sentence with ADR-061's actual figures.
- F13 fix now: add exceed cases for the 512 KiB response cap, 32/128 MiB retained bytes, 5 s seed-commit and 2 s/5 s task-control deadlines.
- F14 fix now: `execution_mode: task` without peer capability = fixed -32602 (input contradicts negotiated session); unqualified/long `auto` = structured `isError` result with a closed remediation code; busy = the existing conservative SANDBOX_DENIED structured contract (ADR-040).
- Missing decisions: state that `auto` never selects synchronous for a Tasks-capable peer (intentional); M3 tools reuse the existing `ready` bootstrap gate (name it); order for a second `tools/call` while a job is active = input validation → mode selection → busy rejection before reservation; stock clients for M3-02 = MCP Inspector (exact version) plus one stock model-driven client, with the explicit fallback that if no available stock client declares the extension, the Tasks path is qualified with the repository's protocol harness and that limitation is documented, while the synchronous path is qualified with the stock clients.
Run after the spike change: `cargo test -p rust-engineering-mcp --test rmcp_tasks_spike --locked --offline` and `cargo clippy -p rust-engineering-mcp --all-targets --locked --offline -- -D warnings`; report counts. Do not touch other files. Final message: Task, Result, Files changed (+SHA-256), Tests executed, Evidence (finding ID → section/line), Risks, Decisions, Open issues.

# Package I04c — Complete M3-04 `rust.semver.check` (integrator Sol, resumed; no Docker in this window)

Two Claude workers started M3-04 and both stopped early (the second hit its CLI session limit mid-run). What already exists in the tree, partly written by you while unblocking compilation:
- `crates/domain/src/semver_check.rs` (SemverCommandOptions and friends), `crates/execution-adapter/src/semver_output.rs` (bounded text parser skeleton), `RustCommand::{SemverCheck, SemverChecksVersion}` in `crates/domain/src/rust_execution.rs`, and semver argv arms plus a `Phase::IngestBaseline`-style extension in `crates/execution-adapter/src/rust_gateway.rs`.
Missing: the application port/use case, the gateway phase composition for the second read-only `/baseline` volume, the port implementation, the MCP tool + snapshot + protocol coverage, fixtures, calibration document, docs and matrix rows.

Take exclusive ownership of M3-04 and finish it to the same standard as the nextest vertical you delivered in I02b. Decision source: `docs/adr/ADR-062-coverage-accounting-and-semver-baselines.md` (Accepted), plus ADR-060/061/063/064. Read the pinned tool help you captured in P02: `docs/validation/m3-provisioning/help/cargo-semver-checks-help.stdout` and `...-check-release-help.stdout` — that help shows **no machine-readable findings output**, so ADR-062 §11's fallback applies: an authoritative coarse outcome (exit code + deny/warn counts) plus best-effort per-finding fields from a bounded, golden-tested parser over non-colored output, `completeness: Partial`, raw output retained as an artifact, parser failure → `Incomplete` (never a false "no break").

## Scope
1. Application: `crates/application/src/semver_check.rs` — `ProjectSemverPort`, `SemverOptions` (closed selection grammar identical for both sides; reject divergent selections before any execution), `SemverObservation`; the use case captures **baseline then candidate** in that stable order via the existing source-capture flow, revalidates each immediately after capture and again before publication, keeps two distinct `SnapshotEvidence` values and never claims atomicity across roots; missing `lib` target detected from the captured `ProjectStructure` before running → `Unavailable`.
2. Execution adapter: compose the phases for two read-only mounts (`/source` candidate, `/baseline`), reusing your nextest report-volume/export machinery for the raw-output artifact; env additions `GIT_DIR=/nonexistent`, `GIT_CEILING_DIRECTORIES=/`, `NO_COLOR=1` (plus `--color never` if the pinned help offers it); the quality seccomp profile (ADR-064) since rustdoc/cargo spawn processes; extend the applied-mount verifier and the configuration fingerprint for the new phase/volume shape; `crates/execution-adapter/src/semver_port.rs` implements the application port.
3. MCP: `crates/mcp-server/src/stdio/semver.rs` with the closed DTO (two project refs, selection, `execution_mode`), snapshot `crates/mcp-server/tests/snapshots/semver-tool.json`, registration as tool 21 after `rust.coverage`, protocol coverage across the five versions, 512 KiB budget with omissions. Terra is concurrently finishing `rust.coverage`; re-read `stdio.rs`/`protocol.rs` immediately before each patch and keep both registrations consistent (if his lands after yours, the ordered inventory must still be correct).
4. Fixtures `fixtures/semver/*` as baseline/candidate pairs with hand-written `Cargo.lock` (identical, removed pub fn, trait method with/without default, enum non-exhaustive vs exhaustive, feature-gated item removed, no-lib, broken baseline). Never run cargo against them on the host.
5. Tests: unit (argv snapshots, selection-mismatch rejection, capture ordering/revalidation, no-lib detection, parser goldens marked provisional until calibration), and `#[ignore]`d Docker runtime tests with exact names covering every fixture family plus: warn-level-only findings still surfaced under exit 0; exit 100 with zero parsed findings → `Blocked`; a `.git` planted inside `/source` never discovered; a baseline that would need the registry fails recognisably instead of hanging; cancel/EOF with an active child joined; both roots immutable after the run.
6. Documents: `docs/tools.md` section, `docs/validation/M3-04-semver-calibration.md` (protocol + placeholders for the observed exit codes/goldens, clearly marked "pending Docker calibration"), `docs/validation/M3-matrix.md` row M3-04, `docs/implementation-status.md` row.

## Constraints for this window
**No Docker**: Terra owns the socket right now for the coverage runtime gate. Write the Docker tests as `#[ignore]` with exact names and list them in your report; the calibration run (exit codes, parser goldens, rustdoc target-dir question from ADR-062's open issue) happens in the next Docker window. Everything else must be finished and green: `cargo fmt --check`, `cargo check/clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline` (non-Docker), `python3 -B scripts/check-architecture.py`; pre-existing snapshots byte-identical. No commits/push/installs/downloads. If a co-worker's in-flight edit breaks the build, retry every 2 minutes up to 20 minutes and keep working on what compiles.
Delivery: Task, Result, Files changed (+full SHA-256), Tests executed, Evidence, Risks, Decisions, Open issues (including the exact Docker test names awaiting calibration).

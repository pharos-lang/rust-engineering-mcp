# M3-04 — recibo de calificación `rust.semver.check`

Fecha: 2026-09-06. Resultado: **Qualified, 18/18**.

Comando:

```text
RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock \
python3 -B scripts/test-m3-runtime.py --output-dir target/m3-runtime-q01-attempt4
```

El gate consolidado usa la imagen
`sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a`,
ejecuta cada caso con `--exact --ignored --test-threads=1`, y exige exactamente
un test aprobado. Las 16 selecciones del execution adapter y las dos MCP pasaron
en 285.763 s. El gate total terminó 47/48 porque coverage se bloqueó después de
completar SemVer; el corte SemVer sí quedó completo y calificado.

Evidencia principal:

- exit codes 0/100/101 y warn-only 0 observados y fijados;
- goldens reemplazados por output real no coloreado;
- ambos roots read-only e inmutables; target/build bajo `/work/target`;
- `.git` plantado ignorado y registry-required falla sin red ni hang;
- cancelación/EOF une el hijo;
- Resource durable index/chunk y fallback Stage 0 ante `Busy` reales.

Recibo: [`M3-runtime.json`](M3-runtime.json), corrida vigente de **62/62**
selecciones, SHA-256
`02b085bf2d00d52cd2a821f059ec6aa4a5ea3b4fc16ffb06b4671b02f131c63b`.
Detalle de calibración: [M3-04-semver-calibration.md](M3-04-semver-calibration.md).
La higiene posterior encontró cero containers/volumes etiquetados.

## Recalificación W3 / V-SEC

Una falla individual al publicar stdout/stderr durable se representa como omisión
explícita y `Incomplete`; no aborta silenciosamente un lote ni deja un locator
inalcanzable. Las lecturas quality usan `try_lock` y conservan disponible el loop
de Tasks. W3 volvió a pasar las 18 selecciones SemVer dentro del gate completo
55/55; esa fue la corrida histórica de W3. La corrida vigente posterior de W6
es de **62/62** y está en [`M3-runtime.json`](M3-runtime.json), SHA-256
`02b085bf2d00d52cd2a821f059ec6aa4a5ea3b4fc16ffb06b4671b02f131c63b`.

# M8-04 — Matriz wire y clientes 0.8.0: registro

Estado: **en curso** (Docker-free calificado; `--with-runtime` pendiente tras
el commit de las correcciones V03). Arnés: `scripts/test-m8-clients.py`
(W19/W20/W21/W23/W28), sesión Inspector `scripts/m8-inspector-session.mjs`.

## Matriz wire (5 revisiones + stdio)

Acreditada por los protocol tests del gate `core` sobre los bytes finales
([core-gate.json](core-gate.json), etapa `test`, `crates/mcp-server/tests/protocol.rs`:
2024-11-05, 2025-03-26, 2025-06-18, 2025-11-25 vía `initialize`; 2026-07-28
vía `server/discover`). **Defecto wire hallado y corregido en M8-04** (W21):
`resources/list`, `resources/templates/list` y `prompts/list` carecían de
`ttlMs`/`cacheScope` bajo 2026-07-28 (rmcp `Default`), rechazados por el SDK
TS del Inspector; ahora los cuatro listados los llevan y
`resources/templates/list` anuncia las dos plantillas (RFC 6570 tras V03 S-1).
HTTP: no anunciado (M7 Deferred).

## Clientes (Docker-free, recibo [clients.json](clients.json), `attempt-13`)

| Cliente | Versión exacta | Modo | Resultado | Evidencia |
| --- | --- | --- | --- | --- |
| Inspector (obligatorio, determinista) | 2.5.0 | `server/discover` 2026-07-28, stdio | **passed**: igualdad de contrato 36/36 contra `freeze-0.8.0.json` (hashes canónicos), 30 refusals estructurados + `catalog.status` observación, 4 negativos genéricos, `resources/list` vacío | `clients/attempt-13/inspector-docker_free-session.json` |
| Codex CLI (obligatorio, dirigido por modelo `gpt-5.6-sol`) | 0.154.0 | stdio | **passed**: discovery + `project.open` + `project.inspect` (oráculo endurecido por wire en W28) | `clients/attempt-13/codex-events.jsonl`, `protocol.jsonl` |
| Claude Code (`claude-sonnet-5`) | 2.1.268 | stdio, `--strict-mcp-config` | **passed**: `project.open`, `project.inspect`, `catalog.status`; negativo interceptado por el propio cliente (variación del modelo, documentada) | `clients/attempt-13/claude-model-events.jsonl` |
| Gemini CLI (`agy`, `gemini-3.8-flash-high`) | 1.2.2 | stdio (`agy mcp add/remove` aislado) | **unavailable**: el modo headless `-p` deniega las tools MCP sin regla de permiso; no se fuerza → **no se anuncia como calificado** (docs: configuración, no calificación) | `clients/attempt-13/gemini-events.json` |

Obligación ADR-086 §1 (todas las `stable` ejercitadas por cliente stock antes
de RC1): Inspector Docker-free ejercita las 31 con refusal/observación; los
positivos con runtime (`--with-runtime`, Docker) componen los arneses M2–M6 y
se ejecutan sobre bytes commiteados tras las correcciones V03 (pendiente en
este registro).

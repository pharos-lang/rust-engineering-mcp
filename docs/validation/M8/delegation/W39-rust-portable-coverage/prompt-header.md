# W39 — cobertura portable (Linux llvm-cov) del código Rust nuevo de M8: `doctor.rs`, `mutation_cli.rs`, `host_config.rs`

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de tests (Rust). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `crates/mcp-server/src/doctor.rs`, `crates/mcp-server/src/mutation_cli.rs`, `crates/mcp-server/src/host_config.rs` (solo `#[cfg(test)] mod tests` o refactors puros mínimos para testear), `crates/mcp-server/tests/doctor.rs`, `crates/mcp-server/tests/cli.rs`. No toques snapshots.

SonarCloud (PR #22) mide la cobertura de código nuevo con `cargo llvm-cov` en **Linux**; el quality gate exige ≥ 80 % y el repo **prohíbe** excluir `crates/**` (`test-gate-reporting.py`). Líneas nuevas sin cubrir: `crates/mcp-server/src/doctor.rs` 96/135, `mutation_cli.rs` 31/31, `host_config.rs` 13/13, `capability_document.rs` 30/312, `contract_cli.rs` 3/26, `main.rs` 4/6. Los tests que hoy cubren `doctor.mutation_journals` y `mutation list` dependen del store macOS (`crates/project-adapter/src/filesystem/macos`) o de `#[cfg(target_os = "macos")]`, así que en Linux quedan a 0. Precedente aceptado (M6 PR #20, W14): **tests unitarios portables de las funciones puras**, sin `#[cfg(target_os)]`, sin Docker, sin `#[ignore]`.

Tareas: (1) `doctor.rs`: extrae en funciones puras (si no lo están) la clasificación de registros → `MutationJournalsReport` (conteos por kind/fase, `unknown_format`, `downgrade_blocking_kinds`, `downgrade_blocked`, notas, rama `Busy`/`RecoveryRequired`/error), y cúbrelas con `#[test]` que construyan `MutationRecordSummary`/errores sintéticos (sin store real); cubre también la línea humana del informe y el caso `mutation_journals: null`. (2) `mutation_cli.rs`: cubre la rama «store no inicializado» (directorio vacío → `passed`, `count 0`, `store_initialized: false`) y «directorio inexistente → error» con un `#[test]` portable que llame a la función de ejecución con un `tempfile::tempdir()` (o el mecanismo de temporales que ya usen los tests del crate) — si la función abre el store macOS solo cuando el directorio existe, ambos casos son portables; separa la serialización del informe en una función pura y cúbrela. (3) `host_config.rs`: tests portables de las nuevas ramas (`--rustsec-snapshot`/`--catalog-*` dentro de una root → error; fuera → ok) usando rutas sintéticas (no hace falta que existan si el parser solo compara prefijos; si exige existencia, usa un tempdir). (4) `contract_cli.rs`/`main.rs`: cubre las ramas de parseo (`--json`, `--human`, argumento inválido) con tests portables. Mide con `cargo llvm-cov -p rust-engineering-mcp --lib --tests --locked --offline --summary-only` (está instalado) antes/después y reporta el % por archivo; objetivo: ≥ 80 % de líneas nuevas en esos archivos. Verificación: `cargo fmt --all -- --check`; `cargo clippy -p rust-engineering-mcp --all-targets --locked --offline -- -D warnings`; `cargo test -p rust-engineering-mcp --locked --offline` (lib + `doctor` + `cli`); `git status --short crates/mcp-server/tests/snapshots` sin cambios. Informe en `docs/validation/M8/delegation/W39-rust-portable-coverage/report.md` y en tu última respuesta. No commit.

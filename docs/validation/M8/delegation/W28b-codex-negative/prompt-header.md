# W28b — Codex: negativo observable en el wire (`project_ref` inválido); Gemini: clasificación honesta

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. Sin Docker. **Archivos permitidos:** `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`, `scripts/m8-inspector-session.mjs` (solo si el proxy vive ahí).

`docs/validation/M8/clients/attempt-14/receipt.json`: Codex `partial` con `project_open_observed: true`, `project_inspect_observed: true`, `unknown_tool_refused_on_wire: false` — el modelo (correctamente) no invoca una tool que no existe tras el discovery, así que ese negativo no es observable en el wire por diseño. **Decisión del orquestador**: el negativo obligatorio de Codex pasa a ser `rust.project.inspect` con `project_ref` bien formado pero nunca abierto (`prj_00000000000000000000000000000000`), que el servidor rechaza de forma estructurada (`PROJECT_NOT_FOUND`); la tool inexistente queda como paso opcional informativo. Cambia el prompt del turno de Codex en consecuencia y el oráculo: `passed` = `open` + `inspect` válido + `inspect` con ref inválido observados como `tools/call` en `protocol.jsonl` **y** el rechazo confirmado — si el proxy no registra el `error_code` de la respuesta, amplía sus `safe_keys` con `structuredContent.status` y `structuredContent.error_code` (no sensibles) para poder confirmarlo por wire y no por transcript; mismo cambio para Claude Code y Gemini si comparten el oráculo. Gemini (`attempt-14`): `partial` con 0 `tool_calls_observed` y `gemini-events.json` sin llamadas — lee ese archivo y la salida de `agy` y clasifica con evidencia: si el modelo no invocó tools por denegación de permisos headless → `unavailable` con el texto exacto; si el modelo simplemente no las usó → `partial` con extracto; en ningún caso `passed`. Ejecuta `python3 -B scripts/test-m8-clients.py --run` (Docker-free, foreground, timeout amplio) y deja el recibo promovido a `docs/validation/M8/clients.json` con Inspector+Codex `passed` si el oráculo nuevo lo confirma. `test-m8-clients-unit.py` verde. Informe en `docs/validation/M8/delegation/W28b-codex-negative/report.md` y en tu última respuesta. No commit.

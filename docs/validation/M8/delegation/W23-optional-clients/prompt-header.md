# W23 — M8-04: Claude Code y Gemini CLI en la matriz (Docker-free)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms — úsalo; `run_in_background` está PROHIBIDO.** No commit. Sin Docker. **Archivos permitidos:** `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`. No toques `scripts/test-m6-clients.py` (sus constantes pertenecen al recibo M6).

Recibo actual `docs/validation/M8/clients.json` (`attempt-9`): Inspector y Codex `passed`; **Claude Code `unavailable`** («Claude ran another version: 2.1.268») porque `test-m8-clients.py` reutiliza `m6.validate_claude_session`, que compara contra `m6.CLAUDE_VERSION = "2.1.267 (Claude Code)"`; **Gemini CLI `unavailable`** («authentication failed or timed out») porque el arnés lanza `agy -p` con `HOME` aislado (`isolated_home: true`) y `agy` guarda sus credenciales en el `HOME` real.

1. Claude Code: antes de llamar al validador compartido, fija `m6.CLAUDE_VERSION = CLAUDE_VERSION` (o pasa la versión esperada si el validador lo admite) y registra en el recibo `validator_version_pin_overridden_from: "2.1.267 (Claude Code)"`; el turno de Claude Code debe quedar `passed`/`partial` con sus `tool_calls_observed` reales.
2. Gemini CLI: usa el `HOME` real para `agy` (credenciales), pero aísla la **configuración del servidor MCP**: registra el servidor con `agy mcp add <nombre-único-m8> <binario> serve --stdio --root <fixture> …` antes del turno y elimínalo con `agy mcp remove <nombre>` en `finally` (comprueba con `agy mcp list` antes/después y registra ambas salidas en el recibo); no toques otros servidores del usuario. Si `agy -p` sigue sin poder usar MCP en modo headless (p. ej. permisos de tool denegados sin regla), registra `unavailable` con la salida exacta — no lo fuerces con `--dangerously-skip-permissions` (el clasificador del host lo bloquea; documenta si lo intentaste y fue bloqueado).
3. Ejecuta `python3 -B scripts/test-m8-clients.py --run` (Docker-free, foreground, timeout amplio) hasta tener un recibo con los cuatro clientes en su estado real; `test-m8-clients-unit.py` verde. Verifica que el recibo nuevo se promueve a `docs/validation/M8/clients.json` (si el guard de «recibo preservado» lo impide, explica cómo promoverlo sin borrar intentos previos).

Informe (Task / Result por cliente / Files changed / salidas `agy mcp list` antes/después / tests) en `docs/validation/M8/delegation/W23-optional-clients/report.md` y en tu última respuesta. No commit.

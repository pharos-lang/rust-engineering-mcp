# W25 — correcciones V03 en Rust: `doctor.mutation_journals` (D-1..D-5), plantilla quality (S-1..S-4), ADR-088 §8

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de implementación (Rust). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms — úsalo para `cargo`; `run_in_background` está PROHIBIDO y termina tu sesión sin informe.** No commit. No Docker. **Archivos permitidos:** `crates/domain/src/mutation.rs` (solo campo aditivo en `MutationRecordSummary`), `crates/project-adapter/src/**` (rellenar el campo), `crates/mcp-server/src/doctor.rs`, `crates/mcp-server/src/host_config.rs` (solo si D-5 lo exige), `crates/mcp-server/src/stdio.rs`, `crates/mcp-server/src/stdio/resources.rs`, `crates/mcp-server/src/stdio/capability_document.rs`, `crates/mcp-server/tests/doctor.rs`, `crates/mcp-server/tests/cli.rs`, `crates/mcp-server/tests/protocol.rs`, `crates/mcp-server/tests/snapshots/doctor-report.json`, `docs/tools.md` (secciones `doctor` y Resources), `docs/adr/ADR-088-migration-rollback-policy.md` (§3 y §8, redacción), `CHANGELOG.md` (viñetas 0.8.0). **Los 36 snapshots `*-tool.json` deben quedar byte-idénticos** (`contract-freeze.py verify --strict` passed).

Lee `docs/validation/M8/delegation/V03-review-m8-03-04-05/disposition.md` (filas D-1..D-6, S-1..S-4) y `report.md` §1 y §3 (evidencia con archivo:línea). Aplica exactamente:

1. **D-1 (P1)**: `MutationRecordSummary` gana `kind` (aditivo; rellénalo en el adapter desde el envelope). En `doctor`: recuentos `pending`/`terminal` **por kind**, `unknown_format`, y `downgrade_blocked = pending > 0 || any(kind ∉ KINDS_KNOWN_TO_0_3_0)` con `KINDS_KNOWN_TO_0_3_0` = los cinco kinds M2 (`manifest_patch`, `fmt_apply`, `fix_apply`, `dependency_add`, `dependency_remove` — verifica los nombres reales del enum) y `downgrade_blocking_kinds: [...]` en el informe; `DOWNGRADE_NOTE`: «recover, complete or prune (`mutation prune`) with 0.8.0 before installing an older binary». Test: journal Committed `analyzer_action_apply` → `downgrade_blocked: true`, `terminal: 1`, `pending: 0`, kinds `{analyzer_action_apply: 1}`.
2. **D-2**: renombra el test del kind desconocido (hoy falla en el checksum, no en el kind) y añade tres casos con conteos exactos: marcador `-journal-v2`→`-journal-v9` (llega a la rama de formato desconocido), bytes basura, archivo ajeno (`.DS_Store`); en cada uno `unknown_format == 1`, `pending == terminal == 0`, notas presentes. **D-3**: si `list_records` devuelve `Busy` (lock exclusivo de `serve`), nota propia («journal busy: a mutation is in progress; rerun doctor»), no «could not be read». **D-4**: la nota pasa a «unreadable or unknown». **D-5**: `doctor` calcula `mutation_journals` cuando hay `--state-root` aunque falte la tupla Docker completa (misma lectura que `mutation list`); si `host_config.rs` obliga a la tupla, admite `--state-root` solo para `doctor` con el mínimo cambio y un test.
3. **S-1 (P2 contrato, antes del freeze)**: la plantilla de quality pasa a `rust-quality-artifact://{project_ref}/{quality_job_id_or_artifact_id}{?offset,length}` (RFC 6570) en `stdio.rs` y `capability_document.rs`; **S-2**: una sola constante compartida (p. ej. en `resources.rs`) usada por ambos, y un test que compara la lista de `resources/templates/list` con `contract --json` `resources[]` (igualdad exacta); **S-3**: el test de listas itera sobre `LEGACY` como sus vecinos; **S-4**: viñetas en CHANGELOG 0.8.0 para las listas wire (`ttlMs`/`cacheScope`), la plantilla y `doctor.mutation_journals` por kind. Actualiza `docs/tools.md` y ADR-088 (§3: recuentos por kind y regla de bloqueo; §8: la revocación ocurre tras `Published`, antes de completar — como prueba `revoked_destination_permissions_leave_a_recoverable_non_terminal_journal`).

Verificación (foreground): `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --locked --offline -- -D warnings` (el campo nuevo del dominio se usa en varios crates); `cargo test -p rust-engineering-mcp --locked --offline --test doctor --test cli --test protocol`; `cargo test -p rust-engineering-project --lib --locked --offline`; `cargo test -p rust-engineering-domain --locked --offline`; `python3 -B scripts/contract-freeze.py verify docs/validation/M8/freeze-0.8.0.json --strict`; `git status --short crates/mcp-server/tests/snapshots` → solo `doctor-report.json`. Informe (Task / Result por finding / Files changed / tests) en `docs/validation/M8/delegation/W25-v03-rust/report.md` y en tu última respuesta. No commit.

# W38 — SonarCloud PR #22: 22 hallazgos de taint en scripts M8 (rating E) y exclusiones de cobertura de arneses host-only

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de scripts. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano (Bash `timeout` hasta 600000 ms), sin commit, sin Docker. **Archivos permitidos:** `scripts/contract-freeze.py`, `scripts/test-contract-freeze.py`, `scripts/measure-m8-performance.py`, `scripts/soak-m8.py`, `scripts/test-m8-performance-unit.py`, `scripts/gate.py` (solo si cambia la invocación de `contract-freeze verify`), `sonar-project.properties`, `docs/ci.md` (una frase por exclusión).

SonarCloud (quality gate del PR #22) reporta `new_security_rating` E por 22 vulnerabilidades del motor de taint Python: `pythonsecurity:S2083` (path traversal) en `measure-m8-performance.py:628` y `contract-freeze.py:141`; `S8707`/`S8701`/`S8705` («Path Traversal / Command (Argument) Injection via faulty LLM-supplied CLI arguments») en `measure-m8-performance.py` (80, 86, 92, 147, 611, 627, 628, 748, 749), `soak-m8.py` (90, 239, 250, 647, 648) y `contract-freeze.py` (77, 140, 141, 154, 296, 297). Regla de la casa (memoria del proyecto, precedente M5/M6): el motor ignora `argparse type=` y cualquier validación posterior; **las rutas deben ser constantes derivadas de `ROOT`** y los parámetros variables llegan **por stdin como JSON** validado contra un esquema cerrado (regex/allowlist), nunca por argumentos de línea de comandos que acaben en `open()`/`subprocess`. Aplica:

1. `contract-freeze.py`: `generate` escribe siempre en `docs/validation/M8/freeze-0.8.0.json` (constante); `verify` lee esa constante (el gate llama `verify` sin ruta; ajusta `gate.py`); `diff` toma `base` y `only` de un JSON por stdin (`{"base": "v0.3.0", "only": [...], "out": "since_v0.3.0"}`) donde `base` debe cumplir `^(v[0-9]+\.[0-9]+\.[0-9]+|[0-9a-f]{7,40})$` y `out` es una **clave** de un diccionario de destinos constantes (`docs/validation/M8/02-schema-diff.json` compuesto por el propio script con ambas bases) — nada del stdin llega a una ruta; los argv de `git` se construyen con constantes + el ref validado tras `--end-of-options`. Mantén el comportamiento de las 20+ pruebas (adáptalas al nuevo contrato de invocación; los dobles siguen valiendo).
2. `measure-m8-performance.py` y `soak-m8.py`: sin `--out`/`--binary` arbitrarios: salidas constantes (`docs/validation/M8/05-measurement.json`, `docs/validation/M8/05-soak-core.json`, comparaciones en `target/m8-performance/`), binario constante `target/release/rust-engineering-mcp`; parámetros numéricos (`repeat`, `cycles`, `hours`, `sample_every`, `open_churn`, `ttl_wait_seconds`) por argparse con `int`/`float` está bien (no son rutas ni argv de proceso), o por stdin si el motor los marca; `--compare` recibe **claves** de recibos bajo `target/m8-performance/` (nombres validados por regex `^[a-z0-9-]+$`), no rutas. Los argv del servidor (`serve --stdio --root <fixture constante> …`) son constantes.
3. `sonar-project.properties`: añade a `sonar.coverage.exclusions` los arneses **host/Docker-only** (precedente: `test-m5-clients.py`, `build-m6-runtime.py`): `scripts/measure-m8-performance.py`, `scripts/soak-m8.py`, `scripts/test-m8-rollback.py`, `scripts/test-m8-clients.py`, `scripts/m8-inspector-session.mjs`; **no** excluyas `scripts/contract-freeze.py` (portable, etapa del gate). Una frase en `docs/ci.md` justificando cada exclusión.

Verificación: `python3 -B scripts/test-contract-freeze.py`, `python3 -B scripts/test-m8-performance-unit.py`, `python3 -B scripts/test-gate-reporting.py` (incluye la regla que prohíbe excluir `crates/**`), `python3 -B scripts/contract-freeze.py verify` (passed), `python3 -B scripts/docs-hygiene.py links-check`. Informe (mapa hallazgo → cambio) en `docs/validation/M8/delegation/W38-sonar-taint-coverage/report.md` y en tu última respuesta. No commit.

# W19 — M8-04: arnés de matriz wire/cliente 0.8.0 (`scripts/test-m8-clients.py`)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de scripts (Python + Node para Inspector). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano.** No commit. **Docker: no** en este paquete (el orquestador ejecuta `--run --with-runtime`); tú entregas el arnés, sus tests herméticos y el preflight. **Archivos permitidos:** `scripts/test-m8-clients.py` (nuevo), `scripts/test-m8-clients-unit.py` (nuevo), `scripts/m8-inspector-session.mjs` (nuevo, si hace falta; mira `scripts/m6-inspector-session.mjs`), `scripts/gate.py` (una etapa `core`: `m8-client-harness-tests`), `.github/workflows/sonarcloud.yml` (línea de coverage), `sonar-project.properties` (añadir `scripts/test-m8-clients.py` y el `.mjs` a `sonar.coverage.exclusions` como los M5/M6).

## Contexto (plan M8-04 y `docs/validation/M8/02.md`)

Matriz obligatoria para lo anunciado en 0.8.0: **Inspector 2.5.0 y Codex stock (CLI 0.154.0) obligatorios**; **Claude Code 2.1.268** y **Gemini CLI (`agy` 1.2.1)** calificados antes de anunciarlos; stdio únicamente; las 5 revisiones MCP se acreditan con los protocol tests del gate `core` (cita el recibo `docs/validation/M8/core-gate.json`, no las repitas); skip = no calificado; nada que no corrió es pass. Reutiliza el patrón de `scripts/test-m6-clients.py` (preflight, `--run`, `--with-runtime`, `proxy` subcomando, receipt.json + protocol.jsonl por intento, `run_claude`, Inspector vía `.mjs`; lee sus `-unit.py` para el estilo hermético y las reglas Sonar: rutas constantes, params por stdin, sin `/tmp` literal, sin `type=`). Clientes: Codex se configura con `codex mcp add <name> -- <cmd…>` o por `-c mcp_servers.<name>.command=…`/`args` en `codex exec` (mira `codex exec --help`, `codex mcp add --help`; usa un `CODEX_HOME` temporal para no tocar la config del usuario); Gemini CLI con `agy mcp add <name> <cmd> [args…]` (usa un `HOME`/config temporal si `agy` lo permite; si no, registra el servidor y elimínalo al final con `agy mcp remove`), turno con `agy --model gemini-3.8-flash-high -p "<prompt>" --output-format json --sandbox`; Claude Code como en M6 (`--strict-mcp-config`, `--mcp-config`, `--allowedTools mcp__…`).

## Matriz que debe producir el arnés (por cliente/versión, todo con recibo)

1. **Inspector (determinista, autoritativo)**: `tools/list` completo → compara nombre, `annotations`, `description` e `inputSchema`/`outputSchema` de las 36 tools contra el manifiesto `docs/validation/M8/freeze-0.8.0.json` (recalculando los hashes canónicos: `sha256(json.dumps(obj, sort_keys=True, separators=(",", ":"), ensure_ascii=False))`) → `contract_equality: true/false` con lista de discrepancias; `resources/list` = `[]`; para **cada una de las 31 `stable`** al menos una invocación: sin Docker, camino estructurado `unavailable`/`blocked` (p. ej. `rust.check` sin runtime, `rust.fmt.apply` sin grant, `rust.catalog.status` sin catálogo) y, con `--with-runtime`, los positivos que ya cubren M2–M6 (reutiliza/importa los pasos de sus arneses o invócalos como subprocesos y agrega sus recibos: no dupliques 500 líneas si puedes componer). Negativos: `unknown tool`, argumentos inválidos (schema), `project_ref` inválido, unknown fields. Cancel: `notifications/cancelled` sobre una llamada `rust.check` en vuelo (con runtime) y EOF. Resource: lectura de un `rust-artifact://` tras `rust.check` (con runtime).
2. **Codex stock (obligatorio)**: turno dirigido por modelo: discovery (36) → `rust.project.open` → una lectura (`rust.project.inspect` o `rust.catalog.status`) → un negativo (tool inexistente o `project_ref` inválido) → resumen; registra los `tools/call` observados vía el proxy (`proxy` subcomando como en M6) y clasifica `passed/partial/capacity_refused` honestamente.
3. **Claude Code**: como en M6, sobre un subconjunto (discovery + 2 lecturas + negativo + Resource si hay runtime).
4. **Gemini CLI**: igual que Claude Code; si `agy` no soporta MCP stdio para `-p`, regístralo como `unavailable` con la evidencia (salida del CLI), no como pass.
5. Recibo `docs/validation/M8/clients.json` (`status` global `passed` solo si Inspector completo + Codex + los demás no tienen `failed`; `unavailable` de un cliente opcional se anota y **no** lo anuncia como calificado) y matriz por celda: producto, SDK `rmcp 3.2.0`, revisión MCP negociada, transporte, cliente/versión exacta, OS/arch, perfil, resultado, test, recibo.

## Entregables

`test-m8-clients.py` con `--preflight` (versiones exactas: `npx @modelcontextprotocol/inspector@2.5.0 --version` o el método de M6, `codex --version`, `claude --version`, `agy --version`; binario 0.8.0 `version --json`; imagen M6 por digest si `--with-runtime`) y `--run [--with-runtime]`; `test-m8-clients-unit.py` (≥ 15 tests herméticos: parseo, equality de contrato con manifiesto sintético, clasificación de resultados, receipt sin `passed` por skip, argv de cada cliente); `gate.py` + `sonarcloud.yml` + `sonar-project.properties`. **Ejecuta** `python3 -B scripts/test-m8-clients.py --preflight` de verdad y reporta la salida; **no** ejecutes `--run` (lo hace el orquestador).

Verificación: `python3 -B scripts/test-m8-clients-unit.py`, `python3 -B scripts/test-gate-reporting.py`, `python3 -B scripts/docs-hygiene.py links-check`. Informe (Task / Result / Files changed / Preflight / Tests / Risks / Open issues) en `docs/validation/M8/delegation/W19-clients-harness/report.md` y en tu última respuesta. No commit.

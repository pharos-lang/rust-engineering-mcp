# W24 — M8-07: ensayo de distribución 0.8.0 (archive local, inventario, SBOM/notices, smoke desde descarga limpia) + workflow RC + ADR-090 (D14)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de release engineering. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms — úsalo; `run_in_background` está PROHIBIDO.** No commit. **No publiques nada** (sin `gh release`, sin tags, sin push). **Archivos permitidos:** `.github/workflows/release-candidate.yml`, `scripts/release-smoke.py`, `scripts/release-artifact.py`, `scripts/release-inventory.py` (solo si hace falta para 36 tools/0.8.0), sus `scripts/test-release-*.py`, `docs/adr/ADR-090-offline-verification-and-incident-response.md` (nuevo), `docs/adr/README.md` (viñeta), `docs/roadmap/adr-backlog-m2-m8.md` (§D14 Status), `docs/publication.md` (sección «Verificación offline» y «Respuesta a compromiso»), `docs/validation/M8/07-release-rehearsal.json` (recibo), `docs/validation/M8/07.md` (solo añadir la sección «Resultado del ensayo»).

## Decisión D14 (vinculante): `docs/validation/M8/07.md`. Alcance: ADR-087 (solo `aarch64-apple-darwin`). Fuentes: ADR-047/048, `docs/publication.md`, `docs/release/0.1.0/`, `docs/validation/M1/17-*` (cómo se calificó 0.1.0), `.github/workflows/release-candidate.yml`, `scripts/release-*.py`.

## Tareas

1. **Workflow RC** (`release-candidate.yml`): W22 detectó que la línea ~219 exige `counts.tools == 31`. Cambia la comprobación para que el conteo y los nombres esperados salgan del manifiesto de freeze del repo (`docs/validation/M8/freeze-0.8.0.json`, `tool_count`/`tools`) o de `rust-engineering-mcp contract --json` del binario construido (elige la fuente que ya esté disponible en el job; documenta cuál); revisa el resto del workflow por literales `0.3.0`/`31`/`v0.3` y alinéalos con 0.8.x sin cambiar la política OIDC/attestations. Revisa que `release-smoke.py`/`release-artifact.py`/`release-inventory.py` acepten el inventario de 36 (docstring ya dice 36) y que la comprobación de versión use `Cargo.toml` (0.8.0), no un literal.
2. **Ensayo local** (foreground, sin red): construye el archive core con `scripts/release-artifact.py` (lee su `--help`; usa `target/release` ya construido en 0.8.0 o reconstruye con `--locked --offline`), genera inventario/SBOM/notices como lo hace el workflow (los scripts locales), y ejecuta `scripts/release-smoke.py` **desde un directorio temporal limpio** (bajo `target/`, sin `/tmp` literal) simulando «descarga limpia»: extrae, verifica SHA-256 contra el `SHA256SUMS` generado, `version --json` = 0.8.0, `contract --json` = 36 tools con hashes iguales al manifiesto, `doctor` pasivo, `serve --stdio` responde `tools/list` con 36 y `resources/templates/list` con 2. Escribe `docs/validation/M8/07-release-rehearsal.json` (`format_version 1`, `generated_utc`, `head_commit`, `tree_dirty`, hashes del archive y de cada asset generado, tamaños — compara el tamaño del archive con el presupuesto `archive_size_core_bytes` de `docs/validation/M8/05-budgets.json` (12,7 MB) y registra `within|over`, pasos con comando/exit/extracto, `status` global `passed` solo si todo pasa; lo que no se pueda hacer sin red o sin tag (attestations OIDC, redescarga desde GitHub) se marca `unavailable` con motivo y **queda para RC1**).
3. **ADR-090** (Accepted, decisión del orquestador bajo autorización del owner): los 5 puntos de D14 con Alternatives (clave organizacional adicional; dos operadores; no publicar bundles) y Consequences; `docs/publication.md`: secciones «Verificación offline» (comandos exactos `gh attestation verify --bundle … --owner pharos-lang`, verificación mínima por SHA-256 + inventario) y «Respuesta a compromiso» (los tres puntos); backlog §D14 → Accepted. Índice ADR.
4. Verificación: `python3 -B scripts/test-release-artifact.py`, `python3 -B scripts/test-release-smoke.py`, `python3 -B scripts/test-gate-reporting.py`, `python3 -B scripts/docs-hygiene.py links-check` (0 rotos); si tocaste el workflow, valida YAML con `python3 -c "import yaml"` si está disponible o con una lectura cuidadosa.

Informe (Task / Result / Files changed / pasos del ensayo con resultado / tamaño del archive vs presupuesto / lo que queda para RC1 / tests) en `docs/validation/M8/delegation/W24-release-rehearsal/report.md` y en tu última respuesta. No commit.

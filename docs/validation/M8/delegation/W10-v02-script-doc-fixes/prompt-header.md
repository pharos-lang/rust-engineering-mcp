# W10 — correcciones V02 en scripts y docs

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de correcciones. Orquestador: Claude Fable 5.1. No puedes lanzar subagentes. **Nunca corras comandos en segundo plano.** No hagas commit. **Archivos permitidos:** `scripts/contract-freeze.py`, `scripts/test-contract-freeze.py`, `scripts/gate.py` (solo las dos líneas de la etapa `contract-freeze`), `docs/validation/M8/01-census.json`, `CHANGELOG.md`, `docs/tools.md`, `docs/compatibility.md`. **No regeneres** `freeze-0.8.0.json` ni `02-schema-diff.json` (lo hará el orquestador tras el commit).

## Contexto

Lee `docs/validation/M8/delegation/V02-review-freeze/disposition.md` (P2-2, P2-3, P2-4 y los P3 asignados a W10) y `report.md`. Reglas Sonar de la casa: sin `/tmp` literal, sin `type=` en argparse, sin `shell=True`, rutas desde `ROOT`.

## Tareas

1. **P2-2** (`contract-freeze.py` `verify`): clasifica cada tool con la clase **registrada en el manifiesto**; si la clase actual (constantes del script) difiere de la registrada para cualquier tool → **fallo** (`class_changed: [...]` en el resumen), con o sin `--strict`; cambios de conteo/nombres → fallo (ya). Test: reclasificar `rust.check` a preview + cambiar su schema → `failed` sin `--strict`.
2. **P2-3** (`gate.py`): la etapa `contract-freeze` se ejecuta **siempre** (sin `if exists`); si el manifiesto falta, `verify` debe fallar con mensaje claro (exit 1) — añade ese caso al script y su test.
3. **P3 procedencia**: `generate` y `diff` escriben `tree_dirty: true|false` (`git status --porcelain -- crates/mcp-server/tests/snapshots` no vacío) junto a `head_commit`. `verify` comprueba `format_version == 1` y que `canonical` sea la cadena esperada.
4. **P3 `git show`**: lee bytes (`subprocess.run([...], capture_output=True)` sin `text=True`) y decodifica UTF-8 explícitamente; valida `--base` con `git rev-parse --verify --end-of-options <ref>^{commit}` antes de usarlo y pásalo con `--end-of-options` en `git show`/`ls-tree`.
5. **P3 bytes**: `diff` compara además `snapshot_sha256` (bytes del archivo) y reporta `bytes_identical: bool` por tool en `unchanged`/`changed`.
6. **P3 tests**: añade casos para cambio de `description`, `annotations` y `outputSchema` en un `stable`; desajuste solo de conteo; reclasificación (P2-2); manifiesto ausente (P2-3); subcomando `diff` contra un ref real (`HEAD`: debe dar 36 unchanged sobre el árbol actual… ojo: los 5 analyzer están modificados sin commit respecto a HEAD, así que espera 5 `changed` en `description` y úsalo como aserción); vector canónico no-ASCII (`{"café": "ñ"}` con su hash calculado).
7. **P2-1 censo**: añade a cada `tools[]` de `01-census.json` el campo `executes_project_code` (bool) con la semántica «puede ejecutar build scripts, proc macros, tests o binarios del proyecto en el guest»: `true` exactamente para check, clippy, test, test.nextest, quality.gate, quality.gate.v2, coverage, semver.check, mutation.test, miri, benchmark.run, profile.flamegraph, binary.bloat, fix.apply (14); `false` para el resto (22), y un campo `executes_project_code_reason` de una frase por tool.
8. **P2-4 docs**: en `docs/tools.md` (sección «Clases de estabilidad y documento de contrato»), `docs/compatibility.md` (línea del documento de contrato) y CHANGELOG (viñeta del subcomando): declara que el subcomando `contract` y su documento (`document_kind: rust_engineering_capabilities`, `format_version: 1`) son **`stable`** desde 0.8.0 (cambios de formato = minor + migration notes); lista **todos** los campos (`document_kind`, `format_version`, `server_version`, `protocol{primary_version, negotiable_versions, sdk}`, `tools{name → stability, annotations, input_schema_sha256, output_schema_sha256, description_sha256, executes_project_code, requires_runtime}`, `resources[]{uri_template, stability}`, `tool_count`) con la semántica de `executes_project_code`; y corrige la cadena de verificación: «los protocol tests exigen igualdad servidor vivo ↔ snapshots; `tests/cli.rs` exige igualdad `contract --json` ↔ snapshots; la etapa `contract-freeze` exige igualdad snapshots ↔ manifiesto».
9. **P3 redacción CHANGELOG**: «30 contratos `stable` byte-idénticos» → mantén la afirmación solo si `diff` con `snapshot_sha256` lo confirma (ejecuta `python3 -B scripts/contract-freeze.py diff --base v0.3.0 --out /dev/stdout` tras tu cambio y mira `bytes_identical`); si no, «canónicamente idénticos». «Las 31 tools restantes quedan `stable`» → añade «condicionadas a superar la matriz de clientes stock M8-04 antes de RC1 (ADR-086 §1)». `--rust-image`: el flag existe desde M1; lo nuevo es la imagen M6 admitida (ADR-085).

## Verificación (foreground)

`python3 -B scripts/test-contract-freeze.py`; `python3 -B scripts/test-gate-reporting.py`; `python3 -B scripts/contract-freeze.py verify docs/validation/M8/freeze-0.8.0.json` (passed — el manifiesto actual sigue siendo válido); `python3 -c "import json;d=json.load(open('docs/validation/M8/01-census.json'));print(sum(t['executes_project_code'] for t in d['tools']))"` → 14; `python3 -B scripts/docs-hygiene.py links-check`. **Escribe tu informe final también en `docs/validation/M8/delegation/W10-v02-script-doc-fixes/report.md`**. No commit.

# W31 — arnés de clientes: modo `--with-runtime` (sesión Docker-free + sesión runtime separadas)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. **No ejecutes `--run --with-runtime` completo** (dura más de lo que permite tu sesión; lo corre el orquestador); sí puedes probar sesiones Inspector acotadas con Docker (socket `/Users/cburgosro/.docker/run/docker.sock`, imágenes ya presentes) si lo necesitas. **Archivos permitidos:** `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`, `scripts/m8-inspector-session.mjs`.

Fallo real (`python3 -B scripts/test-m8-clients.py --run --with-runtime --docker-socket …`): `RuntimeError: rust.project.inspect did not land on a structured refusal: passed` en `validate_negative_rows` — en modo runtime el arnés ejecuta el plan de negativos Docker-free contra un servidor con runtime real, así que las tools que solo se rechazaban por falta de runtime ahora pasan. Diseño correcto (plan M8-04 y `02.md`): `--with-runtime` debe ejecutar **dos sesiones Inspector**: (1) `docker_free` idéntica al modo sin runtime (host con runtime inalcanzable; negativos de las 31 `stable` + genéricos + igualdad de contrato) y (2) `runtime` (host con Docker/imagen M2–M6 real y los grants de escritura sobre una copia temporal del fixture) que compone los positivos de M2–M6 (`compose_prior_receipts` o pasos propios) y además: `notifications/cancelled` sobre un `rust.check` en vuelo, EOF a mitad de llamada, lectura de un `rust-artifact://` tras `rust.check`. Los turnos de modelo (Codex obligatorio, Claude Code, Gemini) en modo runtime hacen su flujo positivo con runtime (open → inspect real `passed` → un negativo `PROJECT_NOT_FOUND`). El recibo `inspector` lleva ambas claves (`docker_free`, `runtime`); `status` global `passed` solo si `docker_free` pasa, `runtime` pasa (Inspector + Codex) y ningún obligatorio falla; `unavailable` de opcionales se registra. Reglas Sonar de la casa. Verificación: `test-m8-clients-unit.py` (tests para la separación de modos), `--preflight --with-runtime` → `ready`, y una sesión `runtime` acotada de Inspector real (open + check + cancel + Resource) si cabe en tu tiempo, con su salida en el informe. Informe en `docs/validation/M8/delegation/W31-runtime-mode-fix/report.md` y en tu última respuesta. No commit.

# W37 — `test-m8-clients-unit.py`: tests herméticos (SonarCloud «Generate Python coverage» falla en Linux)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano (Bash `timeout` hasta 600000 ms), sin commit. **Archivos permitidos:** `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`, `scripts/test-m8-performance-unit.py`, `scripts/test-m8-rollback-unit.py`, `scripts/test-contract-freeze.py`.

En el runner de SonarCloud (Linux, sin `target/m1-17-inspector/`), `python3 -m coverage run … scripts/test-m8-clients-unit.py` termina `FAILED (errors=8)`: `RunInspectorModeSeparationTests` llama a `run_inspector(...)` real, que escribe el puente en `ROOT/"target/m1-17-inspector"/m8-<attempt>-<mode>-bridge.mjs` (`test-m8-clients.py:1061-1063`) → `FileNotFoundError`. Haz herméticos esos tests: extrae la carpeta del puente a una constante/función (`BRIDGE_DIR`) y parchea en los tests (`unittest.mock.patch.object`) tanto `BRIDGE_DIR` como `INSPECTOR` (bytes ficticios) y `NODE`/`m3.run_bounded` (doble que no ejecuta nada), de forma que ningún test toque `target/m1-17-inspector/` ni requiera Node/Docker/git. Verifica la hermeticidad simulando el runner: ejecuta cada suite de la lista de `sonarcloud.yml` (`test-coverage-reports`, `test-gate-reporting`, `test-release-artifact`, `test-release-smoke`, `test-codex-model-qualifier`, `test-public-export`, `test-summarize-m4-budgets`, `test-contract-freeze`, `test-m8-performance-unit`, `test-m8-clients-unit` — comprueba la lista real en `.github/workflows/sonarcloud.yml`) con `HOME=$(mktemp -d)`-equivalente bajo `target/` y con `target/m1-17-inspector` **temporalmente renombrado** (`mv target/m1-17-inspector target/m1-17-inspector.bak` y restáuralo en `finally`, pase lo que pase) y `GIT_DIR=/nonexistent`; todas deben quedar `OK`. Reglas Sonar de la casa. Informe (suites ejecutadas y resultado con y sin la carpeta) en `docs/validation/M8/delegation/W37-clients-unit-hermetic/report.md` y en tu última respuesta. No commit.

# W12 — `docs/ci.md`: conteos de etapas del gate y etapas M8 (censo F8)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort low`). Rol: worker de documentación (un archivo). Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano, sin commit. **Único archivo permitido:** `docs/ci.md`.

Evidencia: el recibo `docs/validation/M8/core-gate.json` (gate `core` verde del 2026-09-14, `sha256:f2c2fe69…`) tiene **28 etapas** (lee `steps[].name`), incluidas las dos nuevas de M8-02: `contract-freeze-tests` (`scripts/test-contract-freeze.py`) y `contract-freeze` (`scripts/contract-freeze.py verify docs/validation/M8/freeze-0.8.0.json`, obligatoria: manifiesto ausente = fallo). `scripts/gate.py` `full` = las 28 de core más las etapas nativas/Docker que añade su rama `full` (cuenta y nombra exactamente las etapas de esa rama leyendo `scripts/gate.py`; hoy `docs/ci.md` dice «26 etapas core», «42 en full» y «14 etapas nativas», y el censo M8-01 (F8) marcó esos conteos como desalineados).

Tareas: (1) en `docs/ci.md` §M5 «Tres etapas core nuevas» (~líneas 335-364) y donde se citen los totales, corrige a los conteos reales (28 core; full = 28 + N nativas = total exacto) listando las etapas nativas por su nombre real; (2) añade una subsección «## M8 — etapas de freeze de contrato» que describa las dos etapas nuevas, su orden (tras `m6-runtime-unit-tests`, antes de `vendor`), qué verifican (manifiesto `freeze-0.8.0.json`: hash canónico de schemas/annotations/description por tool, clase registrada, conteo; cambio en `stable`/clase/conteo = fallo; `preview` = aviso salvo `--strict`) y el recibo M8-02; (3) no toques texto histórico de M4/M5 más allá de los conteos vigentes. Verifica con `python3 -B scripts/docs-hygiene.py links-check` (0 rotos). Informe: Files changed / conteos antes→después / Open issues.

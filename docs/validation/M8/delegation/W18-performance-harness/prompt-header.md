# W18 — M8-05: arnés de medición de performance, presupuestos y soak

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de scripts (Python). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano** (mediciones en primer plano con `timeout` amplio en tu llamada). No commit. **Docker: no** en este paquete (las magnitudes Docker quedan `unavailable` en el recibo con motivo; las mide el orquestador después). **Archivos permitidos:** `scripts/measure-m8-performance.py` (nuevo), `scripts/soak-m8.py` (nuevo), `scripts/test-m8-performance-unit.py` (nuevo), `docs/validation/M8/05-budgets.json` (nuevo), `docs/validation/M8/05-measurement.json` (recibo generado), `scripts/gate.py` (una etapa `core`: `m8-performance-unit-tests`), `.github/workflows/sonarcloud.yml` (una línea de coverage para el test nuevo).

## Decisiones (no las cambies): `docs/validation/M8/05.md`; método: `docs/validation/M8/05-budgets-analysis.md` §2 y §4

Reglas Sonar de la casa: rutas desde `ROOT`, sin `/tmp` literal (usa `tempfile` bajo `target/`), sin `type=` en argparse, sin `shell=True`. Servidor: `target/release/rust-engineering-mcp serve --stdio --root <fixture>`; protocolo JSON-RPC por stdin/stdout (`initialize` 2025-06-18 + `notifications/initialized` + `tools/list`; `tools/call` con `name`/`arguments`); mira `scripts/test-m6-clients.py` para el framing y `docs/tools.md` para los argumentos de `rust.project.open` (root + fixture bajo `fixtures/`) y `rust.catalog.status` (sin catálogo → resultado estructurado `unavailable`/degradado, no error de protocolo: eso ES el camino a medir, tiempo de dispatch).

## Tareas

1. `docs/validation/M8/05-budgets.json`: `format_version 1`, `fixed_utc: 2026-09-14`, `recalibration_rule`, `regression_rule: "2 of 3"`, y por magnitud `{id, budget, unit, statistic (p95|max), n, provisional: true|false, basis}` exactamente con los valores de `05.md`.
2. `scripts/measure-m8-performance.py`: `--binary PATH` (por defecto `target/release/rust-engineering-mcp`), `--out PATH`, `--repeat N` (30), `--profile core|local` (local solo si se pasan `--catalog-store/--catalog-trust/...`; si no, `unavailable`). Mide: startup cold (N procesos nuevos, tiempo hasta la respuesta de `tools/list`; descarta el primero como warm-up del host, regístralo aparte), startup warm (segundo `tools/list`), dispatch `rust.project.open` y `rust.catalog.status` (N llamadas en un mismo proceso tras warm-up), RSS idle (`ps -o rss=` tras 5 s de reposo, N procesos) y RSS pico durante los ciclos de dispatch (muestreo 100 ms), tamaño del binario (`stat`). Calcula min/median/p95/max y compara con `05-budgets.json` → `verdict` por magnitud (`within|over|unavailable`) y global; escribe el recibo con `generated_utc`, `head_commit`, `binary_sha256`, host (`uname -m`, `sw_vers` si existe, CPU count), `raw_samples` completos (provenance/variabilidad), `noise_controls` (mismo binario, sin otros procesos del repo, N, warm-up descartado). Las magnitudes Docker (`cancel_observed_p95`, `cleanup_p95`, `local` RSS) se emiten `unavailable` con `reason` cuando no hay Docker/flags.
3. `scripts/soak-m8.py`: `--profile core --cycles N --hours H --sample-every 20 --out PATH`, implementa exactamente el ciclo y los criterios de fallo de `05.md` §Soak para `core` (sin Docker); registra métricas por muestra (RSS, FDs vía `lsof -p`, hijos vía `pgrep -P`, archivos/bytes en `state-root`), calcula la meseta al 5 % y aplica los criterios al final; `--profile local` puede quedar como `NotImplemented` honesto con mensaje (lo hará el orquestador con Docker) — no lo finjas.
4. `scripts/test-m8-performance-unit.py`: tests herméticos (estadísticos p95/mediana, comparación con presupuestos, veredicto 2-de-3 sobre tres recibos, parseo de `ps`/`lsof` con dobles, criterios de soak con series sintéticas, recibo con `unavailable` sin `passed`). `gate.py`: etapa `m8-performance-unit-tests` tras `contract-freeze`. `sonarcloud.yml`: línea de coverage para el test.
5. **Ejecuta una vez** la medición real `core` (`--repeat 30`) con el binario `release` actual (0.8.0) y guarda `docs/validation/M8/05-measurement.json`; ejecuta `soak-m8.py --profile core --cycles 50 --hours 0.1` como calibración corta del arnés (recibo en `target/m8-soak-calibration.json`, no en docs) y reporta si el arnés funciona.

Verificación: `python3 -B scripts/test-m8-performance-unit.py`; `python3 -B scripts/test-gate-reporting.py`; `python3 -c "import json;json.load(open('docs/validation/M8/05-measurement.json'))"`. Informe (Task / Result / Files changed / Tabla de medidas vs presupuesto / Salida de tests / Risks / Open issues) en `docs/validation/M8/delegation/W18-performance-harness/report.md` y en tu última respuesta. No commit.

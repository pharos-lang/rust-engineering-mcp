# W36 — `scripts/test-doctor.py`: admitir y validar el campo aditivo `mutation_journals`

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de scripts. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano (Bash `timeout` hasta 600000 ms), sin commit. **Archivos permitidos:** `scripts/test-doctor.py`, `docs/ci.md` (solo si describe los campos del informe de `doctor`).

La etapa `doctor` del gate `full` falla con `RuntimeError: Unexpected doctor report fields` (`scripts/test-doctor.py:132-134`: `set(report) == {...}` sin `mutation_journals`). Desde M8-03 (`crates/mcp-server/src/doctor.rs`, ADR-088 §3) el informe JSON de `doctor` incluye el campo aditivo `mutation_journals` (`null` sin `--state-root`; objeto con `pending`, `terminal`, `unknown_format`, `kinds`, `downgrade_blocking_kinds`, `downgrade_blocked`, `notes` con `--state-root`) y `format_version` sigue en 1. Actualiza el conjunto esperado y **valida** el campo en los dos casos (sin state-root → `null`; con `--state-root` vacío → objeto con `pending == 0`, `terminal == 0`, `unknown_format == 0`, `downgrade_blocked == false`), reutilizando la forma de invocación que el script ya usa (mira cómo construye los argumentos de `doctor` y si dispone de un `--state-root` en algún caso; si no, añade un caso mínimo con un directorio temporal bajo `target/`). Ejecuta `python3 -B scripts/test-doctor.py` con el mismo entorno que el gate `full` (`RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock RUST_MCP_E5_DIR=<repo>/target/m1-15-candidate/assets/model ORT_LIB_LOCATION=/Users/cburgosro/Library/Caches/ort.pyke.io/dfbin/aarch64-apple-darwin/612739f75438dc0a075461e1fb454226b4a1eb175e60a7271ba966bbbb972cd4`; lee `scripts/gate.py` para los argumentos exactos de la etapa `doctor`) y pega el resultado. Informe en `docs/validation/M8/delegation/W36-test-doctor-fields/report.md` y en tu última respuesta. No commit.

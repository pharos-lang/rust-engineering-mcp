# W16 — ADR-088 (D12) y documentación de upgrade/rollback/backup

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de documentación. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano, sin commit. **Archivos permitidos:** `docs/adr/ADR-088-migration-rollback-policy.md` (nuevo), `docs/adr/README.md` (una viñeta), `docs/roadmap/adr-backlog-m2-m8.md` (solo §D12 Status), `docs/compatibility.md` (nueva sección «Upgrade, rollback y backup»), `README.md` (sección de operación: backup/restore/rollback, ≤ 25 líneas), `docs/validation/M8/01-census.json` (solo `disk_formats[]`: `floor_or_trust_state` de RustSec y vendor tree → `false` + nota), `CHANGELOG.md` (una viñeta en 0.8.0).

Fuente de la decisión (no la reinterpretes): `docs/validation/M8/03.md` §D12 (8 puntos) con la evidencia de `docs/validation/M8/03-formats-analysis.md`. Estructura de la casa: mira ADR-086/ADR-087. `Date: 2026-09-14`, `Status: Accepted` (decisión del orquestador bajo autorización del owner en sesión, 2026-09-14). Alternatives: CLI de migración genérica ahora (descartada: sin formato real que migrar, prohibido por el plan); bloqueo de `serve` con journal pendiente (descartada: DoS por journal ajeno; visibilidad en `doctor` + fail-closed por registro); bloquear en RustSec stale (descartada: ruptura de contrato M1). Consequences: incluye los puntos 6 (prueba con dos binarios, recibo `03-rollback.json`) y 7 (procedimiento de backup).

`docs/compatibility.md` §«Upgrade, rollback y backup»: tabla de los 10 formatos (marcador de versión, fail-closed, cambio desde 0.3.0, recuperación) a partir de la tabla (a) del análisis, en ≤ 20 filas/líneas, y el procedimiento de rollback de binario (servidor parado; journal pendiente ⇒ resolver con el binario nuevo o `mutation prune` explícito antes de bajar de versión; floors de catálogo no retroceden). README: procedimiento operativo breve con `doctor`. Verificación: `python3 -B scripts/docs-hygiene.py links-check` 0 rotos; `python3 -c "import json;json.load(open('docs/validation/M8/01-census.json'))"`. Informe en `docs/validation/M8/delegation/W16-adr-d12/report.md` y en tu última respuesta. No commit.

# W20 — correcciones de arneses: soak reutiliza `project_ref`; pin de Gemini CLI 1.2.2

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de scripts. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: la herramienta Bash acepta `timeout` hasta 600000 ms — úsalo; `run_in_background` está PROHIBIDO y termina tu sesión sin informe.** No commit. No Docker. **Archivos permitidos:** `scripts/soak-m8.py`, `scripts/test-m8-performance-unit.py`, `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`, `docs/validation/M8/05.md` (solo una nota en §Soak).

## 1. Soak (`scripts/soak-m8.py`)

La calibración de 50 ciclos (`target/m8-soak-calibration.json`) falló solo por `fd_growth` (8 → 57): el ciclo llama a `rust.project.open` en **cada** iteración y cada apertura conserva handles del proyecto hasta su TTL (comportamiento esperado, acotado por TTL/límite de sesión), lo que contradice la decisión de `docs/validation/M8/05.md` («no crear proyectos nuevos en cada ciclo; reutilizar el mismo `project_ref`»). Cambia el ciclo a: abrir el proyecto **una vez** al inicio (y re-abrir solo si el servidor responde que el `project_ref` expiró — registra cuántas reaperturas), y por ciclo `rust.catalog.status` → `rust.crate.search` (+ `rust.project.inspect` **no**: requiere runtime). Añade una fase opcional `--open-churn N` (por defecto 20, ejecutada al final) que abre el mismo path N veces y registra FDs antes/después y tras esperar `--ttl-wait-seconds` (por defecto 0 = no espera) — evidencia, no criterio de fallo. Actualiza el criterio `fd_growth` para medir sobre la fase principal (sin churn) y deja constancia en el recibo (`notes[]`) de por qué el churn queda fuera del criterio (handles acotados por TTL, ver `docs/tools.md` §«Límite de sesión»). Actualiza los tests de `test-m8-performance-unit.py` que cubran el soak (series sintéticas) para el nuevo ciclo. Ejecuta de verdad `python3 -B scripts/soak-m8.py --profile core --cycles 30 --hours 0.05 --sample-every 10 --out target/m8-soak-calibration-2.json` (foreground, timeout amplio) y reporta `criteria` y `status` (debe quedar `passed` si el servidor no crece).

## 2. Matriz de clientes (`scripts/test-m8-clients.py`)

El preflight bloquea por `gemini_cli 1.2.1 -> 1.2.2`: el orquestador decide fijar **`agy` 1.2.2** (versión instalada y verificada con `agy --version`). Cambia el pin y cualquier mención en docstring/tests; añade en el recibo de preflight el campo `pinned_versions` con los cuatro clientes. Ejecuta `python3 -B scripts/test-m8-clients.py --preflight` de verdad: debe quedar `status: ready`. `test-m8-clients-unit.py` verde.

Añade en `docs/validation/M8/05.md` §Soak una línea: «Calibración 1 (50 ciclos): `fd_growth` falló por reaperturas de proyecto por ciclo (handles acotados por TTL, no leak); el ciclo pasa a reutilizar `project_ref` y el churn de aperturas se mide aparte.»

Informe (Task / Result / Files changed / salidas de soak y preflight / tests / Risks / Open issues) en `docs/validation/M8/delegation/W20-harness-fixes/report.md` y en tu última respuesta. No commit.

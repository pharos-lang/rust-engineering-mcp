# W33 — endurecimientos V04: `persist-credentials: false` (F-05) y rutas de host dentro de roots (F-06)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de implementación. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `.github/workflows/ci.yml`, `.github/workflows/release-candidate.yml`, `.github/workflows/sonarcloud.yml`, `.github/workflows/codeql.yml` (solo añadir `persist-credentials: false` a cada `actions/checkout`), `crates/mcp-server/src/host_config.rs`, `crates/mcp-server/tests/cli.rs` (o el test de host config existente), `docs/tools.md` (frase de `serve` sobre rutas de host).

Lee `docs/validation/M8/delegation/V04-security-audit/report.md` F-05 y F-06. (1) Añade `with: persist-credentials: false` a cada paso `actions/checkout` de los cuatro workflows sin cambiar nada más (verifica que ningún paso posterior necesite el token del checkout: `gh` usa `GITHUB_TOKEN` por env, no las credenciales persistidas). (2) `host_config.rs`: rechaza `--rustsec-snapshot`, `--catalog-store`, `--catalog-trust`, `--catalog-model-dir` y `--catalog-index-store` cuando la ruta está dentro de (o es igual a) cualquier `--root`, con el mismo mensaje/patrón que ya usan `--security-policy`, `--cargo-vendor-dir` y la captura (líneas ~206-255); mantén la regla del state-root tal cual. Añade tests (uno por flag: dentro de root → error de usage con exit 2; fuera → aceptado) siguiendo los tests vecinos de `cli.rs`. Comprueba que ningún arnés/fixture del repo configura esas rutas dentro de una root (`grep -rn 'catalog-store\|rustsec-snapshot' scripts/ fixtures/ | head`); si alguno lo hace, repórtalo en vez de romperlo. Verificación: `cargo fmt --all -- --check`; `cargo clippy -p rust-engineering-mcp --all-targets --locked --offline -- -D warnings`; `cargo test -p rust-engineering-mcp --locked --offline --test cli`; `python3 -B scripts/test-gate-reporting.py`; YAML válido (`python3 -c "import yaml,sys;[yaml.safe_load(open(f)) for f in sys.argv[1:]]" .github/workflows/*.yml` si `yaml` está disponible). Informe en `docs/validation/M8/delegation/W33-v04-hardening/report.md` y en tu última respuesta. No commit.

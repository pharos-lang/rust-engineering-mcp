# W08 — M8-02: versión 0.8.0, CHANGELOG y migration notes, docs del freeze

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de versión y documentación. Orquestador: Claude Fable 5.1. No puedes lanzar subagentes. **Nunca corras comandos en segundo plano.** No hagas commit. No hagas Docker. **Archivos permitidos:** `Cargo.toml` (solo `version`), `Cargo.lock` (solo vía `cargo update --workspace --offline`), `CHANGELOG.md`, `docs/tools.md`, `docs/compatibility.md`, `docs/client-configuration.md`, `docs/implementation-status.md`, `scripts/release-smoke.py` (solo el docstring de conteo), `README.md` (solo menciones de versión del checkout).

## Contexto

Lee `docs/validation/M8/02.md` (decisiones 1, 2, 9), `docs/validation/M8/01.md` §4 (F5, F6), `docs/validation/M8/02-schema-diff.json` (before/after real generado por W06), `docs/adr/ADR-086-deprecation-and-freeze-policy.md`, `docs/adr/ADR-087-1.0-host-scope.md`. Ya en el árbol: subcomando `contract` (W05), prefijo `Preview (ADR-086): ` en 5 descripciones, `scripts/contract-freeze.py` (W06), manifiesto `docs/validation/M8/freeze-0.8.0.json`.

## Tareas

1. **Versión**: `Cargo.toml` workspace `version = "0.8.0"`; refresca `Cargo.lock` con `cargo update --workspace --offline` (solo deben cambiar las entradas de los 8 crates del workspace: verifica con `git diff Cargo.lock | grep '^[-+]version'`). Luego `cargo check -p rust-engineering-mcp --locked --offline` debe pasar.
2. **CHANGELOG**: renombra «## Sin publicar» a «## 0.8.0 — freeze de contratos (sin publicar; RC en M8-09)» y añade **al principio** de esa sección un bloque «Migration notes 0.3.0 → 0.8.0» con, exactamente y con evidencia: (a) inventario 31 → 36 (cinco `rust.analyzer.*`, clase `preview`, ADR-086); (b) nuevo grant de host `--allow-analyzer-action-write WORKSPACE_ROOT` y la imagen M6 vía `--rust-image`; (c) 30 contratos `stable` byte-idénticos a 0.3.0; único cambio: `rust.binary.bloat`, cuyo `inputSchema`/`outputSchema` cambian **solo en el texto `description` de `$defs/BloatProfile`** (ruta de evidencia reescrita por la hygiene del repo; sin cambio de validación ni de annotations) — cita `docs/validation/M8/02-schema-diff.json` (`keys_changed: [inputSchema, outputSchema]`); (d) las trece M1 byte-idénticas a `0.1.0`; (e) **deprecaciones anunciadas en 0.8.0: ninguna** → por ADR-086 §4 nada se retira en 1.0; (f) nuevo subcomando `rust-engineering-mcp contract [--json]` (spec §56) y manifiesto de freeze verificado por el gate `core`; (g) clase `preview` visible por prefijo de descripción en las cinco tools analyzer; (h) alcance 1.0 = macOS ARM64 (ADR-087). Sin inventar fechas de release.
3. **`docs/tools.md`**: añade una sección «Familias de códigos de error» (F5): envelope operacional `SCREAMING_SNAKE_CASE` (`OperationalErrorCode` + extensiones cerradas por tool; 31 tools, incluida `rust.analyzer.action.apply`) y envelope de mutación M2 `snake_case` (`Reason`; 5 tools) — `error_code` se lee por tool, nunca como enum global; y una nota (F6) de que las Resources son dinámicas por sesión (`resources/list` = `[]` por diseño; plantillas `rust-artifact://…` y `rust-quality-artifact://…`) y que los ejemplos de spec §9.2 no se implementaron (ADR-011). Añade también una subsección «Clases de estabilidad y documento de contrato» que enlace ADR-086, `contract --json` y el manifiesto.
4. **`docs/compatibility.md`**: fila «Checkout de desarrollo» → `0.8.0` (freeze), 36 tools (31 `stable`, 5 `preview`); sección de política: sustituye la nota `preview` duplicada por una sola frase que enlace a la de `client-configuration.md` o viceversa (elige una fuente única; V01b P3). Añade una fila/línea «Documento de contrato: `rust-engineering-mcp contract --json`; manifiesto `docs/validation/M8/freeze-0.8.0.json`».
5. **`docs/implementation-status.md`**: añade una sección «## M8 — Stabilization 0.8–0.9» con filas M8-01 (Done local: enlaza `validation/M8/01.md`) y M8-02 (En curso → los entregables de este paquete), D11 (ADR-086), D13 (ADR-087), y actualiza la fila de la tabla «Assessment» que mencione «M6 en desarrollo» o «31 tools» si existe. Además, en la tabla de hitos (~línea 177-178): M7 → «Deferred con decisión (acta `m7-g0-decision.md`, 2026-09-13)» y M8 → «En curso (M8-01 Done local; M8-02 freeze)». No toques filas M0–M6.
6. `scripts/release-smoke.py` docstring: corrige el conteo si sigue diciendo «31 tools since 0.3.0; 32 …» para reflejar 36 en 0.8.0 (solo comentario). `README.md`: donde diga «aún en versión `0.3.0`», ponlo en 0.8.0 (freeze, sin release).

## Verificación (foreground)

`cargo check -p rust-engineering-mcp --locked --offline`; `python3 -B scripts/docs-hygiene.py links-check` (0 rotos); `python3 -B scripts/contract-freeze.py verify docs/validation/M8/freeze-0.8.0.json` (passed); `git diff --stat`. Informe: Task / Result / Files changed / Diff de `Cargo.lock` (solo versiones del workspace) / Risks / Open issues. No commit.

# W04 — correcciones V01 (ADR-086 §1, censo, docs públicas)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de correcciones acotadas de documentación. Orquestador: Claude Fable 5.1. No puedes lanzar subagentes. **Nunca corras comandos en segundo plano.** No hagas commit. **Archivos que puedes editar (y ningún otro):** `docs/adr/ADR-086-deprecation-and-freeze-policy.md`, `docs/compatibility.md`, `docs/client-configuration.md`, `README.md`, `docs/validation/M8/01-census.json`, `docs/validation/M8/01-census.md`.

## Contexto

La revisión V01 (`docs/validation/M8/delegation/V01-review-census/report.md`) y la disposición del orquestador (`…/V01-review-census/disposition.md`) aceptaron estos findings. Aplica exactamente las decisiones; no reinterpretes.

## Tareas

1. **ADR-086 §Decision punto 1** (F-A, F-D). Reescribe el punto 1 para que diga, además de lo actual: (a) las clases son `stable`, `preview`, `experimental` e `internal`; `experimental` es la categoría de spec §57 para tools anunciadas pero no calificadas, con opt-in por namespace o metadata, **hoy sin uso** (ninguna tool se anuncia experimental); `internal` designa elementos existentes que no se anuncian (`tools/list`/docs públicas) — comandos CLI y formatos privados; (b) **definición de «consumidor real»**: una invocación registrada en un recibo de cliente stock (Inspector, Codex, Claude Code, Gemini CLI) **o** un test end-to-end nativo que atraviesa el wire MCP contra el servidor real; un test unitario o de contrato no cuenta; (c) toda tool `stable` debe ser ejercitada por al menos un cliente stock en la matriz M8-04 antes de RC1; si no lo supera, se degrada a `preview` antes del freeze, con constancia en el changelog. Añade en `Consequences` una viñeta: «Las tres tools M3 (`rust.coverage`, `rust.semver.check`, `rust.mutation.test`) hoy solo tienen e2e nativo como consumidor y quedan condicionadas a M8-04». Actualiza la fecha de enmienda en una línea «Amended: 2026-09-14 (V01 F-A/F-D)» bajo `Date`.
2. **`docs/compatibility.md` §«Política de deprecación y freeze (0.8 → 1.0)»**: refleja las cuatro clases y la definición de consumidor real en ≤ 6 líneas nuevas. En la fila «Checkout de desarrollo» (línea ~8) y/o en esa sección, añade una frase: las cinco `rust.analyzer.*` son clase **`preview`** (deuda de contrato conocida: `SANDBOX_DENIED` transitorio vs permanente, diagnósticos sintaxis-only, assists no deterministas — `docs/validation/M6/matrix.md` §Deuda), las 31 restantes `stable`.
3. **`docs/client-configuration.md` §«Configurar las tools M6»** (F-M): añade un párrafo corto (≤ 5 líneas) tras el primer párrafo: clase `preview` (ADR-086), deuda conocida (las tres de arriba) y que su contrato puede cambiar en 0.8/0.9 con migration notes.
4. **`README.md:26`**: la frase «solo M6 está en desarrollo local, sin integración remota ni release» es falsa: M6 está integrado en `main` (PR #20, `e50c3fe`) y calificado; sin tag ni release todavía. Corrige esa frase (y solo esa) manteniendo el estilo del párrafo y añade «(clase `preview`, ver docs/compatibility.md)».
5. **`01-census.json`** (F-G): añade a cada `tools[]` el campo booleano `stock_client_invoked` = `true` si `real_consumers[]` cita un recibo de cliente stock (`docs/validation/M*/clients*.json`, `clients/attempt-*/protocol.jsonl`, `17-inspector.md`, `17-codex-model.md`, `17-matrix.md`), `false` si solo cita tests nativos (`*_runtime.rs`, `*_native*.rs`). Deben salir exactamente 33 `true` y 3 `false` (`rust.coverage`, `rust.semver.check`, `rust.mutation.test`); si sale otra cosa, repórtalo sin forzar. Añade `error_model`-independiente un campo top-level `stock_client_coverage: {"invoked": 33, "native_e2e_only": ["…"]}`.
6. **`01-census.md`** §2 tabla resumen: añade una columna «Cliente stock» (sí/no) o un párrafo con los 3 `no`; §3.2 (F-H): añade una nota metodológica de que el porcentaje por tool usa como numerador solo `inputSchema+outputSchema` (sin `description`/`annotations`) y como denominador los bytes totales del `tools/list` live, por lo que subestima ligeramente cada porcentaje individual; la conclusión (ningún outlier) no cambia.

## Verificación (foreground)

`python3 -c "import json;d=json.load(open('docs/validation/M8/01-census.json'));print(sum(t['stock_client_invoked'] for t in d['tools']))"` → 33; `python3 -B scripts/docs-hygiene.py links-check` → 0 rotos en documentos vivos. Informe final: Task / Result / Files changed (líneas) / Evidencia / Risks / Open issues. No commit.

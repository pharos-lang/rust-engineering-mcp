# W27b — soak: criterio `fd_after_ttl` tras TTL + una apertura (expiración perezosa)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de scripts. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `scripts/soak-m8.py`, `scripts/test-m8-performance-unit.py`.

Decisión del orquestador en `docs/validation/M8/05.md` §«Hallazgo del soak»: la expiración de proyectos es perezosa (se recolecta en el siguiente `rust.project.open`; verificado 29 → 10 FDs). Cambia la fase `open_churn` para registrar tres medidas: `fd_count_after` (tras el churn), `fd_count_after_ttl_wait` (tras esperar `--ttl-wait-seconds`, sin llamadas) y **`fd_count_after_reclaim_open`** (tras una apertura adicional del mismo path, que dispara la recolección). El criterio `fd_after_ttl` pasa a evaluarse sobre `fd_count_after_reclaim_open ≤ plateau + 10`; añade un campo informativo `retained_until_next_open = fd_count_after_ttl_wait - plateau` y una nota en `notes[]` que describa la expiración perezosa como propiedad de diseño acotada (TTL + límite de sesión) sin afirmar nada no medido. Actualiza los unit tests (series sintéticas para pasa/falla del criterio nuevo). Ejecuta de verdad `python3 -B scripts/soak-m8.py --profile core --cycles 20 --hours 0.05 --sample-every 5 --out target/m8-soak-smoke-2.json` (foreground): reporta `criteria` y `status` (esperado `passed` si la recolección funciona). `python3 -B scripts/test-m8-performance-unit.py` verde. Informe en `docs/validation/M8/delegation/W27b-soak-ttl-criterion/report.md` y en tu última respuesta. No commit.

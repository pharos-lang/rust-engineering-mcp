# W06 — M8-02: manifiesto de freeze, diff before/after y etapa de gate

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de scripts (Python). Orquestador: Claude Fable 5.1. No puedes lanzar subagentes. **Nunca corras comandos en segundo plano.** No hagas commit. No hagas Docker. **Archivos permitidos:** `scripts/contract-freeze.py` (nuevo), `scripts/test-contract-freeze.py` (nuevo), `scripts/gate.py` (solo añadir una etapa `core`), `docs/validation/M8/02-schema-diff.json` (generado). **No generes** `docs/validation/M8/freeze-0.8.0.json` todavía (el orquestador lo genera cuando W05 haya regenerado 5 snapshots) — pero el comando debe existir y estar probado.

## Contexto

Lee `docs/validation/M8/02.md` (decisiones 5–7), `docs/adr/ADR-086-deprecation-and-freeze-policy.md`, `scripts/gate.py` (cómo se registran etapas `core`: `run('nombre', [sys.executable,'-B','scripts/x.py', …])`), `scripts/test-gate-reporting.py` y `scripts/test-docs-hygiene.py` (estilo de tests de la casa: `unittest`, sin dependencias externas). Snapshots: `crates/mcp-server/tests/snapshots/*-tool.json` (36 `*-tool.json`; `doctor-report.json` no es tool). Clases: `preview` = `rust.analyzer.symbols|references|diagnostics|actions|action.apply`; `stable` = las 31 restantes (constante cerrada en el script; el test comprueba que la unión son exactamente los 36 nombres de los snapshots).

**Reglas SonarCloud de la casa (obligatorias):** rutas constantes desde `pathlib.Path(__file__).resolve().parents[1]`; sin literales `/tmp`; en `argparse` no uses `type=` para rutas (valida a mano); sin `shell=True`; subprocess solo con listas de argumentos fijas (`git`).

## Tareas

1. `scripts/contract-freeze.py` con subcomandos:
   - `generate --out PATH`: escribe el manifiesto `{"format_version":1,"generated_utc":…,"head_commit":…,"canonical":"json sort_keys separators(',',':') ensure_ascii=False sha256","tools":{name:{"stability":…,"annotations":{…},"input_schema_sha256":…,"output_schema_sha256":…,"description_sha256":…,"snapshot_sha256":…(bytes del archivo)}},"tool_count":36,"stable_count":31,"preview_count":5}`. Hash canónico exactamente `hashlib.sha256(json.dumps(obj, sort_keys=True, separators=(",", ":"), ensure_ascii=False).encode("utf-8")).hexdigest()`.
   - `verify MANIFEST [--strict]`: recalcula desde los snapshots actuales; cualquier diferencia en una tool `stable` (nombre ausente/nuevo, schema, annotations, description) → exit 1 con lista; diferencia en `preview` → aviso en stdout y exit 0, salvo `--strict` (exit 1). Cambio de `tool_count` → exit 1 siempre. Imprime un resumen JSON de una línea al final (`{"status":"passed|failed","stable_changed":[…],"preview_changed":[…]}`).
   - `diff --base REF --out PATH`: obtiene los snapshots del ref con `git show REF:crates/mcp-server/tests/snapshots/<file>` (`git ls-tree --name-only REF crates/mcp-server/tests/snapshots/`), compara con el árbol actual y escribe `{"base":REF,"base_commit":…,"head_commit":…,"added":[…],"removed":[…],"changed":[{"name":…,"keys_changed":["description",…],"input_schema_changed":bool,"output_schema_changed":bool,"annotations_changed":bool}],"unchanged":[…]}`.
2. Genera **ahora** `docs/validation/M8/02-schema-diff.json` como un objeto con dos entradas: `"since_v0.3.0"` (diff base `v0.3.0`) y `"since_v0.1.0_m1_only"` (diff base `v0.1.0` restringido a los 13 nombres M1: project.open, project.inspect, toolchain.inspect, check, fmt.check, clippy, test, dependencies.audit, diagnostics.explain, quality.gate, catalog.status, crate.search, crate.inspect — añade una opción `--only NAME,NAME…`). Resultado esperado: desde `v0.1.0` los 13 M1 `unchanged`; desde `v0.3.0` 5 `added` (analyzer) y `binary.bloat` `changed` solo en `description`. Si observas otra cosa, repórtalo sin maquillar. (Nota: W05 está regenerando en paralelo los 5 snapshots `analyzer-*`; si al leerlos el JSON está a medias, reintenta; los 31 restantes no cambian.)
3. `scripts/test-contract-freeze.py` (`unittest`): fixtures en un directorio temporal (`tempfile.TemporaryDirectory()`), casos: generate→verify passed; cambio de schema en stable → failed; cambio de description en preview → passed con aviso y failed con `--strict`; tool nueva → failed; hash canónico contra un vector fijo; clases cubren exactamente los 36 nombres reales del repo.
4. `scripts/gate.py`: añade la etapa `core` `contract-freeze-tests` (`scripts/test-contract-freeze.py`, `require_test_groups=True`) justo después de `m6-runtime-unit-tests`, y la etapa `contract-freeze` = `verify docs/validation/M8/freeze-0.8.0.json` **solo si el manifiesto existe** (usa `Path.exists()`; si no existe, registra la etapa como `skipped` con motivo, siguiendo el patrón que ya use `gate.py` para etapas condicionales, o si no hay patrón, omítela y dilo en el informe). No cambies nada más del gate.

## Verificación (foreground)

`python3 -B scripts/test-contract-freeze.py` verde; `python3 -B scripts/contract-freeze.py diff --base v0.3.0 --out /dev/stdout | head`; `python3 -c "import json;json.load(open('docs/validation/M8/02-schema-diff.json'))"`; `python3 -B scripts/test-gate-reporting.py` (si valida la lista de etapas). Informe: Task / Result / Files changed / Resumen del diff (added/changed/unchanged por base) / Salida de tests / Risks / Open issues. No commit.

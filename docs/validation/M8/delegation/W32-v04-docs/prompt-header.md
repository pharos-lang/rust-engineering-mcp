# W32 — correcciones documentales V04 (threat model, ADR-089, ADR-088)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de documentación. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano (Bash `timeout` hasta 600000 ms), sin commit. **Archivos permitidos:** `docs/validation/M8/08-threat-model.md`, `docs/adr/ADR-089-residual-risk-register.md`, `docs/adr/ADR-088-migration-rollback-policy.md` (§3, una frase), `docs/security-model.md` (lista RR), `docs/validation/M8/checklist-1.0.md` (fila 10/11: ítems bloqueantes de RC1).

Lee `docs/validation/M8/delegation/V04-security-audit/disposition.md` y `report.md`. Aplica: F-01 (citas de `env_clear` → `crates/execution-adapter/src/lib.rs:333-346` y call sites `rust_gateway.rs:1832`, `mutation_gateway.rs:369,412`, `analyzer_gateway.rs:551`; verifica las líneas con Grep antes de escribirlas); F-02 (citas B8: permisos `release-candidate.yml:41-44`, verify `:147-164`, `contents: write` `:178-179`, checkout `:47`; cerrar el issue §9.1 con evidencia y la consecuencia de ADR-089 §98-100); F-03 (ADR-088 §3 y threat model B7: `doctor` abre el store, crea `mutation-store.lock` si falta y toma un `flock` no bloqueante durante el escaneo → `Busy` con nota propia); F-07 (RR-12 se divide: riesgos aceptados quedan; «re-observar branch protection», «provenance 0.8.x» y «D14 verificado sobre assets» pasan a `checklist-1.0.md` como ítems bloqueantes de RC1); RR-10 ampliado con la exposición del token OIDC al job `build` (mitigación: `persist-credentials: false` — W33 — y split del job como condición de reevaluación); nuevo **RR-19** (Baja): sin firma de código ni notarización macOS; integridad por `SHA256SUMS` + attestation; condición de reevaluación: distribución por canal que active Gatekeeper (Homebrew/instalador). `docs/security-model.md` lista RR-19. Verificación: `python3 -B scripts/docs-hygiene.py links-check` 0 rotos (la ruta `06-reproduction.md` puede seguir rota hasta W29: ignórala). Informe en `docs/validation/M8/delegation/W32-v04-docs/report.md` y en tu última respuesta. No commit.

# W22 — M8-08: threat model completo 0.8/1.0 y registro de riesgos residuales (ADR-089)

Modelo solicitado: Claude Opus 5 (`claude -p --model opus --effort high`). Rol: worker de seguridad (documentación normativa; solo lectura del código). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano** (Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO). No commit. **Archivos permitidos:** `docs/validation/M8/08-threat-model.md` (nuevo), `docs/adr/ADR-089-residual-risk-register.md` (nuevo), `docs/adr/README.md` (una viñeta), `docs/security-model.md` (sección nueva «Riesgos residuales 1.0» + correcciones de hechos desactualizados si los hay, con cita), `SECURITY.md` (solo si contradice el estado actual).

## Marco (vinculante)

Plan `docs/roadmap/m8-stabilization.md` §M8-08 y §«Migración, recovery y seguridad» (threat review: dependencias comprometidas, secretos en source/evidencia, poisoning del catálogo/modelo, escapes de containment, credenciales de publicación; cuotas/admisión/cancelación/entorno/red = G2/G3; permisos de journals/artifacts y su retención/borrado; «una revisión automática no se denomina auditoría humana»); checklist 1.0 (`§Checklist`): «Security model coincide con enforcement y cada capability tiene oráculo nativo; auditoría independiente sin P0/P1» y «Registro de riesgos residuales con ADR de aceptación, alcance, condición de reevaluación y re-review M8-08; security model público los enumera». G2 (`docs/roadmap/m2-m8.md`). Alcance de hosts: ADR-087 (solo macOS ARM64 + gateway Docker Linux ARM64). Estado: 36 tools (31 `stable`, 5 `preview`, ADR-086), formatos y rollback (ADR-088, `docs/validation/M8/03-formats-analysis.md`), deuda M6 (`docs/validation/M6/matrix.md` §Deuda: `SANDBOX_DENIED` transitorio, diagnósticos sintaxis-only, assists no deterministas, precisión de `admitted`). Fuentes de enforcement: `docs/security-model.md`, ADR-009/024/030/031/050/077/084/085, `crates/execution-adapter` (gateway, seccomp `*.json`, admisión), `crates/project-adapter/src/filesystem/macos` (no-follow), `crates/mcp-server/src/host_config.rs` (grants), `crates/mcp-server/src/stdio/mutation/audit.rs`, workflows `.github/workflows/*.yml` (OIDC/attestations), `docs/publication.md`, ADR-047/048.

## Entregable 1 — `08-threat-model.md`

Threat model **completo** del producto 0.8.0 en macOS ARM64, con tablas: (1) actores y activos; (2) trust boundaries (host/operador, cliente MCP/agente, proyecto hostil incl. build.rs/proc-macros/tests/rust-analyzer, guest Docker, catálogo/bundle firmado, modelo E5/ORT, evidencia/receipts, pipeline de publicación GitHub/OIDC); (3) por cada frontera: entradas hostiles, abusos, **control existente con cita archivo:línea o test/oráculo nativo que lo demuestra** (no afirmes sin cita; si no hay oráculo, dilo), y riesgo residual; (4) los cinco temas obligatorios del plan (dependencias comprometidas — `cargo audit/deny`, pins `=`, vendor SHA-256; secretos en source/evidencia — redacción, `assert_no_credentials` de los arneses, qué NO se detecta; poisoning de catálogo/modelo — firma Ed25519, floor, E5 SHA-256, LanceDB derivado; escapes de containment — seccomp, red, PIDs, memoria, cancel/join, cuarentena; credenciales de publicación — OIDC, sin claves largas, branch protection); (5) permisos, retención y borrado de journals/artifacts/state-root (qué garantiza el código, qué es procedimiento de operador); (6) capacidades anunciadas ↔ oráculo nativo que las prueba (tabla `docs/security-model.md` vs tests `*_native*.rs`, `security_native_adversarial.rs`, `test-m4-*`), marcando cualquier capability sin oráculo; (7) lista de **riesgos residuales** con id `RR-n`, severidad, alcance, mitigación existente, condición de reevaluación.

## Entregable 2 — ADR-089 «Registro de riesgos residuales 1.0»

Status **Accepted** (decisión del orquestador bajo autorización del owner del 2026-09-14 para una primera versión estable en macOS): lista `RR-n` con aceptación explícita, alcance, condición de reevaluación y la re-review obligatoria en M8-08; incluye **explícitamente**: (a) la auditoría independiente de 1.0 es una **revisión de modelo** (Opus 5 High, read-only, alcance declarado) y **no** una auditoría humana ni un pentest — riesgo residual aceptado para la primera versión estable en macOS, con condición de reevaluación (antes de anunciar otro host o remoto); (b) hosts Linux/Windows no calificados (ADR-087); (c) deuda M6 (analyzer `preview`); (d) `rust.dependencies.audit` degrada (no bloquea) ante snapshot RustSec stale (ADR-088 §5); (e) lo que resulte del threat model. Alternatives: contratar auditoría humana antes de 1.0 (descartada por decisión de alcance; queda como condición de reevaluación); no publicar 1.0. `docs/security-model.md`: sección «Riesgos residuales 1.0» que enumere los `RR-n` (requisito del checklist) y enlace al ADR y al threat model.

Sin inventar: cada control citado debe existir; si un test citado no existe, no lo cites. Verificación: `python3 -B scripts/docs-hygiene.py links-check` 0 rotos. Informe (Task / Result / Files changed / conteo de fronteras, controles con oráculo y sin oráculo, `RR-n` / Risks / Open issues) en `docs/validation/M8/delegation/W22-threat-model/report.md` y en tu última respuesta. No commit.

# W39b — cobertura de subprocesos: los tests de integración de `mcp-server` propagan `LLVM_PROFILE_FILE` al binario que lanzan

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de tests (Rust). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `crates/mcp-server/tests/*.rs` (solo los helpers que construyen `std::process::Command`/`tokio::process::Command` con `.env_clear()`); ningún archivo de `src/`, ningún snapshot, ningún `Cargo.toml`.

Hallazgo de W39 (`docs/validation/M8/delegation/W39-rust-portable-coverage/report.md`): todos los helpers de spawn de los tests de integración (`tests/cli.rs:8,118,128`, `protocol.rs:60,2307`, `capabilities_cli.rs:32`, `quality_artifact_cli.rs:33`, `catalog_cli.rs:48`, `catalog_status.rs:102,135`, `crate_inspect.rs:102,137`, `crate_search.rs:102,137`, `doctor.rs:149`, `inspection_runtime.rs:159,374,1662`, `analyzer_runtime.rs:107`) usan `.env_clear()`, que elimina `LLVM_PROFILE_FILE`; `cargo llvm-cov` recoge la cobertura de subprocesos únicamente por esa variable, así que las 61 pruebas de `protocol.rs`, las 23 de `cli.rs`, etc. aportan **cero** cobertura al binario en SonarCloud (Linux, `cargo llvm-cov --workspace --all-targets --lcov`). El aislamiento de entorno es deliberado (sentinelas de `PATH` hostil, `HOME`, etc.) y debe conservarse.

Tarea: tras cada `.env_clear()` de esos helpers, reinyecta **solo** `LLVM_PROFILE_FILE` cuando esté definida en el proceso padre (`if let Some(v) = std::env::var_os("LLVM_PROFILE_FILE") { cmd.env("LLVM_PROFILE_FILE", v); }`), sin tocar ningún otro aspecto del entorno ni del comportamiento de los tests. Evita duplicar el bloque 20 veces si un helper compartido cabe sin cambiar la estructura (p. ej. una función `fn instrumented(cmd: &mut Command) -> &mut Command` local por archivo de test; los tests de integración son crates separados, no crees `tests/common/` nuevo si eso implica tocar más de lo necesario). Mantén `clippy -D warnings` (el workspace deniega `unwrap_used`/`expect_used`/`panic`).

Verificación obligatoria (reporta salida): `cargo fmt --all -- --check`; `cargo clippy -p rust-engineering-mcp --all-targets --locked --offline -- -D warnings`; `cargo test -p rust-engineering-mcp --locked --offline` (todo verde); medición **antes/después** con `cargo llvm-cov -p rust-engineering-mcp --all-targets --locked --offline --summary-only` (está instalado; usa `cargo llvm-cov clean --workspace` entre mediciones) reportando el % de líneas de `src/main.rs`, `src/contract_cli.rs`, `src/doctor.rs`, `src/mutation_cli.rs`, `src/host_config.rs`, `src/stdio.rs`, `src/stdio/capability_document.rs` y el total del crate; `git status --short crates/mcp-server/tests/snapshots` vacío; `git status --short` sin archivos fuera de `crates/mcp-server/tests/*.rs`. Informe en `docs/validation/M8/delegation/W39b-llvm-profile-passthrough/report.md` y en tu última respuesta. No commit.

# W28c — arnés de clientes: sesión runtime del Inspector primero, recibo incremental, turnos con timeout registrados

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano (Bash `timeout` hasta 600000 ms), sin commit, sin ejecutar `--run --with-runtime` completo. **Archivos permitidos:** `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`.

Evidencia (`docs/validation/M8/clients/attempt-20`): en `--run --with-runtime`, el turno de Codex (obligatorio) se quedó 600 s tras `initialize`/`tools/list` sin invocar tools (sin `codex-events.jsonl`), `subprocess.TimeoutExpired` mató el arnés **antes** de la sesión `runtime` del Inspector y sin escribir `receipt.json` completo. Cambios: (1) orden en `run()`: `docker_free` Inspector → **`runtime` Inspector** → Codex → Claude Code → Gemini (la evidencia determinista primero); (2) recibo incremental: escribe `receipt.json` tras cada bloque (con `status: "running"`) y al final; (3) todo turno de modelo que expire o lance excepción se registra como `{"status"|"classification": "unavailable", "reason": "timeout after N s" | "<excepción>"}` con los artefactos parciales, y el arnés continúa; el `status` global sigue exigiendo Inspector (ambos modos) + Codex `passed`; (4) el timeout del turno de Codex en modo runtime pasa a 900 s (Docker), documentado; (5) `run()` devuelve 1 si `failed`. Unit tests para el orden, el recibo incremental y la clasificación por timeout (dobles, sin clientes reales). Informe en `docs/validation/M8/delegation/W28c-harness-order-resilience/report.md` y en tu última respuesta. No commit.

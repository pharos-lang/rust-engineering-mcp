# W31c — disposición del orquestador (2026-09-15)

Veredicto: **aceptado**. `readResource(artifactUri)` (string) según la firma del bundle; firmas del resto de llamadas verificadas por el worker; unit tests verdes con la guardia de regresión. Causa raíz hallada por el orquestador con volcado crudo (`tee`) del wire.

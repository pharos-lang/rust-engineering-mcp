# W31c — puente Inspector: `readResource` recibe un string (causa real del `-32601` en la sesión runtime)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano (Bash `timeout` hasta 600000 ms), sin commit. **Archivos permitidos:** `scripts/m8-inspector-session.mjs`, `scripts/test-m8-clients-unit.py`.

Evidencia del orquestador (volcado crudo de la sesión con `tee`): `scripts/m8-inspector-session.mjs:195` llama `client.readResource({ uri: artifactUri })`, pero `InspectorClient.readResource` del bundle (`target/m1-17-inspector/node_modules/@modelcontextprotocol/inspector/clients/cli/build/index.js`, método `async readResource(uri, …)`) recibe el **URI como string**; el SDK envió `{"method":"resources/read","params":{"uri":{"uri":null},…}}` y rmcp respondió `-32601 Unknown method` (params malformados de un método conocido caen en `on_custom_request`). Corrige la llamada a `client.readResource(artifactUri)` y revisa **todas** las llamadas del puente contra las firmas reales del bundle (`callTool(tool, args)`, `listTools()`, `listResources()`, `listResourceTemplates()` si existe, `cancelToolCall()`, `disconnect(ms)`, `getProtocolEra()`), citando la línea del bundle de cada firma en tu informe. Añade en `test-m8-clients-unit.py` un test que lea el `.mjs` y afirme que `readResource(` no recibe un literal de objeto (guardia contra regresión). Verifica con una sesión `runtime` acotada real si cabe (Docker disponible), o al menos `node --check scripts/m8-inspector-session.mjs` y los unit tests. Informe en `docs/validation/M8/delegation/W31c-bridge-read-signature/report.md` y en tu última respuesta. No commit.

# W31d — puente Inspector: forma del resultado de `readResource` y del resto de aserciones runtime

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano (Bash `timeout` hasta 600000 ms), sin commit. **Archivos permitidos:** `scripts/m8-inspector-session.mjs`, `scripts/test-m8-clients-unit.py`.

`docs/validation/M8/clients/attempt-21`: la sesión `runtime` ya completa `resources/read` en el wire (fila cliente + respuesta del servidor), pero `m8-inspector-session.mjs:196-197` falla: `Array.isArray(resourceRead?.contents) && …` → «Inspector could not read the published artifact». Lee la implementación de `InspectorClient.readResource(uri, …)` en `target/m1-17-inspector/node_modules/@modelcontextprotocol/inspector/clients/cli/build/index.js` (busca `async readResource(`) y su tipo de retorno (probablemente `{ result, … }` como `callTool`, o un objeto con `contents` anidado) y corrige la aserción para leer `contents` de donde realmente está (sin relajarla: debe exigir ≥ 1 contenido con `uri` y `blob`/`text`). Revisa con el mismo rigor las aserciones posteriores del plan runtime (cancel: resultado de `callTool` tras `cancelToolCall()`; EOF; `findArtifactUri(checkStructured)` con `structuredContent` real) contra las formas devueltas por el bundle, citando líneas. Ejecuta una sesión `runtime` acotada real (Docker disponible; usa `python3 -c` importando `test-m8-clients` y llamando `run_inspector(...)` en modo runtime sobre un `attempt` temporal bajo `target/`, o el equivalente que el módulo permita) y pega el `outcome` en el informe. Unit tests verdes. Informe en `docs/validation/M8/delegation/W31d-bridge-read-shape/report.md` y en tu última respuesta. No commit.

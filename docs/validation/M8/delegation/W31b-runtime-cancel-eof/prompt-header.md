# W31b — puente Inspector: cancel y EOF conformes al SDK; sesión runtime completa

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de arnés (Node + Python). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. **Archivos permitidos:** `scripts/m8-inspector-session.mjs`, `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`. Docker disponible (`/Users/cburgosro/.docker/run/docker.sock`, imágenes presentes) **solo** para probar la sesión `runtime` del Inspector de forma acotada; no ejecutes `--run --with-runtime` completo (lo corre el orquestador).

Fallo real (`docs/validation/M8/clients/attempt-17/inspector-runtime-session.stderr`): tras `open → rust.check (passed) → resources/read` el puente lanza `ProtocolError: Unknown method (-32601)`: `scripts/m8-inspector-session.mjs:203` llama `client.cancelToolCall()`, que no existe en `@modelcontextprotocol/client` (el SDK envía una petición desconocida). La cancelación MCP se hace con `AbortController` pasado como `{ signal }` en la opción de `callTool` (el SDK emite `notifications/cancelled` con el `requestId` en vuelo y rechaza la promesa con `McpError`/AbortError). Corrige: (1) cancel = lanzar `rust.check` con `signal`, abortar tras ~300 ms (cuando la llamada esté en vuelo), esperar el rechazo, y comprobar con la siguiente llamada `rust.check` (retried) que el servidor sigue sano y que la cancelación consta en `protocol.jsonl` (`notifications/cancelled` como fila cliente — asegúrate de que el proxy la registra: `method` de notificación). (2) EOF a mitad de llamada: lanza `rust.check` y cierra el transporte (`client.close()`/`transport.close()`) sin esperar; luego abre **una sesión nueva** y comprueba `tools/list` responde (el servidor anterior debe terminar: registra su `exit_code` si el puente lo controla, o al menos que no queda proceso `rust-engineering-mcp serve` huérfano vía el arnés Python con `pgrep -f` acotado al `state-root` de la sesión). (3) Mantén el resto del plan runtime (open, check passed, `rust-artifact://` leído, composición M2–M6 si el arnés la hace en Python). Verifica con una sesión `runtime` real acotada (Inspector + Docker) y pega su resumen en el informe; `test-m8-clients-unit.py` verde. Informe en `docs/validation/M8/delegation/W31b-runtime-cancel-eof/report.md` y en tu última respuesta. No commit.

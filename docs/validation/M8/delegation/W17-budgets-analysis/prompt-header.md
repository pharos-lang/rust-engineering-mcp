# W17 — M8-05 (preparación): análisis de las mediciones M5 para fijar presupuestos ANTES de medir

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de análisis (solo lectura; escribe un documento). Orquestador: Claude Fable 5.1. Sin subagentes, sin segundo plano, sin commit, sin Docker. **Único archivo que puedes escribir:** `docs/validation/M8/05-budgets-analysis.md`.

## Contexto

Plan `docs/roadmap/m8-stabilization.md` §M8-05 y §«Performance, operación y distribución»: fijar presupuestos **antes** de medir y **jamás tras ver un fallo**, a partir de M5: startup cold/warm, dispatch sin Cargo, RSS idle/pico por perfil, normalización, tamaño de binario/artifact, p95 de cancel-observed, tiempo de cleanup; repetir fixtures iguales con provenance y variabilidad; presupuestos no son SLO; soak propuesto 8 h por perfil anunciado y 1 000 ciclos mixed read/job/cancel con cuotas pequeñas y controles de crecimiento; reiniciar contador RC tras cambios materiales. Host positivo único: macOS ARM64 (ADR-087).

## Tarea

1. Inventaría **qué mediciones ya existen** en el repo con sus valores y método: `docs/validation/M5/` (matriz, recibos `*.json`, benchmark/comparación, bloat, flamegraph, `history/`), ADR-075..081 (protocolo estadístico, bloat), `scripts/summarize-m4-budgets.py`, `docs/validation/M4/` budgets (cuotas/límites de runtime), recibos de release (`docs/release/0.1.0/`, `docs/validation/M1/17-*`: tamaño del archive/binary), `scripts/release-smoke.py`/`release-artifact.py` (qué miden), tests de arranque/`doctor` (`crates/mcp-server/tests/cli.rs`, `doctor.rs`) y cualquier `Instant::now()`/tiempo medido en tests de protocolo (p. ej. `TIMEOUT` de 10 s del arnés `protocol.rs`, presupuestos de cancel en `inspection_runtime`). Cita ruta + valor + fecha/commit para cada número.
2. Para cada magnitud del plan (startup cold/warm del binario `serve --stdio` hasta responder `tools/list`; dispatch de una tool sin Cargo — p. ej. `rust.project.open` y `rust.catalog.status` sin catálogo; RSS idle y pico por perfil `core` vs `local`; tamaño de `target/release/rust-engineering-mcp` y del archive core; p95 de cancel-observed; tiempo de cleanup tras cancel/EOF), di: **¿existe ya una medida? valor y variabilidad**; si no existe, **cómo medirla de forma reproducible en este host** (comando, N repeticiones, qué se registra, control de ruido: mismo binario, `CARGO_INCREMENTAL=0`, sin otros procesos, descartar la primera ejecución como cold, etc.).
3. **Propuesta de presupuestos** (no SLO) con margen explícito sobre lo medido (p. ej. p95 × 1,5 o valor + margen absoluto, justifica cada uno) y la regla de regresión (falla si supera el presupuesto en 2 de 3 repeticiones, etc.); marca claramente cuáles son «propuestos sin medida previa» (se fijan provisionalmente y se recalibran una sola vez tras la primera medición, **antes** de cualquier RC, nunca después de un fallo).
4. **Soak**: propuesta concreta y ejecutable en este host para el perfil `core` (sin Docker: ciclos de `project.open` + `catalog.status` + `crate.search` sobre fixtures + cancel; y con Docker si el runtime está disponible: `rust.check` cancel/EOF) — duración, ciclos, cuotas pequeñas, métricas de crecimiento (RSS, FDs, procesos hijos, archivos en state-root) y criterios de fallo fijados antes. Estima coste de máquina.

Todo con citas; nada inventado (si un número no existe, dilo). Verificación: `python3 -B scripts/docs-hygiene.py links-check` 0 rotos. Informe en tu última respuesta (Task / Result / Conteos / Risks / Open issues). No commit.

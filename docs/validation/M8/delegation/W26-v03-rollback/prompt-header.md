# W26 — correcciones V03 del rollback (R-1, R-2, R-4..R-7)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker (Rust test nativo + driver Python). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `crates/project-adapter/tests/rollback_native.rs`, `crates/artifact-adapter/tests/rollback_native.rs` (nuevo, si el artifact M3 se crea por API de ese crate), `scripts/test-m8-rollback.py`, `scripts/test-m8-rollback-unit.py`. **No ejecutes el driver completo** (el orquestador lo corre sobre bytes commiteados por R-3); ejecuta solo los tests nativos nuevos y los unit tests.

Lee `docs/validation/M8/delegation/V03-review-m8-03-04-05/disposition.md` (R-*) y `report.md` §2. Aplica:

1. **R-1**: en el escenario (a) añade controles positivos: (i) `0.8.0 mutation list --json` sobre el mismo state-root → exit 0 y ≥ 1 registro; (ii) el test nativo deja **además** un journal de control con un kind que 0.3.0 conoce (`manifest_patch`, mismo mecanismo de API pública que el `analyzer_action_apply` actual) y `v0.3.0 mutation list` sobre un state-root que contenga **solo** ese control → exit 0 y lista 1 registro (prueba que el journal es compatible y que el fallo de (a) es por el kind); (iii) `0.8.0 doctor --json --state-root …` → `mutation_journals.downgrade_blocked == true` (W25 está implementando esa semántica en paralelo: escribe la aserción; si al probar localmente el campo aún no existe, deja el paso preparado y anótalo).
2. **R-2**: crea un artifact M3 real por API pública (test nativo `#[ignore]` en el crate que la exponga; mira `crates/artifact-adapter`/`quality_artifact_store` y cómo los tests confirman artifacts) para que (b) y (d) exijan `validated >= 1` y `quarantined == 0` en `quality-artifacts recover --json`; si no es alcanzable por API pública, declara el hueco en el recibo (`unavailable` con motivo), nunca `passed` vacío.
3. **R-4**: renombra el escenario/descripción («committed journal of a kind unknown to 0.3.0»). **R-5**: `git worktree add --end-of-options <sha>` y, al reutilizar, verificar `rev-parse HEAD == old_commit` y `status --porcelain` vacío. **R-6**: `isinstance(payload, dict)` antes de `.get`. **R-7**: (d) cubre el journal M2 escrito por 0.3.0 si es alcanzable (el binario 0.3.0 no puede escribir journals sin runtime → declara el hueco explícitamente en el recibo y en el docstring, citando los tests unitarios de lector legado v1→v2 como cobertura de formato).
4. Añade en el recibo `head_tree_dirty` ya existe → asegúrate de `tree_dirty` y `version --json` de cada binario en `binaries`.

Verificación: `cargo test -p rust-engineering-project --locked --offline --test rollback_native -- --ignored` (con `RUST_MCP_ROLLBACK_STATE_ROOT` apuntando a un temporal bajo `target/`), el test M3 nuevo igual, `python3 -B scripts/test-m8-rollback-unit.py`, `cargo fmt --all -- --check`, `cargo clippy -p rust-engineering-project --all-targets --locked --offline -- -D warnings`. Informe en `docs/validation/M8/delegation/W26-v03-rollback/report.md` y en tu última respuesta. No commit.

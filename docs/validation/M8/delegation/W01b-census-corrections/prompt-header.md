# W01b — correcciones del censo M8-01 (findings R01 F10–F13)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de correcciones acotadas de documentación. Orquestador: Claude Fable 5.1. No puedes lanzar subagentes. **Nunca corras comandos en segundo plano.** No hagas commit. No hagas Docker. **Archivos que puedes editar (y ningún otro):** `docs/validation/M8/01-census.json`, `docs/validation/M8/01-census.md`, `docs/client-configuration.md`, `docs/compatibility.md`.

## Contexto

La auditoría independiente R01 (`docs/validation/M8/delegation/R01-census-traceability/report.md`, §3) encontró cuatro defectos en el censo M8-01 y en dos docs públicas; el orquestador los verificó y aceptó (`…/R01-census-traceability/disposition.md`). Corrígelos con evidencia medida, sin reescribir nada más.

## Tareas

1. **F10 — `01-census.json` `tools[].error_codes`** vacíos en `rust.project.open`, `rust.analyzer.references`, `rust.analyzer.diagnostics`, `rust.analyzer.actions`, `rust.analyzer.action.apply`. Extrae con `python3` los valores `enum` de los `$defs` de código de cada snapshot (`crates/mcp-server/tests/snapshots/project-open-tool.json`: `BlockedCode` + `UnavailableCode`; `analyzer-references-tool.json`: `ReferencesCode`; `analyzer-diagnostics-tool.json`: `DiagnosticsCode`; `analyzer-actions-tool.json`: `ActionsCode`; `analyzer-action-apply-tool.json`: `ApplyCode`) y escríbelos ordenados en `error_codes[]`. Si el snapshot tiene más de un enum de código, inclúyelos todos y anota en `error_model.code_casing_by_family` (o campo equivalente ya existente) el nombre del enum. Revisa que ninguna otra tool tenga `error_codes: []`.
2. **F11 — casing**: `rust.analyzer.action.apply` usa `SCREAMING_SNAKE_CASE` (`ApplyCode`, `crates/mcp-server/src/stdio/mutation/analyzer_action.rs` ~171-175), no `snake_case`. Corrige el finding F5 en `01-census.json` (`findings[]`), `error_model.inconsistencies` y `01-census.md` §6 (~249-251) y cualquier frase «6 tools de mutación en snake_case»: son **5** tools M2 en `snake_case` y **31** en `SCREAMING_SNAKE_CASE`.
3. **F12 — consumidor real**: `rust.coverage`, `rust.semver.check` y `rust.mutation.test` no tienen invocación en ningún recibo de cliente stock (`docs/validation/M3/clients/attempt-11/protocol.jsonl` solo invoca `rust.project.open` y `rust.test.nextest`); su único consumidor es el e2e nativo. Corrige `01-census.md` §2/«Clasificación propuesta» (~106-109: la frase «Todos tienen consumidor real positivo (Inspector y/o Claude Code y/o Codex…)» es falsa para esas tres) y el `class_rationale` de esas tres entradas en el JSON: clase `stable` propuesta por publicación en `v0.3.0` + e2e nativo, con la condición de ejercitarlas en la matriz M8-04. Añade un finding **F12** al JSON y a §9 con esa evidencia (`severity: P2`, `blocks_freeze: false`, cierre en M8-04).
4. **F13 — docs públicas con M6 obsoleto**: `docs/client-configuration.md` ~406-427 dice que `rust.analyzer.symbols` «es la primera tool M6 y está en desarrollo, no calificada» y que no hay bandera nueva: hoy hay cinco tools `rust.analyzer.*` integradas en `main` (PR #20, `e50c3fe`) y calificadas (`docs/validation/M6/handoff.md`), con el grant `--allow-analyzer-action-write WORKSPACE_ROOT` para `rust.analyzer.action.apply` y la imagen M6 (`--rust-image`). Reescribe esa sección para describir el estado actual (lee `docs/validation/M6/handoff.md` y el `--help` en `crates/mcp-server/src/main.rs` para los flags exactos). `docs/compatibility.md:8` («… (M6, en desarrollo); … sin commit de integración, PR, tag ni publicación») → M6 integrado en `main` vía PR #20 y calificado; sin tag/publicación todavía (cierto). Añade también un finding **F13** al JSON y a §9 (`severity: P2`, `blocks_freeze: true`, cerrado por este worker).

## Verificación (foreground)

`python3 -c "import json;d=json.load(open('docs/validation/M8/01-census.json'));assert all(t['error_codes'] for t in d['tools']);print(len(d['findings']))"` → 11 findings; `python3 -B scripts/docs-hygiene.py links-check` → 0 rotos en documentos vivos. Informe final: Task / Result / Files changed (líneas) / Evidencia (enums extraídos por tool con conteo) / Risks / Open issues. No commit.

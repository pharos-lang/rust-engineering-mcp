# W24b — repetir el ensayo de distribución 0.8.0 sobre bytes commiteados (`86a5007`, árbol limpio)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de release engineering. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. **No publiques nada.** **Archivos permitidos:** `docs/validation/M8/07-release-rehearsal.json` (regenerar), `docs/validation/M8/07.md` (solo la sección «Resultado del ensayo», actualizar). No toques scripts ni workflows.

Repite exactamente el ensayo de W24 (`docs/validation/M8/delegation/W24-release-rehearsal/report.md` §pasos) sobre el árbol commiteado actual (`git status --short` vacío; `git rev-parse HEAD` = `86a5007…`): construye el archive core con `scripts/release-artifact.py` (`target/release` ya está en 0.8.0; si reconstruyes, `--locked --offline`), inventario/SBOM/notices, smoke desde directorio limpio bajo `target/m8-release-regen/` con `scripts/release-smoke.py` (SHA-256, `version --json`, `contract --json` 36 tools con hashes iguales a `docs/validation/M8/freeze-0.8.0.json`, `doctor`, `tools/list` 36, `resources/templates/list` con las dos plantillas **en forma RFC 6570** `{?offset,length}` — la evidencia anterior citaba la plantilla antigua, V03b P2-3). Escribe el recibo con `head_commit`, `tree_dirty: false`, hashes de assets, tamaño vs presupuesto `archive_size_core_bytes` (12,7 MB), pasos con comando/exit/extracto, `unavailable` para lo que exige tag/red. Actualiza la sección «Resultado del ensayo» de `07.md` (sustituye la anterior). Informe en `docs/validation/M8/delegation/W24b-release-rehearsal-regen/report.md` y en tu última respuesta. No commit.

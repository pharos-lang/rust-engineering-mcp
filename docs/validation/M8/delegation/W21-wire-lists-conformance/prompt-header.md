# W21 — M8-04: conformidad wire de `resources/list`, `resources/templates/list` y `prompts/list` (2026-07-28) + primera ejecución Docker-free del arnés

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de implementación (Rust + arnés). Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: la herramienta Bash acepta `timeout` hasta 600000 ms — úsalo para `cargo` y para el arnés; `run_in_background` está PROHIBIDO y termina tu sesión sin informe.** No commit. **Sin Docker** (`--with-runtime` lo ejecuta el orquestador). **Archivos permitidos:** `crates/mcp-server/src/stdio.rs`, `crates/mcp-server/src/stdio/resources.rs`, `crates/mcp-server/tests/protocol.rs`, `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`, `scripts/m8-inspector-session.mjs`, `docs/tools.md` (solo la nota de Resources dinámicas), `docs/compatibility.md` (matriz wire: una fila). No toques snapshots de tools (los 36 deben quedar byte-idénticos; `contract-freeze.py verify docs/validation/M8/freeze-0.8.0.json --strict` debe seguir `passed`).

## Hallazgo (reproducido por el orquestador)

`python3 -B scripts/test-m8-clients.py --run` (Docker-free) falla en la sesión Inspector: el SDK TS rechaza la respuesta de `resources/list` con `INVALID_RESULT … path: ["cacheScope"] Invalid option: expected "public"|"private"` (`docs/validation/M8/clients/attempt-1/inspector-docker_free-session.stderr`). Con `server/discover` 2026-07-28 el servidor responde `resources/list` → `{"resultType":…, "resources": []}` **sin `ttlMs` ni `cacheScope`** (es `ListResourcesResult::default()` de rmcp 3.2.0, `handler/server.rs:383-388`), mientras `tools/list` sí los lleva (`stdio.rs:756`, `.with_ttl_ms(0).with_cache_scope(CacheScope::Private)`). Las Resources son dinámicas por sesión (`resources.rs` líneas ~202-256 ya usan `CacheScope::Private` en sus lecturas). Las dos plantillas documentadas en `contract --json` son `rust-artifact://{project_ref}/{artifact_id}` y `rust-quality-artifact://{project_ref}/{quality_job_id_or_artifact_id}?offset={n}&length={n}`.

## Tareas

1. **Confirma el requisito** leyendo el schema del SDK TS instalado (`target/m1-17-inspector/node_modules/@modelcontextprotocol/client/dist/*.mjs`, busca `ListResourcesResult`/`cacheScope`/`ttlMs` y qué revisiones lo exigen) y la spec 2026-07-28 citada en `docs/compatibility.md`; anota si `prompts/list` y `resources/templates/list` tienen el mismo requisito.
2. **Producto** (decisión del orquestador): implementa en `EngineeringServer` `list_resources` (lista vacía: las Resources son dinámicas por sesión), `list_resource_templates` (las **dos** plantillas anteriores, con `name`/`description` breves y `mimeType` `application/json` si aplica) y `list_prompts` (vacía), todas con `.with_ttl_ms(0).with_cache_scope(CacheScope::Private)` igual que `tools/list`, y solo si `rmcp` las expone así para las revisiones legacy sin romperlas (verifica con los tests de las 5 revisiones). Sin `unwrap` en rutas normales.
3. **Tests** en `tests/protocol.rs`: para 2026-07-28 (`server/discover` con `_meta` `io.modelcontextprotocol/protocolVersion` + `clientCapabilities`) y para una revisión legacy (2025-06-18 vía `initialize`): `resources/list` → `resources: []` con `ttlMs`/`cacheScope` presentes en la moderna; `resources/templates/list` → 2 plantillas exactas; `prompts/list` → `[]`; y que `tools/list` sigue idéntico (los snapshots no cambian).
4. **Arnés**: vuelve a ejecutar `python3 -B scripts/test-m8-clients.py --run` (Docker-free; foreground con `timeout` de 600000 ms; si excede, ejecuta por partes si el arnés lo permite) hasta que la sesión Inspector complete el plan (contract equality 36/36 contra el manifiesto, 31 negativos, 4 genéricos) y los turnos Codex/Claude Code/Gemini se registren con su resultado real (`passed/partial/unavailable`, nunca un skip como pass). Corrige defectos **del arnés** que aparezcan (argumentos mínimos, códigos, parseo); si encuentras un defecto **del producto** distinto al de arriba, **no lo arregles**: regístralo en tu informe con evidencia y deja el recibo con ese fallo honesto. El recibo va a `docs/validation/M8/clients.json` (+ `clients/attempt-N/`). Actualiza `test-m8-clients-unit.py` para lo que cambies.
5. Docs: `docs/tools.md` nota de Resources dinámicas (añade que `resources/templates/list` anuncia las dos plantillas desde 0.8.0) y una fila en la matriz wire de `docs/compatibility.md`.

Verificación: `cargo fmt --all -- --check`; `cargo clippy -p rust-engineering-mcp --all-targets --locked --offline -- -D warnings`; `cargo test -p rust-engineering-mcp --locked --offline --test protocol`; `python3 -B scripts/contract-freeze.py verify docs/validation/M8/freeze-0.8.0.json --strict`; `python3 -B scripts/test-m8-clients-unit.py`; `git status --short crates/mcp-server/tests/snapshots` vacío. Informe (Task / causa raíz confirmada / Files changed / resultado por cliente del `--run` / tests / Risks / Open issues) en `docs/validation/M8/delegation/W21-wire-lists-conformance/report.md` y en tu última respuesta. No commit.

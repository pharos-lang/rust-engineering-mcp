# W30 — correcciones V03b (P2-1, P2-2, P3s)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). Rol: worker de scripts/docs. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`, `scripts/test-m8-rollback.py`, `scripts/test-m8-rollback-unit.py`, `scripts/measure-m8-performance.py`, `scripts/test-m8-performance-unit.py`, `docs/tools.md` (solo la frase de `doctor` «passive/reads only»), `docs/validation/M8/01-census.json` (solo el literal de la plantilla quality).

Lee `docs/validation/M8/delegation/V03b-rereview/disposition.md` y `report.md`. Aplica: (1) `codex_gate`: `classification == "passed"` exige `unknown_tool_wire_refused`; la evidencia de eventos se registra como `unknown_tool_event_refused` informativo; docstring coherente; test. (2) `test-m8-rollback.py` escenario (a): `doctor_ok = False` si `doctor` falla o no devuelve `mutation_journals.downgrade_blocked` (gap = fallo, como en (b)); test. (3) `regression_verdict`: `insufficient_samples` cuenta como `unavailable`; test. (4) recibo de clientes: campo `wire_confirmation: "positional"` con nota de que el proxy no registra `id` (límite documentado). (5) `docs/tools.md` `doctor`: «no lee el workspace ni el source; abre el store de journals (crea el lock del store si no existe) con la misma lectura que `mutation list`». (6) `01-census.json`: la plantilla quality pasa a `rust-quality-artifact://{project_ref}/{quality_job_id_or_artifact_id}{?offset,length}` donde aparezca.

Verificación: `python3 -B scripts/test-m8-clients-unit.py`, `python3 -B scripts/test-m8-rollback-unit.py`, `python3 -B scripts/test-m8-performance-unit.py`, `python3 -B scripts/test-gate-reporting.py`, `python3 -c "import json;json.load(open('docs/validation/M8/01-census.json'))"`, `python3 -B scripts/docs-hygiene.py links-check`. Informe en `docs/validation/M8/delegation/W30-v03b-fixes/report.md` y en tu última respuesta. No commit.

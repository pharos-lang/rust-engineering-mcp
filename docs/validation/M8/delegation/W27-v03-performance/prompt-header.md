# W27 — correcciones V03 de performance/soak (P-1..P-9)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de scripts. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `scripts/measure-m8-performance.py`, `scripts/soak-m8.py`, `scripts/test-m8-performance-unit.py`, `docs/validation/M8/05-budgets.json` (solo añadir `sha256` de referencia si hace falta; no cambies valores). **No regeneres `05-measurement.json`** (lo hace el orquestador sobre bytes commiteados).

Lee `docs/validation/M8/delegation/V03-review-m8-03-04-05/disposition.md` (P-*) y `report.md` §4. Aplica: **P-1** validar cada respuesta (`status == "passed"`, `isError != true`, id correcto) y registrar descartes; **P-2** `regression_verdict` exige exactamente 3 recibos consecutivos con el mismo `budgets_sha256` y perfil, `indeterminate` si algún `unavailable` decide, y CLI `measure-m8-performance.py --compare r1 r2 r3`; **P-3** `insufficient_samples` si `len(samples) < n` del presupuesto y ventana mínima de muestreo para RSS pico (≥ 30 muestras a 100 ms durante los ciclos); **P-4** recibo con `head_tree_dirty` y `budgets_sha256`; **P-5** el soak arranca el servidor con `--project-ttl-secs 30`, `--ttl-wait-seconds` por defecto 35, criterio nuevo `fd_after_ttl`: FDs tras la espera ≤ meseta + 10 (fallo si no), y retira la afirmación «bounded by TTL… not a leak» de las notas (sustitúyela por el resultado medido); **P-6** revisar huérfanos en el catalog-store del soak o `passed: null`; **P-7** re-hash del binario al final, flag `operator_attested` (por defecto `false`) y `pmset -g batt` registrado si existe; **P-8** `reopens` real (reabrir si `project_ref` expira y contarlo), `request` comprueba `id`, muestra final = última muestra real; **P-9** más tests unitarios para cubrir las rutas nuevas (objetivo ≥ 80 % de líneas nuevas; sin exclusiones Sonar). Reglas Sonar de la casa.

Verificación: `python3 -B scripts/test-m8-performance-unit.py`; una ejecución corta real `python3 -B scripts/measure-m8-performance.py --repeat 5 --out target/m8-perf-smoke.json` y `python3 -B scripts/soak-m8.py --profile core --cycles 20 --hours 0.05 --sample-every 5 --out target/m8-soak-smoke.json` (foreground) — deben terminar y respetar los criterios (si el soak falla por `fd_after_ttl`, es un resultado, no un defecto del arnés: repórtalo con los números). Informe en `docs/validation/M8/delegation/W27-v03-performance/report.md` y en tu última respuesta. No commit.

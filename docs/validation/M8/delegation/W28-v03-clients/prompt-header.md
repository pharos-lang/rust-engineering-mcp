# W28 — correcciones V03 del arnés de clientes (C-1..C-7)

Modelo solicitado: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Rol: worker de arnés. Orquestador: Claude Fable 5.1. Sin subagentes. **Nunca en segundo plano: Bash acepta `timeout` hasta 600000 ms; `run_in_background` está PROHIBIDO.** No commit. No Docker. **Archivos permitidos:** `scripts/test-m8-clients.py`, `scripts/test-m8-clients-unit.py`, `scripts/m8-inspector-session.mjs`. **No ejecutes `--run` completo** (el orquestador lo corre sobre bytes commiteados); ejecuta `--preflight` y los unit tests, y si necesitas validar el `.mjs`, una sesión Inspector Docker-free aislada está bien.

Lee `docs/validation/M8/delegation/V03-review-m8-03-04-05/disposition.md` (C-*) y `report.md` §5. Aplica: **C-1** `--run` llama a `preconditions()` y aborta si falla una obligatoria; el recibo registra versiones **observadas** (no constantes); **C-2** oráculo de Codex derivado de `protocol.jsonl` (`client == "codex"`, `tools/call` con `rust.project.open` **y** `rust.project.inspect`, y el refusal de la tool desconocida observado en el wire o en los eventos); **C-3** negativos genéricos: capturar `error.code` (esperado -32602 para `invalid_args`/`unknown_fields`, -32601 para `unknown_tool`) y confirmar en `protocol.jsonl` que el servidor respondió a ese id; **C-4** `run()` devuelve `0` solo si el recibo es `passed`; **C-5** código de error **no nulo** y **esperado por fila** (fija el esperado observado en `docs/validation/M8/clients/attempt-13/` para cada una de las 30 filas de refusal; `catalog.status` sigue como observación); **C-6** vocabulario: respeta `#[serde(rename)]` y verifica `rename_all` del enum (o documenta el límite); **C-7** `head_commit`/`tree_dirty` en el recibo, sin `type=` en argparse, sin literales `/private/tmp` (usa `target/`), `mkdtemp` bajo `target/`, docstring corregido. Reglas Sonar de la casa.

Verificación: `python3 -B scripts/test-m8-clients-unit.py` (añade tests para cada corrección), `python3 -B scripts/test-m8-clients.py --preflight` → `ready`, `python3 -B scripts/test-gate-reporting.py`. Informe en `docs/validation/M8/delegation/W28-v03-clients/report.md` y en tu última respuesta. No commit.

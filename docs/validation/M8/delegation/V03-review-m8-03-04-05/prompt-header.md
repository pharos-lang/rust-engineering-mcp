# V03 — revisión independiente read-only de M8-03/04/05 (código y arneses)

Modelo solicitado: Claude Opus 5 (`claude -p --model opus --effort high --tools "Read,Grep,Glob"`). Rol: revisor read-only (sin Bash/Edit/Write). Orquestador: Claude Fable 5.1.

Revisa como Principal Engineer, contra `AGENTS.md` (hexagonal, deny-by-default, sin `unwrap` en rutas normales, stdout solo protocolo), ADR-086/087/088, G1–G6 (`docs/roadmap/m2-m8.md`), `docs/validation/M8/03.md` (D12) y `05.md`:

1. **`crates/mcp-server/src/doctor.rs` + `tests/doctor.rs` + `tests/snapshots/doctor-report.json`** (W14/W14b): la sección `mutation_journals` ¿es aditiva y pasiva (no lee workspace ni source; no bloquea `serve`)? ¿`unknown_format` cubre kind desconocido **y** envelope ilegible sin pánico? ¿Algún `unwrap`/`expect`? ¿El snapshot `doctor-report.json` cambia solo por el campo nuevo?
2. **`crates/project-adapter/tests/rollback_native.rs` + `scripts/test-m8-rollback.py` + `docs/validation/M8/03-rollback.json`** (W15): ¿el test nativo usa solo API pública y deja un journal real `analyzer_action_apply`? ¿El driver construye `v0.3.0` sin red (`--locked --offline`), valida el ref (`rev-parse --verify --end-of-options`), y los cuatro escenarios prueban lo que dicen (a: fail-closed por registro; c: `CATALOG_ROLLBACK` por floor)? ¿Falso pass posible (p. ej. exit code no comprobado, JSON no parseado)?
3. **`crates/mcp-server/src/stdio.rs` (list_resources/list_resource_templates/list_prompts) + `tests/protocol.rs` (test nuevo)** (W21): ¿conforme a 2026-07-28 y sin cambiar legacy? ¿Las plantillas se derivan de `resources::PREFIX`/`QUALITY_PREFIX`? ¿`mimeType` correcto? ¿`tools/list` intacto (snapshots byte-idénticos)?
4. **`scripts/measure-m8-performance.py`, `scripts/soak-m8.py`, `scripts/test-m8-performance-unit.py`, `docs/validation/M8/05-budgets.json`, `05-measurement.json`** (W18/W20): ¿la medición respeta el método de `05.md` (N=30, warm-up descartado, p95, raw samples, controles de ruido registrados)? ¿El veredicto 2-de-3 está bien implementado? ¿El soak evalúa criterios fijados antes (sin ajuste a posteriori) y registra el churn aparte? ¿Reglas Sonar (sin `/tmp` literal, sin `type=`, sin `shell=True`)?
5. **`scripts/test-m8-clients.py` + `m8-inspector-session.mjs` + `docs/validation/M8/clients.json`** (W19/W20/W21): ¿la igualdad de contrato recalcula los hashes canónicos contra el manifiesto real? ¿Los 31 negativos son refusals estructurados (no errores de protocolo) salvo `catalog.status` (observación)? ¿El recibo puede marcar `passed` con un cliente obligatorio ausente? ¿`unavailable` de opcionales se anuncia como calificado en algún sitio? (W23 está corrigiendo el pin de Claude y el `HOME` de `agy` en paralelo: ignora ese fragmento.)

Salida: findings por archivo con P0–P3 (P0/P1 bloquean; P2 de contrato/gate/seguridad bloquea readiness), evidencia (cita corta) y acción; veredicto `Approve` / `Approve con findings` / `Block`. Sin ejecutar nada; si necesitas un archivo, léelo.

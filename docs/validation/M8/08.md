# M8-08 — Threat model, riesgos residuales y auditoría: registro

Estado: **threat model y registro de riesgos entregados (W22, Opus High)**;
auditoría independiente = revisiones de modelo read-only (V02/V02b/V03/V04)
con alcance declarado; **no hay auditoría humana ni pentest** (RR-01, ADR-089).
Entradas: [08-threat-model.md](08-threat-model.md), [ADR-089](../../adr/ADR-089-residual-risk-register.md),
`docs/security-model.md` §Riesgos residuales 1.0, [disposición W22](delegation/W22-threat-model/disposition.md).

## Conteo (W22 §8)

8 fronteras; 53 controles evaluados: 31 con oráculo nativo, 12 con oráculo
unit/contract/protocol, 4 con evidencia histórica/config, **6 sin oráculo**
(declarados en el threat model y cubiertos por RR); 18 riesgos residuales.

## Disposición de los issues abiertos de W22 §9

| # | Issue | Disposición |
| --- | --- | --- |
| 1 | `.github/workflows/release-candidate.yml:219` exige `counts.tools == 31` (draft 0.8.x fallaría cerrado) | **Aceptado** → M8-07 (W24): el workflow toma el conteo y los nombres del manifiesto `freeze-0.8.0.json` / `contract --json`, no de un literal |
| 2 | Branch protection registrada exige el check de Windows retirado | **Aceptado** → RR-12; se re-observa con `gh api` antes de RC1 y se ajusta (owner) |
| 3 | Comentarios «pending M3-04 calibration» en `semver_runtime.rs` | **Aceptado** como texto desactualizado (no hueco de oráculo) → se corrige en la pasada de docs de M8-06 (comentarios de crate, sin gate completo — regla del owner) |

## Auditoría independiente (alcance declarado)

- Revisiones de modelo read-only con findings P0–P3 y disposición: V02/V02b
  (freeze), V03 (M8-03/04/05), V04 (auditoría de seguridad de cierre sobre el
  threat model + código de fronteras, pendiente). Ninguna se denomina
  auditoría humana.
- Condición de reevaluación (ADR-089 RR-01): antes de anunciar otro host,
  remoto (M7) o distribución de modelos/catálogo oficial, se requiere
  auditoría humana/pentest con alcance escrito.

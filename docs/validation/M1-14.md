# M1-14 — CLI and explicit doctor

ADR-045 adds the missing diagnostic command and stable human/JSON reports. It
reuses the serve host parser and existing catalog/provider, secure roots and Rust
execution gateway. Exactly thirteen MCP tools remain; all previous tool snapshots
are unchanged. Passive doctor has no PATH discovery or subprocess invocation;
active doctor explicitly calibrates the approved Rust runtime and observes its
three fixed inventory operations. It never runs a user project.

## Evidence

- [Core](M1-14-core.json): 645 tests including doctests, ten stages passed with
  Rust/Cargo1.98.1, CARGO_INCREMENTAL=0, --locked --offline. Protocol37 covers all
  five supported wire versions. [Summary and log hashes](M1-14-summary.json).
- All-features/all-targets Clippy passed on final source with cached ORT and no
  download; final log hash is in the summary.
- Three ordinary doctor CLI tests: stable report snapshot, human/version outputs,
  PATH execution sentinels, signed SQLite read while administrative lease is held,
  staging preservation, invalid configured model, trust symlink and closed syntax.
  Nine capabilities CLI tests retain every previous hostile argument/path case.
- [Active receipt](M1-14-active.json): real successful Rust calibration and exact
  rustc/cargo/component inventory; three further invocations receive SIGINT/SIGTERM/SIGHUP during an
  observed running Cargo check calibration job. Failed/interrupted report is
  emitted after joined cleanup; owned containers and volumes are absent. Active
  and cancelled transcripts remain in target/doctor-security with source receipt.
  Two additional prefilled-pipe cases verify output cancellation/deadline without
  draining stdout or forced termination. Windows signal
  code is compiled conditionally and has no native Windows qualification here.
- [Source receipt](M1-14-source-receipt.json) hashes304 implementation/fixture/script/
  manifest files. Living docs are excluded. Full19 is a final-candidate gate;
  this vertical uses the proportional core and real active/cancellation gates.

A first core invocation caught formatting in the new snapshot assertion. A later
run found the historical CLI test expecting bare doctor to be unsupported; that
case now rejects doctor --unknown, while the new doctor tests verify valid usage.
Neither failure required weakening production behavior. Principal integration
also restored all six existing capabilities argument/security tests after the
worker draft replaced their file; three format cases were appended to the original
bounded harness. The final nine-test suite passes and no old case was removed.

Configured invalid components fail with exit1; optional unconfigured/unchecked or
stale evidence produces warning/exit0, never universal readiness. Syntax errors
exit2. JSON128KiB includes newline; deadline120s passive/900s active is cooperative
and waits for cleanup. Cleanup uncertainty cannot be converted to success. Native
Linux/Windows, Inspector, distribution approval and M1-16 remain separate criteria.

[Independent review](M1-14-review-opus.md), [follow-up](M1-14-review-followup.md)
and [principal disposition](M1-14-review-disposition.md) resolve confirmed findings.
Integration/postmerge evidence is recorded after the local merge.

Integrated a72216d /20689cf; clean-main postmerge smoke3 doctor +9 capabilities
+37 protocol passed,304 source hashes verified. [Receipt](M1-14-postmerge.json).

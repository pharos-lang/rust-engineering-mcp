# M0-10 — Reviewed Rust fixture corpus

Rust/Cargo1.98.1, macOS ARM64. `scripts/test-fixtures.py` runs a closed allowlist
of8 benign fixture directories,11 compiler cases, plus one static audit-input
check. Fresh HOME/CARGO_HOME/TMPDIR/target, explicit toolchain, clean environment,
locked/offline Cargo,30-second deadlines and512KiB per output stream. This is a
trusted development harness, not a sandbox for arbitrary repositories.

Command:

```text
python3 scripts/test-fixtures.py <LOCAL_HOME>/Projects/rust-mcp --cargo <LOCAL_HOME>/.rustup/toolchains/1.98.1-aarch64-apple-darwin/bin/cargo
cargo fmt --all --check
```

11 cases passed: valid test; E0502/E0597 with primary spans; Clippy warning;
deny(unsafe_code) compile failure; two-member workspace/path dependency; feature
none/left/right pass and conflicting pair fails; benign build.rs emits generated
OUT_DIR source consumed by a passing test. No registry dependencies are compiled.
[Results](artifacts/M0-10-fixtures.jsonl), [corpus](../../fixtures/README.md).

The RSA0.9.6 fixture intentionally has an incomplete audit-only lock, never used for
Cargo resolution/build. Its pinned advisory and registry checksum were independently
re-fetched by the principal and matched byte/hash values:
[verification receipt](artifacts/M0-10-upstream.json). Offline checking then verifies
fixture consistency, not a live advisory claim or a real RustSec matching engine.

The malicious build-script source is outside the host allowlist and was never run.
It has finite fs/env/network/output/child probes for the future Cargo gateway. M0's
actual containment evidence remains the separately executed Go/Docker probes;
Rust build.rs/proc-macro containment and approved Cargo image are M1 prerequisites.

The corpus hash receipt is checked before Cargo, including extra files and symlinks.
It detects accidental drift in a trusted checkout, not malicious-author authenticity.
Optimized Python is rejected. Negative gate checks reject modified corpus before
invoking Cargo. Timeouts after early pipe EOF map consistently and kill the group.

Independent Sonnet5 Medium review: [findings/disposition](../reviews/M0-10-claude-sonnet-5.md).
No product interface/dependency changed. Root fmt and fixture gate are proportional
here; complete workspace/security gates are repeated at M0 closure.

Local no-ff merge `b5ee860`; post-merge corpus gate repeated:11 compiler cases and
1 static audit input passed. Branch retained; no remote operation.

# M1-04 — Configured workspace formatting

Date: 2026-09-04. Branch `ai/m1-04-fmt`, from clean main `4ddc696`.
ADR-035; M0 remains closed, no M2, remote, PR or Actions.

## Implemented behavior

`rust.fmt.check` accepts only live project_ref and runs fixed cargo-fmt under the
approved image/network-denied read-only source gateway. All workspace members,
stable style and skip attributes apply; whole-project disable is overridden.
Affected files are normalized captured relative paths, max128 with omissions.
Whole display diff <=32KiB, otherwise explicitly omitted. No source edits.
Configured formatting differences are valid failed results; syntax/parser/output
failures are explicitly incomplete. Same joined workers, logs/quota fallback,
ProjectRef revalidation, retention and snapshot freshness as M1-03.

## Current validation

- Rust/Cargo1.98.1, CARGO_INCREMENTAL=0, locked/offline.
- `python3 scripts/gate.py core --report target/M1-04-core.json`: passed,
  ten stages,355 tests including doctests. [Tracked gate](M1-04-core-gate.json).
- Actual format7cases passed: clean source, differences in both workspace members,
  disable_all override, hard_tabs style, syntax error incomplete, CRLF/newline-only,
  large diff omission; seven Resources readbacks with SHA256/size and private cache.
  Source bytes/tree unchanged and a build-script panic sentinel never executed.
- Application check+format10/10; all application43; parser8 adverse cases;
  MCP format9 and protocol27. These are included in core, not added twice.
- The first core run exposed the historical Docker-shape fixture's missing fixed
  RUSTFMT environment entry. Its current adaptation was updated explicitly; the
  historical receipt was preserved. Core was rerun successfully.
- `RUST_MCP_TEST_SOCKET=<LOCAL_HOME>/.docker/run/docker.sock python3 scripts/test-rust-execution.py`: all seven exact serial Docker tests passed.
  [Calibration receipt](M1-04-calibration-receipt.json), [format receipt](M1-04-format-receipt.json).
- [Sonnet5 Medium external review](../reviews/M1-04-sonnet.md) and
  [principal disposition](../reviews/M1-04-principal-disposition.md): review gaps
  resolved against source invariants/current live evidence; no confirmed unresolved P0/P1.
- Final configuration fingerprint: `sha256:8a7ab398ad1a12400579857bd03dfb10ec1c43ceb166bf0fc641af1071c9fe50`.
- Diff reviewed and `git diff --check` clean. Local commit/merge/smoke follow.

## Limits

No platform qualification beyond current macOS APFS/Docker Linux ARM64. Formatting
honors skip attributes; it does not prove every source byte was checked. Display
diff cannot authorize edits. Rustfmt syntax errors currently retain raw bounded
log and incomplete state; they are not synthesized into compiler diagnostics.
Runtime install/replacement, catalog/RustSec import, signed distribution, durable
antirollback and utility benchmark remain outside this vertical. Integral full
semantic gate is still required for requested final closure.

Integration: commit `5e97806`, merge no-ff `e8253a7`; retained branch. Post-merge actual format smoke1/1,19.53s (`target/M1-04-postmerge.log`).

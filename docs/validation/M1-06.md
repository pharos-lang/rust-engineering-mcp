# M1-06 — Bounded Cargo test execution

Date2026-09-04; ai/m1-06-test from clean main57ca421. ADR-037.
Rust/Cargo1.98.1,CARGO_INCREMENTAL=0,locked/offline. Approved existing image;
no new dependency/image/download. No remote, PR, Actions or M2.

## Behavior

Closed package/filter/features/all_features/installed-target/timeout1..60s(default30).
Frozen JSON Cargo plus fixed single-thread/colorless harness arguments. Distinct
ProjectTestPort shares captured authorization and bounded Resources. Reported build
phase is nullable and precedes doctests; actual failures remain valid results.
Ordinary harness output stays in logs, never fabricated counts or coverage.
Additional Cargo-looking tail events invalidate phase completeness and clear it.
Passed requires complete evidence, successful build phase and real exit0.

## Verification

Core10stages passed,393 tests including doctest; not double-counting focal reruns.
Actual MCP nine cases: pass with raw tail, selected filter, assertion failure,
missing feature failure, feature+target+timeout60, all_features, E0308 compile
failure, selected helper package and timeout15 with actual running-test marker.
Nine Resource hashes/lengths/private cache policy and source tree preserved.
Actual libtest containment checks plus detached test descendants observed with
separate SIDs before timeout/cancel/overflow; all owned containers/volumes absent,
no quarantine. MCP active test binary observed before cancellation and EOF;
discovery responsive, Resources backpressure, cancelled response suppression,
joined cleanup and successful worker reuse.

External Opus5 High identified early forged build-finished as a possible integrity
bug. Actual Docker proc-macro fixture CONFIRMED it: forged success before E0308
and genuine failed build. Fixed parser now reports incomplete/build_succeeded=null,
keeps exit101 and raw evidence; cleanup verified. Unit discriminator also covers
Cargo-looking custom harness output. No adversarial source compiled on host.

The first integrated gate stopped at a receipt-schema check after actual R2 tests
passed (normal observation had no per-item cleanup field). Gate now validates
normal cleanup through the test/global receipt and checks all three interruption
observations separately. No containment assertion or production policy was relaxed.
Final serial13-test Docker gate passed before the final marker hardening:
`RUST_MCP_TEST_SOCKET=<LOCAL_HOME>/.docker/run/docker.sock python3 scripts/test-rust-execution.py`.
[Core](M1-06-core-gate.json), [calibration](M1-06-calibration-receipt.json),
[test cases](M1-06-test-receipt.json), [R2 containment](M1-06-test-containment-receipt.json),
[cancel/EOF](M1-06-test-cancellation-receipt.json), [forgery](M1-06-test-forgery-receipt.json).
Logs target/M1-06-core.log, target/M1-06-rust-security.log, target/rust-security/0..12.log.
Help corrected to list all seven tools; CLI11/11 passed after that text-only change.
[Independent follow-up](../reviews/M1-06-opus-followup.md) confirmed original P1 closed;
its malformed-marker residual was hardened and unit-covered. Final core393/10stages passed after that parser-only refinement. The actual
forgery test passed14.42s; final MCP nine-case repetition passed36.77s. Diff reviewed; git diff --check clean.

[External review](../reviews/M1-06-opus.md),
[principal disposition](../reviews/M1-06-principal-disposition.md).

## Limits

Passed is selected command success, not suite coverage or trusted producer output.
Custom harnesses rejecting fixed arguments can fail. Human log headings are
forgeable. Network/filesystem/process isolation is the same approved Linux ARM64
profile, not new native platform qualification. Fixed budgets/empty CARGO_HOME
limit large suites and registry/git dependencies. Full integral semantic gate still
required for final authorized closure; M1-07 audit/import trust remains pending.

Final calibration configuration: `sha256:27bec2a61deb5a7f4c514a83d4bdb23e7577a19b30c0ac614c1c8826da69556e`.
The13-case gate ran serially; R2 containment41.67s, MCP cases36.82s,
cancel/EOF17.02s and initial forgery14.38s. Parser-only final refinements
do not change the enforced gateway configuration.

Integration: commitf10304d, no-ff merged23e718; branch retained. Actual MCP test
post-merge smoke1/1,36.72s (target/M1-06-postmerge.log).

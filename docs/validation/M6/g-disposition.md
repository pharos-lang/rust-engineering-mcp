# M6 — disposición de cierre G1–G9

Estado: **satisfechas**, 2026-09-12 (gate UTC 2026-09-13). Rama `ai/m6-analyzer`. Sin P0/P1/P2 abiertos.


Fuente de las puertas: [m2-m8.md §G1–G9](../../roadmap/m2-m8.md). M6 introduce
cinco tools de lectura/escritura del analyzer (D25) sobre una instancia
transitoria de rust-analyzer por consulta (D26).

| Puerta | Demostración en M6 | Evidencia |
| --- | --- | --- |
| **G1 Arquitectura y contrato** | `domain`/`application` libres de rmcp/serde_json/proceso/sha2 (analyzer.rs es valores+invariantes); `check-architecture.py` verde en el gate. Cinco contratos nuevos congelados vía ADR-083→snapshot→wire tests→docs; los 31 tools previos byte-idénticos (`previous_unchanged: true`). Ninguna lectura convertida en escritura. | ADR-083/084; `stdio/analyzer*`; snapshots `analyzer-*-tool.json`; etapa `architecture` del full gate |
| **G2 Autoridad y threat model** | El host concede `--allow-analyzer-action-write`; sin él `unavailable/SANDBOX_DENIED` antes de crear estado. Peer/proyecto/URI/fingerprint no conceden permiso. Todo el proyecto (build.rs/proc-macros/tests/analyzer) se trata como hostil dentro del guest; gateway tipado único; env reconstruido. Sin texto del analyzer en el wire. | brief D25/D26 §1.6; `host_config.rs`; `lsp_codec.rs` (saneado/bounded); ADR-085 admisión |
| **G3 Lifecycle, concurrencia, cuotas, auditoría** | Instancia transitoria por consulta unida antes del cleanup; cleanup incierto cuarentena el gateway. Budgets fijados (ADR-084 §8). Auditoría sin tokens/source completo; el writer M2 audita apply. Cancel/EOF/timeout terminan y unen el árbol. | ADR-084 §2/§8; cortes nativos m6-00..m6-08 (lifecycle/cleanup/quarantine); `mutation/audit.rs` |
| **G4 Fixtures y pruebas** | Unit (domain/codec), contract (schemas), protocol (5 versiones wire), integration nativa (12 cortes m6-00..m6-11 + 3 e2e apply), adversariales (codec hostil, oversized, bidi/zero-width). Cliente Inspector 2.5.0 + Claude Code 2.1.267 dirigido por modelo, positivo/negativo/cancel. Oráculo discrimina éxito/fallo-de-proyecto/dato-falso/infra. | `01-calibration.json` (12/12); `analyzer_runtime.rs` (3 e2e); `test-m6-clients.py` (matriz clientes); `02.md`/`03.md` |
| **G5 Gates y evidencia** | Gate focalizado por corte; al cierre `gate.py full` una vez sobre bytes finales con variables host explícitas y nativos ignorados invocados. Suites no tocadas citadas por recibo; m6-runtime re-ejecutado en `full` (fuentes byte-idénticas al recibo 7f935ac1 — corroboración). Audit/deny presentes y verdes. | recibo [M6-full-gate.json](M6-full-gate.json) `sha256:69a0be14c1e2ae0cce07014daeba1818c49fa115aa3b67313bb0baffe07f34d0` (42 etapas, `source_inputs_unchanged: true`); `01-calibration.json`; matriz §"Pruebas ejecutadas y no ejecutadas" |
| **G6 Compatibilidad, migración, rollback** | Versión de contrato de tool separada; `MutationKind` enum cerrado — el journal rechaza kind desconocido/ajeno/reetiquetado antes de efectos (G6 de escritura). Apply invalida el `project_ref`; digest liga analyzer/config/source (un cambio hace el plan stale). Rollback = binario previo; M1–M5 siguen calificadas contra sus digests (limitación declarada en compatibility). | ADR-085; `domain/mutation.rs`; e2e `ACTION_STALE`; `compatibility.md` |
| **G7 Operación y distribución** | Inventario de componentes nuevos con versiones/checksums: rust-analyzer 1.98.1, rust-src 1.98.1, imagen `f39a5b33…` derivada por digest de la M5, `config_digest a2592cfc…`. Aprovisionamiento offline salvo `provision.py`. Artifact core macOS ARM64; sin nuevos targets. | ADR-082; `provisioning.json`; `fixtures/rust-runtime/m6/` |
| **G8 Revisión independiente y bug bar** | V03–V08 (Opus 5 High para seguridad/persistencia/writer; Sonnet 5 para contratos) con P0–P3 por archivo y disposición; P3 materiales cerrados en W05x/W06x/W08b. Sin P0/P1/P2 abiertos. Revisión de cierre G8 = esta disposición + trazabilidad. | `delegation/V03..V08/*`; README register |
| **G9 DoR/DoD** | DoR: cierre M5 verificado live; D25/D26 resueltos; binario/sysroot exactos; fixtures/oráculos definidos; archivos de workers disjuntos. DoD: cortes M6-01..06 satisfacen G1–G8; evidencia reproducible source-bound; docs sin promesas no calificadas; revisión dispuesta; integración+smoke registrados. Release = gate separado (no autorizado). | matriz DoR/DoD; handoff |

# R01 — Research package: rust-analyzer shipped with Rust 1.98.1, LSP 3.17 facts for an MCP analyzer adapter

Model requested: Gemini 3.8 Flash High (`agy --model gemini-3.8-flash-high --effort high`).
Role: research only (read-only, web fetch of primary sources). No implementation, no repository access.

You are a research agent. You have web access (read_url). You must NOT run shell commands, install anything, or write files. Answer ONLY from primary sources you actually fetched in this session: the LSP 3.17 specification, the rust-analyzer book (https://rust-analyzer.github.io/book/ and .../configuration.html), the rust-analyzer repository source at the exact revision named below, the Cargo book, and https://static.rust-lang.org/dist/channel-rust-1.98.1.toml. If you cannot fetch a source, say `UNVERIFIED` for that item — never fill the gap from memory and never present a guess as a fact. Quote the evidence (short excerpt) and give the URL (with line/anchor when possible) for every claim. Rate each answer `verified` / `partially verified` / `UNVERIFIED`.

## Context (fixed by the project; do not redesign)

- Toolchain pinned: Rust 1.98.1 (rustc `48a229cea 2026-09-01`, channel manifest dated 2026-09-03). The Linux ARM64 guest image has `/opt/rust/bin/{rustc,cargo,rustfmt,cargo-clippy}` for `aarch64-unknown-linux-gnu`, no rustup, empty `CARGO_HOME` registry, no network, `HOME=/work` (empty tmpfs), source mounted read-only at `/source`, `--memory=1g`, `--pids-limit=128`, `--cpus=1`, seccomp allowlist (no `socket`/`connect`, but `socketpair` AF_UNIX allowed), user 65534.
- The MCP server will run `rust-analyzer` as an LSP server over stdio inside that guest, one transient instance per query: initialize → initialized → wait for readiness → didOpen (exact bytes captured by the host) → one request (documentSymbol / workspace/symbol / references / diagnostics / codeAction / codeAction resolve) → shutdown → exit → kill/cleanup. Build scripts, proc macros and check-on-save must stay OFF; the project is treated as hostile code.
- The project already rejects `.cargo/config.toml` in captured sources. It can also reject or strip `rust-analyzer.toml` files at capture if needed.

## Questions (answer each with evidence)

Q1. Identify the exact rust-analyzer revision shipped as `rust-analyzer-preview` in Rust 1.98.1: the `src/tools/rust-analyzer` submodule commit in rust-lang/rust at tag `1.98.1` (or the branch that produced `48a229cea`), the corresponding rust-analyzer weekly release tag/date, and what `rust-analyzer --version` prints for the toolchain build. Also confirm from channel-rust-1.98.1.toml the `aarch64-unknown-linux-gnu` `rust-analyzer-preview` xz url + hash and the `rust-src` url + hash, and the license of rust-analyzer (MIT OR Apache-2.0?).

Q2. Position encoding negotiation. Which `positionEncoding` values does rust-analyzer at that revision support (`utf-8`, `utf-16`, `utf-32`)? How does it pick when the client sends `general.positionEncodings`? Cite the LSP 3.17 text and the rust-analyzer source (e.g. `crates/rust-analyzer/src/lsp/capabilities.rs` or wherever `negotiated_encoding` lives). What happens if the client sends only `["utf-8"]`?

Q3. Readiness / quiescence oracle. Document the `experimental/serverStatus` notification: exact method name, the client capability that enables it (`experimental.serverStatusNotification`?), the payload fields (`health`, `quiescent`, `message`) and their exact meaning; when `quiescent: true` is first sent (after workspace load, cache priming?) and whether it can go back to false. Also list the `$/progress` tokens rust-analyzer creates during load ("rustAnalyzer/Roots Scanned", "rustAnalyzer/Fetching", "rustAnalyzer/Building build scripts", "rustAnalyzer/Loading proc-macros", "rustAnalyzer/Indexing"/cache priming…) with exact titles at that revision. Is there any other completion oracle for "diagnostics for this file are complete"?

Q4. Configuration keys — for each, exact key name, type, default at that revision, and whether it is settable from a workspace `rust-analyzer.toml` (the `local`/`global`/`client` classification in `crates/rust-analyzer/src/config.rs` `config_data!` macro): `procMacro.enable`, `procMacro.server`, `procMacro.attributes.enable`, `cargo.buildScripts.enable`, `cargo.buildScripts.overrideCommand`, `cargo.buildScripts.useRustcWrapper`, `cargo.buildScripts.invocationStrategy`, `checkOnSave`, `check.command`, `check.overrideCommand`, `check.invocationStrategy`, `cargo.noDeps`, `cargo.sysroot`, `cargo.sysrootSrc`, `cargo.sysrootQueryMetadata`, `cargo.targetDir`, `cargo.extraEnv`, `cargo.extraArgs`, `cargo.autoreload`, `cargo.cfgs`, `cargo.features`, `cargo.allTargets`, `linkedProjects`, `workspace.discoverConfig`, `files.watcher`, `files.exclude`/`files.excludeDirs`, `cachePriming.enable`, `cachePriming.numThreads`, `numThreads`, `lru.capacity`, `diagnostics.enable`, `diagnostics.experimental.enable`, `diagnostics.disabled`, `diagnostics.styleLints.enable`, `workspace.symbol.search.scope`, `workspace.symbol.search.kind`, `workspace.symbol.search.limit`, `references.excludeImports`, `references.excludeTests`, `runnables.command`, `rustfmt.overrideCommand`, `rustfmt.extraArgs`, `server.extraEnv`, `lens.enable`, `inlayHints.*` (just say whether they matter), `notifications.cargoTomlNotFound`, `imports.*` (whether any runs commands), `semanticHighlighting.*` (ignore). Crucially: can a workspace `rust-analyzer.toml` re-enable build scripts / proc macros / check, or set any `overrideCommand`, `extraEnv`, `procMacro.server`, `runnables.command`, `rustfmt.overrideCommand`? Give the precedence order between defaults, user config file (`$XDG_CONFIG_HOME/rust-analyzer/rust-analyzer.toml`), `initializationOptions`/`workspace/configuration`, workspace-root `rust-analyzer.toml`, and per-directory `rust-analyzer.toml` files, with source citation. Is there any config that disables loading `rust-analyzer.toml` files entirely?

Q5. External processes rust-analyzer spawns during startup with `cargo.buildScripts.enable=false`, `procMacro.enable=false`, `checkOnSave=false`, `cargo.noDeps=true`, `files.watcher="client"`: enumerate them precisely (`cargo metadata` and its exact argv incl. `--no-deps`/`--offline`/`--frozen`/`--locked`?; `rustc -vV`; `rustc --print cfg`; `rustc --print sysroot`/`target-libdir`; `cargo --version`; `cargo config get`; `rustfmt`?), with source citations (`crates/project-model/src/...`). Does `cargo metadata --no-deps` write a `Cargo.lock` (Cargo book)? Does rust-analyzer itself write anything into the workspace or the target dir in this configuration?

Q6. Sysroot. What does rust-analyzer do when `cargo.sysroot="discover"` but `rust-src` is not installed (no `lib/rustlib/src/rust/library`): does it warn via serverStatus, skip the sysroot, and do `use std::...` items then produce `unresolved-import`/`unresolved-extern-crate` diagnostics? What does `cargo.sysroot=null` do? Does rust-analyzer run `cargo metadata` on the sysroot (`cargo.sysrootQueryMetadata`) and can that be disabled? Where must `rust-src` be placed for a non-rustup toolchain at `/opt/rust` (path expected relative to `rustc --print sysroot`)?

Q7. Document symbols and workspace symbols: `textDocument/documentSymbol` — when does rust-analyzer return hierarchical `DocumentSymbol[]` vs `SymbolInformation[]` (client capability `hierarchicalDocumentSymbolSupport`)? Which fields does it fill (`name`, `detail`, `kind`, `tags`, `deprecated`, `range`, `selectionRange`, `children`)? `workspace/symbol`: query semantics (fuzzy? `#`/`*` suffix tricks?), default limit, and how `workspace.symbol.search.scope/kind/limit` apply; does it include dependency/sysroot symbols by default?

Q8. References: `textDocument/references` with `context.includeDeclaration`: does rust-analyzer honor `includeDeclaration`? Does it return locations in dependency/sysroot files (external `file://` URIs outside the workspace)? What do `references.excludeImports` / `references.excludeTests` do?

Q9. Diagnostics: (a) push — when does rust-analyzer send `textDocument/publishDiagnostics` for an opened file, does it include `version`, and are native diagnostics sent separately from flycheck (cargo check) diagnostics? With `checkOnSave=false`, are any cargo-check diagnostics ever produced? (b) pull — does rust-analyzer support `textDocument/diagnostic` (LSP 3.17 pull diagnostics) at that revision; which client/server capability gates it (`diagnosticProvider`, `textDocument.diagnostic`), does it return `RelatedFullDocumentDiagnosticReport` with `resultId`, and does it cover native diagnostics only? Is `workspace/diagnostic` supported? Cite `capabilities.rs` and the handler (`handlers/request.rs` `handle_document_diagnostics`).

Q10. Code actions: `textDocument/codeAction` — what does rust-analyzer return (`CodeAction` with `edit` inline vs lazy via `codeAction/resolve`, gated by client `codeAction.resolveSupport.properties=["edit"]`), what is in `data`; which assists return a `command` (e.g. `rust-analyzer.runSingle`, `rust-analyzer.showReferences`, `rust-analyzer.triggerParameterHints`, `rust-analyzer.rename`), and does rust-analyzer omit `command`-only actions when the client's `experimental.commands` capability does not list them? `SnippetTextEdit`: gated by client `experimental.snippetTextEdit`; if NOT declared, are snippet placeholders stripped to plain `TextEdit`? `WorkspaceEdit` shape: `documentChanges` with `TextDocumentEdit{textDocument:{uri,version}}` (requires client `workspace.workspaceEdit.documentChanges`), `CreateFile`/`RenameFile`/`DeleteFile` resource operations (which assists produce them, e.g. extract module to file; gated by `workspace.workspaceEdit.resourceOperations`? if the client does not declare them, are those assists filtered out or still emitted?), `changeAnnotations` (`changeAnnotationSupport`). Which `CodeActionKind`s does rust-analyzer emit (`quickfix`, `refactor.*`, `source.*`)? Does the `only` filter work?

Q11. Lifecycle & cancellation: `shutdown`/`exit` semantics and rust-analyzer's exit code (LSP says exit code 0 after shutdown else 1) — cite; behaviour on stdin EOF without shutdown; `$/cancelRequest` support and the error codes rust-analyzer returns for cancelled/stale requests (`-32800 RequestCancelled`, `-32801 ContentModified`, `-32802 ServerCancelled`?). Server→client requests rust-analyzer sends and which ones block if unanswered: `client/registerCapability` (when? dynamic `workspace/didChangeWatchedFiles` registration with `files.watcher="client"` and `didChangeWatchedFiles.dynamicRegistration`), `workspace/configuration` (only if client declares `workspace.configuration=true`?), `window/workDoneProgress/create` (only if `window.workDoneProgress=true`), `workspace/applyEdit`, `window/showMessageRequest`. What is the minimum client capability set that makes rust-analyzer send no server→client *requests* at all (only notifications)?

Q12. `initializationOptions` format: does rust-analyzer accept the config as a nested JSON object (`{"cargo": {"buildScripts": {"enable": false}}}`) and/or flattened dotted keys (`{"cargo.buildScripts.enable": false}`) inside `initializationOptions`, and is the `rust-analyzer.` prefix required or forbidden there? Are unknown keys errors or warnings (`window/showMessage`)? Cite `config.rs` (`Config::update`/`apply_change`).

Q13. Memory/threads: `numThreads`, `cachePriming`, `lru.capacity` behaviour; any documented figures for RSS of a tiny crate with sysroot loaded (book "performance"/"troubleshooting"?). Say UNVERIFIED if none.

Q14. `--print-config-schema`: does the toolchain binary support it at that revision (for pinning the config surface)? Any CLI flags relevant to running as a pure LSP server (`rust-analyzer` with no subcommand; `--log-file`; `RA_LOG`)?

## Output format

Markdown. One `### Qn` section per question with: **Answer**, **Evidence** (quoted excerpt + URL), **Status** (verified / partially verified / UNVERIFIED), **Implications for the adapter** (1–3 bullets, factual). End with a `### Sources fetched` list (URLs actually fetched) and a `### Could not fetch` list. Do not pad. Do not propose architecture.

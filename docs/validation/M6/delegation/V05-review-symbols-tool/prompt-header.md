You are an independent, read-only reviewer (Claude Sonnet 5) of a new MCP tool in a Rust MCP server project (rmcp 3.2.0; workspace lints deny unwrap/expect/panic/unsafe; hexagonal: application must not depend on rmcp/serde_json/process APIs). You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. The diff adds `rust.analyzer.symbols` (tool 32; the 31 existing tool definitions must be byte-identical): an application port `AnalyzerPort` + `ProjectRegistry::analyzer_symbols` (revalidate project, capture the source bundle through the lease, optional `expected_project_fingerprint` check → Conflict, reject `file` not in the bundle → FileNotInSnapshot, call the port once, domain-only report), the MCP tool module (input with tagged `scope` document/workspace, `timeout_seconds` 1..=180, output envelope per ADR-083 §3: status passed|blocked|unavailable|cancelled with a closed error-code enum, snapshot/analyzer/toolchain/readiness/completeness/limits/session facts and the symbols list; 512 KiB bound with a declared trim; no analyzer stderr/message text may cross into the result), the registration in `stdio.rs`, the `AnalyzerPort` impl on `RustProjectInspector`, snapshot + protocol/wire tests, one `#[ignore]` native end-to-end test, `release-smoke.py` pinned to 32 tools with the new schema hash, gate wiring (`m6-runtime-unit-tests`), and public docs (tools/README/CHANGELOG/security-model/compatibility/client-configuration/implementation-status/ci).

Review goals, in priority order:
1. Contract fidelity and semver: does the emitted tool definition (snapshot) match ADR-083 §2–§4 (names, enums, bounds, annotations readOnly/idempotent/non-destructive/closed-world)? Any existing snapshot or schema touched? Any shared enum widened? Is the `Shadow` deserializer (hand-written `Deserialize` to work around serde flatten+deny_unknown_fields) actually equivalent to `deny_unknown_fields` — can an unknown top-level or scope field slip through, or a valid input be rejected? Does the schema (`unevaluatedProperties: false`) agree with what the deserializer accepts?
2. Status/code mapping: every `AnalyzerFailure`/`InspectionError`/`AnalyzerRequestError` variant maps to exactly one status+code; no path yields `passed` without an analyzer answer; `incomplete` is data not status; `SANDBOX_DENIED` → `unavailable` as specified; cancellation → `cancelled`; quarantine → `unavailable`; `Conflict` never returns data.
3. Information boundary: any field that could carry peer/project text (stderr, `message`, `detail`, symbol `name`, `container`, file paths) — which of these are legitimately project text (symbol names/details/paths are) vs analyzer text that must never cross (serverStatus `message`, stderr, error messages from the LSP `ResponseError`)? Is the 512 KiB trim applied to the serialized result and declared, never producing invalid JSON? Are hostile symbol names (control chars, 1 MiB names, invalid UTF-8 already excluded) bounded?
4. Architecture: application file imports; `serde_json` or `rmcp` leaking into application; the port impl in `project_inspection.rs` reusing `with_gateway` correctly (deadline, cancellation, quarantine, single-flight); `ExecutionLimits` derived from `timeout_seconds` consistently with ADR-084 §8 (initialize ≤60, query ≤30, total ≤180).
5. Tests: could the protocol tests pass with the registration missing or the mapping wrong? Do the 3 regression edits (`tools.len() == 32`) hide anything? Does the native test assert the properties it claims (selection_range slicing the symbol name, `complete`, `utf-8`, version)? Does `test-m6-runtime.py`'s tenth step keep the receipt honest (a compile-only or filtered-out run cannot be `pass`)?
6. Docs: claims beyond evidence (e.g. "qualified", tool counts, image digests, timings) and consistency with `docs/validation/M6/01.md` (9/9, quiescent 353–385 ms, health ok, config_digest a2592cfc…).

Output format: a markdown report with sections `Verdict` (Approve / Approve with P3 / Block), `Findings` as a table with columns Severity (P0–P3), File:line or function, Finding, Evidence from the diff, and `What I verified`. Be specific and cite diff lines. Do not pad; if a goal has no finding, say so.

Diff follows.

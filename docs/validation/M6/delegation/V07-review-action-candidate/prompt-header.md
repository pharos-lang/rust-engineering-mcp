You are an independent, read-only reviewer (Claude Opus 5) of a security-critical change in a Rust MCP server (workspace lints deny unwrap/expect/panic/unsafe; hexagonal — domain/application must not depend on serde_json/rmcp/process/sha2; there is exactly ONE mutation writer; rust-analyzer is a potentially hostile, project-influenced peer). You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. This diff (W07, M6-04) turns a resolved rust-analyzer code action into an M2 `MutationCandidate` that a later tool (W08) will apply through the existing single M2 writer (`local_coordinated`, ADR-050/052). It adds, on top of an already-shipped analyzer stack (symbols/references/diagnostics/actions resolution in the gateway/codec): `MutationKind::AnalyzerActionApply` (domain); `AnalyzerAction::digest_input` (an order-independent canonical serialization for digesting — domain, no sha2); `apply_action_to_bundle(before, edits)` (applies validated `TextEdit`s to a captured `SourceBundle` → after, using the existing overlap-rejecting `apply_edits`); `action_digest(action, identity, source_fingerprint)` (execution-adapter, sha256 over digest_input ++ analyzer version/binary_sha256/config_digest ++ source_fingerprint); `resolve_action_candidate` (a fresh CodeActions session, match an applicable action by its digest, or `ActionStale`); application `analyzer_actions`/`analyzer_action_candidate` (produces the `MutationCandidate{kind: AnalyzerActionApply, before, after, validation: "m6-analyzer-action-v1"...}` after authorizing via `MutationPublisher` and capturing fresh); new `validation` encoding decoded by a separate `AnalyzerActionValidationView` (method `workspace_edit_structural_only`); and new match arms for the new `MutationKind` in the writer digest, journal operation/scope, resolution, and `preview_diff`. Owner decision A: an applied action is validated only structurally (existing-file non-overlapping bounded TextEdits, version match, no Command/snippet/resource-op/external-URI) plus the exact diff and a plan bound to analyzer/config/source identity — NO cargo check, so it is NOT compile-verified (a documented property).

Review goals, in priority order:
1. Digest soundness / staleness: is `AnalyzerAction::digest_input` genuinely collision-resistant and order-independent (two distinct actions cannot share bytes; edit order cannot change the digest; a length-delimited encoding so field boundaries can't be forged)? Does `action_digest` bind enough (title, kind, edits, analyzer version+binary+config, source fingerprint) that a plan cannot be replayed against a different runtime or source? The digest deliberately excludes `is_preferred` and the `only` filter — is that safe? Can an attacker craft two different edits with the same digest, or make a dangerous action match the digest of a benign one?
2. `apply_action_to_bundle`: can peer-controlled edits, once past the codec's structural validation, corrupt the bundle — overlapping across the multi-file grouping, an edit on a file absent from `before`, an empty edit set, a result exceeding ADR-031 `SourceBundle` limits, non-UTF-8 result, or a `before`/`after` shape mismatch the writer would mishandle? Is every error typed (no panic/unwrap)? Does it preserve untouched files exactly?
3. Single-writer / journal integrity (G6): the new `MutationKind::AnalyzerActionApply` — is EVERY exhaustive `match` on `MutationKind` updated (writer digest, journal op/scope, resolution, preview_diff, validation_view)? Does the journal still REFUSE an unknown or foreign kind before any on-disk effect, and does the digest cover the kind so a relabelled journal is rejected? Is there any path that introduces a second writer/journal, or lets an analyzer candidate bypass the `MutationPublisher` authorize/generation/idempotency checks the other M2 mutations go through?
4. The `validation` encoding: is `m6-analyzer-action-v1` length-prefixed like the others, un-spoofable, and does `AnalyzerActionValidationView` decode exactly what was encoded (analyzer identity + action_digest + the analyzed source fingerprint), refusing malformed/oversized input? Does it correctly NOT reuse a `ValidationMethod` variant that would change the 5 frozen M2 snapshots? Field 9 is the *analyzed* (before) source fingerprint, not the after — is anything downstream assuming it is the after?
5. Architecture / behaviour preservation: domain/application free of serde_json/rmcp/process/sha2; the shared `analyzer_prelude`/report refactor does not change symbols/references/diagnostics behaviour; no existing test or snapshot altered; `MutationPublisher`/`MutationPlans` contracts intact.
6. Native cut `m6-11`: it passed on the real image offering `refactor_inline "Inline variable"` and an introduce-const assist (recorded). Does the cut actually prove what M6-04 needs (an applicable action's edits apply cleanly to produce a changed `after`, and the same action re-resolves by digest in a fresh session), or could it pass vacuously? It records that no Command/snippet/resource-op rejection was elicited — is that gap covered by unit tests elsewhere?

Output format: markdown with `Verdict` (Approve / Approve with P3 / Block), `Findings` table (Severity P0–P3, File:line/function, Finding, Evidence from the diff), `What I verified`. Be specific; cite diff lines. If a goal has no finding, say so. This is the source-mutation boundary; weight soundness over style.

Diff follows.

You are an independent, read-only reviewer (Claude Sonnet 5) of new Rust code in an MCP server project (workspace lints deny unwrap/expect/panic/unsafe). You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. The project will drive rust-analyzer 1.98.1 as an LSP server over stdio inside a Docker guest, one transient instance per query, with hostile input assumed on the wire. This diff adds (1) `rust_engineering_domain::analyzer`: validated domain values (relative `.rs` file paths, `TextRange` over the existing 1-based Unicode-scalar `Position`, a `LineIndex` over captured bytes with `\n` as the only line break, byte/UTF-8/UTF-16 ↔ `Position` translation that must reject offsets inside a code point or surrogate pair, `SymbolKind` (26 LSP kinds), `DocumentSymbol` (selection range inside range, depth ≤ 32), references, diagnostics, `CodeActionKind`, `ActionRejection`, `TextEdit` + `apply_edits` (sorted, non-overlapping, touching allowed), `Completeness` with the invariant Complete ⇒ no `limit_visible` omission, limit constants (512 visible, 32 actions, 128 edits, 1 MiB frame, 4096 messages, 16 MiB/1 MiB stdout/stderr, 60/30/180 s, 512 KiB result)); the domain crate may depend only on serde/semver (no serde_json, no I/O). (2) `rust_engineering_execution::lsp_codec`: an incremental LSP base-protocol decoder with header limits (Content-Length mandatory ≤ 1 MiB, ≤ 8 headers, ≤ 1 KiB header bytes, CRLF only), strict JSON-RPC message parsing (no batches, no both result+error, ids string/integer only), message (≤ 4096) and byte (≤ 16 MiB) counters, `encode`, a `Correlator` (monotonic ids; unknown/duplicate/late responses rejected), typed DTOs for the M6 subset, a fixed `InitializeParams` (client capabilities: no workDoneProgress, no workspace/configuration, no dynamic registration, hierarchical document symbols, `workspaceEdit.documentChanges` true, no resourceOperations, no snippetTextEdit, no experimental.commands, no resolveSupport, pull diagnostics, `positionEncodings: ["utf-8"]`, `experimental.serverStatusNotification: true`) and fixed `initializationOptions` with a canonical-JSON sha256 `config_digest`, and conversions to domain: `file:///source/<rel>` URIs only (everything else is External), symbols/diagnostics/references/code actions, with every `WorkspaceEdit` rejection reason (command, snippet, resource operation, external URI, version mismatch vs. version 1, overlapping ranges, edit/bytes limits, not-UTF-8, unresolved edit).

Review goals, in priority order:
1. Soundness of the byte/UTF-16/Unicode-scalar translation and `apply_edits` (off-by-one at line ends, CRLF, BOM, offset == len, surrogate pairs, combining sequences, empty file, edits at EOF, touching vs overlapping, edits given in unsorted order, multi-byte boundaries). Find inputs that would produce a wrong position silently, or a panic-equivalent (slice out of bounds, integer overflow, infinite loop).
2. Codec robustness against a hostile peer: can any byte sequence make the decoder allocate unboundedly, loop forever, accept a frame over 1 MiB, accept `Content-Length` with leading `+`/whitespace/`0x`, split headers across feeds incorrectly, or desynchronize after an error? Are limits enforced before allocation? Is the decoder's state after a fatal error unusable (good) or silently reset (bad)? Does `Correlator` handle id reuse and late responses as the spec says? Can a server *request* be mistaken for a response or vice versa (both have `id`)?
3. Conversion correctness: URI decoding (percent-encoding, `..`, `//`, case, `file:///SOURCE`, `file://source/`, `file:///source/../etc`), version mismatch semantics, `changes` vs `documentChanges` precedence, detection of `insertTextFormat == 2`, `Command`-bearing `CodeAction`, resource operations in `documentChanges`, `data` fields, ordering/determinism of outputs, the 512 cap and its omission accounting, `is_declaration` handling.
4. Contract fidelity to the fixed capabilities/options above (any key missing, misspelled or with the wrong value is a P1 because rust-analyzer ignores unknown keys silently) and `deny_unknown_fields` on outgoing vs. incoming types.
5. Test quality: tests that would still pass if a guard were removed; missing negative tests.

Output format: a markdown report with sections `Verdict` (Approve / Approve with P3 / Block), `Findings` as a table with columns Severity (P0–P3), File:line or function, Finding, Evidence from the diff, and `What I verified`. Be specific and cite diff lines. Do not pad; if a goal has no finding, say so.

Diff follows.

You are an independent, read-only reviewer (Claude Opus 5) of a security-critical change in a Rust MCP server project (workspace lints deny unwrap/expect/panic/unsafe; every external process goes through one Docker-based execution gateway; project code is hostile). You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. The diff adds to the execution adapter: (1) `lsp_session.rs`, a duplex bounded stdio session that drives a child process (`docker container start --attach --interactive <name>`) as an LSP peer: nonblocking pipes, per-frame 1 MiB / 4096 messages / 16 MiB stdout / 1 MiB stderr limits (stderr never published), deadline and cancellation checks on every wait, `request()` that answers any server→client request with `-32601`, `close()` = shutdown → exit → grace → kill, kill-on-drop; (2) `analyzer_gateway.rs` + `Phase::Analyzer` in `rust_gateway.rs`: inside the gateway's `busy` lock and `WorkBudget`, preflight (verified/quarantine/executable digest/engine identity), volume + ingest, container create/verify (`rust_applied::verify` learns the phase), the LSP lifecycle (initialize with fixed capabilities/options, utf-8 asserted, `experimental/serverStatus` quiescent as readiness, didOpen version 1, exactly one query, close), conversion to domain values, then kill/rm/absence checks for container and volume before releasing `busy`, quarantine on uncertain cleanup; (3) admission of the M6 image digest `sha256:f39a5b33…` (`APPROVED_M6_IMAGE`, `RustGateway::new`, `host_config.rs`), with `execute_analyzer` refusing any other image before any container; (4) capture-time rejection of `rust-analyzer.toml`/`.rust-analyzer.toml` at any depth in `project-adapter/.../source.rs`; (5) `analyzer_native.rs`, nine `#[ignore]` native calibration cuts against the real binary, and `scripts/test-m6-runtime.py`, the gate driver. The invariants: no `Command` outside the execution adapter; the worker must stay joined until cleanup is verified; cancellation/EOF/timeout never mean "process gone"; uncertain cleanup quarantines the gateway; nothing from the peer grants anything; no peer text (stderr, `message` fields) crosses to results/logs unbounded.

Review goals, in priority order:
1. Lifecycle/containment soundness: can the child outlive the session (drop paths, early returns with `?`, panics-equivalent, poll errors, EPIPE on write, partial writes, EAGAIN handling, the grace/kill ordering)? Can `busy` be released with a container or volume still present? Is quarantine set on every uncertain path? Are deadline and cancellation checked inside every blocking wait? Can a slow/hostile peer make `recv` spin or block past the deadline (poll timeout math, saturating conversions, negative durations)? Is the stdin pipe closed correctly on `close()`? Is the docker CLI child (not the container) the thing being killed, and is the container itself killed/removed independently of the CLI process?
2. Limits before allocation: every `Vec`/`String` growth in the session and in the conversions is bounded by a limit checked first; stderr capped without publication; the frame limit applies to what is buffered, not only to what is parsed.
3. Admission and identity: can `execute_analyzer` run on a non-M6 image via any path (calibration constructors, `new_m4_for_qualification`, test hooks)? Is the version/sha256 identity really read from the guest or pinned as constants — and if constants, is the calibration the only thing asserting them? Any new `#[cfg(feature = "test-hooks")]` or env-var bypass that could reach production?
4. Capture-time rejection: does the `rust-analyzer.toml` check cover directories, symlinks/nonregular objects, case, dot-prefixed variants, nested depth, and does it run before any bytes are read into the bundle? Any path where the check could be skipped (e.g. excluded directories, depth limit reached first)?
5. `rust_applied::verify` and `arguments()`: does the Analyzer phase container get exactly the intended flags (read-only rootfs, `/source` ro, network none, cap-drop, no-new-privileges, pids/cpus/memory, seccomp, user 65534, tmpfs sizes) and nothing extra (no `--interactive` semantics leaking a tty, no stdin left open after close)?
6. Calibration oracles (`analyzer_native.rs`): could a cut pass without the property holding (e.g. `top` sampling accepting an empty sample set, the never-ready cut passing by timing out on `initialize` rather than readiness, the crash cut passing because the kill landed after close, absence checks that inspect the wrong name, receipts written before assertions)? Does the gate driver stop on the first failure and never mark a cut `pass` on a skipped run?
7. Python (`scripts/test-m6-runtime.py`): SonarCloud taint (argv paths opened, `/tmp` literals, `shell=True`), constant output paths, honest receipt.

Output format: a markdown report with sections `Verdict` (Approve / Approve with P3 / Block), `Findings` as a table with columns Severity (P0–P3), File:line or function, Finding, Evidence from the diff, and `What I verified`. Be specific and cite diff lines. Do not pad; if a goal has no finding, say so.

Diff follows.

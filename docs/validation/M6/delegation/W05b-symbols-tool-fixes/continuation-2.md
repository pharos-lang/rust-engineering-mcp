# W05d — one harness fix in `crates/mcp-server/tests/analyzer_runtime.rs`

Model requested: Claude Sonnet 5 (`claude -p --model sonnet --effort medium`). You own only `crates/mcp-server/tests/analyzer_runtime.rs`. Do not commit. **Never run a command in the background; run everything in the foreground and end with the report as your last message.**

Root cause established by the orchestrator (2026-09-12 13:30 UTC), reproduced with the real binary: the test passes `--root` as `CARGO_MANIFEST_DIR/../../fixtures/valid-basic` (non-canonical, contains `..`); the server refuses it at startup — stderr `ERROR MCP project authorization initialization failed`, exit 1 — so stdout reaches EOF and the test reports `Error: Disconnected`. The same flow with a canonical root and the test's own 2026-07-28 `_meta` first-frame convention passes (`rust.analyzer.symbols` → `passed`, quiescent, `health ok`, `complete`). The handshake convention is correct; the earlier orchestrator hypothesis about a missing `initialize` was wrong and is withdrawn.

Fix: make `fixture_root()` return the canonicalized path (`std::fs::canonicalize`, propagate the error — no `unwrap`/`expect`), use it both for `--root` and for reading `src/lib.rs`; also, in `Server::start`, when the child exits before the first response, surface its stderr in the error instead of a bare `Disconnected` (bounded, like `finish()` does) so the next harness defect is self-explanatory. Then `cargo fmt --all -- --check`, `cargo clippy -p rust-engineering-mcp --all-targets --locked --offline -- -D warnings`, `cargo test -p rust-engineering-mcp --locked --offline --test analyzer_runtime --no-run`. Do not run the ignored test (no Docker for you). Report: Task / Result / Files changed / Tests executed / Evidence / Risks / Decisions / Open issues.

# W01 — Provision the M6 guest image (rust-analyzer 1.98.1 + rust-src 1.98.1)

Model requested: Claude Sonnet 5 (`claude -p --model sonnet --effort high`). Role: implementation worker with disjoint file ownership. Orchestrator: Claude Fable 5.1 (decides, does not write code).

## Authorization (cite it in the receipt)

The owner approved option A+B+C of `docs/roadmap/m6-provisioning-request.md` on 2026-09-11 ("aprobado opción a+b+c"). Read that dossier first: it fixes the inputs, hashes, destinations and rollback. Nothing else may be acquired. This is the only step of M6 allowed to use the network, and only to download exactly the files below; the Docker build itself runs with `--network=none`.

## Inputs (from the pinned channel manifest, dated 2026-09-03)

| Item | URL | sha256 (xz) | Destination in the image |
| --- | --- | --- | --- |
| A `rust-analyzer-preview` aarch64-unknown-linux-gnu | `https://static.rust-lang.org/dist/2026-09-03/rust-analyzer-1.98.1-aarch64-unknown-linux-gnu.tar.xz` | `a0fd960a9ab36193ae9ba4310e5f780f6ca38fa86160fae739be4ac541b6d10c` | `/opt/analyzer/bin/rust-analyzer` (NOT on PATH; installed via the tarball's `install.sh --prefix=/opt/analyzer --disable-ldconfig`, or by copying the single binary if `install.sh` cannot target that prefix cleanly — document which) |
| B `rust-src` (target `*`) | `https://static.rust-lang.org/dist/2026-09-03/rust-src-1.98.1.tar.xz` | `5c846ebcebcc7e2e0777a4cdaa12051691593f16a7e94edbae5e6241cc62d98c` | `/opt/rust/lib/rustlib/src/rust/library` via `install.sh --prefix=/opt/rust --disable-ldconfig`, so `rustc --print sysroot` (= `/opt/rust`) discovers it |
| Manifest | `https://static.rust-lang.org/dist/channel-rust-1.98.1.toml` | `a7c8774a5fd8441c997d94c029776cbc5eb111e9d72ab5d256fa69866644347e` (already recorded in `fixtures/rust-runtime/sources.json`) | not installed; used to cross-check the two entries above before downloading |

Base image: `rust-engineering-runtime:1.98.1-arm64-m5`, which MUST resolve to `sha256:e0a5ca1661b3e49d0a3d68ee3cc0963453078d08eb7fc43c30538c16b7998aac` immediately before building (verify with `docker image inspect`; abort otherwise). Target tag: `rust-engineering-runtime:1.98.1-arm64-m6`.

## Deliverables (files you own; touch nothing else)

1. `fixtures/rust-runtime/m6/Dockerfile`, `build.sh`, `provision.py`, `README.md`, `test_provision.py` — mirror `fixtures/rust-runtime/m5/` (read it, and `m4-scanner/test_provision.py` for the unit-test shape) with these differences:
   - `provision.py` downloads the manifest, verifies its sha256, parses `[pkg.rust-analyzer-preview.target.aarch64-unknown-linux-gnu]` and `[pkg.rust-src.target."*"]` and requires their `xz_url`/`xz_hash` to equal the constants above; then downloads the two `.tar.xz` files into `target/m6-provisioning/downloads/` (skip a download whose sha256 already matches), verifies sha256, validates every tar member (regular files/dirs only under the expected top-level directory, no absolute paths, no `..`, no links pointing outside), and assembles the build context (`Dockerfile`, `build.sh`, the two tarballs, `build-inputs.json` SBOM with url/sha256/license/size per input, `notices/` with the LICENSE-APACHE/LICENSE-MIT/COPYRIGHT files extracted from the tarballs, `SHA256SUMS`), pinning mtimes to epoch 0 like M5. It must print a JSON summary on stdout. No argv-controlled output paths beyond the M5 `beside_default` pattern (SonarCloud taint rules).
   - `build.sh` (runs inside the builder as root): `sha256sum --check SHA256SUMS`; assert `rustc --version --verbose` reports `release: 1.98.1` and host `aarch64-unknown-linux-gnu`; extract and install A and B as specified; `readelf -h /opt/analyzer/bin/rust-analyzer | grep AArch64`; `ldd` shows no `not found`; `/opt/analyzer/bin/rust-analyzer --version` succeeds and its output is recorded; `test -d /opt/rust/lib/rustlib/src/rust/library` and `test -f .../library/Cargo.lock`; `test ! -e /usr/local/bin/rust-analyzer` and `test ! -e /opt/rust/bin/rust-analyzer`; write `/usr/share/doc/rust-runtime/m6/{installed.json, sbom.json, build-inputs.sha256, license-files.list, license-texts.tar(+.sha256), rust-analyzer-version.txt, runtime-inventory.sha256}`; remove the input/build dirs. Final stage `FROM scratch` + `COPY --from=builder / /`, `USER 65534:65534`, `WORKDIR /work`, same `PATH` as M5.
2. `scripts/build-m6-runtime.py` — mirror `scripts/build-m5-runtime.py` (base-tag→digest check, `provision.py`, content-addressed context, builder prune, `docker build --network=none --pull=false --no-cache`, post-build guest checks: both inputs present, `rust-analyzer` off PATH, M3/M4/M5 binaries intact (`/opt/rust/bin/{cargo,rustc}`, `/opt/security/bin/{cargo-deny,rust-mcp-unsafe-helper}`, `/opt/perf/bin/{cargo-bloat,rust-mcp-profile-helper}`), `rust-src` present, `rust-analyzer --version` captured, context residue check). Receipt at the constant path `docs/validation/M6/provisioning.json` with `schema: "rust-engineering-mcp.m6-provisioning.v1"`, `authorization: "docs/roadmap/m6-provisioning-request.md"`, `authorized_by_owner: "2026-09-11"`, `decision: "docs/adr/ADR-082-m6-runtime-provisioning.md"`, `network_used_for: ["manifest", "rust-analyzer tarball", "rust-src tarball"]`, `build_network: "none"`, per-input url/sha256/bytes, the observed base id, the built image id, the guest-computed `installed.json`, and `status`.
3. `scripts/test-m6-provisioning.py` — unit tests (stdlib `unittest`) for the pure functions of `provision.py`/`build-m6-runtime.py` (manifest parsing, hash checks, tar member validation, receipt assembly), no network, no Docker. Coverage target ≥ 80 % of the new Python lines.
4. Wiring: add `m6-provisioning-tests` to `scripts/gate.py` core mode right after `m5-vendor-tests` (`[sys.executable,'-B','-m','unittest','fixtures/rust-runtime/m6/test_provision.py']`, `require_test_groups=True`) and a matching line for `scripts/test-m6-provisioning.py`; add both test scripts to the Python coverage list in `.github/workflows/sonarcloud.yml` (after the `test-docs-hygiene.py` line). Update the gate-stage counts in `docs/ci.md` only if a sentence there states the exact number of core stages (search for "22 etapas core"); add a short "Imagen guest M6" subsection to `docs/ci.md` mirroring the M5 one.
5. `docs/adr/ADR-082-m6-runtime-provisioning.md` — Status: Accepted (owner authorization 2026-09-11 on the dossier). Sections: Context, Decision (the three items, destinations, no-PATH rule, network only in provisioning, receipt), Alternatives considered (host RA; RA 1.97.1/nightly; RA without rust-src; batch CLI), Consequences (inventory grows by two Rust components under MIT OR Apache-2.0; admission is a separate ADR with native qualification; rollback = M5 digest). Keep it in the language and shape of ADR-075. Do not touch `docs/adr/README.md` unless it indexes ADRs (check; if it does, add the row).
6. Execute the procedure once: `python3 -B scripts/build-m6-runtime.py`. Then run `python3 -B -m unittest fixtures/rust-runtime/m6/test_provision.py`, `python3 -B scripts/test-m6-provisioning.py`, `python3 -B scripts/test-gate-reporting.py`, `python3 -B scripts/docs-hygiene.py links-check` (0 broken in living documents) and `python3 -B scripts/docs-hygiene.py verify-inventories`.

## Constraints

- Do NOT modify any Rust source, `Cargo.*`, existing fixtures, existing images or their scripts, ADR-075/077, or any file outside the list above. Do not admit the image in the gateway (that is a later cut). Do not commit; the integration delegate commits.
- No `--dangerously-*` flags, no `sudo`, no privileged containers. `docker build` with `--network=none`.
- Python: stdlib only, `python3 -B`, no `/tmp` literals (Sonar S5443), no argv-derived paths opened directly (use the M5 `beside_default` pattern), constant output paths.
- Be exact and honest in the receipt: whatever fails is recorded as `failed` with the error; never mask a failure.

## Report (mandatory headings)

Task / Result / Files changed / Tests executed / Evidence (receipt path, image id, rust-analyzer --version output, sha256 of both inputs as verified) / Risks / Decisions / Open issues.

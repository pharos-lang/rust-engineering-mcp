You are an independent, read-only reviewer (Claude Opus 5) of the analyzer WRITE path in a Rust MCP server (workspace lints deny unwrap/expect/panic/unsafe; hexagonal; there is exactly ONE mutation writer, the M2 local_coordinated writer with a journal, ADR-050/052; rust-analyzer is a hostile-influenced peer). You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. This diff (W08, M6-04/05) adds the last two M6 tools on top of already-shipped analyzer read tools and the W07 candidate-production layer: `rust.analyzer.actions` (tool 35, read-only: lists resolved code actions with a per-action `action_digest`, applicability, edits_summary) and `rust.analyzer.action.apply` (tool 36, WRITE: `preview` builds an M2 `MutationCandidate{kind: AnalyzerActionApply}` via W07's `analyzer_action_candidate`, feeds it through the SAME `MutationPlans`/`preview_diff`/`mutation_digest` path as `rust.fmt.apply`, and `commit`/`receipt` go through the existing single `MutationPublisher`/`NativeMutationStore` writer). New host grant `--allow-analyzer-action-write <root>`. Owner decision A: apply is validated only structurally (existing-file non-overlapping bounded TextEdits, version match, no Command/snippet/resource-op/external-URI) plus the exact preview diff plus a plan bound to analyzer/config/source identity — NO cargo check, so an applied action is NOT compile-verified (a documented property). The 34 existing tool snapshots and the 5 M2 tool schemas must be byte-identical. W08 also refactored the M2 audit code to be generic over the output type — the M2 tools' behaviour must be unchanged.

Review goals, in priority order:
1. Single-writer integrity: does `rust.analyzer.action.apply` go through the EXACT `MutationPublisher`/`NativeMutationStore`/`commit_mutation`/`mutation_receipt` path with no bypass of authorize/generation/idempotency? Is there any second writer, second journal, or a path that writes source outside the M2 writer? Does commit invalidate the project_ref exactly as the M2 tools do?
2. The M2 audit refactor (generic-over-output): did it change any observable behaviour, schema, or the audit event of the existing M2 tools? Any shared type whose schema is embedded in the 5 frozen M2 snapshots that moved? Confirm the 5 M2 `Output`/`ValidationView` schemas are untouched.
3. Staleness / plan binding: `preview` recomputes the action over a fresh capture and requires the digest to match (`ACTION_STALE`); `commit` re-checks the live capture against the plan before touching the writer. Can a plan be committed after the source changed under it? Can a plan of a DIFFERENT kind (fmt/fix/manifest) be committed through the analyzer tool or vice versa (the validation-view-per-kind guard)? Is the `plan_digest`/`idempotency_key`/generation contract identical to fmt.apply?
3b. The cross-crate `validation` encode→decode round trip: does `AnalyzerActionProvenance::encode` (application) decode field-for-field in `analyzer_action_validation_for` (mcp-server), and would a field reorder on either side be caught?
4. Information boundary and bounds: no analyzer stderr/serverStatus/message text on the wire; peer-controlled `title`/`kind` on both applicable and REJECTED actions are bounded (≤256 scalars, control chars replaced); the diff/response ≤512 KiB with a declared trim; the touched-file list (an action may rewrite up to 128 files including build.rs/vendored .rs) is made prominent to the caller.
5. Grant and status mapping: without `--allow-analyzer-action-write` every call is `unavailable/SANDBOX_DENIED` before any state is created; a grant for a different root stays `PERMISSION_DENIED`; every failure (CONFLICT, ACTION_STALE, FILE_NOT_IN_SNAPSHOT, PERMISSION_DENIED, bootstrap, timeouts, RESULT_LIMIT) maps to exactly one status+code; no `passed` without a real result.
6. Contract/semver: the two new snapshots (indices 34/35) match ADR-083; `destructiveHint=true`/`readOnlyHint=false` for apply (matching M2 write tools); the 34 existing snapshots byte-identical; the `only` filter and rejection vocabulary as ADR-083 §4/§5.
7. Native e2e (not in diff-runnable, but the tests are): the three `analyzer_runtime.rs` ignored tests (list; preview→commit→disk-bytes→reference-invalidation; ACTION_STALE) — do they prove the writer actually changed the file on disk and that a stale plan is refused, or could they pass vacuously? The lifecycle unit tests use a fake port + fake writer — do they faithfully model authorize-once, receipt-after-invalidation, and the wrong-kind refusal?

Output format: markdown with `Verdict` (Approve / Approve with P3 / Block), `Findings` table (Severity P0–P3, File:line/function, Finding, Evidence), `What I verified`. Be specific; cite diff lines. This is the source-write boundary; weight soundness over style. If a goal has no finding, say so.

Diff follows.

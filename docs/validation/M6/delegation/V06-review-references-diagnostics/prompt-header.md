You are an independent, read-only reviewer (Claude Opus 5) of two new MCP tools in a Rust MCP server project (rmcp 3.2.0; workspace lints deny unwrap/expect/panic/unsafe; hexagonal — application must not depend on rmcp/serde_json/process APIs; rust-analyzer is treated as a potentially hostile LSP peer). You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. The diff adds `rust.analyzer.references` (M6-02) and `rust.analyzer.diagnostics` (M6-03), matching the already-shipped `rust.analyzer.symbols` shape (tool 32; now 34 total; the 32 existing tool snapshots must be byte-identical). Key design decisions already made by the orchestrator and implemented here:
- References `is_declaration`: rust-analyzer's `textDocument/references` does not flag the declaration, so the gateway sends TWO requests in one session (`includeDeclaration: true` and `false`) and marks as declaration every location present only in the first (set difference on (file,range)). The tool layer removes declarations from the visible list and counts `omitted_declarations` when the caller asked `include_declaration:false`. Position is validated against captured bytes (1-based line, 1-based Unicode-scalar column) → `PositionOutOfRange` before any session.
- Diagnostics are pull only (`textDocument/diagnostic`, full report), native rust-analyzer diagnostics only. Diagnostic `message` is project-derived text allowed to cross the wire but bounded (≤4096 scalars, control chars except \n/\t replaced, `message_truncated` flag; `code` ≤128). serverStatus `message`/stderr must NEVER cross.
- A build-script in-band oracle: on fixtures/build-script (real build.rs with include!(concat!(env!("OUT_DIR"),…))), with build scripts disabled the analyzer cannot resolve the include, so the diagnostics answer contains that unresolved diagnostic — deterministic proof no build script ran.

Review goals, in priority order:
1. References correctness: is the two-request set-difference sound (ordering, dedup, ranges that appear in both, an entry that is a declaration in file A but a use in file B, external/sysroot locations)? Does `include_declaration:false` truly remove declarations from the visible list AND count them, without losing non-declaration refs? Is the query budget correctly SHARED across the two requests (a slow first request must not let the second exceed the total, and a timeout on either is classified, not silently dropped)? Position out-of-range validated before the session for every path (including column at end-of-line, multi-byte, empty file)?
2. Diagnostics information boundary and robustness: can any analyzer-controlled text other than the diagnostic message/code/related-message reach the wire (serverStatus message, stderr, ResponseError message)? Is the message bound applied to BOTH the primary and every `related` message? THE KNOWN GAP: the worker reports that `lsp_codec::diagnostics_to_domain` hard-fails the WHOLE call (`ProtocolViolation`) if a single diagnostic's message/code exceeds the domain constructor limits, instead of omitting/truncating that one entry — assess severity: can a hostile or merely verbose project (a long rustc error message, a macro-generated diagnostic) deny the entire diagnostics tool? Is that a P1/P2?
3. Contract/semver: do the two new snapshots match the ADR-083 envelope and the documented input/output? Any existing snapshot or shared enum changed? Are `omitted`/`omitted_declarations`/`message_truncated` in the schema? Annotations readOnly/idempotent/non-destructive/closed-world? Is the `OUTPUT_LIMIT` 2→6 MiB test-harness change safe (it's a test ceiling, but confirm it doesn't mask a real oversize)?
4. Status/code mapping: every failure (PositionOutOfRange, FileNotUtf8, FileNotInSnapshot, Conflict, timeouts, never-ready, crash, frame/message limits, bootstrap, RESULT_LIMIT) maps to exactly one status+code; no `passed` without an analyzer answer; incomplete is data not status.
5. Architecture and dead code: application imports (no serde_json/rmcp/process); the new `answer_references`/`request_references` path vs the untouched `request_for`/`convert` — is the old path now dead code (clippy passed, so maybe `#[cfg(test)]`-only)? Refactor of the shared `analyzer_prelude`/`analyzer_finish` — did it change `rust.analyzer.symbols` behavior (its tests were required to stay green unmodified)?
6. Native cuts (`m6-09-references`, `m6-10-diagnostics-build-script-oracle`): do the oracles actually discriminate (references cut asserts the declaration flagged exactly once and ranges slice the name; diagnostics cut asserts a diagnostic on the include!/env! line, health ok, quiescent, cleanup)? Could either pass without the property holding?

Output format: markdown with `Verdict` (Approve / Approve with P3 / Block), `Findings` table (Severity P0–P3, File:line/function, Finding, Evidence from the diff), and `What I verified`. Be specific; cite diff lines. If a goal has no finding, say so.

Diff follows.

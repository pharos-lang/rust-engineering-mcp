# W01b — informe del worker

| Campo | Valor |
| --- | --- |
| Modelo | Claude Sonnet 5 (`claude -p --model sonnet`), Claude Code 2.1.268 |
| Modelos observados | claude-haiku-4-5-20251001, claude-sonnet-5 (Haiku es el auxiliar interno de Claude Code) |
| Inicio / fin (UTC) | 2026-09-11T22:42:31Z / 2026-09-11T22:47:50Z; 318447 ms |
| Resultado CLI | `subtype: success`, `is_error: False` |
| Permisos denegados por el allowlist | 1: `\
echo "=== test_provision.py ===" && python3 -B -m unittest fixtures/rust-runtime/m6/test_provision` |
| Transcript | fuera del árbol; hashes en `transcripts.sha256` |

## Informe literal del worker

Good — nothing committed, all expected files touched. Not committing, per the prompt-header constraint (the integration delegate commits).

## Report

**Task** — Fix the eight findings (two P2, six P3) from the independent review of the M6 provisioning package, plus two orchestrator-only findings, and rebuild the image since `build.sh`/`provision.py`'s context assembly changed.

**Result** — All 9 items applied; targeted verification passes; image rebuilt and receipt regenerated with `status: "passed"`.

**Files changed**
- `fixtures/rust-runtime/m6/provision.py` — added `OUTPUT_DEFAULT`/`beside_default` (taints `--output`), per-member/aggregate size caps (`MAX_MEMBER_BYTES=64MiB`, `MAX_ARCHIVE_BYTES=512MiB`) in `validate_component_archive`.
- `fixtures/rust-runtime/m6/build.sh` — off-PATH self-check now loops over all 6 `PATH` dirs + `/opt/rust/bin`; glob loop now asserts `test -e` before each `ln -s`, and asserts afterward that a `librustc_driver-*.so` symlink exists.
- `fixtures/rust-runtime/m6/test_provision.py` — added discriminating tests for hardlink/chr/blk/fifo/duplicate-member/backslash-under-correct-root, both size-bound tests (via constant monkeypatch), the output-taint traversal test, and a `validate_context` stray-directory test.
- `scripts/build-m6-runtime.py` — renamed `NEW_COMPONENTS` → `ANALYZER_BINARIES`.
- `scripts/test-m6-provisioning.py` — updated to reference `ANALYZER_BINARIES`.
- `docs/adr/ADR-082-m6-runtime-provisioning.md` — reworded to say the manifest sha256 constant *equals* `fixtures/rust-runtime/sources.json`'s value (cross-check by the reader), not that the script reads it from there.
- `docs/ci.md` — corrected core/full stage counts to 25/40 and added `m5-helper-guest-clippy` to the enumerated core list (only those numbers touched, per instruction).
- `sonar-project.properties` — added `scripts/build-m6-runtime.py` to `sonar.coverage.exclusions`, next to `scripts/build-m5-runtime.py`.
- `docs/validation/M6/provisioning.json` — regenerated by the rebuild.

**Tests executed** (all passed):
```
python3 -B -m unittest fixtures/rust-runtime/m6/test_provision.py   # 28 tests, OK
python3 -B scripts/test-m6-provisioning.py                          # 16 tests, OK
python3 -B scripts/test-gate-reporting.py                           # 13 tests, OK
python3 -B scripts/build-m6-runtime.py                              # {"status": "passed"}
python3 -B scripts/docs-hygiene.py links-check                      # 0 broken in living documents
python3 -B scripts/docs-hygiene.py verify-inventories               # 7 inventories, 0 failures
```

**Evidence**
- New image id: `sha256:f39a5b33ee7d54243664162eb635f8ec223d512042beb7cd18ecf071046b310c` (was `sha256:64b2e614…`).
- `rust_analyzer_version`: `"rust-analyzer 1.98.1 (48a229c 2026-09-01)"`.
- `status: "passed"`, `context_residue: "clean"`, both inputs' sha256 verified (rust-analyzer `a0fd960a…`, rust-src `5c846ebc…`).
- Receipt at `docs/validation/M6/provisioning.json`.

**Risks** — Verified by pure calculation (not just eyeballing) that `provision.py`'s new `OUTPUT_DEFAULT` and `build-m6-runtime.py`'s `CONTEXT_DEFAULT` share the same parent directory (`target/`), so `beside_default` composed on both sides resolves to the identical `context_root` for both the default and any custom `--context`/`--output` value — the round-trip is confirmed by the successful build, not just by construction.

**Decisions**
- Size-cap tests use constant monkeypatching (`provision.MAX_MEMBER_BYTES`/`MAX_ARCHIVE_BYTES` temporarily lowered, restored in `finally`) rather than building multi-hundred-MB fixture archives, since the goal is discriminating coverage of the `raise`, not exercising the real limits.
- Left the pre-existing "14 etapas nativas" undercount in `docs/ci.md` (actually 15, missing `m5-runtime`) untouched — out of scope per the instruction to touch only the numbers named in item 8.

**Open issues** — None. Package is ready for the next delegation step (integration/commit).

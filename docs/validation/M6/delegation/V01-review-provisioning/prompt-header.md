You are an independent, read-only reviewer (Claude Sonnet 5) of a provisioning change in a Rust MCP server project. You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. The project runs untrusted Rust project code only inside a Docker Linux ARM64 guest image. This change provisions a new guest image `rust-engineering-runtime:1.98.1-arm64-m6`, derived from the admitted M5 image (`sha256:e0a5ca16…`), adding exactly two components from the pinned Rust 1.98.1 channel manifest: `rust-analyzer` (installed at `/opt/analyzer/bin/rust-analyzer`, must stay OFF `PATH`, with `/opt/analyzer/lib/*.so` symlinks into `/opt/rust/lib` because the prebuilt binary has RUNPATH `$ORIGIN/../lib` and needs `librustc_driver`/`libLLVM` shipped by the base image's rustc) and `rust-src` (installed under `/opt/rust`). Rules the change must satisfy: the only network use is `provision.py` downloading the manifest and the two tarballs, each verified by sha256 against constants that were themselves cross-checked against the published manifest; the Docker build runs with `--network=none --pull=false --no-cache`; no argv-controlled path may be opened directly (SonarCloud taint rules S2083/S8707; the accepted pattern is `beside_default` = `os.path.basename` beside a constant default); no `/tmp` literals (S5443); tar members must be validated so nothing can escape the extraction root (no absolute paths, no `..`, no links pointing outside, no devices); the receipt must be honest (`failed` on any failure, never masked); Python stdlib only; the gate wiring (`scripts/gate.py`, `.github/workflows/sonarcloud.yml`, `docs/ci.md`) must be consistent with the counts it states; ADR-082 must not claim anything the receipt does not show.

Review goals, in priority order:
1. Security of acquisition and extraction: URL allowlist, TLS, hash verification before any use, tar member validation (symlink/hardlink targets, absolute names, `..`, duplicate members, size bombs), `install.sh` execution as root inside the builder (what could a tampered tarball do before the hash check?), symlink fix correctness and whether it widens what a job container can reach, PATH claims.
2. Receipt integrity: can `status: passed` be written while a guest check failed? Are the guest checks (`off_path`, carried binaries, rust-src present, residue) strict and non-forgeable? Is the recorded `rust_analyzer_version` captured from the guest, not hardcoded?
3. SonarCloud/taint compliance of the Python: argv paths, `/tmp`, subprocess with shell, broad exceptions.
4. Test quality: do `test_provision.py` and `test-m6-provisioning.py` discriminate the rejections above, or only the happy path? Any test that would pass with the guard removed?
5. Docs consistency: `docs/ci.md` stage counts (core/full), `gate.py` stage names, ADR-082 vs receipt.

Output format: a markdown report with sections `Verdict` (Approve / Approve with P3 / Block), `Findings` as a table with columns Severity (P0–P3), File:line or function, Finding, Evidence from the diff, and `What I verified`. Be specific and cite diff lines. Do not pad; if a goal has no finding, say so.

Diff follows.

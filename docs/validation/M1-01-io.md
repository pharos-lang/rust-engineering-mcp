# M1-01 prerequisite — bounded gateway stdin

Branch ai/m1-01-gateway-io from clean main50720a4. Supervisor alone owns spawning.
run_with_input admits at most24MiB before spawn; synchronous nonblocking8KiB
steps keep cancellation/deadline/output checks active while two bounded readers
drain. EOF closes stdin; incomplete input with child exit fails Infrastructure.
Timeout/cancel/output stop preserves its termination reason. No writer thread or
input clone. This does not certify Docker cleanup or hostile project containment.

Principal reviewed the diff and nine discriminating tests: short/interrupted/
blocked writes, EOF, pre-spawn size rejection, real cat pipes, sleep cancellation
and timeout, early true exit and concurrent overflow. Only fixed trusted utilities
run on the host; no project fixture or shell. The existing macOS-only nonblocking
support remains unchanged. Readers are joined through the existing guard.

Core final gate passed10/10, 210 tests including doctest; Rust/Cargo1.98.1,
CARGO_INCREMENTAL=0 and locked/offline. [Report](artifacts/M1-01-io-core.json).
The first run saw a protocol harness trailing-stdout EOF timeout in the batch
fixture under parallel activity. Isolated reproduction passed, and the full core
rerun passed. No code was changed or assertion weakened for the retry. This
intermittent test observation is retained; no diagnosis or universal stability
claim is inferred. Docker security tests remain excluded from this core gate.
Actual input transfer/cleanup in Docker and external security review are part of
the separate Rust gateway integration. M1-01 remains In progress.

# M1-09 — single-capture quality gate

2026-09-04; branch ai/m1-09-quality-gate from clean main897268c. ADR-040 precedes
code. Rust/Cargo1.98.1; CARGO_INCREMENTAL=0; no remote actions or provisioning.

Implementation: tenth operative tool, rust.quality.gate. Required profile fast or
standard; one project capture; closed fmt-all/check-default/Clippy-Strict/test30s/
audit selections. Direct ports, no nested MCP requests or repeated capture. Source
fingerprints agree across stages/metadata; ordinary failures retain subsequent stages.
Global240s joined worker; current per-command budgets and whole-tree cleanup remain.

Grouped publication reuses artifact owner/bytes/metadata/retention checks without
per-log project touch. Final live identity and common retention instant precede one
lease renewal. Failure rolls back new IDs; earlier live logs remain. Quota marks
omitted streams and conservative gate status without bypassing authorization.
Audit normalization is shared pure domain logic, reassessed at final publication.
Typed MCP repair details and selections preserve3/5stage rows; bounded truncation
retains runtime/log references and counts findings/paths/diagnostics/diffs removed.

Validation before integral full:
- core PASS498tests including doctest /10stages (M1-09-core.json). First attempt
  hit a mechanical Clippy collapsible-if during final adapter edit; corrected and
  rerun complete. No suppressed warning or gate relaxation.
- actual Docker MCP3tests /8cases /79.15s (M1-09-runtime.json). Fast pass + format
  and strict Clippy failures with successful check retained; standard pass + real
  test failure with audit afterward + unavailable snapshot.21distinct stage logs
  SHA verified, earlier logs reread, source write denied inside sandbox, source tree
  unchanged. Cancellation and EOF observed actual active libtest binaries; joined
  cleanup and worker reuse verified. No hostile fixture executed on host.
- application8/domain3new tests: exact one source bundle pointer/content, fixed
  profiles/order, operational continuation vs infrastructure/cancel abort, mismatch
  fingerprints, capture/read failure rollback, expired first log during later group
  validation, all removals attempted despite rollback error, quota/identity/TTL/
  cancellation final denial, stale audit at publication, status precedence.
- MCP12tests + five-wire protocol checks: typed schemas, closed profiles, exact
  stages/source identity/log ownership, stale/incomplete observations never passed,
  aggregate payload trimming and artifact references. Initial pathological2MB trim
  fixture took38s; bounded block removal reduced the complete focal suite to2.22s,
  preserving omission counters, including paths from subsequently removed findings.

Runtime configuration remains M1-08:
sha256:487cef3409e76e7d9c37afad9454e565023a8f1c9cf7581bed76c6f8b143f62e.
Actual approved image unchanged; M1-09 changed orchestration/publication, not child
execution policy. M1-09-assets.json verifies original five E5 hashes/revision and
ORT native SHA before full; no installs/refreshes/substitutions.

Scope: M1-01..09 only. M1-10..17, signed catalog import, durable antirollback,
distribution licensing, native Linux/Windows/x86_64, third-party MCP clients and
utility benchmark are not Done. ArtifactStore remains ephemeral; Resources remain
opaque/live/retained. SQLite/FTS5 authoritative; E5/LanceDB derived. paste1.0.15
maintenance warning remains documented. M0-12 evidence remains historical.

External Opus5 High review and critical principal disposition are recorded in
[M1-09-review.md](M1-09-review.md) and [M1-09-review-disposition.md](M1-09-review-disposition.md).
Post-review regressions add cross-stage runtime consistency (audit included),
command-specific fingerprints allowed, capture age through publication, committed
result versus late signal, and diagnostic count limit even below the byte budget.
Core rerun after these changes:498tests/10stages passed.

## Integral current gate

`CARGO_INCREMENTAL=0 python3 scripts/gate.py full --report target/M1-09-full.json`
with explicit socket/E5/ORT paths from M1-09-assets.json: PASS14/14 stages,
753.149s summed stage time. Rust/Cargo1.98.1, offline/locked; no provisioning.
- Core498 tests including doctest and ten gates passed again in full.
- M0 Docker4/4 tests, scoped to probes; Rust20 exact tests executed serially.
- Current post-review quality actual8cases passed within those20; source read-only,
  validation failures retained,18 stage-log hashes plus3 warmup logs, active test
  cancellation/EOF with joined cleanup and previous log group preserved.
- RustSec/SQLite actual macOS network-denied test1/1, positive TCP/UDP IPv4/IPv6
  controls, no runtime temp writes; this is the audit test process, not entire MCP.
- Workspace check and Clippy with all features/all targets passed. Semantic7 ordinary
  tests and1 actual E5/LanceDB/SQLite roundtrip under macOS network deny passed;
  rebuild/mismatch fallback verified. Ordinary semantic tests overlap core counts.
  Actual model roundtrip5.58s; no utility benchmark or native cross-platform claim.

Tracked evidence: [full report](M1-09-full.json), [summary/proof](M1-09-full-summary.json),
[18 runtime receipts and20 log hashes](M1-09-full-runtime.json),
[source hashes](M1-09-source-receipt.json), [original assets](M1-09-assets.json).
All178 source/script/manifest hashes match before and after full; documentation
is excluded from that receipt so results can be recorded. Final log remains local
at target/M1-09-full.log. Current config fingerprint remains487cef3409e76e7d…;
full receipts contain the complete identity. Dependency warnings retained: paste
maintenance and LanceDB unused resolve_arrow_field_path; no suppression or gate relaxation.

[Focused Opus5 Medium follow-up](M1-09-review-followup.md) confirms corrections,
with no confirmed P0/P1 in its reviewed slice. Remaining non-blocking notes are
critically disposed in the principal record. Code, contracts, adverse cases and
current integral evidence reviewed; M1-09 validated. Integration is recorded below.

## Local integration

Feature commit983e5ad; no-ff mergecc04f0c into clean main. Branch
ai/m1-09-quality-gate retained. Post-merge actual standard-profile smoke passed:
1 exact test,3 cases,39.58s,12 log hashes, source unchanged and cleanup verified.
The source tree matches all178 hashes from the full gate. [Receipt](M1-09-postmerge.json).
This integration record is committed separately on ai/m1-09-integration-evidence
from clean main; it changes only documentation, not the tested code or assets.
M1-01..09 are integrated; M1-10..17 remain pending, without remote actions or release.

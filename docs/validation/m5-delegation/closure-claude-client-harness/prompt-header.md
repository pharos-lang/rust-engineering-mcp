You are an independent, read-only reviewer of a change to a qualification harness in a Rust MCP server project. You cannot run anything; review only the diff below. Do not propose implementation; report findings.

Context. `scripts/test-m5-clients.py` qualifies four MCP tools against two stock MCP clients. Previously the second client was the Codex app-server (scripted tool calls plus one model-directed turn). The project owner decided to stop using Codex for this closure and to use stock Claude Code (`claude -p`, version 2.1.267, model claude-sonnet-5) as the agentic client instead. The MCP Inspector remains the deterministic client that converts every planned row and reads every Resource. The Claude client now runs two model-directed flows: (a) Docker-free: open the planned fixture roots and call each of the four tools once with the planned arguments, each answered by a declared refusal; (b) runtime: open the benchmark root, discover Resources, run two real benchmark measurements (run_count 1), compare them (expected verdict inconclusive with reason insufficient_executions), request a comparison against one of its own non-dataset artifacts (expected declared NOT_A_DATASET refusal), and read that artifact as a Resource natively. Artifacts are owner-bound to a per-process project reference, so the Claude session must produce its own datasets; this was verified empirically. Observed facts from a real probe: Claude relays MCP tool results as the server's JSON text with `is_error` mirroring the server's `isError`; tool names are `mcp__rust_engineering__rust_benchmark_compare` (dots become underscores); `ListMcpResourcesTool`/`ReadMcpResourceTool` are built-ins whose structured outcome appears in the `user` event's `tool_use_result`; the `system/init` event carries `model`, `claude_code_version`, `mcp_servers`, `tools`; the `result` event carries `subtype`, `is_error`, `permission_denials`, `modelUsage`.

Review goals, in priority order:
1. Security: credential handling (nothing may be copied; login is read in place), environment hygiene, permission restriction of the client (restricted mode, strict MCP config, allowed tools), evidence staging (nothing credential-shaped under the attempt directory), and any path leakage that would matter.
2. Oracle strictness: can the model-directed flow be reported as passed without the model actually performing every required action with the exact session-issued identifiers? Look for forgeable shapes, missing exactly-once checks, ordering gaps, mis-normalized events, or acceptance of a fallback model.
3. Evidence integrity: receipt fields, digests, and binding to the plan rows; anything that overstates what was verified.
4. Correctness bugs in Python (exceptions on legitimate shapes, wrong dict comparisons, dead code) and test gaps.

Output format: a markdown report with sections `Verdict` (Approve / Approve with P3 / Block), `Findings` as a table with columns Severity (P0–P3), File:line or function, Finding, Evidence from the diff, and `What I verified` listing the concrete properties you checked. Be specific and cite diff lines. Do not pad; if a goal has no finding, say so.

Diff follows.

# M1-12 — bounded crate search

2026-09-04; `ai/m1-12-crate-search` from clean main8ce95be.
ADR043 precedes implementation. Adds tool12, `rust.crate.search`, preserving
all eleven prior tool definition snapshots and all five supported wire versions.

Lexical retrieval uses explicit FTS5 BM25; semantic retrieval uses actual E5 and
native Lance squared L2; hybrid combines ordinal ranks with RRF60. Each channel
has at most50 candidates. SQLite alone selects the highest eligible known version
under yanked/prerelease/MSRV filters, before final result limiting. Stable summary
is independent of filters. Scores describe retrieval, not quality or safety;
advisory IDs are snapshot-listed facts, not a complete vulnerability audit.

Missing, disabled, invalid or failing semantics yields explicit lexical fallback
with identical filters. Cancellation/deadline never becomes successful fallback.
Status and search share one authenticated session generation and one joined worker;
validation, complete MCP encoding and budget trimming remain inside that worker.
The512KiB budget counts both text and structured content; whole lowest-ranked items
are omitted with consistent counts. Deadline120s is cooperative, not hard native
preemption or RSS enforcement. No new network, filesystem or project authority.

## Evidence

[Core](M1-12-core.json):603 tests including doctests,10/10 stages,35 protocol tests.
[Summary](M1-12-summary.json): all-features/all-targets workspace Clippy passed;
`scripts/test-crate-search.py` passed2 ordinary tests and explicitly executed the
single ignored native test. Actual E5/native Lance CLI rebuild and MCP search run
under macOS network deny, with positive IPv4/IPv6 controls outside and denied inside.
Queries exercise English and Spanish without claiming retrieval utility. Native
corruption and absent model fall back with SQLite filters; status/search retain
identical catalog/model/index identities. [Transcript](M1-12-native.txt),
[rehashed assets](M1-12-assets.json), [source hashes](M1-12-source-receipt.json).

New evidence includes2 domain,11 application,8 real SQLite,6 MCP contract/budget,
2 protocol and2 ordinary CLI/MCP tests; those31 additions are included in603.
Unknown semantic IDs, invalid distances/duplicates, cancellation, version selection,
MSRV numeric boundaries, literal FTS queries and full-result truncation are covered.
[Sonnet5 Medium review](M1-12-review-sonnet.md),
[Principal disposition](M1-12-review-disposition.md). Initial Clippy warning fixed;
one initial closed-stdout harness failure did not reproduce in6 isolated runs or
final core. Its unproven scheduling hypothesis is recorded, not suppressed by
production timeout changes.

Full now has17 stages. The unchanged Docker stages were not repeated for this
read-only retrieval vertical; the final M1 candidate still requires the full gate.
M1-13..17, native platform/client qualification, distribution approvals and the
actual utility experiment remain outstanding. M1 is not closed.

Feature d7bc538, local no-ff merge26a0a3c; branch retained. Post-merge smoke2
CLI/MCP search tests and35 protocol tests passed on clean main;283 source hashes
verified. [Receipt](M1-12-postmerge.json).

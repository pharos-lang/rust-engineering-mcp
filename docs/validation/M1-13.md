# M1-13 — paged authoritative crate inspection

2026-09-04; ai/m1-13-crate-inspect from clean main57a4a36, after M1-12.
ADR044 precedes implementation. `rust.crate.inspect` is tool13; all twelve earlier
tool definition snapshots remain unchanged. No M1 milestone closure is inferred.

Overview and versions expose SQLite scalar facts, independent latest_known_stable
and yanked/MSRV/license/time data. Features, dependencies and advisories require an
exact version and expose only recorded schema1 fields. Documentation and package
source URLs remain explicit unknown/not_recorded_in_snapshot; catalog provenance
is not package-source evidence. Missing crate, missing version and empty collection
are distinct. No retrieval score or empty advisory list is promoted to quality/safety.

Pages use deterministic order,1..50 items and bounded offset, with exact snapshot
fingerprint required after offset0. A retained session survives an import coherently;
a new generation rejects the old fingerprint before facts are queried. Pagination
reports total/offset/returned/next_offset; whole-entry byte trimming preserves a
nonempty prefix and advances only over emitted facts. Overview is indivisible.
The entire MCP result, including text and structured data, is capped512KiB.

SQLite queries hydrate at most65 scalar versions and bounded collection pages,
with129-row sentinels for128-entry invariants, rather than a full version graph.
The existing shared CatalogProvider and joined worker own the complete operation,
including JSON validation/encoding. Deadline120s remains cooperative, not native
preemption. Model/index availability is not required; runtime performs no acquisition.

## Evidence

[Core](M1-13-core.json):629 tests including doctests,10/10 stages;37 protocol tests
across all five versions. [Summary](M1-13-summary.json): all-features/all-targets
workspace Clippy passed. New tests:2 domain,8 application,6 SQLite including actual
invalid-staging sentinels,6 MCP unit,2 protocol and2 actual CLI/MCP integration.
These26 additions are included in629.

`scripts/test-crate-inspect.py` executes the two actual local-feature CLI/MCP tests
under macOS network deny. Positive IPv4/IPv6 controls pass outside and fail inside.
No embedding inference is required or claimed for this gate. It reconstructs all
collection pages, checks unknown fields, stable/yanked and exact-version facts,
retained/restarted generation, stale provenance, all lookup outcomes and invalid
page inputs. [Transcript](M1-13-native.txt), [source hashes](M1-13-source-receipt.json).
Unit cases validate closed schemas/required nullable fields, maximal valid pages,
prefix trimming without skipping and cancellation. Twelve old schemas are exact.

[Sonnet5 Medium review](M1-13-review-sonnet.md) found one conditional P2 whose
premise is excluded by the existing SQLite primary key and import validator.
[Principal disposition](M1-13-review-disposition.md) records that evidence, the
initial test-only status-field correction and no remaining confirmed defect.

Full now has18 stages; unchanged Docker and native embedding/search security
stages were not repeated for this bounded read-only vertical. They remain required
on the final M1 candidate. CLI/doctor, distribution/license/client/platform
qualification and the actual utility experiment remain outstanding.

Feature08e41f3, local no-ff merge392a8f2; branch retained. Clean-main smoke2
CLI/MCP inspection and37 protocol tests passed;296 source hashes verified.
[Receipt](M1-13-postmerge.json).

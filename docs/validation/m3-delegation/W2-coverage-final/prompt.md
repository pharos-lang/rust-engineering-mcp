# Package W2 — Amend ADR-065, get the complete M3 runtime gate green, and calibrate coverage for real (integrator Sol, resumed; sole Docker owner)

The validator finished the previous window. Read its report first: `docs/validation/m3-delegation/W1-runtime-gate/last-message.md`. Summary of where you are:
- M3 runtime gate: 47 passed of 48 executed, 7 not executed (fail-fast). nextest 19/19, semver 18/18, mutation 10/10, coverage 0/8. Receipt `docs/validation/M3-runtime-attempt7.json`; `M3-runtime.json` was correctly left untouched because no 55/55 run exists yet.
- Rust security gate: 20/20 on the ADR-065 bytes (`docs/validation/M3-rust-security.json`, previous receipt preserved as `M3-rust-security-pre-adr065.json`).
- Your ADR-065 executable target volume **works**: the instrumented build compiled and executed the test inside `/work/coverage-target`. The original blocker is solved.
- The remaining coverage failure is a defect in ADR-065 itself: all three report phases die with `failed to merge profile data: failed to create file /work/coverage-target/source-profraw-list: Read-only file system`. `cargo-llvm-cov` 0.9.0 writes `<crate>-profraw-list` and `<crate>.profdata` into `CARGO_LLVM_COV_TARGET_DIR` before exporting, and the pinned `report` subcommand has no option to redirect that merge.
- The validator also hardened `scripts/test-m3-runtime.py` with a per-selection step timeout (`RUST_MCP_M3_STEP_TIMEOUT_S`, default 900 s) that kills the process group and records `timed_out`/`-9`, so an unattended stall can no longer leave the receipt in `running`. Keep that behaviour.
- The `catalog_sync` tests that failed under the Codex sandbox pass outside it; treat that as a sandbox artifact, not a defect, and say so in your report.

## Orchestrator decision (authorized)
Amend ADR-065's access matrix: `/work/coverage-target` is **read-write in the three `CoverageReport` phases** as well as in `CoverageRun`. Everything else in that ADR stands unchanged — the volume stays per-job, bounded (`size=512m,nr_inodes=65536,uid=65534,gid=65534,mode=0700,nosuid,nodev`), absent from the keeper's export phase and from every non-coverage phase, and removed by the shared cleanup with quarantine on uncertainty. No other mount, seccomp profile, network mode, user, capability or resource cap changes. Rationale to record in the ADR: the merge step of the pinned plugin must materialise its profraw list and merged profdata inside the target directory it was given; that directory already holds this job's build output, is unreachable from the host, and is destroyed at cleanup, so the write adds no reachable surface. Update the ADR's Consequences and its negative controls, and keep its Status as Proposed with the amendment dated 2026-09-06.

## Work
1. Apply the amendment: the ADR text, the one-line phase change, the applied-mount verifier assertions (report phases writable; export/keeper and every non-coverage phase unchanged; no other volume gains write or exec), and the configuration fingerprint if the shape changes.
2. Negative controls as real tests, extending what already exists: a binary planted on the source volume still cannot be executed; the artifact/report volume stays non-executable; the coverage volume is absent from every non-coverage phase and gone after cleanup; network still denied in the coverage phases.
3. Run the **complete** M3 runtime gate (55 selections) serially in a fresh output directory until green. Preserve each failed attempt as `docs/validation/M3-runtime-attemptN.json` (highest existing is 7) and write the passing run to `docs/validation/M3-runtime.json`.
4. Real coverage calibration once the report phases produce output: record the llvm-cov identity, the JSON/LCOV/HTML actually produced from one profdata, and the observed line/region/function counts for the known-counts fixture. The validator recorded a hand derivation as an unasserted hypothesis in `docs/validation/M3-03.md` (functions 2/2; regions ≥3 with ≥1 uncovered; lines 100%). Compare it with the observed values: if they agree, turn them into hard assertions in the test and in `fixtures/coverage/README.md`; if they disagree, investigate the fixture and the parser before touching any oracle and explain the discrepancy. Also confirm the zero-denominator rule and the shared-file dedupe on their fixtures.
5. Update `docs/validation/M3-03.md`, the `docs/validation/M3-matrix.md` row, `docs/tools.md`'s `rust.coverage` caveat (it must state what is then true), and the measured parts of ADR-062's Open issues.
6. Re-run the Rust security gate only if you changed shared execution paths; if you do, preserve the current receipt as history.
7. Docker hygiene after each gate: zero containers and zero volumes labelled `org.rust-mcp.execution=true`, with the command output in your report.
8. Final workspace gates: `cargo fmt --check`, `cargo check/clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline` (report the `catalog_sync` sandbox artifact honestly), `python3 -B scripts/check-architecture.py`.

## Rules
Branch `ai/m3-quality`; no commits, merge, push, installs, downloads, image build/pull/delete; sole Docker owner; never run two gates at once and never leave one running unattended. Do not touch the M3-02 Tasks files (`crates/mcp-server/src/stdio/tasks.rs`, `crates/domain/src/job.rs`, `scripts/test-m3-clients.py`, `scripts/test-m3-budgets.py`, `docs/validation/M3-02.md`) — the next window qualifies those. Keep all 23 tool snapshots byte-identical. Never weaken an assertion, credit a skip as a pass, or widen containment beyond the amendment above.
Delivery: Task, Result (gate counts per cut), Files changed with SHA-256, Tests executed (command, exit code, counts, duration, receipt path), Calibration table with observed versus derived numbers, Docker hygiene evidence, Risks, Decisions, Open issues.

# Package Q01 — Single Docker qualification window for M3-03/04/05 plus calibration (integrator Sol, resumed; sole Docker owner)

All four M3 verticals are implemented and the workspace is green without Docker: `rust.test.nextest` (tool 19, already Docker-qualified 19/19 in I02b), `rust.coverage` (20, Terra I03/I03b — its Docker attempt was blocked by a concurrent edit and the failed attempt is preserved in `docs/validation/M3-03-runtime-attempt1.json`), `rust.semver.check` (21, yours in I04c — 18 Docker selections wired but never executed), `rust.mutation.test` (22, Claude Opus I05 — 10 `#[ignore]`d Docker tests written; read `docs/validation/m3-delegation/I05-mutation/last-message.md` and `docs/validation/M3-05-mutation-calibration.md`, note its three documented deviations: `--baseline run`, container tmpfs scratch for the mutated copy with the named volume only for reports, and a `--list --json` pre-pass to enforce `max_mutants`).

Objective: qualify coverage, semver and mutation against the real pinned plugins in the approved guest image, calibrate every exit code and parser golden that is still a hypothesis, and leave one coherent set of receipts.

## Work
1. Pre-flight: `cargo fmt --check`, `cargo check/clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline`. Investigate and fix the durable-store expiry test the previous run called independently flaky ONLY if a Claude worker has not already fixed it (check `git diff`/file mtimes on `crates/project-adapter/tests/quality_artifact_store.rs`; if it is being worked on, leave it and say so).
2. Make `scripts/test-m3-runtime.py` cover all four verticals: confirm nextest (19) and coverage selections are present, semver's 18 are present, and add the mutation selections (exact `--exact --ignored` names from `crates/execution-adapter/tests/mutation_runtime.rs` and any MCP-level mutation runtime tests). Keep its input-hashing/receipt discipline and the one-passing-test-per-selection rule. Do not weaken any assertion to make something pass.
3. Run the complete M3 runtime Docker gate serially in a fresh output directory. If a selection fails, diagnose: a real product defect must be fixed (you own integration); a fixture/oracle defect must be corrected honestly; a containment requirement that cannot be met without widening the sandbox must be escalated to the orchestrator with the exact syscall/mount evidence — never widen it yourself beyond ADR-064's authorized shape.
4. Calibration (record observed values, then pin them in code and docs):
   - nextest exit codes actually observed (0 / failures / build error / timeout-cancel; exit 104 was left uncalibrated in M3-01).
   - `cargo-semver-checks` 0.50.0: exit codes for identical, breaking, warn-only, no-lib, broken baseline, and the registry-needing case under network deny; the real non-colored output shape for the parser goldens (replace the provisional goldens with captured ones); and the ADR-062 open question of where rustdoc JSON/build output is written for `--baseline-root` (does `CARGO_TARGET_DIR=/work/target` cover both roots?). Update `docs/validation/M3-04-semver-calibration.md` and the measured parts of ADR-062 §9/§10/§11 and its Open issues.
   - `cargo-mutants` 27.1.0: exit codes 0/1/2/3/4 as observed on the fixtures, the `mutants.out` layout actually produced, and `lock.json` identity values inside the guest. Update `docs/validation/M3-05-mutation-calibration.md`.
5. Receipts: `docs/validation/M3-03-runtime.json`, `M3-04-runtime.json`, `M3-05-runtime.json` (or one consolidated `M3-runtime.json` plus per-cut narratives — your choice, state it), the narratives `docs/validation/M3-03.md`, `M3-04.md`, `M3-05.md` with commands, exit codes, counts, image ID, timings and limitations, and matrix rows updated in `docs/validation/M3-matrix.md`. Preserve every failed attempt (`-attemptN.json`), never rewrite an existing receipt's bytes.
6. Re-run the Rust security Docker gate (`scripts/test-rust-execution.py`, 20 selections) because the shared execution paths changed since I02b, and record its receipt as `docs/validation/M3-rust-security.json` (keep `M3-01-rust-security.json` as history).
7. Docker hygiene: after every gate, assert zero containers and zero volumes labelled `org.rust-mcp.execution=true`; report the check.

## Rules
Branch `ai/m3-quality`, no commits/merge/push/installs/downloads/image build-pull-delete. **Sole Docker owner** — a Claude worker may be editing `crates/project-adapter/tests/quality_artifact_store.rs` concurrently; do not edit quality-store files. Never weaken containment, never relax ADR-030/ADR-060 semantics, never mark a skip as a pass. Preserve all 22 tool snapshots byte-identical except where a calibration legitimately changes a schema (then say so loudly and update the snapshot deliberately).
Delivery: Task, Result (per cut: qualified / blocked, with the reason), Files changed (+SHA-256), Tests executed (each gate: command, exit code, counts, duration, receipt path), Calibration table (tool → observed exit codes/goldens → where pinned), Evidence (oracle → test name), Risks, Decisions, Open issues.

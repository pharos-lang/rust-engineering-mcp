# Package I02 — Integrate the M3-01 vertical: image switch + recalibration, nextest tool wiring, Docker runtime qualification (integrator, with Docker access)

Task / ID: I02 (M3-01 integration). Objective: make `rust.test.nextest` a real, observable vertical on the new guest image: switch the approved image identity, recalibrate the gateway, extend fingerprints, raise the execution ceiling for jobs per ADR-060, register the tool (18 → 19 tools) with its contract snapshot and protocol tests, run the Docker nextest runtime tests and the existing Rust security gate under the new image, and record receipts. Tasks capability advertisement stays OFF until M3-02 (ADR-060 gating); synchronous mode only in this package.
Model / CLI / effort: GPT-5.6 Sol via codex exec, high, workspace-write + `sandbox_workspace_write.network_access=true` (needed only for the Docker unix socket; no network fetches are allowed).
## Common rules
Repo /Users/cburgosro/Projects/rust-mcp, branch ai/m3-quality. Orchestrator Claude Fable 5.1. No commits/merge/push/installs/downloads; no image pulls/builds/deletes. Concurrent workers: Claude Opus 5 (I17c) is editing the quality artifact store files (domain/application `quality_artifact.rs`, project-adapter `quality_artifact_store.rs`, `filesystem/macos/{quality,state_primitives}.rs`, `stdio/resources.rs`) — do NOT edit those; if you need their API, read it and adapt on your side, or report. Everything else in crates/ is now yours as integrator, including files created by I00 (yours), I01 (Sonnet: domain/nextest.rs, execution-adapter nextest_*.rs, rust_gateway.rs, project_inspection.rs) — you may modify I01's files to integrate, but keep a diff summary per file in your report. Read I01's delivery first: docs/validation/m3-delegation/I01-nextest-adapter/last-message.md (7 open issues) and the ADR-060/061/062/063.
## New image (P02, verified 47/47)
`sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a` — docs/validation/M3-image-config.json, docs/validation/M3-provisioning.json, raw under docs/validation/m3-provisioning/ (including verbatim `--help` of all plugins). Env inside image: PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin; tools at /opt/rust/bin/{cargo,rustc,cargo-nextest,cargo-llvm-cov,cargo-semver-checks,cargo-mutants}; llvm-tools under the sysroot (`/opt/rust/lib/rustlib/aarch64-unknown-linux-gnu/bin/{llvm-cov,llvm-profdata}`). Docker: `/Applications/Docker.app/Contents/Resources/bin/docker`, socket `unix:///Users/cburgosro/.docker/run/docker.sock`; `RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock`. Do not run more than one Docker gate at a time (you are the sole Docker owner in this window; I17c never uses Docker).
## Work items
1. Image identity: update `APPROVED_RUST_IMAGE` (and any digest/label constants, `docs`-independent) in crates/execution-adapter to the new ID; extend `configuration_fingerprint()`/`implementation_fingerprint()` to enumerate the TestNextest phase and the new source files (I01 issue 3); keep the M2 image constant available only if a test needs the historical value (prefer none). Recalibration: the existing calibration path must pass against the new image — run `python3 -B scripts/test-rust-execution.py` (the 20 exact serial Docker tests; read scripts/test-rust-execution.py and docs/ci.md for the exact invocation and env) and record the receipt in a NEW directory (e.g. `target/m3-rust-security/`) — do not overwrite M2 receipts under docs/validation. If a historical M2 test hard-codes the M2 image ID, adapt the test to the constant, not the other way round; report every such change.
2. Execution ceiling: `ExecutionLimits::new` caps wall at 60 s (I01 issue 4). Implement the ADR-060 budgets for jobs: nextest job execute phase default 300 s / max 3,600 s, with the gateway/container limits derived from the job budget (keep M1 tools' 60 s cap unchanged for their commands). Cleanup budgets separate. Tests for the exceed behaviour.
3. Nextest observation gaps (I01 issue 5/6): retried/attempts/timed_out/leak derived from the JUnit sample of a real run (fixtures/nextest/{flaky,leaky,slow}); calibrate `NextestExit` codes against the real binary (run the fixtures: passing→0, failing→100?, build error→101?, timeout/cancel→104?) and pin them with the observed values; where the observation contradicts the hypothesis, fix the mapping and record the evidence.
4. Config injection & export (I01 decisions): evaluate the `/source/.rust-mcp-nextest/nextest.toml` + `tar --keep-old-files` injection and the `docker cp` JUnit export against the invariants (no guest-selected host paths; egress by bytes from a fixed guest path; regular file only; size cap; no follow of links inside the container path — `docker cp` follows symlinks in the container by default! Use `docker cp -L`? No: verify behaviour; prefer the M2-style export container running fixed `/usr/bin/cat` or closed `tar` on the fixed path with the volume mounted read-only, which is already the pattern in mutation_gateway.rs). Decide, implement the safe option, and write the negative test (a symlink planted at the JUnit path by the hostile-output fixture must be rejected, not followed).
5. Tool registration: add `rust.test.nextest` to get_tool/list_tools/call_tool in stdio.rs using I00's `stdio/nextest.rs` contract; create the snapshot `crates/mcp-server/tests/snapshots/nextest-tool.json`; extend protocol tests to 19 tools (ordered inventory, schema equality, structured/text mirror on the five versions); the 18 existing snapshots must stay byte-identical. `execution_mode` default `auto` → with no Tasks capability declared and the operation not qualified as short, the call must return the structured `TASKS_REQUIRED` remediation per ADR-060 — BUT for M3-01 the synchronous path must be usable by stock clients: define the qualified-short rule concretely (e.g., `timeout_seconds <= 60` and default profile) so a bounded nextest run of the passing fixture succeeds synchronously within the ADR-060 sync budget (60 s default / 120 s max, excluding cleanup); document the rule in the DTO description.
6. Artifacts: Stage 0 per ADR-061 — publish JUnit + stdout/stderr through the unchanged M1 `ArtifactStore` with `completeness: Truncated` and omission flags when >256 KiB; leave a clearly marked integration point (trait object / builder) for the Stage 1 durable store that Opus is completing (read crates/application/src/quality_artifact.rs to match names); do not wire Stage 1 yet.
7. Docker qualification: run `cargo test -p rust-engineering-execution --test nextest_runtime --locked --offline -- --ignored --test-threads=1` with `RUST_MCP_TEST_SOCKET` set (each ignored case individually with `--exact` as the gate does), plus MCP-level real cases for the new tool in `crates/mcp-server/tests/inspection_runtime/` (new module `nextest.rs`: positive (passing fixture counts), failing counts, ignored, flaky with retries=1, leaky, doc-only (doctests NotRun + zero tests), no-tests, hostile-output (bounded logs, forged markers not trusted), slow → timeout/cancel with observed active child and verified cleanup, EOF during run, source immutability, Resource read of JUnit/logs). Add these exact selections to a new script `scripts/test-m3-runtime.py` modelled on scripts/test-m2-runtime.py (hashing inputs, running/final receipt, exact one-passing-test-per-selection rule) and wire it into `scripts/gate.py` full as a new stage; run it and keep the receipt under a new dir; copy the final receipts into docs/validation/ as `M3-01-runtime.json`, `M3-01-rust-security.json` with the image ID and hashes.
8. Focused gates for your crates after integration: fmt check on your files, `cargo check/clippy --workspace --all-targets --locked --offline -- -D warnings` (report if Opus' in-progress files break it — do not edit them), `cargo test --workspace --locked --offline` (non-Docker) and `python3 -B scripts/check-architecture.py`.
9. Docs you own in this package: docs/adr/ADR-063 (fix the `llvm-tools-preview/` path segment per P02 and record the built image ID + the verification receipt), docs/adr/ADR-031 amendment section ("M3 image identity" with the new ID and calibration receipt), docs/validation/M3-matrix.md rows for M3-01/provisioning (Estado + Evidencia), docs/tools.md section for `rust.test.nextest` (contract, semantics, limits, Stage 0 artifacts, sync-only in this cut), docs/implementation-status.md In Progress note. README/CHANGELOG/SECURITY come later with the docs worker.
Delivery: Task, Result, Files changed (+SHA-256), Tests executed (each command, exit, counts, receipt paths), Evidence (per M3-01 oracle: which test), Risks, Decisions, Open issues. Escalate (do not decide alone): any change to ADR-030 semantics, any need to weaken containment, or a plugin behaviour contradicting ADR-062/063.

## Addendum from I00 delivery (read docs/validation/m3-delegation/I00-jobexecutor/last-message.md)
- Domain all-targets Clippy is red only because of I01-owned `crates/domain/src/nextest.rs` / `tests/nextest.rs` (`expect` uses, constant assertions): fix them as integrator.
- The integration must supply concrete clock, OS-random ID source, JobSignal, authority and executor construction (production instances) and connect I01 publication; register `rust.test.nextest`; Tasks advertisement stays gated until G4 (M3-02).
- Five existing `catalog_sync` tests failed in the Codex sandbox because local socket creation was denied; with network access enabled in this session, rerun `cargo test -p rust-engineering-mcp --locked --offline` completely and report.

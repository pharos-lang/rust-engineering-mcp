# Package W4 — Qualify M3-02: Docker task lifecycle, measured budgets, stock clients, advertisement decision (integrator Sol, resumed; sole Docker owner)

M3-01/03/04/05 are qualified: the complete 55-selection M3 runtime gate and the 20-selection Rust security gate are green on the current bytes, and the independent security review's dispositions have been applied. M3-02 is the last cut with no execution evidence. The independent traceability audit (`docs/validation/m3-delegation/R02-traceability/report.md`) lists exactly what is missing; close it.

## Work
1. **Docker task lifecycle** — implement and run the cases `docs/validation/M3-02.md` marks as pending because they need a real child: D06-T04 (ProjectRef revocation or policy change during execution hides the task, cancels it and prevents publication), T05 (cancel before start, during execution, during publication and during cleanup; `cancelled` never visible before observed joined cleanup), T08 (stdio EOF with an observed hostile child cancels, terminates, drains and joins before shutdown; uncertain cleanup fails the session), T10 (restart exposes no prior running job, resumes nothing, masks old identifiers and reconciles residual gateway objects before admitting). Use the product server over the real protocol harness with a peer that declares the Tasks extension, and a slow fixture (`fixtures/nextest/slow` or the mutation fixtures) as the long operation. Add these selections to `scripts/test-m3-runtime.py` so they live in the same receipt discipline, and re-run the complete gate afterwards.
2. **Measured budgets** — run `scripts/test-m3-budgets.py` for at least 30 cold and 30 warm iterations of each candidate synchronous operation on this host, image and gateway. Emit `docs/validation/M3-02-budgets.json` with raw samples plus p50/p95/p99, phase wall times, response bytes, job-record bytes, poll latency, cancellation-intent-to-observed-cleanup and EOF-to-join. Then **update ADR-060**: replace every proposed number with the measured decision or explicitly reaffirm it, in a "Measured budgets (M3-02)" subsection that cites the evidence file. If a measurement contradicts a proposed value, change the value, not the measurement.
3. **Stock clients (G4)** — run `scripts/test-m3-clients.py`. Qualify MCP Inspector 2.5.0 (discovery of the full ordered inventory with the four new tools, a positive call, a failure, a cancellation, and a Resource read including `rust-quality-artifact://`) and one model-directed stock client (the M2 precedent drives Claude Code restricted to MCP; `scripts/codex-model-qualifier.py` drives Codex app-server — pick whichever you can actually drive here and say which). Record per client whether it declares `io.modelcontextprotocol/tasks`. Preserve every failed attempt under `docs/validation/m3-clients/attempt-N/` and write `docs/validation/M3-02-clients.json`.
4. **Advertisement decision** — if a stock client declares the extension and the task flow passes end to end with it, flip `TASKS_ADVERTISEMENT_READY` and prove the five-version matrix again with the capability on. If no available stock client declares it, leave the flag off, qualify the Tasks path with the repository protocol harness, and document that limitation precisely in `docs/validation/M3-02.md`, `docs/compatibility.md` and `docs/tools.md` — an honest "implemented, not advertised, qualified only by our harness" is acceptable; a silent flip is not.
5. **Receipts and documents** — `docs/validation/M3-02.md` with the D06-T01..T14 table (test name → receipt → status, nothing pending left unexplained), `docs/validation/M3-02-runtime.json` if you keep a separate receipt, the M3-02 row of `docs/validation/M3-matrix.md`, and the Tasks sections of `docs/tools.md`, `docs/client-configuration.md`, `docs/security-model.md` and `docs/compatibility.md`.
6. **Re-run** the complete M3 runtime gate and the Rust security gate on the final bytes of this package; preserve failed attempts; refresh `docs/validation/M3-runtime.json` and `docs/validation/M3-rust-security.json`. Docker hygiene after each gate.
7. Workspace gates at the end: `cargo fmt --check`, `cargo check/clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline`, `python3 -B scripts/check-architecture.py`.

## Rules
Branch `ai/m3-quality`; no commits, merge, push, installs, downloads, image build/pull/delete; sole Docker owner; never leave a gate unattended. Do not weaken ADR-030 (16 slots, retention, 10 s bootstrap, 1 MiB frames) or ADR-060 semantics; `Cancelled` only after observed cleanup; poll and cancel never take the worker permit; masked `-32602`; fixed non-null TTL; no notification promises. Snapshots stay byte-identical unless flipping advertisement legitimately changes a definition, in which case say so and regenerate deliberately. A documentation worker may be editing README/CHANGELOG/SECURITY/architecture/implementation-status in parallel — leave those five files alone and tell me what they must say.
Delivery: Task, Result, the D06-T01..T14 table, the measured budget table (with the ADR-060 changes you made), the client matrix (client | version | declares Tasks | discovery | positive | failure | cancel | Resource | task flow), Files changed with SHA-256, Tests executed (command, exit code, counts, duration, receipt path), Docker hygiene evidence, Risks, Decisions, Open issues.

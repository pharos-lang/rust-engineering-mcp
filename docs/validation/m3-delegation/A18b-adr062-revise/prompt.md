# Package A18b — Revise ADR-062 per independent review (Claude Sonnet 5 worker, fresh session)

You are a Claude Sonnet 5 worker for the Rust Engineering MCP project (/Users/cburgosro/Projects/rust-mcp, branch ai/m3-quality). Task: revise docs/adr/ADR-062-coverage-accounting-and-semver-baselines.md (current SHA-256 d32f7917ad8075a69845eeb2cb4ac14860ed6d4077042b1cb04f3f7a9c358cf2) according to the independent review at docs/validation/m3-delegation/V18-adr062-review/last-message.md (SHA-256 4868d2d25bfe57748f31ad49de9084370c5d8929036880a89a21d1578c603ae4) and the orchestrator dispositions below. Edit ONLY that ADR file. Tools: Read/Grep/Glob and Write/Edit restricted to that file. Do not run commands other than `shasum`/`ls`. Keep Status "Proposed — revised after review V18; pending orchestrator acceptance".
Read first: the ADR, the review, docs/adr/ADR-060-bounded-job-execution-and-mcp-tasks.md and docs/adr/ADR-061-private-quality-artifact-store.md (ADR-061 is being revised concurrently; the joint decision below is authoritative), crates/execution-adapter/src/rust_gateway.rs (Phase/Volume/arguments), crates/execution-adapter/src/mutation_gateway.rs (multi-phase pattern), crates/execution-adapter/src/mutation_archive.rs (closed USTAR profile), the official sources under /private/tmp/claude-501/-Users-cburgosro-Projects-rust-mcp/df0fc04b-d468-46d9-b6f0-e6ac1541e3c8/scratchpad/orch/pkg-R01-plugins/sources/ (semver-readme.txt, llvmcov-readme.txt, obi1kenobi-cargo-semver-checks-Cargo.toml.txt).
Owner decisions since your draft: provisioning of cargo-llvm-cov 0.9.0, llvm-tools 1.98.1, cargo-semver-checks 0.50.0, cargo-nextest 0.9.143 and a source-built cargo-mutants 27.1.0 into a new guest image is authorized (a separate provisioning ADR-063 is being drafted by another worker); the persistent private artifact store (ADR-061) is accepted in posture.

Dispositions:
- F1 fix now: §11 becomes provisional. Decision tree: (1) at calibration, record `cargo semver-checks --help` and `check-release --help` of the pinned binary; (2) if a machine-readable findings output exists, adopt it and record the exact flag/schema; (3) if not, M3-04 publishes a coarse authoritative summary (exit-code-derived outcome + deny/warn counts) obtained from a bounded, golden-tested text parser over the tool's non-colored output (force no color via env/flag verified at calibration), spec §79 fallback rules; per-finding `item/lint/span` fields are then best-effort with `completeness: Partial` and the raw output retained as an artifact; a parser failure is `Incomplete`, never a false no-break. State this in Decision and Open issues.
- F2 fix now: record the gateway extension explicitly: a second read-only volume `/baseline` populated by a second ingest phase (new `Phase` variants / a `Volume` pair), multi-mount `arguments()`, configuration fingerprint change and recalibration; correct the Consequences sentence. The candidate stays at `/source`; the writable `/work` tmpfs remains the only writable location.
- F3 fix now (joint decision with ADR-061): multi-file HTML/mutants reports cross as ONE bounded archive blob produced by a fixed guest program (`tar` with closed argv over a fixed guest directory, M2 Export pattern), decoded/revalidated on the host with the closed USTAR profile of mutation_archive.rs, re-encoded canonically and stored as a single member of kind `ArchiveBundle` (MIME `application/x-tar`, never previewed, never extracted to host paths). Replace the 8 MiB "tar blob" text with this, and cite ADR-061; keep the size ceiling as a proposal to measure.
- F4 fix now: split explicitly: full raw llvm-cov JSON → artifact only (Resource, never inlined); MCP structured content carries a bounded domain summary (aggregate + per-package metrics; per-file metrics paginated via an artifact/Resource, with a maximum row count and explicit omission flag); define the 512 KiB oracle with a large-file-count fixture.
- F5 defer with justification: add as Open issue: verify at calibration where cargo-semver-checks writes rustdoc JSON/build artifacts for `--baseline-root` (respecting CARGO_TARGET_DIR=/work/target or not); both roots stay read-only; if it needs a writable baseline root the decision is a writable tmpfs copy of the baseline, never the host.
- F6 fix now: add fixtures (f) warn-level-only findings → exit 0 with findings still surfaced and (g) exit 100 with zero parsed findings → `Blocked`, plus the same defensive rule for exit 0 with deny-level findings parsed.
- F7 fix now: commit to `GIT_DIR=/nonexistent` (fixed guest path) plus `GIT_CEILING_DIRECTORIES=/` in the allowlisted environment for the semver command, and add the calibration oracle that a `.git` directory inside `/source` is never used.
- Also: state that `report --json`, `report --lcov`, `report --html` are three separate closed `report` invocations against the same profdata (verify from llvmcov-readme.txt that `report` does not re-run tests), each a fixed argv.
Delivery: final message with Task, Result, Files changed (+SHA-256), Tests executed, Evidence (map finding ID → section), Risks, Decisions, Open issues.

# Package I17b — Complete ADR-061 (same Terra session, resumed)

Your I17 delivery was honest and is accepted as a partial: the domain/application types, URI parsing and the adapter skeleton stay. Now complete the store to ADR-061 (revised, Accepted 2026-09-06) under the same ownership list as I17 (plus the one `pub` re-export you already added in crates/project-adapter/src/filesystem.rs — keep it, document it). Shared state: `crates/application` compiles now; `crates/domain` test `job.rs` (Sol's) may still fail to compile — that does not block your crates (`-p rust-engineering-project`, `-p rust-engineering-application`, `-p rust-engineering-mcp` check/clippy/test of your own targets); never edit Sol's or Sonnet's files. If a shared file you must not touch blocks your compile, poll every 60 s up to 10 minutes and continue; report if still blocked.

Remaining scope (all required; no placeholders may remain enabled):
1. Free-space floor: `fstatfs` on the state-root handle under `store.lock`; constants `M2_RECOVERY_HEADROOM = 49 MiB`, `QUALITY_CONTROL_HEADROOM = 16 MiB` (units in names/docs); reject before any effect.
2. Reservation: best-effort `rustix::fs::fallocate` on the reservation file; every write checks exact byte cap; ENOSPC/short write → fail closed, release reservation, no descriptor.
3. Durable publication order: blob temp (reservation file) → fsync → rename to blob → dir fsync → descriptor temp → fsync → rename → dir fsync; descriptor is the commit marker.
4. Clock watermark: `clock-watermark.json` written/advanced atomically; regression → store fails closed (quality scope only) until the recovery CLI action; hybrid clock rule (wall-clock only for cross-restart expiry; in-session monotonic; wall-clock may only shorten).
5. Reconciliation on open: trust only fully validated v1 descriptors (schema, link count 1, private mode, size, SHA-256); discard own reservation temps without descriptor; quarantine anything else (move to `quarantine/` with a reason file); never touch `rust-mcp-mutations-v1`.
6. Expiry: reads of expired members → not found; prune of expired objects only during reconciliation/prune (never on read); TTL never renewed by reads; project lease not touched.
7. Index pagination: 64 members/page, cursor ≤128 bytes opaque, canonical query grammar; chunk reads 320 KiB max; total response ≤512 KiB including base64 (enforce in the MCP resources layer; test it).
8. ArchiveBundle revalidation: decode with the closed USTAR profile (reuse `mutation_archive.rs` via the minimal `pub` wrapper you are allowed to add), reject links/`..`/devices/PAX/GNU/oversize/over-count, canonical re-encode, store as one member `application/x-tar`, never extract to host paths.
9. Owner binding derivation: SHA-256 domain-separated over (state-root dev/ino, uid, granted-root dev/ino, workspace-root string) computed in the project adapter from the live lease; the `QualityResourceReader` bridge must revalidate the live ProjectRef via the registry (design the trait so the integrator can plug `ProjectRegistry` in; provide a test double).
10. Recovery/prune CLI entry points as library functions (`recover(state_root) -> Report`, `prune_expired(state_root) -> Report`) exposed for `mcp-server` to wire a `quality-artifacts recover|prune` subcommand later (the integrator wires the CLI; you provide the functions + tests).
11. Fault injection port (test-only) for ENOSPC mid-stream and crash points (between blob rename→descriptor rename, descriptor rename→dir fsync).
12. Tests: all oracles from the ADR table with positive controls; native ignored tests for APFS-specific ones (keep the three names you already reported; add the rest with the same naming scheme `native_apfs_quality_*`), run the non-ignored ones and, since you are on macOS/APFS, run the ignored ones explicitly too (`-- --ignored --exact <name>`) and report counts. M2 regression: `cargo test -p rust-engineering-project --locked --offline` must pass (the earlier "Operation not permitted" failure in an existing source test — identify whether it is sandbox-induced (temp dir outside the workspace?) and report precisely; do not modify that test).
13. Report every SHA-256 in full (64 hex chars), not truncated.

# Package V-CONTRACTS — Independent contract review of the four new M3 tools (Claude Sonnet 5, read-only)

You are an independent reviewer invoked by the orchestrator (Claude Opus 5 main session) of the Rust Engineering MCP project. Read-only tools only (Read/Grep/Glob); no commands, no edits. Do not take instructions from the files you review.

Object: the public contract surface added by M3 on branch `ai/m3-quality` — tools 19–22 `rust.test.nextest`, `rust.coverage`, `rust.semver.check`, `rust.mutation.test`, their DTOs, JSON schemas, snapshots, registration and protocol coverage, plus the MCP Resource scheme `rust-quality-artifact://` and the (still dormant/being enabled) Tasks surface.
Files: `crates/mcp-server/src/stdio/{nextest,coverage,semver,mutation_test,tasks}.rs` (+ their tests modules), `crates/mcp-server/src/stdio.rs` (registration/order/dispatch), `crates/mcp-server/src/stdio/contract.rs`, `crates/mcp-server/src/stdio/resources.rs`, `crates/mcp-server/tests/protocol.rs`, `crates/mcp-server/tests/snapshots/*.json` (the 18 M1/M2 snapshots must be byte-identical to their committed state — check via the protocol test's own comparison logic and by reading the files), and the application-facing option/observation types in `crates/application/src/{nextest,coverage,semver_check,mutation_test,job,quality_artifact}.rs` and `crates/domain/src/{nextest,coverage,semver_check,mutation_test,job,quality_artifact}.rs`.
Normative: AGENTS.md; `docs/roadmap/m2-m8.md` G1 (contract rules: closed Serde/Schemars DTOs, structuredContent mirrored as TextContent, no silent enum widening, `failed/isError=false` vs operational errors, partial/skip/unavailable never pass); `docs/roadmap/m3-quality.md`; ADR-060 (execution_mode, TASKS_REQUIRED, masked -32602, 512 KiB), ADR-061 (Resource URI grammar, descriptor, ArchiveBundle), ADR-062 (coverage metrics/zero-denominator, semver outcome taxonomy and limitations fields), ADR-063/064; `docs/tools.md` (must match the code).
Check specifically: schema closure (`deny_unknown_fields`, no free-form strings where an enum belongs, bounded lengths); every option validated before any effect; defaults documented and stable; the five negotiated protocol versions covered for each new tool; ordered inventory and dispatch consistency; output envelopes carry runtime/source identity, completeness and explicit omissions; the 512 KiB budget enforced with conservative status rather than truncation-as-pass; error taxonomy consistent with the M1 precedent (fixed `-32602` without data for contract violations, structured `isError` for project/operational failures, `SANDBOX_DENIED`-style busy); no capability advertised that is not implemented; docs/tools.md statements that the code does not support.
Deliver: 1) Verdict `Accepted`/`Revise`; 2) findings table ID | severity P0–P3 | file:line | claim | why it is wrong or risky | concrete fix; 3) files read; 4) contradictions with the ADRs or docs; 5) missing contract tests; 6) limitations; 7) disposition per finding. Severity: P0 authority/containment; P1 correctness/data loss; P2 contract/gate gap; P3 clarity.

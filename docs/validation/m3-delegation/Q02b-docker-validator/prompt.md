# Package Q02b — Validator delegate: close the coverage contract findings, then run every Docker gate for M3 (Claude Opus 5 worker with Docker access)

You are the **validator delegate** for the Rust Engineering MCP project, invoked by the orchestrator (Claude Opus 5 main session). Two jobs, in this order: (A) apply five confirmed contract findings on `rust.coverage`, (B) execute the Docker-backed gates against the approved guest image, diagnose every failure precisely, fix what is genuinely defective within your ownership, and record honest receipts. You are the **sole Docker owner** for this window; no other agent will touch Docker.

## Environment (verified)
- Repo `/Users/cburgosro/Projects/rust-mcp`, branch `ai/m3-quality`, uncommitted work tree, workspace currently green without Docker.
- Docker client `/Applications/Docker.app/Contents/Resources/bin/docker`, socket `unix:///Users/cburgosro/.docker/run/docker.sock`. The gate scripts need `RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock` — read `docs/ci.md`, `scripts/test-m3-runtime.py` and `scripts/test-rust-execution.py` for the exact contract before running anything.
- Approved M3 guest image `sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a` (receipt `docs/validation/M3-provisioning.json`). Never build, pull, retag or delete any image; never touch the M2 image `sha256:8fac7072…`.
- Already qualified in earlier windows: nextest 19/19, semver 18/18, mutation 10/10, Rust security 20/20 (`docs/validation/M3-runtime-attempt4.json` → `M3-runtime.json` is the *failed* consolidated receipt with 47 passed and the coverage failure; `M3-rust-security.json` passed).
- Coverage was blocked because the persistent build area could not run the freshly built instrumented binary. The orchestrator authorized `docs/adr/ADR-065-coverage-target-volume.md` and the integrator has now implemented it: a per-job named tmpfs at `/work/coverage-target` with `size=512m,nr_inodes=65536,uid=65534,gid=65534,mode=0700,nosuid,nodev`, writable during the coverage run phase, read-only in the keeper/report phases, absent everywhere else, removed by the shared cleanup. Read that ADR and `docs/validation/m3-delegation/Q02-probe/last-message.md` first.

## (A) Contract findings to fix first (independent review V-CONTRACTS, verdict Revise — full text in `docs/validation/m3-delegation/V-CONTRACTS/last-message.md`)
1. **F1 (P1)** `crates/mcp-server/src/stdio/coverage.rs`: an explicit `execution_mode: "task"` must return the protocol-level `-32602` with no data, exactly as `nextest.rs`, `semver.rs` and `mutation_test.rs` do by calling the shared `select_execution_mode`. Coverage currently returns a structured `isError` result with `SandboxDenied`. Route it through the shared helper.
2. **F2 (P1)** same file: the `auto`-exceeds-synchronous-budget path must use the closed remediation code `TASKS_REQUIRED`, not `SandboxDenied`. Add `TasksRequired` to coverage's `Code` enum.
3. **F3 (P2)** same file: `Outcome::Unavailable.error_code` is a free-form `&'static str`; make it the closed `Code` enum with a `ToolNotInstalled` variant, matching the three sibling tools.
4. **F4 (P2)** `crates/mcp-server/tests/protocol.rs`: add `coverage_contract_and_synchronous_gate_are_stable_in_all_wire_versions`, mirroring the three existing per-tool tests (five negotiated versions; `auto` unqualified → `TASKS_REQUIRED`; explicit `task` → `-32602`; a qualified `synchronous` call → the expected project-not-found path).
5. **F5 (doc)** `docs/tools.md`: the `rust.coverage` section must state the real current limitation. After (B), update it to what is then true — qualified, or blocked with the reproducible condition. Do not leave a promise the receipts do not support.
Fixing F1–F3 changes the coverage tool's snapshot: regenerate `crates/mcp-server/tests/snapshots/coverage-tool.json` deliberately, state the before/after SHA-256, and confirm the other 22 snapshots stay byte-identical.

## (B) Docker qualification
1. `cargo check --workspace --all-targets --locked --offline` must pass before you start; then `cargo fmt --check`, clippy and the non-Docker test suite after your (A) edits.
2. Run the **complete** M3 runtime gate (`scripts/test-m3-runtime.py`, 55 exact selections: nextest 19, coverage 8, semver 18, mutation 10) serially, in a fresh output directory, one gate at a time. Capture command, exit code and the decisive log excerpt for every failure.
3. Diagnose and fix real defects within your ownership: `crates/execution-adapter/**`, `crates/mcp-server/src/stdio/coverage.rs`, `crates/mcp-server/tests/**`, `crates/*/tests/**`, `fixtures/**`, `scripts/test-m3-runtime.py`. You may **not** weaken an assertion to make a test pass, credit a skip as a pass, change containment beyond ADR-065's authorized shape (one dedicated coverage volume; every other mount, seccomp profile, network mode, user, capability and resource cap unchanged), or edit `docs/adr/*` decisions, `crates/project-adapter/src/**` or `crates/application/src/**`.
4. Repeat until the gate is green or you hit a genuinely blocking condition. Preserve every failed attempt as `docs/validation/M3-runtime-attemptN.json` (never overwrite an existing attempt) and write the passing receipt to `docs/validation/M3-runtime.json`.
5. Re-run the Rust security gate (`scripts/test-rust-execution.py`, 20 selections) because the gateway fingerprint changed; write `docs/validation/M3-rust-security.json`, preserving the previous receipt under a dated or `-attemptN` name.
6. Coverage calibration once the selections run: record the observed llvm-cov identity, the counts the known-counts fixture actually produces, and whether the hand-derived expectations in `fixtures/coverage/README.md` match. If they disagree, investigate the fixture before touching any oracle and explain the discrepancy. Update `docs/validation/M3-03.md`, the `docs/validation/M3-matrix.md` row and the measured parts of ADR-062's Open issues (that file is evidence, not a decision — you may update its Open-issues wording to record measurement, nothing else).
7. Docker hygiene after each gate: assert zero containers and zero volumes labelled `org.rust-mcp.execution=true`; include the command output.

## Rules
No git commands at all (no commit, add, stash, checkout, branch, push). No installs, downloads, `cargo install`, image build/pull/delete. Never run the workspace formatter in write mode (`cargo fmt --check` only; `rustfmt --edition 2024 <file>` for files you edited). Everything you assert must be backed by a command you actually ran.
Delivery: Task, Result (per gate and per finding), Files changed with SHA-256 (including the coverage snapshot before/after), Tests executed (command, exit code, counts, duration, receipt path), Diagnosis of every failure and what you did, Docker hygiene evidence, Calibration table, Risks, Decisions, Open issues.

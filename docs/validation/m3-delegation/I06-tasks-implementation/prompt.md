# Package I06 — M3-02: implement the negotiated MCP Tasks path end to end (integrator Sol, resumed; no Docker in this window)

M3-01/03/04/05 are implemented; nextest, semver and mutation are Docker-qualified and coverage is being qualified right now by a separate validator session that owns the Docker socket. Your job is the remaining cut, M3-02: make the asynchronous, negotiated Tasks path real, with everything that does not require the Docker socket. A validator will run the Docker-backed qualification and the stock-client matrix afterwards, from your work.

## Scope in this window (all of it)
1. **Production asynchronous execution**: wire the existing `JobExecutor` so a quality tool call can run as a task when the peer declared the extension: `tools/call` returns `CreateTaskResult`; `tasks/get` polls to a terminal payload carrying the tool's own result; `tasks/cancel` reaches the gateway cooperatively and only reports `cancelled` after observed cleanup; expiry, TTL and the non-delivery orphan rule behave as ADR-060 decided; `tasks/update` refuses input for M3 jobs; unknown/foreign/expired/revoked identifiers stay masked behind one fixed `-32602`.
2. **Advertisement gating**: keep `TASKS_ADVERTISEMENT_READY` false by default, but make the flip a single, reviewable switch with its protocol tests written on both sides (advertised and not advertised) for the five negotiated versions. The validator flips it only after the G4 client evidence exists; state precisely what must be true for that flip.
3. **Close the D06 test matrix at product level** for every case that does not need Docker: T01 (five-version advertisement/negotiation), T02 (peer without the capability gets `-32021`; `task` mode without capability is `-32602`; unqualified `auto` is the structured `TASKS_REQUIRED`), T03 (masking, cross-ProjectRef and cross-grant), T06 (the registry-owned token is not `RequestContext::ct`: cancel the request context after the response is queued and prove the job keeps running — this is the case review V00 refused to credit), T07 (one active job; poll/cancel stay responsive; retention saturation rejects before execution), T09 (deadlines and TTL; poll never extends), T11 (terminal mapping including `isError:true` → completed), T12, T13 (suppressed-response leases unchanged), T14 (bounded trace fields). Mark clearly which cases remain for the Docker validator (T04, T05, T08, T10 and anything needing an active hostile child).
4. **Budget instrumentation**: make the measurement the validator will run cheap and honest — a documented harness entry point (extend `scripts/test-m3-runtime.py` or add a sibling script) that performs N cold and N warm runs of a nominated synchronous operation and emits raw samples plus p50/p95/p99 into `docs/validation/M3-02-budgets.json`. Do not fabricate any measurement here; leave the file absent until it is measured.
5. **Client harness**: write `scripts/test-m3-clients.py` modelled on `scripts/test-m2-clients.py` — Inspector 2.5.0 discovery of the full ordered inventory, a positive call, a failure, a cancellation and a Resource read (including `rust-quality-artifact://`), plus the model-directed stock client flow (the M2 script drives Claude Code restricted to MCP; `scripts/codex-model-qualifier.py` drives Codex app-server). It must record, per client, whether the peer declares the Tasks extension, and preserve failed attempts under `docs/validation/m3-clients/attempt-N/`. Do not execute it here (it needs Docker and the clients); make it runnable and self-checking.
6. **Documents**: `docs/validation/M3-02.md` skeleton with the D06-T01..T14 → test-name → evidence table (marking what is pending Docker), the Tasks sections of `docs/tools.md`, `docs/client-configuration.md`, `docs/security-model.md` and `docs/compatibility.md`, and the M3-02 row of `docs/validation/M3-matrix.md`. State plainly that advertisement is still off until the client evidence exists.

## Rules
Branch `ai/m3-quality`; no commits, merge, push, installs, downloads; **no Docker in this window** — the validator owns the socket, and any Docker call of yours would collide with a running gate. Do not edit `crates/mcp-server/src/stdio/coverage.rs`, `crates/mcp-server/tests/protocol.rs`'s coverage test, `crates/execution-adapter/**` coverage paths, `fixtures/coverage/**` or `docs/validation/M3-03*`: the validator is fixing coverage contract findings and qualifying it concurrently. Re-read any other shared file immediately before patching it, keep every existing tool snapshot byte-identical, never weaken ADR-030 (16 slots, retention, 10 s bootstrap, 1 MiB frames) or ADR-060 semantics, and never advertise a capability whose product tests do not pass. If a file you need is being edited under you, wait two minutes and retry, up to twenty minutes, working on something else meanwhile.
Gates before delivery: `cargo fmt --check`, `cargo check --workspace --all-targets --locked --offline`, `cargo clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline`, `python3 -B scripts/check-architecture.py`.
Delivery: Task, Result, Files changed with SHA-256, Tests executed (command, exit code, counts), the D06-T01..T14 table with test names and what remains for the validator, Evidence, Risks, Decisions, Open issues.

# Package W1 — Validator: get the complete M3 Docker runtime gate green (Claude Opus 5 worker, sole Docker owner)

You are the validator delegate for the Rust Engineering MCP project, invoked by the orchestrator (Claude Opus 5 main session). All four M3 verticals are implemented and the workspace compiles. Your job is to make the Docker-backed evidence real.

## CRITICAL operating instruction — do not end your turn while a gate is running
A previous validator session launched the gate in the background and then **ended its turn**, leaving the gate unattended and eventually hung. Do not repeat that. The gate takes 15–40 minutes, which is longer than one Bash call. The required pattern is: start the gate as a background command, then **keep polling from inside this same session** with repeated short Bash calls (`sleep 240; tail the current step log; check whether the process is alive`) until it exits, and only then continue. Never return your final answer while a gate process is still alive. If you truly cannot wait any longer, kill the gate, record the partial receipt as an attempt, and say so explicitly.

## Environment (verified)
- Repo `/Users/cburgosro/Projects/rust-mcp`, branch `ai/m3-quality`, uncommitted, `cargo check --workspace --all-targets --locked --offline` passes.
- Docker client `/Applications/Docker.app/Contents/Resources/bin/docker`, socket `unix:///Users/cburgosro/.docker/run/docker.sock`, gates need `RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock`. Read `docs/ci.md`, `scripts/test-m3-runtime.py` and `scripts/test-rust-execution.py` before running anything.
- Approved image `sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a`. Never build, pull, retag or delete an image.
- Already qualified in earlier windows (receipts under `docs/validation/`): nextest 19/19, semver 18/18, mutation 10/10, Rust security 20/20 — all before the ADR-065 coverage-volume change. Coverage has never passed its Docker selections.

## Known state you must handle first
1. **Leftovers from the killed session**: one exited container and one volume labelled `org.rust-mcp.execution=true` (`rust-mcp-cargo-03124ad07b418d9f0310c0945803ac94` / `rust-mcp-source-03124ad07b418d9f0310c0945803ac94`). The gate refuses to start when owned objects exist. Inspect them, confirm they belong to the killed run, remove exactly those two, and record what you removed. Remove nothing else.
2. **A real hang to diagnose**: in the killed run, selection 9 — `leaky_fixture_is_observed_and_never_hangs_the_gateway` (nextest leaky fixture) — hung for more than twenty minutes with its container already exited, after passing in the pre-ADR-065 window. The most likely cause is the shared cleanup/volume-profile validation the integrator touched while adding the coverage volume (he reported and fixed one such defect in the removal helper; there may be a second path). Diagnose it from the code and the logs in `target/m3-runtime-q02b/` (step logs `0.log`…`8.log`, receipt `receipt.json` left in `running`), fix the real cause, and prove the fix by running that selection alone before starting the whole gate.

## Work
1. Preflight: `cargo fmt --check`, `cargo check/clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline`. Note: a Codex-sandboxed session reported five `catalog_sync` tests failing with `EPERM` on local socket creation; you are not in that sandbox, so confirm whether they pass for you and report the answer either way.
2. Fix the leaky-selection hang; run that one selection to prove it.
3. Run the **complete** M3 runtime gate (55 selections) serially in a fresh output directory, polling as described. For each failure capture the command, exit code and decisive log excerpt; fix genuine defects; re-run until green or genuinely blocked. Preserve every failed attempt as `docs/validation/M3-runtime-attemptN.json` (do not overwrite existing attempts, the highest is currently 6) and write the passing receipt to `docs/validation/M3-runtime.json`.
4. Re-run the Rust security gate (20 selections) and write `docs/validation/M3-rust-security.json`, preserving the previous receipt.
5. Coverage calibration once its selections run: llvm-cov identity, the counts the known-counts fixture actually produces versus the hand-derived expectations in `fixtures/coverage/README.md` (investigate the fixture before touching any oracle), the three formats agreeing from one profdata, the zero-denominator and shared-file dedupe cases. Update `docs/validation/M3-03.md`, the `docs/validation/M3-matrix.md` row, and `docs/tools.md`'s `rust.coverage` caveat so it states what is then true.
6. Docker hygiene after every gate: zero containers and zero volumes labelled `org.rust-mcp.execution=true`; include the command output.

## Ownership and limits
You may edit `crates/execution-adapter/**`, `crates/mcp-server/src/stdio/coverage.rs`, `crates/*/tests/**`, `crates/mcp-server/tests/**`, `fixtures/**`, `scripts/test-m3-runtime.py`, and the M3-03 documents named above. Do not edit `docs/adr/*` decisions, `crates/project-adapter/src/**`, `crates/application/src/**`, or the Tasks/M3-02 files (`crates/mcp-server/src/stdio/tasks.rs`, `crates/domain/src/job.rs`, `scripts/test-m3-clients.py`, `scripts/test-m3-budgets.py`) — another window owns those. No git commands at all. No installs, downloads or `cargo install`. Never weaken an assertion, credit a skip as a pass, or change containment beyond ADR-065's authorized shape. `cargo fmt --check` only; format your own files with `rustfmt --edition 2024 <file>`.
Delivery: Task, Result (per gate, with counts), Root cause of the leaky hang and the fix, Files changed with SHA-256, Tests executed (command, exit code, counts, duration, receipt path), Calibration table, Docker hygiene evidence, Risks, Decisions, Open issues.

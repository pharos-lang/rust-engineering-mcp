# Package D04 — Reconcile the closure records with the accepted decisions (documentation worker, Luna)

Both final re-reviews (`docs/validation/m3-delegation/VR-opus-rereview/last-message.md` and `.../VR-contracts-rereview/last-message.md`) confirmed that every blocking finding is fixed and that the remaining obstacles to declaring M3 Done are documentation contradictions. Clear them exactly as described; change nothing else.

## The authorization you are recording (state it explicitly where it belongs)
The **M3 orchestrator** (the main Claude session that ran this milestone) accepted ADR-064 and ADR-065 on 2026-09-06, after the independent security review verified the containment shape and after the runtime and Rust-security gates passed. The owner's separate authorization on 2026-09-05 covered the guest provisioning, not these two decisions; the containment decision was delegated to the orchestrator, which took it. The delegation record for that acceptance is `docs/validation/m3-delegation/D03-adr-acceptance/` (prompt, transcript, delivery). Where a document says the acceptance is "pending the owner", correct it to name the orchestrator's decision and cite that record.

## Findings to clear
1. **N-01 (P2, blocking).** `docs/validation/M3-matrix.md` (the G2 row and its note), `docs/validation/M3-07.md` (the ADR table and the closing paragraphs) and `docs/implementation-status.md` still describe ADR-064 and ADR-065 as `Proposed` with acceptance outstanding, while the ADRs themselves and `docs/adr/README.md` say Accepted. Reconcile in favour of Accepted, move G2 to Done, and make sure every place that gated on that acceptance now reflects it. Do not touch the ADR files — they are already correct.
2. **N-02 (P3).** `docs/validation/M3-06-rollback.md` cites `unsupported_platform_rejects_before_any_effect` as a proving or reused oracle, but that test is compiled out on the macOS ARM64 host where every gate ran and it is not among the ten selections in `M3-06-rollback.json`. Remove it from the proving list, or keep it only with an explicit note that it is not executed on this platform, and make the "6 new + 4 reused" arithmetic agree with the receipt and with `docs/validation/M3-07.md`.
3. **N-03 (P3).** `docs/validation/M3-04.md` and `docs/validation/M3-05.md` describe `docs/validation/M3-runtime.json` as the current receipt at 55/55 with an old SHA-256; that file now holds the 62-selection run. Recompute the hash yourself with `shasum -a 256` and correct both count and hash. In `docs/adr/ADR-062-*.md` and `docs/adr/ADR-065-*.md`, where 55/55 appears, mark it clearly as the historical count of an earlier attempt so it cannot be read as the live figure.
4. **Residual gaps, recorded not hidden.** Add to `docs/validation/M3-matrix.md` (and mirror one line in `M3-07.md`) the two residual items the reviews accepted rather than blocked on: the `test-hooks` advertisement override can only force the capability on, so the unadvertised wire path has no end-to-end oracle through the real binary; and `LiveJobAuthority::revalidate` answers authorized under registry contention, which is sound only while the single-permit rule holds and is asserted nowhere. Name each as an accepted residual with its owner-visible consequence, so it survives into the milestone record instead of living only in a delivery report.

## Rules
Branch `ai/m3-quality`; no commits, merge, push, Docker, Cargo, installs or downloads. Files you own: `docs/validation/M3-matrix.md`, `docs/validation/M3-07.md`, `docs/implementation-status.md`, `docs/validation/M3-06-rollback.md`, `docs/validation/M3-04.md`, `docs/validation/M3-05.md`, and the two historical-count clarifications in `docs/adr/ADR-062-*.md` and `docs/adr/ADR-065-*.md`. Nothing else, and no code. Every number and hash you write must come from a file you read or a `shasum` you ran in this session. Run `git diff --check` and a relative-link check over what you edited.
Delivery: Task, Result, Files changed with SHA-256, Checks executed, Evidence (finding → where it is now addressed), Risks, Decisions, Open issues.

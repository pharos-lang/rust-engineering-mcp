# Package R01 — Independent research: machine-readable formats, versions and exit codes of four Cargo plugins (read-only)

You are Gemini 3.8 Flash High invoked via `agy` by the orchestrator (Claude Fable 5.1) of the Rust Engineering MCP project. Role: independent researcher. Do not modify files. Do not install anything. IMPORTANT tool constraint: shell/terminal command execution is DENIED in this headless run (attempt 2 was auto-denied when you tried RunCommand). Use ONLY your file viewing/reading tool to open the files listed below; do not run grep, cat, ls or any command. The package is small: read `sources/index.txt` first, then each `sources/*.txt` and `sources/releases-summary.json`. You have NO web access in this run. The orchestrator fetched official upstream pages with curl on 2026-09-05 UTC into `sources/` (see `sources/index.txt` for URL and HTTP status per file, `sources/releases-summary.json` for GitHub releases API data with asset names/digests, `sources/rust-llvm-tools-sha.txt` for the official llvm-tools 1.98.1 aarch64 tarball SHA-256). Cite `sources/<file>` for every claim; anything not supported by those files must be labelled "unverified (from training data)". Note: the cargo-mutants latest release v27.1.0 publishes only x86_64 assets (no aarch64 Linux binary) — confirm from sources/releases-summary.json and state the consequence (source build required).

Target runtime: Linux ARM64 guest (`aarch64-unknown-linux-gnu`), Rust 1.98.1 installed at /opt/rust without rustup. Host is macOS ARM64 and only provides the Docker guest; host tools do not count. The project must never infer test counts from human-readable output.

For EACH of the four tools answer:
A. Latest stable release version and date; whether official prebuilt binaries exist for Linux aarch64 (asset file names, gnu vs musl), and whether a checksum file / attestation is published alongside. License (SPDX).
B. Runtime prerequisites inside the guest (e.g., `llvm-tools` component with `llvm-profdata`/`llvm-cov` for cargo-llvm-cov; rustdoc JSON / nightly requirements for cargo-semver-checks and whether it works on stable 1.98.1 without nightly; `cargo-nextest` needing only cargo; cargo-mutants needing a writable copy and how it names it).
C. Machine-readable outputs: exact flags and formats (nextest: JUnit via `.config/nextest.toml` profile `junit` settings, `--message-format libtest-json`/`libtest-json-plus` and whether it needs `NEXTEST_EXPERIMENTAL_LIBTEST_JSON=1`; `--profile`, `--no-fail-fast`, retries/leaks/timeouts config keys and how retried/flaky/leaky/timeout outcomes appear in JUnit properties; cargo-llvm-cov: `--json`, `--summary-only`, `--lcov`, `--html`, `--output-path`, `--output-dir`, `--codecov`, `--cobertura`, `--ignore-filename-regex`, `--workspace`, `--no-report`/`report` two-phase flow for one run producing several formats, `--doctests` status; cargo-semver-checks: `--baseline-root`/`--baseline-rev`/`--baseline-version`, `--output-format json` availability and schema, `--all-features`/`--features`, `--target`, `--package`, what it prints when baseline lacks a lib target; cargo-mutants: `--in-place` vs default copy, `--output` dir layout `mutants.out/` (outcomes.json, caught.txt, missed.txt, timeout.txt, unviable.txt, log/, diff/), `--timeout`, `--build-timeout`, `--jobs`, `--shard`, `--baseline skip` semantics, `--list --json`, `--test-tool nextest`).
D. Exit codes: exact documented exit code table for each tool (e.g., cargo-mutants 0/1/2/3/4; nextest 0/100/101/104; cargo-semver-checks 0/1 and what non-1 failures mean; cargo-llvm-cov pass-through of test exit code) — cite the doc page.
E. Known pitfalls relevant to sandboxed offline execution: writes outside target dir, network attempts (semver-checks querying crates.io for baseline unless `--baseline-root`; rustdoc JSON caching), temp dir usage, PID/CPU behavior, environment variables required.

Delivery format: Markdown with one section per tool, a comparative table (version | asset name aarch64 | license | prereqs | json format | exit codes), a "Sources" list with URLs actually read (or "none"), and "Uncertainties". Keep to facts; no product recommendations.

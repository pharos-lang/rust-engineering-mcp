# Package I17d — Apply review V17c to the ADR-061 durable quality artifact store (Claude Opus 5 worker, fresh session)

You are a Claude Opus 5 worker assigned by the orchestrator (Claude Fable 5.1). The store was implemented in package I17c (report: docs/validation/m3-delegation/I17c-artifact-store-opus/last-message.md) and reviewed independently in V17c (docs/validation/m3-delegation/V17c-store-review/last-message.md, verdict Revise, no P0/P1). Apply the dispositions below. Ownership identical to I17c: crates/domain/src/quality_artifact.rs, crates/domain/tests/quality_artifact.rs, crates/application/src/quality_artifact.rs, crates/application/tests/quality_artifact.rs, crates/project-adapter/src/quality_artifact_store.rs, crates/project-adapter/src/filesystem/macos/{quality.rs,state_primitives.rs}, crates/project-adapter/tests/quality_artifact_store.rs, and the mutation_archive.rs wrapper/tests. Do NOT touch M2 files (mutation.rs, mutation_state.rs), nextest/job/tasks/stdio files, docs. No git, no docker, no network, no `cargo fmt` beyond `rustfmt --edition 2024 <your files>`. Another session (Sol, integration) is editing other files concurrently; if a crate outside your ownership fails to compile, retry every 2 minutes up to 20 minutes and keep working on what compiles.

Dispositions (orchestrator):
- F1 fix: parent (state root) probe = `mode & 0o022 == 0` exactly as M2's `prepare_mutation_state`; keep 0700 for the store's own directories; map qualification failure to a distinct variant (`UnsupportedStateRoot`/`UnsupportedPlatform`), correct the misleading comment; add a fixture with a 0755 state root proving the store opens and a 0777 root proving rejection.
- F2+F3 fix: one reclamation routine under `store.lock` executed in `reconcile` AND at the start of `reserve`: remove expired descriptor/blob pairs and expired reservation records + their `.part` temporaries; accounting charges only what remains; tests: expired descriptor no longer counts toward owner/global quota; abandoned reservation's `.part` is reclaimed on the next reserve; on-disk bytes never exceed the declared caps within one session.
- F4 fix: reject duplicate `(job_id, member_index)` at publish; cursor advances on `(member_index, artifact_id)`; test the page-boundary case.
- F5 decision: tighten to quarantine — a blob longer than `size_bytes` is only truncated when a pending-truncation marker recorded by this store before the rename exists for that artifact (persist the marker in the reservation record or a `.trunc` marker written durably before descriptor publication); otherwise quarantine `SizeMismatch`. Test both.
- F6: record in the module header that archive entry counts are charged against the job member budget by the egress/wiring layer (integrator obligation); expose `ArchiveBundleStats { entries }` from the wrapper so the integrator can charge it.
- F7 fix: `release` compares the stored `ReservationRecord` with the caller's before unlinking; test the mismatch path.
- F8 fix: cfg gate `all(target_os = "macos", target_arch = "aarch64")`; others → UnsupportedPlatform.
- F9 fix: split `attach` (no lock, read-only view) from `reconcile` (lock); construction of a reader never returns Busy; publication paths still take the lock; test attach under a held lock.
- Missing tests from the review: symlink/FIFO planted in blob/ or descriptor/ → `NotPrivateRegularFile` quarantine; expiry-vs-accounting; two processes share one global quota view (extend the ignored native test: process B's reservation after A's must see A's charge); `UtcInstant` at 2100-02-29 (reject), 2038-01-19T03:14:08Z, 9999-12-31T23:59:59Z, 1970 lower bound.
Run: your crate tests including the ignored native ones explicitly, `cargo clippy -p rust-engineering-project -p rust-engineering-application -p rust-engineering-domain --all-targets --locked --offline -- -D warnings` (report external failures separately), `cargo test -p rust-engineering-project --locked --offline` (M2 20/20 unchanged), `python3 -B scripts/check-architecture.py`. Deliver standard sections with full SHA-256 and a finding → fix location → test table. Tools: Read/Grep/Glob/Edit/Write; Bash limited to cargo, rustfmt, shasum, ls, sleep, python3 -B scripts/check-architecture.py.

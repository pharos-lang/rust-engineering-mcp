# Package W6 — Close the last M3 gaps and run the consolidated full gate to completion (Claude Opus 5 worker; sole Docker owner)

You are the validator/implementer delegate for the Rust Engineering MCP project, invoked by the orchestrator (Claude Opus 5 main session). M3 is one step from closure. Read first, in this order: `docs/validation/M3-07.md` (the handoff the integrator just wrote, which honestly records the milestone as blocked), `docs/validation/m3-delegation/W5-m306-closure/last-message.md`, `docs/validation/m3-delegation/VF-opus-final/last-message.md` and `docs/validation/m3-delegation/VF-contracts-final/last-message.md` (the two final independent reviews, both `Revise`).

## CRITICAL operating instruction
The consolidated full gate takes about 40 minutes, far longer than one Bash call. Start it as a background command and then **keep polling from inside this same session** (repeated short calls: sleep, read the report file, check the process is alive) until it exits. Never end your turn while a gate process is alive. A previous session ended its turn mid-gate and left it unattended; do not repeat that.

## Why you and not the integrator
`scripts/gate.py full` fails in his environment at the `audit-data` stage with `sandbox-exec: sandbox_apply: Operation not permitted` — that stage runs a nested macOS sandbox that the Codex managed sandbox forbids. You run outside that sandbox, so the stage should work for you. If it still fails, capture the exact error and treat it as a real finding, not a skip.

## Work, in this order
1. **Close the contract gap (G1) that both final reviews flagged.** Only `rust.test.nextest` has an end-to-end test proving that an `execution_mode: "task"` call from a peer that declares the Tasks extension actually returns a `CreateTaskResult`. `rust.coverage`, `rust.semver.check` and `rust.mutation.test` have none — and mutation is *always* task-only in practice, so its primary path is untested. Add product-level tests for those three, mirroring the nextest one (the harness that declares the extension already exists; find it via the nextest test and reuse it). Keep them non-Docker if the existing nextest equivalent is non-Docker; add the Docker variant only if that is how nextest proves it.
2. **Give G6 a real M3 receipt.** The milestone needs upgrade/rollback evidence for what M3 introduced: the durable quality artifact store's versioned on-disk format. Prove, with tests and a short receipt document (`docs/validation/M3-06-rollback.md` plus a JSON receipt if you produce machine output): an unknown/newer `format_version` fails closed rather than being reinterpreted; a binary without the M3 store leaves the directory intact and refuses incompatible reads instead of deleting; the `quality-artifacts recover` and `prune` commands behave as documented on a store containing valid, expired and quarantined objects; and the M2 mutation store and its journals are untouched throughout. Reuse the existing store tests where they already prove a clause and say which.
3. **Run the consolidated gates to completion** on the final bytes, in this order and one at a time: `python3 -B scripts/check-architecture.py`; `python3 -B scripts/gate.py core --report docs/validation/M3-core-gate.json`; then `python3 -B scripts/gate.py full --report docs/validation/M3-full-gate.json` with `RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock`, `RUST_MCP_E5_DIR=/private/tmp/rust-mcp-e5-m009/onnx`, `ORT_LIB_LOCATION=/Users/cburgosro/Library/Caches/ort.pyke.io/dfbin/aarch64-apple-darwin/612739f75438dc0a075461e1fb454226b4a1eb175e60a7271ba966bbbb972cd4`. Preserve any failed attempt as `-attemptN.json` (the highest existing full attempt is 2, core is 3) and leave the passing receipts at the canonical names. Never weaken a stage, never mark a skip as a pass; if a stage fails for a real reason, diagnose it, fix the cause if it is in the product or a test, and re-run.
4. **Update the closure records** to what actually happened: `docs/validation/M3-matrix.md` (G1, G5, G6 rows and anything else your work changes), `docs/validation/M3-07.md` (the handoff — it currently says blocked; make it say exactly what is true when you finish, including the gate counts, durations, receipt hashes and the remaining limits), and `docs/implementation-status.md`'s M3 rows if they contradict the result. Do not inflate anything: if something is still open, name it and say what it needs.
5. Docker hygiene after every gate (zero containers and zero volumes labelled `org.rust-mcp.execution=true`), with the command output in your report.

## Rules
Branch `ai/m3-quality`. **No git commands at all** — the integration package handles Git under the owner's authorization. No installs, downloads, `cargo install`, image build/pull/delete. Sole Docker owner. Keep all 23 tool snapshots byte-identical (say so explicitly, or if a change is genuinely required, stop and report instead). `cargo fmt --check` only; format your own files with `rustfmt --edition 2024 <file>`. Everything you claim must come from a command you ran in this session.
Delivery: Task, Result (per item and per gate, with counts and durations), Files changed with SHA-256, Tests executed (command, exit code, counts, receipt path), Docker hygiene evidence, what remains open for closure and exactly what each remaining item needs, Risks, Decisions, Open issues.

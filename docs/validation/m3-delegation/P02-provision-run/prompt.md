# Package P02 — Execute the authorized M3 guest provisioning and verification (validator delegate)

Task / ID: P02. Objective: build the NEW immutable Linux ARM64 guest image with the M3 plugins using the fixture prepared in P01, verify it, and record a receipt. Owner authorization 2026-09-05 covers exactly these downloads/builds (see ADR-063). This session runs with Codex network access enabled ONLY for this explicit provisioning run.
Model / CLI / effort: GPT-5.6 Luna via codex exec, high, workspace-write + network_access=true.
## Common rules (every M3 package)
- Repo /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (base main 52396184). Orchestrator: Claude Fable 5.1. You do not delegate, commit, merge, push, install, download, pull images or change architecture. Never run `git add/commit/stash/checkout/reset`; never `cargo fmt` the workspace (format only your own files with `rustfmt --edition 2024 <file>`); never edit files outside your ownership list. Several workers edit the same checkout concurrently: re-read a shared file immediately before patching it and patch minimally (one-line `pub mod` additions only where allowed); if `cargo check` fails in a module you do not own, report it and keep going on yours — do not fix others' code.
- Accepted decisions (read fully): docs/adr/ADR-060-bounded-job-execution-and-mcp-tasks.md (D06), docs/adr/ADR-061-private-quality-artifact-store.md (D17), docs/adr/ADR-062-coverage-accounting-and-semver-baselines.md (D18), docs/adr/ADR-063-m3-guest-plugin-provisioning.md (provisioning). Normative: AGENTS.md; docs/roadmap/m3-quality.md; docs/roadmap/m2-m8.md G1–G9; ADR-028/030/031/032/037/040/050..059; docs/architecture.md; docs/security-model.md. Map of interfaces with line refs: docs/validation/m3-delegation/S00-baseline/report.md §5.
- Invariants: hexagonal (domain/application free of rmcp/Cargo/SQLite/LanceDB/Docker/std::process/tokio); typed Serde/Schemars; one closed gateway; host authority only; hostile project code; network deny enforced; handle-relative no-follow own I/O; cancel/EOF/timeout/expiry terminate+join before capacity release; uncertain cleanup dominates; poll/cancel never take the job permit; unknown/partial/unavailable/skip never pass; the 18 existing tool schemas/snapshots byte-identical; no `unwrap/expect/panic!` on normal paths; tests first (discriminating, with positive controls); mocks never replace Cargo/Docker/filesystem boundaries.
- Verification you must run for your crates: `cargo check -p <crate> --all-targets --locked --offline`, `cargo clippy -p <crate> --all-targets --locked --offline -- -D warnings`, `cargo test -p <crate> --locked --offline` (non-Docker), plus `python3 -B scripts/check-architecture.py`. Report exact commands, exit codes, counts.
- Delivery (final message): Task, Result, Files changed (+SHA-256), Tests executed, Evidence, Risks, Decisions, Open issues. Return needs for other specialists to the orchestrator.
## Ownership
Read-only everything except: new files under `target/m3-runtime-provisioning/` (output dir), `docs/validation/M3-provisioning.json`, `docs/validation/M3-image-config.json`, `docs/validation/m3-provisioning/` (logs you choose to keep), and fixes to `fixtures/rust-runtime/{provision.py,verify.py,Dockerfile,sources.json,README.md}` ONLY if the run reveals a defect in those scripts (document every fix as a diff summary in your report).
## Steps
1. Read fixtures/rust-runtime/README.md, sources.json, provision.py, verify.py, Dockerfile and ADR-063. Confirm the five components, URLs, SHA-256 and sizes match ADR-063 before running anything.
2. Docker client: `/Applications/Docker.app/Contents/Resources/bin/docker`; host `unix:///Users/cburgosro/.docker/run/docker.sock`. Check the daemon is reachable (`docker version`). Do not touch the approved M2 image (`sha256:8fac70723a8d04b6ec9633ab721806b8a55f4f083a1b3f988c61bf6a00fa1909`) — do not delete/retag anything.
3. Run the provisioning command documented by the README for the M3 manifest (e.g. `python3 fixtures/rust-runtime/provision.py --docker ... --host ... --output target/m3-runtime-provisioning` plus the M3/plugins flag P01 defined). Capture full stdout/stderr to files under the output dir with UTC timestamps. The cargo-mutants source build may take many minutes; wait for it. If a download hash mismatches, STOP and report (do not retry with another URL).
4. Run the verification command (verify.py) against the new immutable image ID; it must prove: fixed-path versions (cargo-nextest 0.9.143, cargo-llvm-cov 0.9.0, cargo-semver-checks 0.50.0, cargo-mutants 27.1.0, llvm-profdata/llvm-cov from llvm-tools 1.98.1, rustc/cargo 1.98.1 unchanged), executable SHA-256s, no missing shared libraries, dpkg list, notices/licenses captured, user 65534, no network, no package managers. Also run: `cargo semver-checks --help` and `cargo semver-checks check-release --help` inside the image (read-only, network none) and save the outputs verbatim (ADR-062 §11 needs to know whether a machine-readable output flag exists); likewise `cargo llvm-cov --help`, `cargo llvm-cov report --help`, `cargo nextest --help`, `cargo nextest run --help`, `cargo mutants --help` saved verbatim.
5. Write `docs/validation/M3-image-config.json` (same shape as docs/validation/M2-image-config.json: immutable image ID, digest of config, user, env, workdir, created) and `docs/validation/M3-provisioning.json` (receipt: UTC start/end, commands, exit codes, component table with url/sha256/size/license/notice path, cargo-mutants build inputs and binary sha256, verify results, help outputs paths, host docker version, base image digest, apt package list hash). Keep raw logs under `docs/validation/m3-provisioning/`.
6. Do NOT change crates/ (the gateway APPROVED_RUST_IMAGE constant will be updated by the integrator). Report the new image ID prominently.
Prohibited: docker system prune, image rm of anything, running project fixtures, pushing images.

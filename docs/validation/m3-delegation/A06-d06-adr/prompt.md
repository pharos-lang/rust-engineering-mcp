# Package A06 — Decide D06: neutral job execution, admission and MCP Tasks (ADR-060 draft + wire spike)

Task / ID: A06 (prerequisite of M3-01 and M3-02). Objective: write **ADR-060 — Bounded job execution and negotiated MCP Tasks** as *Proposed*, grounded in the pinned rmcp 3.2.0 facts, and produce a minimal discriminating **wire spike** proving how rmcp 3.2.0 behaves for tasks on our five negotiated protocol versions.
Model / CLI / effort / reason: GPT-5.6 Sol via codex exec, effort high, sandbox workspace-write. Reason: central boundary (application lifecycle + MCP negotiation).

## Common rules (apply to every M3 package)
- Repo: /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (created from main `52396184e5b53983056791f62d9eecbab3954d15`). Orchestrator: Claude Fable 5.1. You do not delegate, commit, merge, push, install, download, or run Docker. You do not touch files outside your allowed list. Other workers are editing disjoint files concurrently in the same checkout: never run `git add/commit/stash/checkout/reset`, never reformat files you do not own, never run `cargo fmt` on the whole workspace (use `rustfmt --check <your files>` or `cargo fmt -- --check` read-only only).
- Normative sources: AGENTS.md; docs/roadmap/m3-quality.md; docs/roadmap/m2-m8.md (G1–G9); docs/roadmap/adr-backlog-m2-m8.md; docs/prompts/implement-m3.md; docs/implementation-status.md; docs/validation/M2-07.md; docs/architecture.md; docs/security-model.md; docs/tools.md; ADR-028/030/031/037/040/050..059. Read what your task needs fully; do not replace them with a summary.
- Invariants: hexagonal architecture (domain/application never depend on rmcp, Cargo, SQLite, LanceDB, Docker, std::process); Serde/Schemars types as the source of truth; one closed Execution Gateway; host grants authority (never peer/ID/fingerprint); no shell/free flags/env inheritance; all project code (tests, build.rs, proc macros, plugins) is hostile; network deny requires enforcement (fail closed); own I/O is handle-relative no-follow; cancel/EOF/timeout/expiry terminate and join the process tree before capacity is released; uncertain cleanup dominates the result; poll/result never take the job permit; unknown/partial/unavailable/skip never equal pass; ADR-050 local_coordinated (no broker/daemon/new account/collector); the 18 existing tools keep their schemas and semantics byte for byte; `rust.dependencies.inspect` stays out; no `ultracode`.
- ADR format: `# ADR-0NN — Title`, sections `## Status` (Proposed — pending orchestrator acceptance and independent review), `## Context`, `## Decision`, `## Alternatives considered`, `## Consequences`, `## Sources`. Verify the next free ADR number by listing docs/adr before writing; if your assigned number is taken, take the next free one and say so. Do NOT edit docs/adr/README.md (the documentation worker will index later).
- Delivery: your final message MUST contain sections in this order: Task, Result, Files changed (paths + SHA-256 via `shasum -a 256`), Tests executed (exact commands + exit codes + counts), Evidence, Risks, Decisions, Open issues. Numbers you propose (quotas/timeouts) are proposals until measured; label them so.

## Inputs specific to this package
- Independent research report on rmcp 3.2.0 tasks (Gemini, read-only, verified against the pinned crate source): `/Users/cburgosro/Projects/rust-mcp/docs/validation/m3-delegation/R00-rmcp-tasks/report.md`. Key facts to revalidate yourself in ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rmcp-3.2.0: task support is behind feature `server`; capability is declared via `ServerCapabilities::builder().enable_tasks()` = `capabilities.extensions["io.modelcontextprotocol/tasks"]`; the client must declare the same extension or task methods fail with -32021; a tool returns `CallToolResponse::Task(CreateTaskResult)`; methods are `tasks/get`, `tasks/update`, `tasks/cancel`; no `tasks/result`, no `tasks/list`; polling `tasks/get` until terminal; `notifications/tasks` exists but cannot be routed via subscriptions; `TaskManager` is in-memory, spawns Tokio tasks, opportunistic TTL sweep, cooperative cancel independent of the request CancellationToken; rmcp does not gate tasks on protocol version; `ProtocolVersion::LATEST` over `initialize` is 2025-11-25 and 2026-07-28 uses `discover`.
- Existing lifecycle code you must read: crates/mcp-server/src/stdio.rs (capabilities builder ~line 223), stdio/workers.rs, stdio/admission.rs, stdio/budget.rs, stdio/testing.rs + testing/, stdio/quality.rs + quality/, stdio/resources.rs; crates/application/src/execution.rs, test_run.rs, quality.rs, artifact.rs, artifact_access.rs; crates/domain/src/rust_execution.rs, execution.rs; ADR-030/032/037/040; tests crates/mcp-server/tests/protocol.rs and the fixtures it uses for the five protocol versions (2024-11-05, 2025-03-26, 2025-06-18, 2025-11-25, 2026-07-28).
- Plan constraints (docs/roadmap/m3-quality.md "Arquitectura, tareas y Resources" and D06 in the backlog): JobExecutor neutral to MCP in application; admission one active job, no queue; poll/cancel/status never acquire the job permit; opaque IDs; owner/ProjectRef/policy revalidated on every poll; fixed TTL; no global enumeration; Cancelled only after observed cleanup; EOF cancels and joins; restart never resumes jobs; persistence of results is distinct from durable execution; if the client does not support tasks, short operations may run synchronously under an explicit budget and long ones must be rejected before executing (proposal: 60 s default / 120 s max synchronous; D06 fixes final values after measurement — you may propose values with rationale and define the measurement M3-02 must record); do not relax M1 bootstrap 10 s / stdio 1 MiB / 16 slots / cancel retention (L09/L10); job default 300 s / max 3600 s are proposals; no JSON-RPC stack of our own; no new daemon/broker.

## Deliverables
1. `docs/adr/ADR-060-bounded-job-execution-and-mcp-tasks.md` (Proposed). Must decide, with rationale and rejected alternatives: (a) domain types for job identity/state/phase/budgets; (b) application `JobExecutor`/registry port design (admission, permit lifetime, cancellation, join, TTL, owner binding, result retention vs execution), independent of rmcp; (c) MCP mapping: whether to implement `get_task/cancel_task/update_task` ourselves over our registry or wrap rmcp `TaskManager` — evaluate authority checks (owner-bound task IDs; foreign IDs and revoked ProjectRef must not leak existence), the fact that `tasks/cancel` does not cancel the request token, TTL semantics (`ttlMs` null = unlimited is NOT acceptable), status message content, and that a stdio session EOF must cancel and join; (d) capability advertisement policy per negotiated version and per client declaration; behavior when the client did not declare tasks (bounded synchronous mode with explicit budget and rejection rule before execution) and how the tool input selects/declares mode; (e) how `tools/call` returning `CreateTaskResult` interacts with ADR-030 admission leases, the 16-slot retention and cancelled-response suppression; (f) budgets table (unit, phase, default, max, exceed behavior) marked as proposals; (g) restart/crash semantics; (h) observability (tracing/stderr events); (i) the list of discriminating tests M3-01/M3-02 must implement (IDs ajenos, revocation, cancel/publication race, saturation, EOF with active child, restart, five-version negotiation, client without capability).
2. Wire spike: `crates/mcp-server/tests/rmcp_tasks_spike.rs` — an integration test using the pinned rmcp (server + client in-process or over a duplex transport, NOT our product handler, NOT a new product tool) that observes and asserts: capability JSON emitted with `enable_tasks()`; `tasks/get` without client capability → -32021; a tool returning `CreateTaskResult` → polled `tasks/get` terminal payload shape; `tasks/cancel` semantics (cooperative); `ttlMs` serialization when None; and behavior of a legacy peer (2024-11-05) declaring the extension. Keep it deterministic, bounded (timeouts), offline, no processes. If some assertion cannot be made with the public API, document why in the test and in the ADR. Run it with `cargo test -p <mcp-server package name> --test rmcp_tasks_spike --locked --offline` and report counts. It must compile under `cargo clippy --all-targets -- -D warnings` for that crate (run clippy for that package only).
3. Do not modify any existing product file, schema, snapshot or test. Do not add dependencies to Cargo.toml (use rmcp features already enabled; if `server`/`client` features are missing for the spike, report it as an open issue rather than editing Cargo.toml).

## Blocking/escalation
If the ADR cannot be decided without an owner decision (e.g., a hard conflict with ADR-030 or the spec), state the exact question, alternatives and consequence in Open issues. Return the need for any other specialist to the orchestrator; do not spawn agents.

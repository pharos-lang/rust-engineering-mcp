# Package D01 — Documentation synchronisation for M3 (documentation worker)

Task / ID: D01. Objective: bring the public and specialised documentation in line with what M3 has decided and implemented so far, marking clearly what is still under qualification. Model/CLI/effort: GPT-5.6 Luna via codex exec, medium.
## Common rules
Repo /Users/cburgosro/Projects/rust-mcp, branch ai/m3-quality. Orchestrator Claude Fable 5.1 → Claude Opus 5 (owner relay). No commits/merge/push. Docs only: you own README.md, CHANGELOG.md, SECURITY.md, docs/architecture.md, docs/security-model.md, docs/compatibility.md, docs/client-configuration.md, docs/implementation-status.md, docs/adr/ADR-063-m3-guest-plugin-provisioning.md (path fix + built image ID + receipt links), docs/adr/ADR-061-private-quality-artifact-store.md (ONLY add an "Implementation notes" paragraph at the end: M2 `StateRoot` refactor deferred — the shared primitive serves the quality store; `UnsupportedStateRoot` variant; `.trunc` marker; cursor grammar 49 bytes; cfg macOS+aarch64), docs/roadmap/m3-quality.md (ONLY the Estado line: "In progress"), docs/tools.md (ONLY if the integrator has not yet written the `rust.test.nextest` section — check first; if it exists, do not touch tools.md). Do NOT edit docs/validation/M3-matrix.md, M3-01.md or delegation records (integrator/validator own them) nor any code/fixtures/scripts. Read first: AGENTS.md (README rules), ADR-060/061/062/063 (Accepted), docs/validation/M3-provisioning.json, M3-image-config.json, docs/validation/m3-delegation/{I02-integration-nextest,I17d-store-fixes,P02-provision-run}/last-message.md, docs/validation/M3-01.md, and the existing M2 wording you must preserve (do not rewrite M2 facts or receipts).
## Content to produce (facts only; label pending qualification explicitly)
- README (user guide): M3 status section: `rust.test.nextest` available as tool 19 in the 0.3.0-dev checkout (synchronous mode, ≤60 s qualified-short rule, `execution_mode` field, Tasks not yet advertised), how the host enables the M3 guest image (new image ID, provisioning via fixtures/rust-runtime with `--plugins`, owner authorization, versions: nextest 0.9.143, llvm-cov 0.9.0 + llvm-tools 1.98.1, semver-checks 0.50.0, mutants 27.1.0 source-built), the persistent private quality artifact store (state root, TTL 1 h default, quotas, `quality-artifacts recover|prune` CLI — mark "in integration" if main.rs lacks it), what is NOT yet available (coverage/semver/mutation tools: in progress; Tasks: pending M3-02), platform limits (macOS ARM64/APFS positive; Linux/Windows fail closed). Keep README a user guide; evidence goes to validation docs.
- CHANGELOG: `0.3.0-dev` (unreleased) entries: added/changed/security with links to ADRs; M2 entries untouched.
- SECURITY.md: new privacy boundary of persistent artifacts (uid + state root + host-granted root re-access; retention/TTL; not peer isolation), quality seccomp profile (pending ADR-064 acceptance — state that a dedicated profile for quality jobs exists/is being qualified and that M1/M2 profiles are unchanged), the guest image change, task IDs are not authority (ADR-060), reporting scope unchanged.
- docs/security-model.md: sections for job lifecycle (single worker permit = job permit; poll/cancel; masked errors; TTL), quality artifact store (layout, owner binding, reservation/floor, watermark, quarantine, ArchiveBundle hostile content policy), nextest execution (closed argv, config ingest, export by bytes from fixed path, JUnit parser limits), image identity and plugin provenance; mark measured vs proposed numbers.
- docs/compatibility.md: image ID + plugin versions table; five protocol versions unchanged; Tasks extension status; tool count 19 (and "20/21/22 planned in M3" without announcing them as available).
- docs/client-configuration.md: `--state-root` now also hosts `rust-mcp-quality-artifacts-v1`; recovery/prune procedure for quality artifacts (mirroring the M2 section's tone); `execution_mode` usage; Resource URI `rust-quality-artifact://` reading (index + chunks) — Stage 1 availability note.
- docs/architecture.md: M3 sections: domain job/nextest/quality_artifact modules, application JobExecutor/registry/ports, execution-adapter nextest gateway phases and quality seccomp profile, project-adapter quality store and shared primitives, MCP tasks handlers (dormant) and tool 19.
- docs/implementation-status.md: M3 table (M3-01 In progress/Blocked-on-seccomp per M3-01.md; M3-02..06 Not started/In progress as true today), D06/D17/D18/provisioning Done (decided) with ADR links, assessment rows updated (image, tools count 19 opt-in in checkout, store), "Backlog inmediato" M3 row → In progress; do not touch M0/M1/M2 rows.
- ADR-063: fix the llvm-tools archive member paths (`llvm-tools-1.98.1-aarch64-unknown-linux-gnu/llvm-tools-preview/lib/rustlib/...` per P02), add "Built image" subsection with the ID `sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a`, config digest, verification 47/47, receipts.
Checks: `git diff --check` on your files; internal link check (python one-liner over relative links you added); no promises of unqualified capabilities. Deliver standard sections with SHA-256 per file.

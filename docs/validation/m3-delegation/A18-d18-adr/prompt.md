# Package A18 — Decide D18: coverage metrics/merge and SemVer baseline (ADR-062 draft)

Task / ID: A18 (prerequisite of M3-03/M3-04). Objective: write **ADR-062 — LLVM coverage accounting and authorized SemVer baselines** as *Proposed*.
Model / CLI / effort / reason: Claude Sonnet 5 via claude CLI as an explicitly assigned worker, effort high, tools restricted to Read/Grep/Glob and writing only docs/adr/ADR-062-*. Reason: bounded contract decision with strong dependence on official tool formats.

## Common rules (apply to every M3 package)
- Repo: /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (created from main `52396184e5b53983056791f62d9eecbab3954d15`). Orchestrator: Claude Fable 5.1. You do not delegate, commit, merge, push, install, download, or run Docker. You do not touch files outside your allowed list. Other workers are editing disjoint files concurrently in the same checkout: never run `git add/commit/stash/checkout/reset`, never reformat files you do not own, never run `cargo fmt` on the whole workspace (use `rustfmt --check <your files>` or `cargo fmt -- --check` read-only only).
- Normative sources: AGENTS.md; docs/roadmap/m3-quality.md; docs/roadmap/m2-m8.md (G1–G9); docs/roadmap/adr-backlog-m2-m8.md; docs/prompts/implement-m3.md; docs/implementation-status.md; docs/validation/M2-07.md; docs/architecture.md; docs/security-model.md; docs/tools.md; ADR-028/030/031/037/040/050..059. Read what your task needs fully; do not replace them with a summary.
- Invariants: hexagonal architecture (domain/application never depend on rmcp, Cargo, SQLite, LanceDB, Docker, std::process); Serde/Schemars types as the source of truth; one closed Execution Gateway; host grants authority (never peer/ID/fingerprint); no shell/free flags/env inheritance; all project code (tests, build.rs, proc macros, plugins) is hostile; network deny requires enforcement (fail closed); own I/O is handle-relative no-follow; cancel/EOF/timeout/expiry terminate and join the process tree before capacity is released; uncertain cleanup dominates the result; poll/result never take the job permit; unknown/partial/unavailable/skip never equal pass; ADR-050 local_coordinated (no broker/daemon/new account/collector); the 18 existing tools keep their schemas and semantics byte for byte; `rust.dependencies.inspect` stays out; no `ultracode`.
- ADR format: `# ADR-0NN — Title`, sections `## Status` (Proposed — pending orchestrator acceptance and independent review), `## Context`, `## Decision`, `## Alternatives considered`, `## Consequences`, `## Sources`. Verify the next free ADR number by listing docs/adr before writing; if your assigned number is taken, take the next free one and say so. Do NOT edit docs/adr/README.md (the documentation worker will index later).
- Delivery: your final message MUST contain sections in this order: Task, Result, Files changed (paths + SHA-256 via `shasum -a 256`), Tests executed (exact commands + exit codes + counts), Evidence, Risks, Decisions, Open issues. Numbers you propose (quotas/timeouts) are proposals until measured; label them so.

## Inputs specific to this package
- Official upstream documentation fetched by the orchestrator on 2026-09-05 UTC (read-only, cite file names): `/private/tmp/claude-501/-Users-cburgosro-Projects-rust-mcp/df0fc04b-d468-46d9-b6f0-e6ac1541e3c8/scratchpad/orch/pkg-R01-plugins/sources/` — `index.txt` (URL + HTTP status per file), `llvmcov-readme.txt`, `semver-readme.txt`, `nextest-*.txt`, `mutants-*.txt`, `releases-summary.json` (GitHub releases: cargo-llvm-cov v0.9.0 with aarch64-unknown-linux-gnu asset; cargo-semver-checks v0.50.0 with aarch64-unknown-linux-gnu asset; cargo-nextest 0.9.143; cargo-mutants v27.1.0 x86_64 only), `rust-llvm-tools-sha.txt` (official llvm-tools 1.98.1 aarch64 tarball SHA-256).
- Repository code to read: crates/application/src/source.rs, inspection.rs, test_run.rs, quality.rs; crates/domain/src/source.rs, test_run.rs, quality.rs, evidence.rs; crates/execution-adapter/src/rust_gateway.rs (how commands/args/env are fixed), project_metadata.rs; docs/tools.md (rust.test and rust.quality.gate sections) ; ADR-037/040; spec §26.2/26.4/104 (docs/spec/rust-engineering-mcp-propuesta-v0.3.md lines ~1522-1575 and ~4695-4720); docs/roadmap/m3-quality.md ("Oráculos por herramienta" paragraphs on coverage and semver); backlog D18.

## Deliverables
`docs/adr/ADR-062-coverage-accounting-and-semver-baselines.md` (Proposed) deciding:
- Coverage: exact plugin/LLVM identity to record (cargo-llvm-cov version, llvm-profdata/llvm-cov versions, rustc), authoritative JSON (which cargo-llvm-cov JSON: full export vs `--summary-only`; which fields become domain metrics: lines/regions/functions counts + covered + percent, per file/package), derived LCOV/HTML from the *same run* (two-phase `--no-report` then `report --json/--lcov/--html` from the same profraw/profdata — confirm from the README that this is how one run yields several formats), denominators/exclusions/cfg/generated/doctests flags explicitly reported, "zero data is not 100 %" rule (how detected), merge rules for multi-package (only identical source fingerprint/target/features/instrumentation; shared files counted once), baseline identity (source fingerprint + config, not branch name), partial/unavailable statuses and what never passes, and the artifacts to retain (JSON authoritative, LCOV/HTML as artifacts with sizes bounded — HTML is hostile content).
- SemVer: input contract = two authorized ProjectRefs (baseline/candidate) captured and revalidated in a stable lock order with distinct snapshot identity; no URL/version/registry/Git ref; identical selection/target/features/rustdoc/plugin between both; how to feed cargo-semver-checks a local baseline (`--baseline-root` with a captured copy inside the guest; confirm flag from README) with network denied and how to detect any attempt to fetch; outcome taxonomy breaking/no-break vs unavailable/incomplete (tool absent, lib target missing, rustdoc JSON failure, version mismatch, parser failure), exit-code calibration approach (exit codes must be measured against the pinned version in the guest — write the calibration protocol, do not copy numbers), report fields (item/lint/span/limitations), and the fixture families M3-04 must have (removed pub fn, trait method added, enum variant added non-exhaustive vs exhaustive, feature-gated item removed, incompatible baseline).
- Test/oracle list for M3-03 and M3-04 with a positive control each, including a coverage fixture with known counts and a repeated file across packages.
- Open issues that require the owner or a measurement.
Do not edit any other file.

# Package I02b — Apply the V00 review dispositions to the D06 core (same Sol session, resumed after I02)

The independent Opus 5 review of your I00 core is at docs/validation/m3-delegation/V00-jobexecutor-review/last-message.md. Verdict Revise. Apply ALL of the following (you now own these files as integrator; keep the 19 snapshots byte-identical; keep ADR-030 tests green):
- V00-01 (P1) fix: `watchdog` must not apply `work_deadline` to a record in `JobPhase::Cleanup`; in `finalize` an existing `completion` wins over a `Deadline` intent (publish `TimedOut` only when `completion.is_none()`); regression test: finish with cleanup pending → work deadline passes → observed cleanup → `Completed`, not `Failed`; and `ClientCancellation` recorded is never rewritten to `Failed`.
- V00-02 fix: join cleanup whenever `phase == Cleanup` regardless of cancellation; enforce `cleanup_deadline` → `CleanupFailed`/quarantine (never a work-budget timeout); test.
- V00-03 fix (integration obligation): provide `run_joined_with(permit, …)` (or equivalent) so a job holding the ADR-030 permit executes its own joined work; test `admit_job` + joined run executes instead of `Busy`; and the M3-01 vertical must actually use it.
- V00-04 fix: real MCP-level masking test with `Tasks::new(executor)` and two records (authorized / denied): byte-identical `-32602` + `"task unavailable"` + `data: None` for malformed/unknown/revoked/expired/foreign IDs.
- V00-05 fix: delete the fake `request_cancelled` assertion; with the production `JobSignal` you wire in integration, add the rmcp-level test that cancels the request context after the response is queued and asserts the job token is not cancelled and the job stays `Running` (D06-T06). Until it exists, do not credit D06-T06.
- V00-06 fix: call `request_cancellation()` outside the registry `mutate` lock (document non-blocking contract); add a bounded task-control deadline wrapper (2 s default / 5 s max) with just-under/over tests.
- V00-07 fix: measure the serialized completion at publish; reject/degrade to bounded omission when > reservation or > 512 KiB; exact-512-KiB and one-byte-over tests; a `finish` with uncertain cleanup holds the permit and does not publish (test the CleanupUncertain branch).
- V00-08/09/10/11: terminal phase/message; loops accumulate errors; enforce `can_transition_to` in finalize (or delete it — enforce is preferred); distinct error code for incomplete evidence; construct the structured `TASKS_REQUIRED` result for unqualified `auto` (D06-T02) now that the tool is registered.
- V00-12/13: add `JobOwnerBinding::derive(state_root_identity, uid, granted_root_identity, workspace_root)` in domain with domain separation + test, and use it from the authority adapter; artifact liveness at read time → member `Unavailable` (use the artifact port already available; Stage 0 M1 store liveness at minimum).
- Missing tests listed in §5 of the review: cancel during publication and during cleanup; cancellation racing the commit point (rollback branch); just-under 5 s seed-commit; poll-does-not-extend-TTL assertion that can actually fail; TracingJobEvents output asserted; second `tools/call` and Resource read return busy while `tasks/*` respond (adapter half of D06-T07).
Run the focused gates again (fmt on your files, check/clippy workspace, non-Docker tests, architecture check) and the Docker nextest runtime selection if any behaviour in the job path changed. Deliver the standard sections with an explicit table finding → fix location → test name.

## Escalation answer: seccomp for cargo-nextest (orchestrator decision)
Your I02 finding: on the new image, `cargo nextest run` exits 101 because Tokio's signal handling creates a `UnixStream` and the current `seccomp-rust.json` denies it (only a restricted `socketpair` shape is allowed). Decision, following the ADR-056 precedent (dedicated profile with the narrowest syscalls, Docker network none unchanged):
1. Create `crates/execution-adapter/src/seccomp-rust-quality.json` derived byte-for-byte from `seccomp-rust.json` plus ONLY the syscall/arg shapes actually required, established empirically: first identify the exact failing call (Tokio 1.x `UnixStream::pair` → `socketpair(AF_UNIX, SOCK_STREAM|SOCK_CLOEXEC|SOCK_NONBLOCK, 0)`; or `socket(AF_UNIX, ...)` + `bind/connect`) by reading the pinned tokio/mio source in ~/.cargo/registry and the nextest error text; allow that exact family+type+flags shape for AF_UNIX only; never AF_INET/AF_INET6/AF_NETLINK; never `bind/listen/accept` unless proven necessary. Record the evidence (source file:line of the syscall, the seccomp rule added) in a new ADR-064 "Quality job seccomp profile" (Proposed → Accepted by the orchestrator upon its containment review), with Context/Decision/Alternatives/Consequences/Sources.
2. Use the quality profile ONLY for the M3 quality commands (nextest now; coverage/semver/mutants later if they need it, each documented); M1/M2 commands keep `seccomp-rust.json`/`seccomp-rust-fix.json` byte-identical (add a test asserting the M1 profile bytes and the phase→profile mapping).
3. Negative controls in the Docker runtime tests: with the quality profile, a fixture test that opens an AF_INET/AF_INET6 socket or `connect`s to 127.0.0.1 is denied; a fixture that tries `bind` on a unix path outside /work is denied (if `bind` is not allowed); network none still holds (existing probes). Positive control: the passing fixture completes under nextest with exit 0.
4. Include the profile bytes' SHA-256 in the gateway configuration fingerprint and recalibrate.
This authorization does not extend to any other containment change; escalate again if nextest needs more.

## Scope additions for this resume (after your I02 delivery)
A. Seccomp: authorized as described above — implement `seccomp-rust-quality.json` (+ ADR-064 Proposed), the phase→profile mapping test, negative controls, fingerprint inclusion; then re-run the M3 runtime gate (`scripts/test-m3-runtime.py`) completely under Docker with a fresh output dir, then the existing Rust security gate again only if the profile mapping touched shared code paths (state why). Keep `docs/validation/M3-01-runtime.json` history: write the new receipt as the current file and preserve the failed one as `M3-01-runtime-attempt1.json` (do not rewrite bytes).
B. Stage 1 wiring: the durable store is complete and reviewed (I17c + I17d; see docs/validation/m3-delegation/I17d-store-fixes/last-message.md). Wire it into the nextest publication path when a state root is configured (Stage 1 default; Stage 0 fallback when absent or on `UnsupportedStateRoot`/`Busy` at attach per F9), implement the `QualityAuthority` over the live `ProjectRegistry` (owner facts: granted-root dev/ino + workspace root from the lease), register the Resource reader (`Resources::with_quality_reader`), add the CLI subcommands `quality-artifacts recover|prune` in main.rs over the library functions, charge ArchiveBundle entry counts against the job member budget (F6), and add MCP-level tests: JUnit read back via `rust-quality-artifact://` index + chunk, expired member → not found + task projection `Unavailable`, Stage 0 fallback when no state root.
C. Calibration record: with the quality profile in place, pin `NextestExit` codes from the observed runs (passing 0, failing ?, build error ?, timeout/cancel ?) and the leak/flaky/retry JUnit shapes from the real fixtures; update the M3-01 evidence document accordingly.
D. Sync qualification: keep the sync rule (auto|synchronous ≤ 60 s) and record the measured wall times of the passing fixture (cold/warm, at least 5 runs each here; the 30/30 series is M3-02's) in docs/validation/M3-01.md.
Do all of A–D; if A is blocked again by a second syscall, stop A at that point with the exact evidence and finish B–D with the fixtures that do run (or unit level), reporting precisely what remains.

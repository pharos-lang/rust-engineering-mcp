# Package I17c — Complete the ADR-061 durable quality artifact store (Claude Opus 5 worker, delimited critical implementation)

You are a Claude Opus 5 worker explicitly assigned by the orchestrator (Claude Fable 5.1) to a delimited, security-critical implementation: finishing the owner-bound durable artifact store decided in docs/adr/ADR-061-private-quality-artifact-store.md (Accepted 2026-09-06). Two previous Terra sessions (packages I17/I17b, see docs/validation/m3-delegation/I17*/last-message.md) left an honest skeleton: domain descriptor types, application ports, URI parsing/Resource reader hook (`Resources::with_quality_reader`), an APFS adapter skeleton with a free-space floor, three placeholder native ignored tests. Everything else in the ADR is still missing. Your job: make it complete, tested and honest. Do not stop at "partial" unless a true blocker outside your ownership persists after you have done everything else; in that case finish every independent item first.

## Ownership (create/edit ONLY these)
crates/domain/src/quality_artifact.rs, crates/domain/tests/quality_artifact.rs, crates/application/src/quality_artifact.rs, crates/application/tests/quality_artifact.rs, crates/project-adapter/src/quality_artifact_store.rs, crates/project-adapter/src/filesystem/macos/quality.rs, crates/project-adapter/src/filesystem/macos/state_primitives.rs, crates/project-adapter/tests/quality_artifact_store.rs, crates/project-adapter/src/filesystem.rs (only the existing re-export line region), crates/project-adapter/src/lib.rs and filesystem/macos.rs (mod/pub lines only), crates/mcp-server/src/stdio/resources.rs + resources/tests.rs (quality scheme only; `rust-artifact://` byte-identical), and at most ONE minimal `pub` wrapper at the end of crates/execution-adapter/src/mutation_archive.rs for closed-USTAR decode/canonical re-encode. Nothing else. Never edit job.rs, nextest*, rust_gateway.rs, stdio.rs, docs, fixtures, scripts. No git commands, no docker, no network.

## Rules
Read fully first: ADR-061 (revised), ADR-060 (JobId, owner binding, 512 KiB, busy policy), ADR-028, ADR-050/052/053/054/059, docs/security-model.md artifacts/state-root sections, crates/project-adapter/src/mutation_state.rs and filesystem/macos/mutation.rs (StateRoot pattern: openat/renameat/unlinkat, private-file checks, fsync/F_FULLFSYNC ordering, flock), crates/application/src/artifact_access.rs and lib.rs (ProjectRef/lease model), crates/mcp-server/src/stdio/resources.rs (M1 scheme), crates/execution-adapter/src/mutation_archive.rs (closed USTAR profile), and the current skeleton files. Invariants: hexagonal; handle-relative no-follow I/O only via rustix (no new dependencies; no `libc`); domain/application without rustix/tokio/rmcp; no unwrap/expect/panic on normal paths; tests first; the M2 mutation store, journals and ALL existing project-adapter tests must remain byte-identical/passing.

## Required scope (all)
1. Free-space floor (exists — verify) under `store.lock` (exclusive non-blocking flock; busy → bounded rejection).
2. Reservation files with best-effort `rustix::fs::fallocate` and exact byte caps; ENOSPC/short write → fail closed, release, no descriptor.
3. Publication order: blob temp→fsync→rename→dir fsync→descriptor temp→fsync→rename→dir fsync; descriptor = commit marker; truncate-to-actual after descriptor publication with the accounting rule stated in code docs.
4. Clock watermark file, atomic advance, regression → quality store fails closed (scoped) until `recover`; hybrid clock rule.
5. Reconciliation on open: trust only fully validated v1 descriptors (schema, private regular file, link count 1, size, SHA-256 of blob); discard own reservation temps without descriptor; quarantine anything unknown/corrupt with a reason file; never touch `rust-mcp-mutations-v1`.
6. Reads: authorized by owner-binding revalidation through a `QualityAuthority` port that the integrator implements over the live `ProjectRegistry` (design the trait now; test double in tests); expired → not found; never renew artifact TTL nor project lease; chunk 320 KiB max; index page 64 members, cursor ≤128 bytes canonical; total MCP response ≤512 KiB including base64 (enforce + test).
7. Owner binding: domain-separated SHA-256 over (state-root dev/ino, host uid, granted-root dev/ino, workspace-root string), computed in the adapter from values the authority port provides.
8. ArchiveBundle: revalidate with the closed USTAR profile via the mutation_archive wrapper; reject links/`..`/devices/PAX/GNU/oversize/over-count; canonical re-encode; one member `application/x-tar`; never extract to host paths.
9. `recover(state_root) -> RecoveryReport` and `prune_expired(state_root) -> PruneReport` library functions (the integrator wires the CLI later).
10. Test-only fault injection port: ENOSPC mid-stream; crash between blob rename→descriptor rename and between descriptor rename→dir fsync; watermark regression.
11. Quotas as named constants with units: 32 MiB/artifact, 64 MiB & 128 members/job, 128 MiB/owner, 256 MiB/global, TTL 3600 s default / 86400 s host max.
12. Linux/Windows → UnsupportedPlatform before any effect (compile-guarded; test on cfg).
13. Tests: every oracle row of the ADR table with its positive control. Ordinary tests wherever a temp dir on APFS suffices (they run in core); `#[ignore]`d `native_apfs_quality_*` only where truly needed (two-process lock, headroom with real fstatfs, crash points if they need real fsync ordering). You are on macOS/APFS: run the ignored ones explicitly too (`cargo test -p rust-engineering-project --test quality_artifact_store --locked --offline -- --ignored`) and report counts. Also run `cargo test -p rust-engineering-project --locked --offline` (all), `cargo clippy -p rust-engineering-project -p rust-engineering-application -p rust-engineering-domain --all-targets --locked --offline -- -D warnings`, resources tests in `-p rust-engineering-mcp` (`cargo test -p rust-engineering-mcp resources --locked --offline`), and `python3 -B scripts/check-architecture.py`. If a shared crate outside your ownership does not compile, record the exact error and retry every 2 minutes up to 20 minutes; meanwhile keep working on what compiles.
Delivery: Task, Result, Files changed (+full SHA-256), Tests executed (commands, exit codes, counts incl. ignored run explicitly), Evidence (oracle row → test name), Risks, Decisions, Open issues. Tools: Read/Grep/Glob/Edit/Write; Bash limited to cargo, rustfmt, shasum, ls, python3 -B scripts/check-architecture.py.

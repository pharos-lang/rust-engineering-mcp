# Package I03 — M3-03 `rust.coverage`: instrumented run, LLVM JSON accounting, LCOV/HTML derived from the same run (adapter + application + MCP worker)

Task / ID: I03 (M3-03). Objective: implement `rust.coverage` end to end per ADR-062 (Accepted): closed RustCommand variants for the two-phase cargo-llvm-cov flow (`--no-report` run, then `report --json`, `report --lcov`, `report --html` on the same profdata), bounded parsing of the full llvm-cov JSON into typed domain metrics with the zero-denominator rule and shared-file dedupe, derived LCOV/HTML retained as artifacts (HTML as one `ArchiveBundle`), plugin identity probe (LlvmCovVersion + llvm-tools identity), a `JobKind::Coverage` job through the existing JobExecutor, DTO/schema/snapshot for the 20th tool, Docker runtime tests with a fixture of known counts, and docs/tools.md section.
Model / CLI / effort: (assigned by orchestrator) — see launch record.
## Common rules
Repo /Users/cburgosro/Projects/rust-mcp, branch ai/m3-quality. Orchestrator Claude Fable 5.1. No commits/merge/push/installs/downloads/image changes. Read fully: ADR-062, ADR-060, ADR-061, ADR-063, docs/validation/m3-provisioning/help/cargo-llvm-cov-help.stdout and cargo-llvm-cov-report-help.stdout (verbatim pinned help), /private/tmp/claude-501/-Users-cburgosro-Projects-rust-mcp/df0fc04b-d468-46d9-b6f0-e6ac1541e3c8/scratchpad/orch/pkg-R01-plugins/sources/llvmcov-readme.txt, the M3-01 integration as landed (crates/execution-adapter/src/nextest_gateway.rs, nextest_port.rs, rust_gateway.rs; crates/application/src/{job,nextest,quality_artifact}.rs; crates/mcp-server/src/stdio/{nextest.rs,tasks.rs,stdio.rs}) and docs/validation/m3-delegation/I02-integration-nextest/last-message.md. Follow the nextest vertical's patterns exactly (single worker, budgets, Stage 0/Stage 1 artifact publication, execution_mode, 512 KiB budget, snapshots).
## Ownership
New files: crates/domain/src/coverage.rs (+ `pub mod` line), crates/domain/tests/coverage.rs, crates/application/src/coverage.rs (+ `pub mod` line; `ProjectCoveragePort`, `CoverageOptions` with the closed selection grammar, `CoverageObservation` typed summary + artifact streams), crates/application/tests/coverage.rs, crates/execution-adapter/src/coverage_gateway.rs, coverage_json.rs (bounded parser), coverage_port.rs, tests/coverage_runtime.rs (Docker, ignored), crates/mcp-server/src/stdio/coverage.rs (+ tests), tests/snapshots/coverage-tool.json, fixtures/coverage/{known-counts,shared-file-workspace,zero-denominator,no-tests} (hand-written Cargo.lock; do not cargo-run fixtures on the host), docs/tools.md section (append), docs/validation/M3-matrix.md row M3-03. Minimal edits: crates/domain/src/rust_execution.rs (add variants `CoverageRun(CoverageOptions)`, `CoverageReport(CoverageReportFormat)`, `LlvmCovVersion`; nothing else), crates/execution-adapter/src/rust_gateway.rs (argv/env arms for the new variants + fingerprint enumeration; env must add `LLVM_COV`/`LLVM_PROFDATA` fixed sysroot paths and `CARGO_LLVM_COV_TARGET_DIR=/work/llvm-cov-target`; never `--doctests`, never `--branch/--mcdc`, never custom ignore regex), crates/execution-adapter/src/lib.rs, crates/application/src/job.rs ONLY to add the `JobKind::Coverage` arm where the nextest arm is (read how I02 wired TestNextest), crates/mcp-server/src/stdio.rs (register tool 20 exactly like tool 19), tests/protocol.rs (extend counts to 20). Do not touch nextest/semver/mutation files beyond these arms.
## Requirements (from ADR-062)
Two-phase same-run: run `--no-report` (with `--frozen --offline`, closed selection, `--jobs 1`) then three `report` invocations against the same profdata (`--json --output-path /work/coverage/coverage.json`, `--lcov --output-path /work/coverage/lcov.info`, `--html --output-dir /work/coverage/html`); egress each from fixed guest paths (JSON/LCOV as regular files; HTML dir as one closed-tar ArchiveBundle); JSON authoritative: parse the full export with a bounded streaming/limited parser (depth/size limits; the file may be large — parse per-file summaries and totals, never materialise unbounded DOM), compute per-file/per-package/aggregate `lines/regions/functions {count, covered, percent}`; a scope with count 0 is ABSENT from percent-bearing metrics (type-enforced); dedupe shared files across packages in the aggregate; record `cargo_llvm_cov.version` from the JSON and compare with the pinned identity (mismatch → Blocked); record effective default ignore set, cfg(coverage) state, doctests NotRun, target, selection; MCP structured content = bounded summary (aggregate + per-package; per-file rows paginated via artifact with max rows and omission flag); status: Unavailable (plugin/llvm-tools absent or version mismatch, detected before running), Blocked (timeout/output limit/incomplete parse/zero-denominator request), Passed (complete); no threshold Failed in M3. Budgets per ADR-060 job table (default 300 s execute, max 3,600 s; sync only if qualified short — define the rule as for nextest). Artifacts: Stage 0 via M1 store with truncation flags; Stage 1 hook to the durable store (match the nextest integration point).
## Tests
Unit: parser with hostile inputs (fixtures/hostile-reports/coverage-deep.json, huge file lists, missing fields, version mismatch), argv snapshots per option, zero-denominator type invariants, dedupe. Docker (ignored, exact names): known-counts fixture → exact line/region/function counts asserted (hand-computed; write the expected numbers in fixtures/coverage/README.md and derive them from a first calibration run only if you record it as calibration, not as oracle), shared-file dedupe, zero-denominator absence, three formats agree on totals, no-tests fixture, timeout mid-build → Blocked, cancel/EOF with active child → cleanup joined, hostile HTML (external URI/script) stored as ArchiveBundle without preview. Run the Docker tests yourself only if the package launch record says you have Docker access; otherwise mark them for the integrator. Focused gates for your crates + `python3 -B scripts/check-architecture.py`; the 19 existing snapshots must stay byte-identical.
Delivery: standard sections with SHA-256 and exact test counts.

## Shared-file discipline for this launch (important)
Sol (integrator, package I02b) is concurrently editing crates/application/src/job.rs, crates/execution-adapter/src/rust_gateway.rs (seccomp profile mapping), crates/mcp-server/src/stdio.rs, stdio/nextest.rs, stdio/tasks.rs, main.rs and the quality-store wiring; Claude Opus finished the store (do not touch quality_artifact* files). Build your vertical in YOUR new files first so they compile standalone (own modules, own tests). Only at the END add the minimal arms/lines to the shared files (`rust_execution.rs` variants, `rust_gateway.rs` argv/env arm + fingerprint entry, `job.rs` JobKind arm, `stdio.rs` registration, `protocol.rs` count), re-reading each shared file immediately before each patch and patching only the lines you add. Do NOT run `cargo fmt` on the workspace; format only your files. You have NO Docker access in this session: write the Docker tests as `#[ignore]` with exact names and expected oracles; the integrator/validator runs them with the image afterwards; parser goldens that need real tool output are produced in a follow-up calibration session — design the parser against the pinned `--help` text and the official docs now, with clearly labelled provisional fixtures. Model/CLI/effort per the launch record.

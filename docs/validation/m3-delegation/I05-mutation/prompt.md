# Package I05 — M3-05 `rust.mutation.test`: baseline, private mutated copy, outcomes/diffs/artifacts (Claude Opus 5 worker — delimited containment-critical implementation)

You are a Claude Opus 5 worker assigned by the orchestrator (Claude Opus 5 main session) to the containment-critical vertical of M3: running `cargo-mutants` over hostile project code inside a private writable copy, without ever touching the host source. Implement it completely; do not deliver a skeleton.

## Repository state you build on (read these first)
Branch `ai/m3-quality` (uncommitted, shared). Landed and qualified: M3-01 `rust.test.nextest` (tool 19) end to end — read `docs/validation/M3-01.md` and `docs/validation/m3-delegation/I02b-v00-fixes/last-message.md` for the exact vertical pattern; `crates/execution-adapter/src/nextest_gateway.rs` (report volume + guardian + fixed-argv `tar` exporter + strict decoder), `crates/execution-adapter/src/rust_applied.rs` (applied-mount verifier), `crates/execution-adapter/src/seccomp-rust-quality.json` + `docs/adr/ADR-064-quality-job-seccomp-profile.md` (only AF_UNIX anonymous stream `socketpair` added), the production `JobExecutor`/registry (`crates/application/src/job.rs`, `crates/domain/src/job.rs`), Stage 0/Stage 1 artifact publication and `QualityAuthority` (`crates/application/src/quality_artifact.rs`, `crates/project-adapter/src/quality_artifact_store.rs`), MCP tool wiring in `crates/mcp-server/src/stdio.rs` + `stdio/nextest.rs`.
M2 staging you must reuse for the private writable copy: `crates/execution-adapter/src/mutation_gateway.rs` (tmpfs volume `size=64m,nr_inodes=8192,uid=65534,gid=65534,mode=0700,nosuid,nodev,noexec`, Guardian/Ingest/…/Export phases, cleanup with its own never-cancelled budget and quarantine), `rust_applied.rs`, `mutation_archive.rs` (closed USTAR profile), ADR-053/054/056.
Decisions: `docs/roadmap/m3-quality.md` (mutation paragraph), ADR-060 (job budgets, execution_mode, masked errors), ADR-061 (artifact members, ArchiveBundle), ADR-063 (image), ADR-064 (seccomp). Official tool docs captured offline: `/private/tmp/claude-501/-Users-cburgosro-Projects-rust-mcp/df0fc04b-d468-46d9-b6f0-e6ac1541e3c8/scratchpad/orch/pkg-R01-plugins/sources/mutants-{out,exit,timeouts,inplace,baseline,nextest}.txt` and the pinned help `docs/validation/m3-provisioning/help/cargo-mutants-help.stdout` (cargo-mutants 27.1.0 in the guest image `sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a`). Fixtures already exist: `fixtures/mutation/{caught-all,missed-one,timeout-loop,unviable,baseline-failing,hostile-writer}` + `canary.txt`, with oracles in `fixtures/mutation/README.md` and `docs/validation/M3-05-fixtures.md`.

## Ownership (create/edit only these)
New: `crates/domain/src/mutation_test.rs` (+ one `pub mod` line), `crates/domain/tests/mutation_test.rs`, `crates/application/src/mutation_test.rs` (+ mod line), `crates/application/tests/mutation_test.rs`, `crates/execution-adapter/src/mutation_test_gateway.rs`, `crates/execution-adapter/src/mutation_outcomes.rs`, `crates/execution-adapter/src/mutation_test_port.rs`, `crates/execution-adapter/tests/mutation_runtime.rs`, `crates/mcp-server/src/stdio/mutation_test.rs` (+ its tests module), `crates/mcp-server/tests/snapshots/mutation-test-tool.json`, `docs/validation/M3-05-mutation-calibration.md`.
Minimal shared patches, re-read immediately before each edit and add only your lines: `crates/domain/src/rust_execution.rs` (`MutationTest(...)`, `MutantsVersion`), `crates/execution-adapter/src/{lib.rs,rust_gateway.rs,rust_applied.rs,mutation_gateway.rs}` (argv/env/phase/verifier/fingerprint entries), `crates/application/src/{lib.rs,job.rs}` (`JobKind::MutationTest` arm), `crates/mcp-server/src/stdio.rs` (tool registration), `crates/mcp-server/tests/protocol.rs` (inventory), `docs/tools.md` (append a section), `docs/validation/M3-matrix.md` (M3-05 row), `docs/implementation-status.md` (M3-05 row).
Never touch: quality_artifact* files, nextest*, coverage*, semver* (other workers own them right now), scripts/gate.py, fixtures/ (read-only for you), M2 mutation journal code paths beyond additive phases.

## Contract and invariants (non-negotiable)
- Input DTO: `project_ref`, closed selection grammar (package/features/all_features/no_default_features/target), `max_mutants` 1..=100 (default 100), `mutant_timeout_seconds` 1..=60 (default 60), `execution_mode` (`auto|task|synchronous`, same rule as nextest: only a qualified-short run may go synchronous — a real mutation run will normally exceed it and must return the structured `TASKS_REQUIRED` remediation while Tasks advertisement is off).
- Baseline is mandatory (`--baseline auto`): a failing baseline is a valid tool outcome (`failed`, isError=false) with the baseline evidence, never a clean mutation report.
- Outcomes come ONLY from `mutants.out/outcomes.json` plus the `caught/missed/timeout/unviable` lists, parsed by a bounded JSON parser with depth/size/count limits; human text is never an oracle; forged `mutants.out`-looking output from the project must not be trusted (the hostile-writer fixture emits some).
- `missed >= 1` → tool status failed. `timeout`/`unviable`/incomplete never count as clean. Every count carries its explicit denominator (viable/total/tested).
- The mutated copy lives only in the M2-style private tmpfs volume; `/source` from the host capture is read-only for the mutators; nothing is ever written back to the host and no mutated source is exported — only report bytes (`outcomes.json` and the bounded `diff/` + `logs/` as one `ArchiveBundle`) leave through the fixed-argv exporter.
- Budgets: per-mutant timeout ≤ 60 s (`--timeout`), build timeout bounded (`--build-timeout`), total within the ADR-060 job execute budget (default 300 s, max 3600 s); `--jobs 1`, `--no-shuffle`, `--cargo-arg=--offline`, `--cargo-arg=--frozen`, `--output` inside the report volume. No sharding, no `--in-place`, no free flags.
- Quality seccomp profile for the run phase (cargo build/test spawn processes), Docker network none, non-root, read-only root, bounded tmpfs; cleanup terminates and joins the tree before capacity is released and quarantines on uncertainty.
- Privacy: `mutants.out/lock.json` records username/hostname from inside the guest — assert they are the guest values (`nobody`/container id) and redact if anything host-shaped appears.
- Host source intact: compare the captured source fingerprint before/after and assert `fixtures/mutation/canary.txt` is unmodified.

## Tests
Unit (must run here): outcomes parser against hostile/edge JSON (truncated, deeply nested, unknown fields, forged), argv snapshots per option, budget/limit validation (exceed cases), denominator invariants, the "missed → failed" mapping, baseline-failure mapping, redaction of lock.json identity.
Docker runtime (`crates/execution-adapter/tests/mutation_runtime.rs`, all `#[ignore]` with exact names — you have **no Docker access in this window**; list the names in your report for the validator): caught-all → clean, missed-one → failed with the named function, timeout-loop → timeout class, unviable → unviable class, baseline-failing → baseline failure/exit 4 path, hostile-writer → containment (no host write, no surviving child, no network, bounded output, forged output not trusted), host source + canary intact, `max_mutants` cap enforced, cancel/EOF with an active child → joined cleanup and zero leftover containers/volumes, ArchiveBundle of diffs bounded and never extracted to host paths.
Gates you must run: `cargo fmt --check`, `cargo check --workspace --all-targets --locked --offline`, `cargo clippy --workspace --all-targets --locked --offline -- -D warnings`, `cargo test --workspace --locked --offline`, `python3 -B scripts/check-architecture.py`. Pre-existing tool snapshots byte-identical; yours is new. Concurrency: two other workers are editing shared files; retry transient breakage every 2 minutes for up to 20 minutes and keep working on what compiles; never run workspace `cargo fmt` (only `cargo fmt --check`, and `rustfmt --edition 2024 <your files>` to format).
Escalate instead of deciding alone: any need for a syscall beyond ADR-064, any host-writable path, any weakening of the M2 staging guarantees.
Delivery: Task, Result, Files changed (+full SHA-256), Tests executed (commands, exit codes, counts), Evidence (oracle → test name), Risks, Decisions, Open issues (with the exact ignored Docker test names).
Tools: Read/Grep/Glob/Edit/Write; Bash limited to cargo, rustfmt, shasum, ls, sleep, python3 -B scripts/check-architecture.py. No git, no docker, no network.

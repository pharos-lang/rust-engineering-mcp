# Package S01 — Diagnose and fix the flaky durable-store expiry test (Claude Opus 5 worker, small delimited task)

Context: you (a previous Opus session, packages I17c/I17d) implemented the ADR-061 durable quality artifact store. During a later full-workspace run the integrator observed **one independently flaky test in the durable store's expiry area** (`crates/project-adapter/tests/quality_artifact_store.rs`, the expiry/TTL family — e.g. `apfs::expiry_reclaims_only_known_bytes_and_reads_never_renew` or `apfs::expired_evidence_and_claims_stop_being_charged_and_leave_the_volume`). A flaky oracle is a real defect: it either hides a race in the store or asserts something time-dependent that the implementation does not guarantee.

Task: find the actual cause and fix it properly.
1. Reproduce: run the expiry-family tests in a loop (at least 30 iterations, both `--test-threads=1` and the default parallel mode, and with the ignored native ones included) and capture the failure text. Use `cargo test -p rust-engineering-project --test quality_artifact_store --locked --offline` with a name filter, and a shell loop.
2. Diagnose honestly: is it (a) a test that depends on wall-clock progression or on `SystemTime::now()` between two assertions, (b) a real race between reclamation under `store.lock` and a concurrent read/attach, (c) an ordering assumption about `reclaim` running inside `reserve`/`reconcile`, or (d) filesystem timestamp granularity on APFS? State the evidence.
3. Fix the cause, not the symptom: prefer making the store's behaviour deterministic (inject the clock where a test needs to control it — the store already has a clock concept; do not add sleeps) over loosening an assertion. If the correct fix is that the *test* was over-specified, say so explicitly and keep an assertion that still discriminates the ADR-061 rule it protects (TTL never renewed by reads; only known bytes reclaimed).
4. Re-run the whole file 30× to show stability, plus `cargo test -p rust-engineering-project --locked --offline` (M2 mutation-store tests must stay 20/20) and `cargo clippy -p rust-engineering-project --all-targets --locked --offline -- -D warnings`.

Ownership: ONLY `crates/project-adapter/tests/quality_artifact_store.rs`, `crates/project-adapter/src/filesystem/macos/quality.rs`, `crates/project-adapter/src/quality_artifact_store.rs`, `crates/domain/src/quality_artifact.rs`, `crates/application/src/quality_artifact.rs` (and their test files). The integrator is running a long Docker qualification concurrently and owns everything else — do not touch any other file, do not run Docker, no git, no network, no workspace `cargo fmt`. If the workspace fails to compile because of the integrator's in-flight edits, retry every 2 minutes for up to 20 minutes.
Delivery: Task, Result, Root cause (with the captured failure), Files changed (+full SHA-256), Tests executed (including the 30× loop and its pass count), Evidence, Risks, Decisions, Open issues.

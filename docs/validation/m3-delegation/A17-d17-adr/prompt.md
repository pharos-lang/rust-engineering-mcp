# Package A17 — Decide D17: private persistent artifacts (ADR-061 draft)

Task / ID: A17 (prerequisite of M3-01). Objective: write **ADR-061 — Owner-bound private artifact store for quality jobs** as *Proposed*.
Model / CLI / effort / reason: GPT-5.6 Terra via codex exec, effort high, sandbox workspace-write. Reason: adapter/persistence-heavy decision with an already established M2 durable-store pattern to reuse.

## Common rules (apply to every M3 package)
- Repo: /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (created from main `52396184e5b53983056791f62d9eecbab3954d15`). Orchestrator: Claude Fable 5.1. You do not delegate, commit, merge, push, install, download, or run Docker. You do not touch files outside your allowed list. Other workers are editing disjoint files concurrently in the same checkout: never run `git add/commit/stash/checkout/reset`, never reformat files you do not own, never run `cargo fmt` on the whole workspace (use `rustfmt --check <your files>` or `cargo fmt -- --check` read-only only).
- Normative sources: AGENTS.md; docs/roadmap/m3-quality.md; docs/roadmap/m2-m8.md (G1–G9); docs/roadmap/adr-backlog-m2-m8.md; docs/prompts/implement-m3.md; docs/implementation-status.md; docs/validation/M2-07.md; docs/architecture.md; docs/security-model.md; docs/tools.md; ADR-028/030/031/037/040/050..059. Read what your task needs fully; do not replace them with a summary.
- Invariants: hexagonal architecture (domain/application never depend on rmcp, Cargo, SQLite, LanceDB, Docker, std::process); Serde/Schemars types as the source of truth; one closed Execution Gateway; host grants authority (never peer/ID/fingerprint); no shell/free flags/env inheritance; all project code (tests, build.rs, proc macros, plugins) is hostile; network deny requires enforcement (fail closed); own I/O is handle-relative no-follow; cancel/EOF/timeout/expiry terminate and join the process tree before capacity is released; uncertain cleanup dominates the result; poll/result never take the job permit; unknown/partial/unavailable/skip never equal pass; ADR-050 local_coordinated (no broker/daemon/new account/collector); the 18 existing tools keep their schemas and semantics byte for byte; `rust.dependencies.inspect` stays out; no `ultracode`.
- ADR format: `# ADR-0NN — Title`, sections `## Status` (Proposed — pending orchestrator acceptance and independent review), `## Context`, `## Decision`, `## Alternatives considered`, `## Consequences`, `## Sources`. Verify the next free ADR number by listing docs/adr before writing; if your assigned number is taken, take the next free one and say so. Do NOT edit docs/adr/README.md (the documentation worker will index later).
- Delivery: your final message MUST contain sections in this order: Task, Result, Files changed (paths + SHA-256 via `shasum -a 256`), Tests executed (exact commands + exit codes + counts), Evidence, Risks, Decisions, Open issues. Numbers you propose (quotas/timeouts) are proposals until measured; label them so.

## Inputs specific to this package
Read fully: ADR-028 (ephemeral store), ADR-014 if present, ADR-031 (source transfer/egress by bytes), ADR-052/053/054/058/059 (M2 journal, staging, publication, observability, replay), crates/domain/src/artifact.rs, crates/application/src/artifact.rs + artifact_access.rs, crates/artifact-adapter/src/lib.rs + tests.rs, crates/project-adapter/src/mutation_store.rs + mutation_state.rs + filesystem/ (handle-relative no-follow I/O and the state-root layout used by M2), crates/mcp-server/src/stdio/resources.rs + resources/, docs/security-model.md (artifacts/Resources sections), docs/client-configuration.md (state root, planes/receipts/recovery), docs/roadmap/m3-quality.md ("Artifacts ricos D17" and budgets paragraph), backlog D17, traceability rows C11/L11.

Constraints from the plan: egress by bytes from fixed guest paths, regular files only (no links), no archive-selected host writes; bounded parsers (XML entities, deep JSON, external URIs, HTML active content); descriptor with kind/MIME/format version/hash/size/completeness/sensitivity/TTL/source/runtime identity; M1 logs keep their URI/retention semantics; new Resources are owner-bound, paginated/chunked, never trigger work; HTML/SVG are hostile (no scripts/remote resources in any preview); source/symbols may hold secrets — retain/export requires host permission; bounded secret scan complements redaction and guarantees nothing; proposed budgets 32 MiB/artifact, 64 MiB/job, 128 MiB/owner, 256 MiB/global, 128 members/job, TTL 1 h (host up to 24 h), 512 KiB MCP per response; disk requires protected state root and quota/reservation (not RSS); no eviction of artifacts promised to another owner; the M1 ephemeral store must not silently become persistent and M2 journals must never be evictable; migration of on-disk format defined before introducing it; crash/recovery; ADR-050 (no daemon/broker); macOS ARM64/APFS is the only positive host, Linux/Windows fail closed.

## Deliverables
1. `docs/adr/ADR-061-private-quality-artifact-store.md` (Proposed) deciding: store location and layout under the host-provided state root (reusing the M2 handle-relative primitives; name the exact module/functions you will reuse), on-disk format + version + descriptor schema (as Rust types to be added, module path proposed), owner binding and authorization at read time (live ProjectRef revalidation, no cross-owner existence leak), quotas/reservation model (bytes, members, per job/owner/global, how reservation happens before writing, what happens at exhaustion — reject before producing), TTL/expiry/reconciliation at publish time, crash/partial-write recovery on restart (what is trusted, what is discarded, never repairing M2 journals), migration rule for future format bumps (unknown version fails closed), Resource URI scheme compatible with M1 URIs (distinct scheme/prefix, pagination/chunk parameters, 512 KiB response cap), ingestion contract from the guest (bytes over the existing egress channel, fixed guest paths, regular-file-only, size caps), hostile-content handling per kind (JUnit XML, JSON, LCOV, HTML/SVG, mutants diffs/logs) with the bounded-parser rules, sensitivity classes and what requires host permission, observability events, and rollback (uninstall keeps or rejects incompatible readers; never deletes other owners' artifacts).
2. An explicit **test/oracle list** for M3-01 (XML entities/billion laughs, deeply nested JSON, external URIs in HTML, HTML with script, output flood, two owners, TTL expiry, quota exceed, crash mid-write, symlink in guest path, restart) with the discriminating positive control for each.
3. No product code edits in this package. Only the ADR file. If you conclude that a decision requires the owner (e.g., persisting artifacts beyond process lifetime changes the privacy posture announced in SECURITY.md), state it precisely in Open issues with alternatives.

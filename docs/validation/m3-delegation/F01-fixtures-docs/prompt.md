# Package F01 — nextest fixtures, hostile inputs, M3 matrix skeleton and ADR status/index (Luna, second session)

Task / ID: F01. Objective: (1) create the fixture crates the nextest adapter and its Docker tests need; (2) hostile JUnit/JSON/HTML/tar samples for parser/egress tests; (3) create `docs/validation/M3-matrix.md` skeleton; (4) set the ADR statuses accepted by the orchestrator and index ADR-060..063 in `docs/adr/README.md`.
Model / CLI / effort: GPT-5.6 Luna via codex exec, medium.
## Common rules (every M3 package)
- Repo /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (base main 52396184). Orchestrator: Claude Fable 5.1. You do not delegate, commit, merge, push, install, download, pull images or change architecture. Never run `git add/commit/stash/checkout/reset`; never `cargo fmt` the workspace (format only your own files with `rustfmt --edition 2024 <file>`); never edit files outside your ownership list. Several workers edit the same checkout concurrently: re-read a shared file immediately before patching it and patch minimally (one-line `pub mod` additions only where allowed); if `cargo check` fails in a module you do not own, report it and keep going on yours — do not fix others' code.
- Accepted decisions (read fully): docs/adr/ADR-060-bounded-job-execution-and-mcp-tasks.md (D06), docs/adr/ADR-061-private-quality-artifact-store.md (D17), docs/adr/ADR-062-coverage-accounting-and-semver-baselines.md (D18), docs/adr/ADR-063-m3-guest-plugin-provisioning.md (provisioning). Normative: AGENTS.md; docs/roadmap/m3-quality.md; docs/roadmap/m2-m8.md G1–G9; ADR-028/030/031/032/037/040/050..059; docs/architecture.md; docs/security-model.md. Map of interfaces with line refs: docs/validation/m3-delegation/S00-baseline/report.md §5.
- Invariants: hexagonal (domain/application free of rmcp/Cargo/SQLite/LanceDB/Docker/std::process/tokio); typed Serde/Schemars; one closed gateway; host authority only; hostile project code; network deny enforced; handle-relative no-follow own I/O; cancel/EOF/timeout/expiry terminate+join before capacity release; uncertain cleanup dominates; poll/cancel never take the job permit; unknown/partial/unavailable/skip never pass; the 18 existing tool schemas/snapshots byte-identical; no `unwrap/expect/panic!` on normal paths; tests first (discriminating, with positive controls); mocks never replace Cargo/Docker/filesystem boundaries.
- Verification you must run for your crates: `cargo check -p <crate> --all-targets --locked --offline`, `cargo clippy -p <crate> --all-targets --locked --offline -- -D warnings`, `cargo test -p <crate> --locked --offline` (non-Docker), plus `python3 -B scripts/check-architecture.py`. Report exact commands, exit codes, counts.
- Delivery (final message): Task, Result, Files changed (+SHA-256), Tests executed, Evidence, Risks, Decisions, Open issues. Return needs for other specialists to the orchestrator.
## Ownership (ONLY)
- `fixtures/nextest/` (new tree): crates `passing` (3 tests pass), `failing` (2 pass, 1 fail with a deterministic assertion message), `ignored` (1 pass, 2 #[ignore]), `flaky` (a test that fails on first run and passes on retry using a marker file under `std::env::temp_dir()` — must be deterministic under retries=1), `leaky` (a test that spawns a detached child process sleeping 30 s so nextest reports a leak with default leak-timeout), `doc-only` (only doctests, no #[test]), `no-tests` (lib with no tests), `hostile-output` (a test printing 8 MiB of output and fake JUnit/Cargo-looking lines to stdout/stderr), `slow` (a test sleeping 120 s for timeout/cancel oracles). Each fixture: minimal Cargo.toml with `[workspace]` table (isolated), edition 2024, no dependencies, `Cargo.lock` generated? — NO: do not run cargo on the host against hostile fixtures; write Cargo.lock by hand for dependency-free crates (version 4 lock with the single package) mirroring how existing fixtures under `fixtures/` do it (inspect fixtures/*/Cargo.lock and scripts/test-fixtures.py for the corpus rules; add your fixtures to the corpus inventory if the script requires it). Add a `fixtures/nextest/README.md` with expected nextest outcomes per fixture (counts) as the oracle table.
- `fixtures/hostile-reports/` (new): `junit-billion-laughs.xml`, `junit-external-entity.xml`, `junit-deep-nesting.xml` (generated, depth 10k), `junit-huge-attribute.xml`, `junit-forged-markers.xml`, `junit-nextest-sample-flaky-rerun-leak.xml` (hand-written following nextest JUnit docs), `coverage-deep.json` (nesting 10k), `coverage-external-uri.html`, `report-with-script.html`, `bundle-with-symlink.tar`, `bundle-with-dotdot.tar`, `bundle-oversize-member.tar` (tars built with Python tarfile, USTAR format, deterministic mtimes), plus `README.md` with SHA-256 of each and what each must trigger.
- `docs/validation/M3-matrix.md` (new skeleton following docs/validation/M2-matrix.md format: milestone status, Elemento|Estado|Evidencia y límite rows for M3-01..06, D06/D17/D18/provisioning, G1–G9; all "Pending" with pointers to ADRs and delegation records).
- `docs/adr/README.md`: add index entries for ADR-060, ADR-061, ADR-062, ADR-063 in the existing style. In the four ADR files change ONLY the Status section to: `Accepted 2026-09-06 by the M3 orchestrator after independent reviews V06/V17/V18 (ADR-063: owner provisioning authorization 2026-09-05); implementation and qualification pending — see docs/validation/M3-matrix.md`. Do not change any other line of those ADRs.
- `scripts/test-fixtures.py` only if adding fixtures to an inventory is mandatory for the existing gate to keep passing — run `python3 -B scripts/test-fixtures.py` before and after and report.
Not yours: crates/, other docs.
Delivery per common rules, with SHA-256 of every new file.

# Package I17 — ADR-061: owner-bound durable quality artifact store (adapter worker)

Task / ID: I17 (M3-01 prerequisite). Objective: implement ADR-061 Stage 0/Stage 1 storage: domain descriptor types, application ports and authorized read/publish use cases, the macOS/APFS durable adapter under the state root reusing M2 primitives per the revised ADR, the new Resource URI scheme in the MCP resources handler, and the discriminating tests/oracles from the ADR table (native ignored tests where APFS is required, ordinary tests otherwise).
Model / CLI / effort / reason: GPT-5.6 Terra via codex exec, high. Reason: adapter/persistence work with an already decided contract.
## Common rules (every M3 package)
- Repo /Users/cburgosro/Projects/rust-mcp, branch `ai/m3-quality` (base main 52396184). Orchestrator: Claude Fable 5.1. You do not delegate, commit, merge, push, install, download, pull images or change architecture. Never run `git add/commit/stash/checkout/reset`; never `cargo fmt` the workspace (format only your own files with `rustfmt --edition 2024 <file>`); never edit files outside your ownership list. Several workers edit the same checkout concurrently: re-read a shared file immediately before patching it and patch minimally (one-line `pub mod` additions only where allowed); if `cargo check` fails in a module you do not own, report it and keep going on yours — do not fix others' code.
- Accepted decisions (read fully): docs/adr/ADR-060-bounded-job-execution-and-mcp-tasks.md (D06), docs/adr/ADR-061-private-quality-artifact-store.md (D17), docs/adr/ADR-062-coverage-accounting-and-semver-baselines.md (D18), docs/adr/ADR-063-m3-guest-plugin-provisioning.md (provisioning). Normative: AGENTS.md; docs/roadmap/m3-quality.md; docs/roadmap/m2-m8.md G1–G9; ADR-028/030/031/032/037/040/050..059; docs/architecture.md; docs/security-model.md. Map of interfaces with line refs: docs/validation/m3-delegation/S00-baseline/report.md §5.
- Invariants: hexagonal (domain/application free of rmcp/Cargo/SQLite/LanceDB/Docker/std::process/tokio); typed Serde/Schemars; one closed gateway; host authority only; hostile project code; network deny enforced; handle-relative no-follow own I/O; cancel/EOF/timeout/expiry terminate+join before capacity release; uncertain cleanup dominates; poll/cancel never take the job permit; unknown/partial/unavailable/skip never pass; the 18 existing tool schemas/snapshots byte-identical; no `unwrap/expect/panic!` on normal paths; tests first (discriminating, with positive controls); mocks never replace Cargo/Docker/filesystem boundaries.
- Verification you must run for your crates: `cargo check -p <crate> --all-targets --locked --offline`, `cargo clippy -p <crate> --all-targets --locked --offline -- -D warnings`, `cargo test -p <crate> --locked --offline` (non-Docker), plus `python3 -B scripts/check-architecture.py`. Report exact commands, exit codes, counts.
- Delivery (final message): Task, Result, Files changed (+SHA-256), Tests executed, Evidence, Risks, Decisions, Open issues. Return needs for other specialists to the orchestrator.
## Ownership (create/edit ONLY these)
- `crates/domain/src/quality_artifact.rs` (new) + one `pub mod quality_artifact;` line in crates/domain/src/lib.rs; tests `crates/domain/tests/quality_artifact.rs`.
- `crates/application/src/quality_artifact.rs` (new: ports `QualityArtifactStore` {reserve, ingest member stream with exact cap, publish descriptor, read chunk, read index page, reconcile/recover, prune-expired}, `ArchiveBundle` acceptance contract, admission of declared members, authorization by owner binding + live ProjectRef, Stage 0 fallback interface to the M1 `ArtifactStore`), + `pub mod` line in crates/application/src/lib.rs; tests `crates/application/tests/quality_artifact.rs` (with an in-memory test double of the new port — allowed only in tests).
- `crates/project-adapter/src/quality_artifact_store.rs` (new durable adapter), `crates/project-adapter/src/filesystem/macos/quality.rs` (new) and the shared `pub(crate)` handle-relative primitive module the ADR requires (new file, e.g. `crates/project-adapter/src/filesystem/macos/state_primitives.rs`), the minimal extraction of the fixed-child helper from `crates/project-adapter/src/mutation_state.rs` (M2 journal bytes and ALL existing M2 tests must remain byte-identical/pass — run `cargo test -p rust-engineering-project --locked --offline` including the mutation store tests), mod lines in crates/project-adapter/src/lib.rs and filesystem/macos.rs; tests `crates/project-adapter/tests/quality_artifact_store.rs` (+ native ignored tests for APFS-specific oracles, named so the gate can invoke them exactly).
- `crates/mcp-server/src/stdio/resources.rs` + `resources/`: add the `rust-quality-artifact://` scheme (index page + chunk read; 320 KiB chunk, 512 KiB response cap, 64 members/page, 128-byte cursor) keeping the M1 `rust-artifact://` path byte-identical; tests in `stdio/resources/tests.rs`. Do not edit stdio.rs (the integrator wires the store field; expose a constructor/trait the integrator can call and document it in your report).
- `crates/execution-adapter/src/mutation_archive.rs`: read-only for you; if you need a `pub` decode entry point for ArchiveBundle revalidation, add ONE minimal `pub` wrapper function at the end of that file and nothing else.
Not yours: job.rs, nextest, rust_gateway.rs, docs, scripts, fixtures.
## Requirements (from ADR-061; do not weaken)
Layout `<state-root>/rust-mcp-quality-artifacts-v1/{store.lock, clock-watermark.json, reservation/, blob/, descriptor/, quarantine/}`; 0700/0600; regular files link count 1; opaque IDs `qart_`+32 hex; `JobId` `job_`+32 hex (define a local newtype if domain::job is not yet available at compile time — coordinate: Sol is adding `crates/domain/src/job.rs` concurrently; if it exists when you compile, use `domain::job::JobId`; otherwise define `QualityJobId` with the same grammar and note it for integration); descriptor v1 with deny_unknown_fields and closed enums (kind incl. ArchiveBundle, MIME enum, payload_format_version, completeness, sensitivity, created/expires wall-clock + in-session monotonic deadline rule, owner_binding 32 bytes, ArtifactSource/ArtifactRuntime closed structs, GuestArtifactName enum); owner binding = domain-separated SHA-256(state-root dev/ino, uid, granted-root dev/ino, workspace root string); fstatfs free-space floor = reservation + 49 MiB + 16 MiB; best-effort fallocate, fail closed on ENOSPC/short write; exclusive non-blocking flock; descriptor is the commit marker (blob rename → fsync → descriptor temp → fsync → rename → dir fsync); reconciliation trusts only fully validated v1 descriptors, discards own reservation temps without descriptor, quarantines anything else; clock watermark regression → fail closed scoped to quality artifacts; quotas (32 MiB/artifact, 64 MiB & 128 members/job, 128 MiB/owner, 256 MiB/global, TTL 3600 s default, host max 86400 s) as constants with units; reads never renew artifact TTL nor the project lease; never passed to reap_artifacts/retain_owners; Linux/Windows return UnsupportedPlatform before any effect; hostile-content policy: store bytes only — parsers live with the producers (nextest/coverage) — but ArchiveBundle revalidation (closed USTAR profile) is yours.
## Tests/oracles (from the ADR table; each with the positive control)
ENOSPC mid-stream (inject via a test-only fault port, not by filling APFS) releases reservation; two owners + same-root two sessions; two concurrent processes on store.lock (spawn a helper thread/process in a native ignored test); quality reservation never pushes below M2 headroom (compute with fstatfs on a temp APFS dir); TTL expiry / just-before-expiry read; quota exceed rejects before any effect; crash points between blob rename→descriptor rename and descriptor rename→dir fsync (fault injection port); symlink/fifo/dir at a member path rejected; restart recovery quarantines corrupt/unknown version/hash mismatch; M1 regression (existing resources tests unchanged); ArchiveBundle: link/`..`/oversize/non-regular rejected without extraction, bounded regular tar canonicalizes; index pagination bounds; 512 KiB response cap with base64.
Report which oracles are native-ignored and their exact test names for the gate.

# Evidencia M0-01 — Bootstrap

Fecha: 2026-09-03. Host: macOS `aarch64-apple-darwin`.
Ejecución histórica de M0-01: Rust `1.97.1 (8bab26f4f 2026-07-14)`;
Cargo `1.97.1 (c980f4866 2026-06-30)`, conforme a los commits citados abajo.
El owner actualizó el toolchain a 1.98.1 en `cafe721`; su validación nueva queda en
M0-02. La actualización del toolchain no cambia la versión de una ejecución pasada.

## Alcance y trazabilidad

- Baseline inicial preservada en `main`: `be27aef`.
- Rama del corte: `ai/m0-01-foundation`.
- Implementación revisada: `4533abe`.
- [ADR-021](../adr/ADR-021-minimal-bootstrap.md): un binario sin dependencias,
  licencia pendiente y ausencia explícita de MCP/sandbox.
- Modelo del worker read-only de preflight: GPT-5.6 Sol, Medium. El principal
  conserva decisiones, implementación, integración y aceptación.

## Gates antes de integrar

| Comando | Resultado |
| --- | --- |
| `cargo fmt --check` | Exit 0 |
| `cargo check --workspace --all-targets --locked --offline` | Exit 0 |
| `cargo clippy --workspace --all-targets --locked --offline -- -D warnings` | Exit 0 |
| `cargo test --workspace --all-targets --locked --offline` | Exit 0: 8 tests de integración; 0 fallos |
| `cargo audit --no-fetch --json` | Exit 0, 0 vulnerabilidades; 1225 advisories locales, fecha/commit desconocidos |
| `cargo metadata --offline --locked --no-deps --format-version 1` | Un paquete, cero dependencias, `publish=[]`, sin licencia concedida |
| `git diff --check` | Exit 0 |

El audit usó la DB local sin refresh; no acredita freshness. `cargo-deny` está
instalado, pero no hay `deny.toml` ni política de licencias aprobada: no ejecutado.
No se instaló ninguna herramienta ni se descargaron crates para este corte.

Los tests del [binario real](../../crates/mcp-server/tests/cli.rs) cubren ayuda,
versión desde metadata, invocación vacía, modos futuros rechazados, argumentos
sobrantes, diagnóstico independiente de contenido no confiable, bytes no UTF-8 en
Unix y streams de salida cerrados. Este último caso verifica código 1 para ayuda,
versión y error en stderr. Los dos tests específicos de Unix no acreditan Windows.
Los gates se repitieron tras añadir esa cobertura; no cambió código de producción.

## Revisión independiente y resolución

[Respuesta completa de Claude Code](../reviews/M0-01-claude-sonnet-5.md):
Sonnet 5, Medium; recomendación **Accept**, sin Critical/High/Medium ni violaciones
de contrato/ADR. Se revisó la implementación y el diff; los gates los ejecutó el
principal.

| Observación | Resolución del principal |
| --- | --- |
| Falta test de error de escritura | Corregido: `closed_output_stream_returns_one_without_panicking`, con socket Unix sin lector, prueba stdout y stderr del binario real. |
| `write_all` versus `writeln!` | Sin cambio: salida constante versus versión formateada; ambos manejan error y ahora se prueban. |
| `resolver = "3"` presuntamente redundante | No aplica: el workspace es virtual, sin package edition raíz; se conserva explícito según documentación Cargo. |
| Commit combina política de agentes y bootstrap | Observación de trazabilidad aceptada; conserva el cambio explícitamente pedido por el usuario. No se reescribe historia. |

La revisión propia reforzó además el test de no reflexión: compara stderr con una
invocación desconocida ordinaria, detectando también una filtración parcial o
sanitizada. No se requiere segunda revisión externa: corrección exclusiva de tests,
sin modificación del comportamiento ni de contratos/seguridad.

Fuentes verificadas para el bootstrap: [Cargo workspaces](https://doc.rust-lang.org/cargo/reference/workspaces.html),
[rustup toolchain](https://rust-lang.github.io/rustup/overrides.html),
[Claude CLI](https://code.claude.com/docs/en/cli-reference),
[Claude modelos](https://code.claude.com/docs/en/model-config).

## Integración

Corrección y evidencia pre-merge: `623653d`. Working tree limpio antes de integrar.
Merge local `--no-ff` en `main`: `c86a82a`; rama del corte conservada.

Gate proporcional post-merge en `main`:

- `cargo fmt --check`: exit 0.
- `cargo test --workspace --all-targets --locked --offline`: 8 passed, 0 failed.
- `cargo run --locked --offline -- version`: exit 0, `rust-engineering-mcp 0.1.0-dev.1`.
- `git status --short --branch`: `## main`, sin cambios.

M0-01 aceptado después de revisión, corrección de tests e integración. No hay
remoto ni publicación. La actualización final del tablero/evidencia es documental.
M0 y M1 permanecen abiertos; el siguiente corte es M0-02 (dominio/contratos base),
seguido de M0-03 (primer adapter MCP ejecutable).

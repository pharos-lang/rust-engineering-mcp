# M3-05 — recibo de calificación `rust.mutation.test`

Fecha: 2026-09-06. Resultado: **Qualified, 10/10**.

Comando:

```text
RUST_MCP_TEST_SOCKET=/Users/cburgosro/.docker/run/docker.sock \
python3 -B scripts/test-m3-runtime.py --output-dir target/m3-runtime-q01-attempt4
```

Las diez selecciones de `mutation_runtime` pasaron en 218.572 s contra
`cargo-mutants 27.1.0` en la imagen aprobada
`sha256:384a1742ecc53cdd3a9c0bf36c6f8b66db73ddd118aeeae6e55654ea998ae36a`.
Cada una usa `--exact --ignored --test-threads=1` y la regla de exactamente un
test aprobado.

La calificación observó y fijó exits 0/1/2/3/4, el schema real de
`outcomes.json`, las listas y el layout del bundle, y la identidad guest validada
desde `lock.json`. También verificó source/canary inmutables, cap antes del build,
output hostil no confiable, cancelación con cleanup unido y ausencia de extracción
a host. El intento inicial exit 1 descubrió y corrigió el modo 0444 de la copia
privada; el source original sigue read-only.

Recibo: [`M3-runtime.json`](M3-runtime.json), corrida vigente de **62/62**
selecciones, SHA-256
`02b085bf2d00d52cd2a821f059ec6aa4a5ea3b4fc16ffb06b4671b02f131c63b`.
Detalle: [M3-05-mutation-calibration.md](M3-05-mutation-calibration.md).
La higiene posterior encontró cero containers/volumes etiquetados.

## Recalificación W3 / V-SEC

El contrato ya no expone `source_unchanged`: el gateway no puede observar una
comparación host antes/después y no publica una afirmación tautológica. La
inmutabilidad se impone con `/source` read-only y el verifier de mounts, y se
acredita con `host_source_and_canary_are_unchanged_after_every_mutation_run`.
El snapshot deliberado cambió de SHA-256
`6d96291bfe05824298901fff90853eb08c2d7dc1276c72948b206b730414ec50`
a `08f37709c507b827f22a58fb75a1f80a35b19da510f22c449e5ba0e6a55a080a`;
los otros 22 snapshots de tool permanecen byte-identical.

W3 volvió a pasar mutation 10/10 dentro de su corrida histórica 55/55. La
corrida vigente posterior de W6 es de **62/62** en [`M3-runtime.json`](M3-runtime.json),
SHA-256 `02b085bf2d00d52cd2a821f059ec6aa4a5ea3b4fc16ffb06b4671b02f131c63b`.

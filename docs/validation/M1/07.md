# M1-07 — captured dependency audit

2026-09-04, branch ai/m1-07-audit from clean main d23e718. ADR-038. Rust/Cargo
1.98.1, CARGO_INCREMENTAL=0. No remote operations. M0 unchanged in scope.

Implementation: rust.dependencies.audit (eighth operative tool) combines one owned
source generation, approved frozen metadata and host-expected RustSec0.32.0 records
staged in authoritative SQLite. Host flags only, no runtime download. Exact registry
identities, v4 lock graph, representative workspace paths, patched/unaffected ranges,
CVSS severity, withdrawn/informational separation, bounded output and latest_known
project/RustSec evidence. See ADR for conservative limits and unsupported sources.

Validation:
- python3 scripts/gate.py core --report target/M1-07-core-final.json: PASS10stages,
  455passed tests including doctest, audit --no-fetch and deny offline. Tracked
  M1-07-core.json. Existing paste1.0.15 maintenance warning remains.
- python3 scripts/test-rust-execution.py: PASS16exact serial Docker tests before
  review corrections. Current image/config calibration in M1-07-calibration.json.
  Includes actual Cargo hostile build.rs/proc-macro/libtest, timeout/cancel/overflow
  cleanup, MCP runtime/tools/log Resources and original13audit calls.
- python3 scripts/test-audit-data.py: PASS after corrections, actual RustSec+SQLite
  under macOS test-process network deny (TCP/UDP IPv4/IPv6 denied with positive
  controls outside sandbox). M1-07-audit-data.json. Not whole-MCP isolation evidence.
- Post-review audit runtime: three exact serial tests cover15calls, including actual
  RSA unpatched advisory and medium severity; two captured lock generations; invalid
  and ambiguous locks; synthetic patched/withdrawn/informational variants; stale with
  and without findings; unknown age; absent/unconfigured snapshot, hash/path denial
  and no-follow symlink denial. Snapshot count/sequence exposed; all owned Docker
  objects removed and source/snapshot bytes unchanged. Final receipt recorded below.
- New filesystem tests exercise exact8MiB/overflow, links/hardlinks/FIFO/ancestors,
  real file/root/replacement races and cancellation. Parser tests cover ambiguous
  sources, orphan/cyclic graphs, exponential routes under bounded BFS, names/size/
  cancellation/deadline. App tests bind lock and metadata to the same generation.

External review: Claude Code2.1.259, explicit Opus5 High; focused followup Medium.
Full findings and critical principal disposition in docs/reviews/M1-07-*.md. No
review substitutes execution evidence. Snapshot count does not prove publisher
completeness, SHA expected by host is not authenticated import, sequence is not
antirollback. CLI/import/signatures/global catalog remain M1-10. Metadata --no-deps
is not a complete manifest/lock feature-resolution check. Actual active RustSec
cancellation oracle is not claimed; cooperative and joined-worker tests cover it.

Normal runtime only uses configured files and installed approved image. Development
acquisition and exact feature/lock receipt: M1-07-dependencies.md and dependency
receipt. No image/model/runtime substitution. Native Linux/Windows/x86_64, third-
party clients, distribution licensing and utility benchmark remain unqualified.
Integral full will be rerun after M1-09 with explicit original local assets.

Final post-review runtime: PASS3/3,15calls,175.48s; M1-07-runtime.json.
Configuration sha256:7882c961340bb62b235c378aad720f8d903d9b944b9086ef5ada73d9bbdae212.

Final core455/10stages rerun passed after focused followup origin rejection and
created_at defense. Schema unchanged; final diff reviewed and format clean.

Integration commit be74318 / no-ff merge c6236af. Branch retained. Post-merge
actual MCP smoke1/1 (six snapshot classification cases),86.18s; cleanup verified
(target/M1-07-postmerge.log), after final followup corrections.

# M1-03 — Captured Cargo check and live artifact Resources

Status: implementation and actual runtime tests complete; core332 tests /10stages
passed. Opus5 High review and Medium follow-up critically integrated; local merge/smoke follow. Branch ai/m1-03-check from clean main882fb6e.

## Delivered

rust.check with seven closed Cargo selection options, no arbitrary flags, frozen/
offline execution in approved LinuxARM64 Rust/Cargo1.98.1, normalized diagnostics,
relative captured spans/Unicode columns, grouped multipart suggestions and bounded
logs. Compiler errors E0502/E0106 are valid failed/isError=false. Partial validation
never passes; exact frozen startup lock errors are blocked and retain their logs.
No source/lock updates, package installation or runtime downloads.

ArtifactStore remains process-local memory. Resources expose canonical owner/id
URIs only, live ProjectRef/retention revalidation, private cache/ttl0, base64 and
full response cap. Retired owners reclaim quota, live logs survive failed new
publication, and combined stdout/stderr each retain budget and truncation markers.
Diagnostic and log output can be written by project code; neither is authenticated.
See ADR-034 and principal review disposition for decisions and limitations.

## Reproducible gates

```
CARGO_INCREMENTAL=0 python3 scripts/gate.py core
RUST_MCP_TEST_SOCKET=<LOCAL_HOME>/.docker/run/docker.sock \
  CARGO_INCREMENTAL=0 python3 scripts/test-rust-execution.py
```

Rust gate: six exact sequential tests passed. Includes the existing real six-scenario
build.rs/proc-macro containment calibration, both MCP inspections, calibration
cancel/EOF, check success/borrow/lifetime/frozen locks/Resources, and cancel/EOF
after observing active build-script executables. No Cargo fixture runs on host.
Logs: target/M1-03-rust-security.log and target/rust-security/0.log..5.log.

Tracked receipts: M1-03-calibration-receipt.json, M1-03-check-receipt.json and
M1-03-cancellation-receipt.json. Runtime image identity unchanged:
sha256:8fac70723a8d04b6ec9633ab721806b8a55f4f083a1b3f988c61bf6a00fa1909.
Current configuration fingerprint:
sha256:6e030696c487138fcf3ce4454e88346e6d1e5074b273b13286da76f46ad64df2.

Check receipt:2passed,2failed(E0502/E0106),6hashedlogs,5ordinary Resource reads
plus2frozen-error log reads, owner substitution denied, renamed ProjectRef denied,
only intentional fixture edits and verified no remaining owned Docker objects.
Cancellation receipt:2real active build scripts observed, discovery responsive,
request cancellation suppressed by SDK, capacity reused only after cleanup, EOF
joined and objects absent. Resource busy is a fixed server error during active job.

## Limits

Cargo30s phase,120s worker preparation/job budget; joined cleanup may outlast the
caller deadline within control limits. Explicit empty literal-redaction policy;
not detection of arbitrary project secrets. Source capture is verified bounded
bytes, not an atomic filesystem snapshot. Libraries requiring a new lock receive
typed frozen failure; no lock resolution strategy is silently added.

This gate is proportional to M1-03. Integral full semantic/E5/LanceDB, native OS/
architecture qualifications, third-party clients, distribution/license and utility
benchmark remain separate gates; paste1.0.15 warning remains visible.

Final core: 332 tests including doctests,10stages passed; tracked report
M1-03-core-gate.json and target/M1-03-core.log.

Alcance de dependencias: std y crates path capturados; CARGO_HOME efímero vacío,
sin cache registry/git ni descarga. No se acredita compilación de proyectos con
dependencias externas no aprovisionadas.

Follow-up corrections: retention capacity returns safe validation/diagnostics with
log=null and explicit retention_capacity rather than blocking the agent loop or
evicting live logs. Post-admission Resource interruption maps -32000; internal
failures retain -32603. Lossy UTF8 expansion is bounded before normalization and
retains partial evidence. Tests cover quota success/late authorization failure and
public nullability/diagnostics. No additional model/tool/runtime provisioned.

Integración local: commit `96fc984`, merge no-ff `4ddc696`; rama conservada. Smoke post-merge `check_reports_success_and_borrow_errors_with_live_log_resources`: 1/1,18.72s, `target/M1-03-postmerge.log`.

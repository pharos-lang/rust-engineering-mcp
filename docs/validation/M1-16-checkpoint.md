# M1-16 resumed checkpoint

Historical checkpoint. The approved freeze was subsequently executed exactly
once: all 24 runs and oracles completed, and M1-16 is now recorded in
[the final validation](M1-16.md). The remaining-sequence text below describes the
state at restart and is retained as chronological evidence.

2026-09-04. Utility measurements have not started. Current branch preserves the
prospective24-run protocol and corrected research controller; main remains the
integrated M1-15 local preparation. No production Rust change in this vertical.

The restart restored CUA. Actual Inspector web UI completed13tools and in-flight
cancellation; its evidence belongs to the separate pending M1-17 integration.

## Qualified before freeze

- Four std-only repair fixtures:20 actual gateway executions, four initial failures
  and16 reference passes; actual final audit observes no registry dependencies.
-15-crate/16-version source-grounded projection,69 source hashes, real signed
  fixture import and E5/LanceDB rebuild; identical emitted baseline facts.
-42 actual SDK calls/all13tools/Resource read;76 native benchmark calls across
  eight EN/ES queries and three modes. These are not agent utility outcomes.
-33 participant,26 broker,15 evaluator,8 analyzer,13 driver+1bundle tests. Actual corrected
  feature preflight, exact Sol/medium echo, active raw/MCP cancellations and
  full model/read/edit/quality roundtrip in both arms pass with joined cleanup.
- Failed post-restart preflight and cancellation receipts are retained. Their
  causes and fixes are recorded in the [principal disposition](../research/m1-16/qualification/principal-disposition.md).

The exact feature map is41keys, including preserved host network_proxy=null.
No full native-tool inventory or OS sandbox for V8 is claimed. Driver cancellation
preserves errors and waits/drains the product before accepting server exit0;
per-run read-only Docker absence observations and input hashes fail closed.

## Remaining sequence

Independent Opus follow-up completed, with all P1 findings disposed and current
qualification passed. Match reproducible sources, commit and approve immutable freeze; execute
all24 paired runs in prospective order; first/final hidden oracles; blinded source
review; honest paired metrics including infrastructure failures. No silent reruns.
Then local integration/smoke, final full19-stage gate and M1-17 DoD review.

Native Linux/Windows/x86 evidence, product license/copyright and approved
publisher/channel/trust-key custody remain external/owner inputs. They do not
prevent the private controlled experiment and are not waived by its success.
Do not publish, push, install substitutes, delete target research assets or advance M2.

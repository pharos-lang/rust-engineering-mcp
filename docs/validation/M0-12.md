# M0-12 — Foundation closure gate

Host macOS26.6.2/APFS ARM64; Rust/Cargo1.98.1, edition2024. M0 implementation units
are merged locally, M1 has not started. This is foundation validation, not release
0.1.0 or cross-platform qualification.

## Actual full gate

```text
RUST_MCP_TEST_SOCKET=<LOCAL_HOME>/.docker/run/docker.sock RUST_MCP_E5_DIR=/private/tmp/rust-mcp-e5-m009/onnx ORT_LIB_LOCATION=<LOCAL_HOME>/Library/Caches/ort.pyke.io/dfbin/aarch64-apple-darwin/612739f75438dc0a075461e1fb454226b4a1eb175e60a7271ba966bbbb972cd4 python3 scripts/gate.py full --report target/M0-12-full.json
```

**12 stages passed:** fmt, workspace check, Clippy with warnings denied, normal
Rust tests, doctests, architecture checks, vendor verification, Cargo fixture
corpus, cargo-audit, cargo-deny, Docker security and local semantic gate.
All Cargo build/test operations were locked/offline and non-incremental. The
semantic gate additionally checked/Clippied workspace all-features/all-targets.

**185 distinct Rust tests:**173 core,1 doctest,4 explicitly executed Docker tests,
6 real LanceDB tests and1 real E5 integration. The semantic bundle unit test also
runs again in its feature-local binary; that duplicate is not counted twice.
Additionally11 Cargo compiler cases and1 static RSA audit-input check passed.
Artifact redaction's single oracle test enumerates229,950 combinations.

- [Per-step report and durations](artifacts/M0-12-full.json).
- [Machine-readable test counts](artifacts/M0-12-counts.json).
- [Compact actual test output](artifacts/M0-12-results.txt).
- [Code/config/test/fixture hashes](artifacts/M0-12-source-receipt.json).
- [Actual Docker capabilities report](artifacts/M0-12-capabilities.json).
- [Audit JSON](artifacts/M0-12-audit.json).

Docker Engine29.7.2, Linux aarch64, runc/cgroupsv2:12 active observations,10 verified
guarantees, no invalid evidence; project_code_available=false. Image rebuilt from
reviewed Go fixture and calibrated as immutable
`sha256:7c9c6c0656d1a1c5565d2c9bea25bac1c16edc8617144a897e405d75e03d9e98`.
Image build is cache-assisted, not claimed clean-room. This report is historical
evidence and is never accepted by the runtime as authorization for another config.

Semantic gate: exact five E5 file sizes/hashes, explicit native static ORT identity,
telemetry disabled, no download features, real384-dimensional normalized vectors,
ES/EN retrieval, SQLite rehydration, rebuild and mismatch fallback. Network deny
calibrated with IPv4/IPv6 bind controls; configured nonexistent TMPDIR stayed absent.
No claim of general filesystem-write isolation or hard native allocator CPU/RAM caps.

Audit:581 lockfile packages,0 known vulnerabilities; paste1.0.15 maintenance warning
retained. Deny0.19.7 advisories/bans/sources passed, no advisory ignores; duplicate
versions remain warnings. License checks are an explicit M1 distribution gate.

## Closure scope and remaining M1 gates

SQLite snapshots, semantic generations and artifacts are real internal adapters,
not new public tools. Only rust.project.open is announced. Project validation is
structural/bounded and protected I/O is macOS/APFS-only. Gateway supports the closed
probe image, not Cargo/source transfer/build.rs/proc macros. ArtifactStore is
process-local memory, and Resources/live-owner validation remain M1. M1 must provide
its approved Rust runtime, worker/cancellation/backpressure integration, authenticated
catalog/native/model distributions, persistent antirollback/index import, benchmark,
license decision and actual native runners/clients before release.

See [M1 prerequisites](../m1-prerequisites.md), [CI matrix](../ci.md), ADR-024..029 and
[continuation prompt](../prompts/continue-m1.md). No scope advances to M2.

## Independent review / final disposition

[Independent Claude Opus5 High review and Principal disposition](../reviews/M0-12-claude-opus-5.md)
record no P0 or demonstrated blocking safety bug. Four P1 verifications/configuration
improvements were resolved: schema resolver feature bans, Docker input/failure paths,
public license/scope claims, and per-tool M1 contract obligations. P2 items were
corrected, fact-checked or explicitly assigned to M1 as detailed in the review.
The final full gate was repeated after deny/config and ignored-test harness changes;
all12 stages passed, including Docker26.204s and semantic6.398s. M0 is Done.
M1 has not started; no release/distribution approval is inferred.

## Local integration and post-merge checks

Unit commit `ce574a7`, branch `ai/m0-12-foundation-gate`, merged into main with
no-ff commit `85309fb`. Branch retained; no remote/push/PR/Actions.

On the merged tree:

- `CARGO_INCREMENTAL=0 cargo test -p rust-engineering-mcp --test protocol --locked --offline`:19 passed.
- `python3 scripts/check-architecture.py`:passed.
- `python3 scripts/verify-vendor.py`:published hash and93 files passed.
- All122 source/config/test/fixture hashes still match the successful final full gate.

[Actual post-merge output](artifacts/M0-12-postmerge.txt). The final documentation
commit only records this result; runtime/config bytes remain those fully validated.

# M1-11 — verified read-only catalog status

2026-09-04; `ai/m1-11-catalog-status` from clean main `bd32337`.
ADR-042 precedes implementation. This adds the eleventh tool, `rust.catalog.status`,
with empty closed input, per-component identities and independently assessed
freshness. The ten existing tool snapshots remain unchanged.

Catalog, model and index retain one authenticated/loaded session generation;
RustSec observes the existing host audit source separately on every call. The
runtime uses optional protected reads and the shared typed sequence floor, without
an admin lease, staging cleanup, download or synchronization. It retains a pending
reservation even when active bytes are absent. E5 and native Lance availability
require successful actual load/restore, exact binding and complete crate membership;
semantic failure preserves valid SQLite facts. No publisher or distribution license
is inferred from fixture keys.

The shared joined worker enforces admission and discards late success after
cancellation/deadline. The deadline is120s cooperative, not native preemption or a
hard RSS cap. The128KiB limit counts the entire encoded MCP result, including both
structured and text representations. Network metadata declares acquisition API
authority absent; it does not claim OS isolation by itself.

## Current evidence

[Core gate](M1-11-core.json):10/10 stages,572 tests including doctests,33 protocol
tests across all five supported versions. [Receipt](M1-11-summary.json) records
all-features/all-targets workspace Clippy and the actual native MCP gate:
`scripts/test-catalog-status.py`, two ordinary tests plus one ignored test explicitly
executed with E5/Lance under macOS network deny. IPv4/IPv6 positive controls succeed
outside the sandbox and fail inside it. Tests check immutable session state,
restarting after index corruption, and model absence while SQLite remains available.
[Rehashed assets](M1-11-assets.json), [source hashes](M1-11-source-receipt.json).

Discriminating additions: nine domain/application tests; fourteen MCP unit tests,
including twelve provider filesystem/authentication/freshness-source cases; two shared
floor tests and three added filesystem/getter tests; protocol closed arguments,
bootstrap denial and exact snapshot; real CLI/MCP tests. Counts overlap gate suites.
CLI floor extraction preserves the prior canonical bytes/digest and rotation policy;
existing CLI tests run in core. Symlinks/hardlinks/insecure modes are denied; held
administration leases and staging sentinels survive runtime status reads.

No full Docker gate was repeated for this isolated read-only vertical; unchanged
runtime security evidence remains historical M1-10. Full now includes catalog-status
as stage16 and must run on the final M1 candidate. Native Linux/Windows/x86_64,
third-party clients, distribution approvals and the actual utility experiment remain
separate unresolved qualification requirements. M1 is not closed.

Sonnet5 High review and Medium follow-up are complete with no remaining confirmed
actionable defect. [Principal disposition](M1-11-review-disposition.md) preserves
findings, counterevidence and fixes. The principal fixed a RustSec sequence-domain
mismatch discovered while checking the review and added regressions for actual
read errors, feature-disabled semantics and index identity mismatch.
[Actual native transcript](M1-11-native.txt) includes the final extended run.

Feature261b0c4, local no-ff mergeae6cc83; branch retained. Post-merge smoke2
CLI/MCP status tests and33 protocol tests passed on clean main,269 source hashes
verified. [Receipt](M1-11-postmerge.json).
